import type { Problem } from '../../types'

const problems: Problem[] = [
  {
    id: 'rs-ch19-t-001',
    chapter: 19,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'What the unsafe Keyword Unlocks',
    prompt: `A beginner thinks the keyword unsafe turns off all of Rust's safety checks. List the FIVE specific abilities (sometimes called the "unsafe superpowers") that an unsafe block actually grants, and state in one sentence what unsafe does NOT turn off.`,
    hints: [
      'There are exactly five extra things you may do inside unsafe.',
      'The borrow checker and ownership still apply.',
    ],
    solution: `Inside an unsafe block you gain exactly five abilities: dereference a raw pointer, call an unsafe function or method, access or modify a mutable static variable, implement an unsafe trait, and access fields of a union. That is all unsafe adds. It does NOT disable the borrow checker or any of Rust's other safety checks; ownership, borrowing rules, and lifetimes still apply normally inside an unsafe block. unsafe simply tells the compiler "trust me, I have upheld the invariants these five operations require."`,
    tags: ['unsafe', 'superpowers', 'basics'],
  },
  {
    id: 'rs-ch19-t-002',
    chapter: 19,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'Creating Raw Pointers Is Safe',
    prompt: `Consider this code: let mut n = 5; let r1 = &n as *const i32; let r2 = &mut n as *mut i32. None of these lines is inside an unsafe block, yet they compile. Why is CREATING raw pointers allowed in safe code, and what is the one operation on them that does require unsafe?`,
    hints: [
      'Making a pointer is different from following it.',
      'Think about when something could actually go wrong.',
    ],
    solution: `Creating a raw pointer is harmless because the pointer is just a value holding an address; nothing is read or written, so no invariant can be violated yet. The dangerous step is DEREFERENCING the raw pointer, that is, following it to read or write the memory it points at, because the pointer might be null, dangling, or unaligned. Therefore Rust lets you make *const T and *mut T anywhere, but you must wrap any *r access in an unsafe block. This keeps the unsafe surface as small as possible.`,
    tags: ['unsafe', 'raw-pointers', 'basics'],
  },
  {
    id: 'rs-ch19-t-003',
    chapter: 19,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'What a Type Alias Really Is',
    prompt: `You write type Kilometers = i32; and then let x: Kilometers = 5; let y: i32 = 5; let z = x + y;. Does z compile, and what is its value? Explain what a type alias does and does not do compared to the newtype pattern.`,
    hints: [
      'An alias is just another name for the same type.',
      'Contrast with struct Kilometers(i32).',
    ],
    solution: `Yes, it compiles and z is 10. A type alias with the type keyword creates a synonym, not a new distinct type: Kilometers and i32 are literally the same type, so values mix freely and you get no extra type safety. Its purpose is convenience, mainly shortening long type names like a long Box<dyn Fn> signature. This is different from the newtype pattern (struct Kilometers(i32)), which makes a genuinely separate type that the compiler will NOT let you accidentally mix with a plain i32.`,
    tags: ['type-alias', 'newtype', 'basics'],
  },
  {
    id: 'rs-ch19-t-004',
    chapter: 19,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'Function Pointer Type Versus Closure Traits',
    prompt: `A parameter declared as f: fn(i32) -> i32 has a type written with a lowercase fn. A parameter that accepts a closure is usually written with one of Fn, FnMut, or FnOnce. In one or two sentences each, explain the difference between the lowercase fn type and the uppercase Fn family.`,
    hints: [
      'One is a concrete type; the others are traits.',
      'Which kind of value can capture its environment?',
    ],
    solution: `Lowercase fn is a concrete TYPE called a function pointer; a value of type fn(i32) -> i32 points at an actual function (or a non-capturing closure that coerces to one) and has a known fixed size. The uppercase Fn, FnMut, and FnOnce are TRAITS that both functions and closures implement, used as trait bounds because closures can capture their environment and so do not all share one type. A handy rule from the book: a function pointer fn implements all three of Fn, FnMut, and FnOnce, so anywhere a closure trait bound is expected you can also pass a function name.`,
    tags: ['function-pointers', 'closures', 'basics'],
  },
  {
    id: 'rs-ch19-t-005',
    chapter: 19,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Why split_at_mut Needs Unsafe Inside',
    prompt: `The standard library's slice method split_at_mut returns two mutable subslices of one slice. Its public signature is completely safe to call, yet its body uses unsafe. Explain why the borrow checker would reject a purely safe implementation, and what term describes a function like this whose API is safe even though its insides are not.`,
    hints: [
      'Two &mut into the same slice value at once.',
      'The borrow checker cannot see that the ranges do not overlap.',
    ],
    solution: `A naive safe version would try to return two mutable references that both borrow the same slice at the same time, and the borrow checker forbids more than one mutable borrow of one value, even though the two ranges actually do not overlap. The compiler cannot reason that the halves are disjoint, so the only way to express it is with raw pointers and pointer arithmetic inside an unsafe block. Because the programmer guarantees the ranges do not overlap, the operation is genuinely sound. A function like this is called a SAFE ABSTRACTION over unsafe code: callers see a safe signature and never write unsafe themselves.`,
    tags: ['unsafe', 'safe-abstraction', 'slices'],
  },
  {
    id: 'rs-ch19-t-006',
    chapter: 19,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Mutable Statics Require Unsafe',
    prompt: `You have static mut COUNTER: u32 = 0; and want to do COUNTER += 1; from a function. The compiler forces that line into an unsafe block, while reading or writing an immutable static (static MAX: u32 = 100;) needs no unsafe. Explain the danger that makes accessing a mutable static unsafe.`,
    hints: [
      'Statics have a fixed address for the whole program.',
      'Think about more than one thread.',
    ],
    solution: `A static has a single fixed memory location shared across the entire program, so if it is mutable, multiple threads could read and write it at the same time, causing a data race. Rust cannot prove at compile time that all accesses to a mutable static are properly synchronized, so it requires every access (read or write) to be in an unsafe block, putting the burden on you to avoid races. An immutable static is fine without unsafe because it never changes, so concurrent reads cannot conflict. The book notes that for shared mutable state you should usually prefer thread-safe types like a Mutex or atomics instead of static mut.`,
    tags: ['unsafe', 'static-mut', 'data-race'],
  },
  {
    id: 'rs-ch19-t-007',
    chapter: 19,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Associated Type Versus Generic Parameter',
    prompt: `The Iterator trait is declared trait Iterator { type Item; fn next(&mut self) -> Option<Self::Item>; }. Suppose it had instead been written trait Iterator<T> { fn next(&mut self) -> Option<T>; }. Describe one concrete difference this makes for a type that implements the trait, and explain why the associated-type design is preferred here.`,
    hints: [
      'How many times can one type implement Iterator for itself?',
      'With a generic, the implementor and callers must repeat the type everywhere.',
    ],
    solution: `With an associated type, a type can implement Iterator only ONCE, fixing a single Item type, so callers of next never have to annotate what Item is. With a generic parameter Iterator<T>, a single type could implement Iterator<u32>, Iterator<String>, and so on for many T, which means every call site would have to specify which implementation it means, adding annotations and ambiguity. For iteration that single-implementation, no-annotation behavior is exactly what you want, so the associated type is the better fit. Generics are the right choice when you genuinely want multiple implementations differing by the type parameter.`,
    tags: ['associated-types', 'generics', 'traits'],
  },
  {
    id: 'rs-ch19-t-008',
    chapter: 19,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Add With a Default Type Parameter',
    prompt: `The Add trait is declared as trait Add<Rhs = Self> { type Output; fn add(self, rhs: Rhs) -> Self::Output; }. The Rhs = Self part is a default generic type parameter. For a normal impl Add for Point, what does Rhs default to, and name a situation where you would write impl Add<SomethingElse> instead.`,
    hints: [
      'What does "default" mean if you do not specify Rhs?',
      'Think about adding two different types, like meters plus millimeters.',
    ],
    solution: `When you write impl Add for Point and do not mention Rhs, it defaults to Self, that is Point, so you are defining Point + Point. The default generic type parameter exists so the common same-type case needs no extra annotation. You would override the default by writing impl Add<Meters> for Millimeters when you want to add two DIFFERENT types, for example a Millimeters that can be added to a Meters; there you specify Rhs explicitly because it is not Self. This feature lets one trait serve both the common case and the rarer mixed-type case cleanly.`,
    tags: ['operator-overloading', 'add', 'default-type-param'],
  },
  {
    id: 'rs-ch19-t-009',
    chapter: 19,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Returning a Closure Needs a Box',
    prompt: `This function does not compile: fn make_adder() -> Fn(i32) -> i32 { |x| x + 1 }. The compiler complains about Sized. Explain why a bare closure trait cannot be returned directly, and give the standard fix using a Box.`,
    hints: [
      'Each closure has its own anonymous, unknown-size type.',
      'Box gives the value a known size: a pointer.',
    ],
    solution: `A return type must have a size known at compile time, but Fn(i32) -> i32 is a trait, not a concrete type, and different closures have different sizes, so the compiler does not know how big the returned value is. The classic fix from the book is to return a trait object on the heap: fn make_adder() -> Box<dyn Fn(i32) -> i32> { Box::new(|x| x + 1) }. A Box is a pointer, which has a known fixed size, so the function signature is now Sized. (Later Rust also allows impl Fn(i32) -> i32 for a single concrete closure, but the boxed dyn form is the general answer the chapter teaches.)`,
    tags: ['closures', 'box', 'dyn', 'sized'],
  },
  {
    id: 'rs-ch19-t-010',
    chapter: 19,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'A Tiny macro_rules Reading',
    prompt: `Read this macro and predict what call_me!() expands to: macro_rules! call_me { () => { println!("hi"); }; }. Then explain what the empty parentheses () before the => mean, and why macro arms look like match arms.`,
    hints: [
      'The left side is a pattern; the right side is what it becomes.',
      'Empty () matches zero tokens of input.',
    ],
    solution: `call_me!() expands to println!("hi");. A macro_rules! definition is a set of arms, each shaped as (matcher) => { expansion };, and it works a lot like a match: the macro tries each matcher against the tokens passed to it and, on the first match, replaces the call with the corresponding expansion. Here the matcher () matches an invocation with no arguments, so call_me!() with empty parentheses matches and produces the println! statement. The match-like structure is why each arm has a pattern on the left and a template on the right.`,
    tags: ['macros', 'macro-rules', 'declarative'],
  },
  {
    id: 'rs-ch19-t-011',
    chapter: 19,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Two Methods Named fly',
    prompt: `A type Human implements its own inherent method fly, and also implements two traits Pilot and Wizard that each define a method named fly. If you write a_human.fly();, which fly runs by default? How do you call the Pilot version instead?`,
    hints: [
      'Method-call syntax prefers one kind of method.',
      'You can name the trait explicitly.',
    ],
    solution: `By default a_human.fly() calls the type's own INHERENT method, the fly defined directly on Human, because method-call syntax resolves to the inherent method when one exists. To call a trait's version you name the trait explicitly: Pilot::fly(&a_human) runs the Pilot implementation and Wizard::fly(&a_human) runs the Wizard one. This works because the methods take &self, so passing &a_human as the first argument disambiguates which fly you mean. The compiler picks the inherent method only because it is the closest match by default.`,
    tags: ['traits', 'method-resolution', 'disambiguation'],
  },
  {
    id: 'rs-ch19-t-012',
    chapter: 19,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'When to Reach for unsafe',
    prompt: `A teammate suggests sprinkling unsafe through ordinary application code to "make it faster." Give two LEGITIMATE reasons unsafe exists, and explain the guiding principle for how much code should be marked unsafe.`,
    hints: [
      'Think about hardware and other languages.',
      'Keep the unsafe surface small and wrapped.',
    ],
    solution: `Two legitimate reasons: first, some low-level tasks are inherently unsafe, such as directly interacting with the operating system, hardware, or memory, which Rust's safe subset cannot express; second, interoperating with other languages through FFI requires unsafe because the compiler cannot verify foreign code. The guiding principle is to keep unsafe blocks as small as possible and wrap them in a safe abstraction whose public API guarantees the invariants, so the rest of the program stays fully checked. Using unsafe merely as a speed hack in normal code is discouraged, because the safe compiler is usually just as fast and far less error-prone.`,
    tags: ['unsafe', 'design', 'when-to-use'],
  },
  {
    id: 'rs-ch19-t-013',
    chapter: 19,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Newtype to Implement Display for Vec',
    prompt: `You want to implement the Display trait for Vec<String> but the compiler rejects it. After explaining the rule that blocks this, describe how the newtype pattern lets you do it anyway.`,
    hints: [
      'Recall the orphan rule about local types and local traits.',
      'Wrap the Vec in a one-field tuple struct you own.',
    ],
    solution: `The orphan (coherence) rule says you may implement a trait for a type only if at least one of the trait or the type is local to your crate; both Display and Vec<String> are external, so the impl is forbidden. The newtype pattern works around this: define a thin tuple struct you own, like struct Wrapper(Vec<String>);, which is a LOCAL type. Now you can write impl fmt::Display for Wrapper, satisfying the rule because Wrapper is yours. The only cost is reaching the inner value through self.0, which you can smooth over by also implementing Deref if you want the wrapper to behave like the inner Vec.`,
    tags: ['newtype', 'orphan-rule', 'traits'],
  },
  {
    id: 'rs-ch19-t-014',
    chapter: 19,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Predict the Raw Pointer Output',
    prompt: `Predict the printed value: let mut num = 5; let r1 = &num as *const i32; let r2 = &mut num as *mut i32; unsafe { *r2 += 10; println!("{}", *r1); }. Two raw pointers (one const, one mut) point at the same variable. What prints, and why is this allowed even though one is *const and the other is *mut?`,
    hints: [
      'Both pointers refer to the same address.',
      'Raw pointers are exempt from the aliasing rules that references follow.',
    ],
    solution: `It prints 15. Both r1 and r2 hold the address of num, so when *r2 += 10 writes through the mutable raw pointer, num becomes 15, and reading *r1 then sees 15. This is allowed because raw pointers, unlike references, are NOT subject to the borrow checker's rule against having a mutable and an immutable alias to the same data at once. The tradeoff is that the compiler no longer protects you: it is now your responsibility to ensure these accesses do not cause a data race or other undefined behavior, which is exactly why the dereferences must sit in an unsafe block.`,
    tags: ['unsafe', 'raw-pointers', 'predict-output'],
  },
  {
    id: 'rs-ch19-t-015',
    chapter: 19,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'extern Blocks and the C ABI',
    prompt: `Explain the meaning of each part of this declaration: extern "C" { fn abs(input: i32) -> i32; }. Why must calls to abs be wrapped in unsafe, and what does the "C" string specify?`,
    hints: [
      'The block declares functions defined elsewhere.',
      'Rust cannot verify the foreign function upholds its guarantees.',
    ],
    solution: `The extern block declares the signatures of functions that are defined OUTSIDE Rust, here the C library function abs, so Rust can link to and call them. The "C" string names the Application Binary Interface (ABI) to use, telling the compiler how to lay out arguments and the call at the machine level; "C" is the most common ABI because nearly every language can speak it. Every call into such a foreign function must be inside an unsafe block because the Rust compiler cannot check that the foreign code upholds Rust's safety guarantees; you are vouching for it. So extern declares the contract, the ABI fixes the calling convention, and unsafe acknowledges Rust cannot verify the other side.`,
    tags: ['ffi', 'extern', 'unsafe'],
  },
  {
    id: 'rs-ch19-t-016',
    chapter: 19,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'A Supertrait Requirement',
    prompt: `You write trait OutlinePrint: fmt::Display { ... } and inside OutlinePrint you call self.to_string(). Explain what the : fmt::Display part declares, and what error a user gets if they try to implement OutlinePrint for a type that does not implement Display.`,
    hints: [
      'A supertrait is a trait your trait depends on.',
      'The bound is enforced at the impl site.',
    ],
    solution: `The : fmt::Display syntax declares Display as a SUPERTRAIT of OutlinePrint, meaning any type that implements OutlinePrint must also implement Display. This is what lets OutlinePrint's default methods rely on Display functionality such as to_string(), because the compiler can now guarantee that method exists. If a user tries impl OutlinePrint for Point without also implementing Display for Point, the compiler rejects it with an error saying Point does not satisfy the Display bound required by OutlinePrint. The fix is to also write impl fmt::Display for Point, after which the OutlinePrint impl is accepted.`,
    tags: ['supertraits', 'traits', 'display'],
  },
  {
    id: 'rs-ch19-t-017',
    chapter: 19,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Associated Function Name Clash',
    prompt: `Both the trait Animal and the type Dog define an ASSOCIATED function baby_name with no self parameter. Writing Animal::baby_name() fails to compile. Explain why removing self changes how you disambiguate, and write the fully qualified syntax that calls Animal's version for Dog.`,
    hints: [
      'With no self, there is no receiver to infer the type from.',
      'Fully qualified syntax names both the type and the trait.',
    ],
    solution: `When a method takes &self you can disambiguate by passing the receiver, as in Animal::eat(&dog), and Rust infers the type from it. An ASSOCIATED function like baby_name has no self, so there is nothing for the compiler to use to figure out which implementation you mean, and Animal::baby_name() is ambiguous. You must use fully qualified syntax, which names the concrete type as well as the trait: <Dog as Animal>::baby_name(). The general form is <Type as Trait>::function(args), and it is needed precisely whenever Rust cannot otherwise tell which implementation to call.`,
    tags: ['fully-qualified-syntax', 'associated-functions', 'traits'],
  },
  {
    id: 'rs-ch19-t-018',
    chapter: 19,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'The Never Type and panic in a match',
    prompt: `In a match, one arm is Some(v) => v and another is None => panic!("no value"). The first arm produces an i32, yet the whole match still type-checks even though the two arms seem to produce different types. Using the never type, explain how this works.`,
    hints: [
      'panic! has the special type written with an exclamation mark.',
      'The never type coerces to any other type.',
    ],
    solution: `Expressions that never return a value, such as panic!, continue, and a loop with no break, have the special never type written ! ("the never type" or "empty type"). The key rule is that ! can coerce into ANY other type. So in the match, the None arm has type !, which the compiler happily treats as i32 to agree with the Some(v) arm, letting the whole match settle on i32. This is exactly why arms that panic, continue a loop, or return early are allowed alongside arms that yield real values; the never type bends to whatever the rest of the expression needs.`,
    tags: ['never-type', 'match', 'coercion'],
  },
  {
    id: 'rs-ch19-t-019',
    chapter: 19,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Function Name as a Function Pointer',
    prompt: `You can write list.iter().map(|x| x.to_string()) using a closure. The book shows you can also write list.iter().map(ToString::to_string). Explain why passing the function name works here, and contrast what map receives in each case.`,
    hints: [
      'map wants something implementing FnMut.',
      'A function pointer implements all three closure traits.',
    ],
    solution: `map accepts any value whose type implements FnMut (specifically a function from the item to a new value). In the closure version map receives a closure value, and in the second version map receives a function pointer to ToString::to_string. Passing the function name works because a function pointer fn type implements all three closure traits Fn, FnMut, and FnOnce, so it satisfies map's bound directly without wrapping it in a closure. The second form is just a more concise way of saying "call this function on each element," and the two compile to the same behavior.`,
    tags: ['function-pointers', 'closures', 'map'],
  },
  {
    id: 'rs-ch19-t-020',
    chapter: 19,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Why str Is a DST',
    prompt: `The type str (note: not &str) is a dynamically sized type, and you cannot hold a value of type str directly in a variable like let s: str = ...;. Explain what "dynamically sized" means here and why &str works fine even though str does not.`,
    hints: [
      'Different strings have different lengths.',
      'A &str stores more than just an address.',
    ],
    solution: `str is dynamically sized because its size depends on the actual text, and Rust does not know that size at compile time, so it cannot decide how much stack space a plain str value would need; that is why let s: str is rejected. The trick is that a &str is a FAT pointer: it stores both the address of the bytes AND the length, so the reference itself has a known, fixed size (two usizes) even though the data behind it does not. This pattern, putting a dynamically sized value behind a pointer that carries the extra metadata, is how Rust handles all DSTs, including trait objects behind &dyn Trait or Box<dyn Trait>.`,
    tags: ['dst', 'str', 'sized'],
  },
  {
    id: 'rs-ch19-t-021',
    chapter: 19,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Comparing Two Solutions to External Trait Plus External Type',
    prompt: `You need Vec<i32> to implement an external trait Summary. Coworker A proposes editing the standard library, Coworker B proposes the newtype wrapper struct MyVec(Vec<i32>). Evaluate both ideas: which is actually possible in Rust, and what is the practical downside of the workable one?`,
    hints: [
      'You cannot change crates you do not own.',
      'The wrapper is a brand-new type, not a Vec.',
    ],
    solution: `Coworker A's idea is impossible: you cannot edit or re-impl on a crate you do not own, and the orphan rule blocks implementing an external trait on an external type directly. Coworker B's newtype is the workable solution, because MyVec is a local type so impl Summary for MyVec is allowed. The practical downside is that MyVec is a genuinely new type and does NOT automatically have Vec's methods; to use them you reach inside with self.0 or implement Deref<Target = Vec<i32>> so method calls forward to the inner vector. So the newtype gives you the impl you need at the cost of a thin layer of forwarding boilerplate.`,
    tags: ['newtype', 'orphan-rule', 'compare-approaches'],
  },
  {
    id: 'rs-ch19-t-022',
    chapter: 19,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Declarative Versus Procedural Macros',
    prompt: `The chapter describes two broad families of macros: declarative (macro_rules!) and procedural. In a few sentences, describe what input each family fundamentally operates on, and name the three kinds of procedural macros the book lists.`,
    hints: [
      'One matches token patterns; the other runs code over a token stream.',
      'Custom derive, attribute-like, and function-like.',
    ],
    solution: `Declarative macros, written with macro_rules!, work by pattern-matching against the structure of the Rust code (tokens) passed to them and substituting a replacement, much like a match expression operating on syntax. Procedural macros are more like functions: they take a stream of tokens (a TokenStream) as input, run arbitrary Rust code to transform it, and produce a TokenStream as output. The book lists three kinds of procedural macros: custom derive macros (for #[derive(...)]), attribute-like macros (which define new attributes usable on items), and function-like macros (which look like function calls). The key contrast is pattern substitution versus code that programmatically generates code.`,
    tags: ['macros', 'procedural', 'declarative'],
  },
  {
    id: 'rs-ch19-t-023',
    chapter: 19,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Reading a Macro Fragment Specifier',
    prompt: `In a macro arm the matcher is ( $x:expr ) and the body is { println!("{}", $x); }. Explain what $x and the :expr part mean, and predict the expansion of the call show!(2 + 3).`,
    hints: [
      'The dollar-name captures a piece of syntax.',
      'expr is a fragment specifier for an expression.',
    ],
    solution: `In a macro matcher, a name beginning with a dollar sign, like $x, is a metavariable that captures a chunk of the input code, and the part after the colon is a fragment specifier saying what kind of syntax it must be; :expr means "match any Rust expression." So this arm matches one expression and binds it to $x. The call show!(2 + 3) captures the expression 2 + 3 as $x, and the macro expands to println!("{}", 2 + 3);, which prints 5. The fragment specifier is what tells the macro parser how much input the metavariable should consume.`,
    tags: ['macros', 'fragment-specifier', 'macro-rules'],
  },
  {
    id: 'rs-ch19-t-024',
    chapter: 19,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Why the FFI Wrapper Function Itself Is unsafe',
    prompt: `Compare two ways to expose the C function abs. Version 1: a public Rust function call_abs(x: i32) -> i32 { unsafe { abs(x) } } with only the FFI call wrapped. Version 2: leaving every call site to write unsafe { abs(x) }. Discuss when wrapping the FFI call in a SAFE function is justified and when it is not, using the idea of upheld invariants.`,
    hints: [
      'A safe wrapper is a promise that the function is always sound to call.',
      'abs has no preconditions, but many C functions do.',
    ],
    solution: `Wrapping an FFI call in a safe function is justified only when YOU can guarantee the foreign function is sound for every possible input, because a safe signature is a promise to all callers that nothing can go wrong. abs is a good candidate: it has no preconditions and never reads invalid memory, so call_abs can soundly present a safe API, keeping the single unsafe block tiny and centralized. It would NOT be justified for a C function with preconditions, such as one that dereferences a pointer the caller must ensure is valid; hiding that behind a safe wrapper would let safe code trigger undefined behavior, so such a function should stay unsafe (or validate its inputs first). The rule is: only mark a wrapper safe if you have actually upheld every invariant the foreign function requires.`,
    tags: ['ffi', 'unsafe', 'safe-abstraction'],
  },
  {
    id: 'rs-ch19-t-025',
    chapter: 19,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Designing Add for Mixed Units',
    prompt: `You have struct Millimeters(u32) and struct Meters(u32) and want millimeters + meters to produce Millimeters. Explain which type the impl Add goes on, what Rhs and Output you choose, and why you could NOT also get meters + millimeters for free from the same impl.`,
    hints: [
      'add(self, rhs) takes self as the left operand.',
      'Add is not automatically symmetric.',
    ],
    solution: `Because the left operand of + becomes self in add(self, rhs), and you want Millimeters + Meters, the impl goes on Millimeters: impl Add<Meters> for Millimeters with type Output = Millimeters. Here Rhs is overridden from its default Self to Meters, and Output is Millimeters, so the body converts the meters to millimeters and adds. This impl ONLY defines Millimeters + Meters; it says nothing about Meters + Millimeters, because the Add trait is not symmetric and the compiler does not derive the reversed operation. To support meters + millimeters you must write a second, separate impl Add<Millimeters> for Meters. This shows that operator overloading is per-(left type, right type) pair, giving you precise control but no automatic commutativity.`,
    tags: ['operator-overloading', 'add', 'design'],
  },
  {
    id: 'rs-ch19-t-026',
    chapter: 19,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'The Implicit Sized Bound',
    prompt: `Every generic function fn foo<T>(t: T) secretly has the bound T: Sized added by the compiler. The book shows you can opt out with fn foo<T: ?Sized>(t: &T). Explain why the default exists, why opting out forces the parameter to be a reference (or other pointer), and what ?Sized means.`,
    hints: [
      'Most types have a known size; the compiler assumes it.',
      'A DST cannot be passed by value.',
    ],
    solution: `By default every generic type parameter gets an implicit T: Sized bound because the vast majority of types have a size known at compile time, and Rust needs that to generate code that passes values by value on the stack. Writing T: ?Sized relaxes this: the ? before Sized means "T may or may not be Sized," allowing dynamically sized types like str or dyn Trait to be used for T. But because a DST has no compile-time size, you cannot take it by value, so the parameter must be placed behind a pointer such as &T or Box<T>, whose size IS known. So ?Sized widens what types are allowed at the cost of requiring the value to live behind a pointer. Note ?Sized is unusual: it loosens a bound rather than adding one.`,
    tags: ['sized', 'dst', 'generics'],
  },
  {
    id: 'rs-ch19-t-027',
    chapter: 19,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Type Alias to Tame a Verbose Type',
    prompt: `A module repeatedly uses the type Box<dyn Fn() + Send + 'static>, in a field, several function parameters, and a return type, which is noisy and error-prone. Explain how a type alias improves this, and clarify whether the alias creates any new type-safety boundary or runtime cost.`,
    hints: [
      'type Thunk = Box<dyn Fn() + Send + ...>;',
      'An alias is a pure synonym.',
    ],
    solution: `You define one alias, for example type Thunk = Box<dyn Fn() + Send + 'static>;, and then write Thunk everywhere the long type appeared, in the field, parameters, and return type. This cuts repetition, makes signatures readable, and means a future change to the underlying type happens in a single place. Crucially, the alias is a pure synonym: Thunk and the full Box<...> are the SAME type, so values interchange freely, the alias adds no new type-safety boundary, and there is zero runtime cost. It is purely a compile-time readability and maintenance aid, in contrast to the newtype pattern, which would create a distinct type and a real boundary.`,
    tags: ['type-alias', 'readability', 'design'],
  },
  {
    id: 'rs-ch19-t-028',
    chapter: 19,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Returning Different Closures From One Function',
    prompt: `A function should return one of TWO different closures depending on a boolean argument, where the two closures capture different variables and so have different types. Explain why returning impl Fn would fail here and how Box<dyn Fn(...)> solves it.`,
    hints: [
      'impl Trait means one single concrete hidden type.',
      'Trait objects erase the concrete type behind a pointer.',
    ],
    solution: `Returning impl Fn promises ONE specific concrete type that the compiler infers and hides, so every return path must produce the same closure type. Two closures that capture different variables are different anonymous types, so the two branches violate that single-type requirement and impl Fn fails to compile. The fix is to return a trait object: Box<dyn Fn(...)>, which erases the concrete type behind a pointer, allowing each branch to box its own distinct closure (Box::new(...)) and return them as the same Box<dyn Fn> type. The trade-off is a heap allocation and dynamic dispatch versus impl Trait's static dispatch, but it is exactly what you need when the returned type genuinely varies at runtime.`,
    tags: ['closures', 'box', 'dyn', 'impl-trait'],
  },
  {
    id: 'rs-ch19-t-029',
    chapter: 19,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Why a Macro Cannot Be an Ordinary Function',
    prompt: `vec! can be called as vec![1, 2, 3] or vec![0; 5] and accepts any number of elements of any type, producing code that pushes each one. Explain two capabilities a macro_rules! macro has that an ordinary generic function does not, which is why vec! must be a macro.`,
    hints: [
      'Functions have a fixed number of parameters.',
      'Macros run at compile time before type checking and generate code.',
    ],
    solution: `First, a macro can accept a VARIABLE number of arguments (variadics) via repetition patterns like ( $( $x:expr ),* ), whereas an ordinary function must declare a fixed number of parameters, so vec![1,2,3] versus vec![0;5] could not be one function. Second, a macro is expanded at compile time and GENERATES code, so it can emit a sequence of push statements (one per element) and even define a different number of statements depending on the input, which a runtime function cannot do because it cannot change the program's structure. A macro can also be defined and used flexibly without the strict signature and type rules functions follow at the call site. These metaprogramming powers are precisely why vec! is implemented as a macro rather than a function.`,
    tags: ['macros', 'variadic', 'compare-approaches'],
  },
  {
    id: 'rs-ch19-t-030',
    chapter: 19,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Implementing an Unsafe Trait',
    prompt: `Some traits are declared with the unsafe keyword, for example unsafe trait Foo { }, and you implement them with unsafe impl Foo for Bar { }. Using Send and Sync as the motivating example, explain what it means for a TRAIT to be unsafe and why the unsafe keyword appears on the IMPL rather than only at the call site.`,
    hints: [
      'The trait carries an invariant the compiler cannot verify.',
      'The promise is made when you implement it.',
    ],
    solution: `A trait is marked unsafe when it has an invariant that the compiler cannot check but that other (safe) code relies on for soundness; implementing it is a promise that your type upholds that invariant. Send and Sync are the classic examples: they assert a type is safe to move to, or share between, threads, which the compiler usually derives automatically but cannot verify for a type built from raw pointers. Because the promise is made at the moment you provide the implementation, the unsafe keyword goes on the impl (unsafe impl), signaling that YOU are vouching for the invariant; there is nothing for a caller to mark, since using a Send type across threads is ordinary safe code that simply trusts your impl. In short, the unsafe is on the impl because that is where the unverifiable guarantee is being asserted.`,
    tags: ['unsafe', 'unsafe-trait', 'send-sync'],
  },
]

export default problems
