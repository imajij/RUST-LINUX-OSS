import type { Problem } from '../../types'

const problems: Problem[] = [
  {
    id: 'lx-ch16-t-001',
    chapter: 16,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'Why Rust In The Kernel At All',
    prompt: `A C kernel developer asks why anyone would add a second language to a project that has worked in C for decades. In a short paragraph, give the main argument for allowing Rust in the Linux kernel and name the broad class of bugs it is meant to address.`,
    hints: ['Think about memory safety and what a compiler can prove.', 'Whole bug classes, not just individual bugs.'],
    solution: `The core argument is that a large fraction of serious kernel bugs and security vulnerabilities are memory-safety errors: use-after-free, buffer overflows, data races, and uninitialized memory. C cannot catch these at compile time, so they slip into production and are exploited. Rust enforces ownership, borrowing, and lifetime rules at compile time, which eliminates most of those bug classes for safe code without a garbage collector and without runtime overhead. The goal is not to rewrite the kernel but to let new drivers and subsystems be written in a language where the compiler rules out an entire category of mistakes, improving safety and reducing the long-term maintenance and security burden.`,
    tags: ['rust-for-linux', 'memory-safety', 'motivation'],
  },
  {
    id: 'lx-ch16-t-002',
    chapter: 16,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'What The kernel Crate Is',
    prompt: `When you write Rust kernel code you write "use kernel::prelude::*" instead of pulling in the C headers directly. Explain in plain terms what the kernel crate is and why a Rust driver leans on it rather than calling raw C functions everywhere.`,
    hints: ['It is the standard library for Rust kernel code.', 'It offers safe, idiomatic wrappers.'],
    solution: `The kernel crate is effectively the standard library for Rust code running inside Linux. Because Rust kernel code is no_std (it cannot use the normal std that assumes an operating system underneath), the kernel crate provides the types, traits, and helpers a driver needs: error types, allocation, synchronization primitives, the module macro, and safe wrappers over kernel facilities. A driver uses it instead of calling raw C directly because the crate exposes idiomatic, mostly safe Rust APIs, hiding the unsafe foreign-function calls behind reviewed abstractions. This keeps driver code clean and lets the unsafe interaction with C live in one audited place rather than scattered through every driver.`,
    tags: ['rust-for-linux', 'kernel-crate', 'no_std'],
  },
  {
    id: 'lx-ch16-t-003',
    chapter: 16,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'Bindings Versus Abstractions',
    prompt: `In Rust-for-Linux people talk about "bindings" and "abstractions" as two different layers. Describe what each layer is and how they relate, using the C kernel's spinlock as an example.`,
    hints: ['Bindings are auto-generated and unsafe.', 'Abstractions are hand-written safe wrappers on top.'],
    solution: `Bindings are the raw, machine-generated declarations of the C kernel's functions, structs, and constants, produced by a tool like bindgen so Rust can call into C at all. They mirror C exactly and are inherently unsafe: calling spin_lock through a binding is just as unguarded as in C. Abstractions are hand-written safe Rust types built on top of those bindings; for the spinlock there is a SpinLock type whose lock method returns a guard, so the data is only reachable while held and is released automatically on drop. So bindings give access to C, and abstractions wrap that access in an API that the borrow checker and ownership rules can make safe to use.`,
    tags: ['rust-for-linux', 'bindings', 'abstractions'],
  },
  {
    id: 'lx-ch16-t-004',
    chapter: 16,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'What The module! Macro Saves You',
    prompt: `A Rust kernel module is declared with the module! macro that takes fields like type, name, author, description, and license. Explain what this macro generates for you and why it is preferable to writing that glue by hand.`,
    hints: ['It wires up init and exit entry points.', 'It records metadata the kernel expects.'],
    solution: `The module! macro is the entry point declaration for a loadable Rust module. From the fields you provide it generates the low-level glue the kernel needs: the module init and exit functions with the correct C-visible symbols, the registration that ties your type's lifecycle to load and unload, and the metadata such as name, author, description, and license that the kernel and tools read. Writing this by hand would mean manually exporting symbols, matching C calling conventions, and embedding metadata in special sections, all error-prone and unsafe. The macro lets you declare intent once and have the boilerplate produced correctly, so you focus on the module's actual behavior.`,
    tags: ['rust-for-linux', 'module-macro', 'metadata'],
  },
  {
    id: 'lx-ch16-t-005',
    chapter: 16,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'The Module Trait As A Lifecycle Contract',
    prompt: `The module! macro expects you to name a type that implements the Module trait, whose init function returns Result<Self>. Explain how implementing Module ties an object's lifetime to the module being loaded and unloaded.`,
    hints: ['init runs on insmod, returns your state in Self.', 'Drop runs on rmmod.'],
    solution: `The Module trait makes your module a normal Rust object with a clear lifecycle. When the module is loaded, the generated init code calls Module::init, which constructs and returns an instance of your type wrapped in Result; that returned value is stored and represents the live module. Anything the module owns, such as registered devices or allocated buffers, lives inside that struct. When the module is unloaded, the stored instance is dropped, so your type's Drop implementation runs cleanup automatically. This binds resource setup to load and teardown to unload through ownership, which is safer than the C pattern of manually pairing an init function with a matching exit function.`,
    tags: ['rust-for-linux', 'module-trait', 'lifecycle'],
  },
  {
    id: 'lx-ch16-t-006',
    chapter: 16,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Why init Returns A Result',
    prompt: `In C, a module init function returns an int where zero means success and a negative errno means failure, and the developer must remember to undo partial setup on the error paths. Explain why having Module::init return Result<Self> is a cleaner contract.`,
    hints: ['Result forces the caller to handle failure.', 'Ownership handles partial cleanup.'],
    solution: `Returning Result<Self> makes success and failure two distinct, type-checked outcomes rather than a magic integer you might forget to check. On success you hand back a fully constructed Self that owns all the resources the module set up; on failure you return an Err and the half-built value is simply never produced, so there is nothing partially registered to leak. Because resources are owned by values, the question mark operator can propagate an error mid-init and any already-constructed locals are dropped automatically, running their cleanup. This removes the classic C bug of forgetting a goto-style unwind on an error path, since the compiler and ownership rules do the unwinding for you.`,
    tags: ['rust-for-linux', 'result', 'error-handling'],
  },
  {
    id: 'lx-ch16-t-007',
    chapter: 16,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'no_std And The Missing Standard Library',
    prompt: `Rust kernel code is compiled with no_std, so the usual std library is unavailable. Explain why std cannot be used in the kernel and name the kind of facilities the kernel crate must therefore provide instead.`,
    hints: ['std assumes a hosted OS, syscalls, threads, a heap allocator.', 'The kernel is that OS.'],
    solution: `The Rust std library assumes it is running as a normal userspace program on top of an operating system: it uses syscalls for I/O, a process heap, OS threads, and unwinding-based panics. Inside the kernel none of those assumptions hold, because the kernel is the thing providing such services and must control allocation and concurrency itself. So kernel Rust uses core (and a kernel-specific allocation layer) instead of std. The kernel crate then supplies the equivalents the kernel can offer: a kernel allocator and collection types, synchronization primitives like mutexes and spinlocks, error handling, and printing, all implemented in terms of the kernel's own infrastructure rather than userspace facilities.`,
    tags: ['rust-for-linux', 'no_std', 'kernel-crate'],
  },
  {
    id: 'lx-ch16-t-008',
    chapter: 16,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Kernel Error Types Versus Panicking',
    prompt: `A newcomer suggests handling an out-of-memory situation in a Rust driver by calling unwrap, reasoning that a panic is simple. Explain why returning a kernel error type is strongly preferred over panicking in kernel code.`,
    hints: ['A panic in the kernel is far worse than in userspace.', 'Allocation can legitimately fail.'],
    solution: `In userspace a panic ends one process, but in the kernel a panic can take down the whole machine or leave it in an inconsistent state, so it must be reserved for truly unrecoverable bugs. Conditions like allocation failure are expected and recoverable, so they should be reported, not treated as crashes. The kernel crate models these as an Error type carrying an errno-like code, returned inside Result, so a driver can propagate the failure with the question mark operator and let the caller back out gracefully. Using unwrap converts a normal, handleable failure into a kernel panic, which is both unsafe in practice and contrary to how kernel APIs are expected to behave under memory pressure.`,
    tags: ['rust-for-linux', 'error-handling', 'panic'],
  },
  {
    id: 'lx-ch16-t-009',
    chapter: 16,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Where Unsafe Is Allowed To Live',
    prompt: `Rust-for-Linux does not claim that no unsafe code exists in the kernel. Explain the intended placement of unsafe in the architecture, and why concentrating it there is better than spreading it out.`,
    hints: ['Unsafe lives inside abstractions, not in driver code.', 'Audit surface shrinks.'],
    solution: `Unsafe is unavoidable because the kernel does inherently unsafe things and must call into C, but the design pushes that unsafe code down into the abstraction layer. Each safe wrapper contains the unsafe calls needed to talk to C and is responsible for upholding the invariants those calls require, so the driver author above it writes only safe Rust. Concentrating unsafe this way means the dangerous code lives in a small, well-reviewed set of abstractions rather than being copied into every driver. That shrinks the audit surface dramatically: reviewers can scrutinize the handful of unsafe blocks in the wrappers, and bugs in safe driver code cannot, by construction, cause memory unsafety.`,
    tags: ['rust-for-linux', 'unsafe', 'abstractions'],
  },
  {
    id: 'lx-ch16-t-010',
    chapter: 16,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Reading An errno Through Rust Eyes',
    prompt: `The kernel crate exposes constants like EINVAL and ENOMEM and an Error type. Explain how a Rust driver typically produces such an error and how that maps back to what the C side ultimately sees.`,
    hints: ['Err(EINVAL) or returning the code via question mark.', 'The boundary converts Error to a negative int.'],
    solution: `In Rust kernel code you signal failure by returning an Err carrying one of the kernel error constants, for example Err(EINVAL) for a bad argument or Err(ENOMEM) for allocation failure, or by letting the question mark operator propagate an error from a called function. These map to the same numeric errno values the C kernel uses. At the boundary where Rust hands control back to C, the abstraction or generated glue converts the Error into the conventional negative integer return that C kernel code expects, since C functions report errors as negative errno. So the Rust side works with a rich Result type, while the C side still sees the familiar negative-errno convention.`,
    tags: ['rust-for-linux', 'errno', 'error-handling'],
  },
  {
    id: 'lx-ch16-t-011',
    chapter: 16,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Why A Safe Wrapper Returns A Guard',
    prompt: `The Rust Mutex and SpinLock abstractions give you the protected data only through a guard object returned by lock, rather than letting you touch the data and separately call unlock. Explain what bug class this guard design prevents.`,
    hints: ['You cannot access the data without holding the lock.', 'Unlock happens on drop.'],
    solution: `By returning a guard, the lock makes access to the protected data conditional on holding the lock: you cannot reach the data except through the guard you get from lock, so it is impossible to read or write it while unlocked. The guard owns the locked state and releases it automatically when it goes out of scope via Drop, so you cannot forget to unlock and you cannot unlock twice. This prevents the classic C bugs of accessing shared data without taking the lock, leaking the lock by returning early on an error path, or double-unlocking. The compiler enforces correct lock discipline structurally instead of relying on the programmer to pair lock and unlock by hand.`,
    tags: ['rust-for-linux', 'mutex', 'raii'],
  },
  {
    id: 'lx-ch16-t-012',
    chapter: 16,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'How To Start Contributing',
    prompt: `Someone with Rust experience wants to start contributing to Rust-for-Linux but is intimidated. Outline a realistic first set of steps and the kind of early contribution that is welcome, given the project's current status.`,
    hints: ['Build a kernel with Rust enabled, read the docs, follow the mailing list.', 'Abstractions and small fixes are good entry points.'],
    solution: `A realistic start is to build a recent kernel with Rust support enabled, run the sample modules, and read the project's documentation and the Documentation/rust files in the tree. Following the relevant mailing lists and the rust-for-linux discussion channels shows what is actively being worked on and which abstractions are still missing. Good early contributions include improving documentation, fixing small bugs, writing or extending an abstraction for a C subsystem that lacks one, or porting a simple driver. Because the effort is young and many subsystems still have no Rust abstractions, there is real, mergeable work for newcomers; the key is to follow the kernel's patch-and-review workflow and to keep unsafe code minimal and well-justified.`,
    tags: ['rust-for-linux', 'contributing', 'status'],
  },
  {
    id: 'lx-ch16-t-013',
    chapter: 16,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Why Not Rewrite The Whole Kernel',
    prompt: `A blog commenter argues that if Rust is safer, Linux should just be rewritten in Rust. Explain why the Rust-for-Linux project deliberately does not pursue a wholesale rewrite and what its actual scope is.`,
    hints: ['Millions of lines of battle-tested C exist.', 'Incremental, new code first.'],
    solution: `Linux is tens of millions of lines of mature, heavily tested C that works and is maintained by a huge community, so rewriting it would discard enormous proven value, introduce fresh bugs, and is simply infeasible in practice. The project's scope is instead incremental: keep the existing C core, and allow new code, primarily drivers and self-contained subsystems, to be written in Rust where the safety benefits are highest. Rust and C coexist in the same kernel, with Rust calling into C through abstractions. This lets the kernel gain memory-safety benefits in the areas most prone to bugs without betting the entire system on a risky rewrite.`,
    tags: ['rust-for-linux', 'scope', 'interop'],
  },
  {
    id: 'lx-ch16-t-014',
    chapter: 16,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Designing A Safe Wrapper For A C Resource',
    prompt: `You are wrapping a C kernel object that is created with foo_create returning a pointer and destroyed with foo_destroy. Describe how you would design a safe Rust abstraction around it and which Rust feature ensures the destroy call is never forgotten or doubled.`,
    hints: ['Own the raw pointer in a struct, implement Drop.', 'Construction validates; Drop frees exactly once.'],
    solution: `You would define a Rust struct that owns the raw pointer returned by foo_create, with the unsafe creation call hidden inside a constructor that checks for null and returns Result so failure is reported rather than producing an invalid object. The struct's methods expose only safe operations, each performing the necessary unsafe C call internally while upholding the object's invariants. The key feature is Drop: you implement Drop to call foo_destroy exactly once when the owner goes out of scope, and because Rust moves rather than copies the owner and guarantees a single drop, the destroy call cannot be forgotten or run twice. The result is an RAII handle where lifetime of the C resource follows the lifetime of the Rust value.`,
    tags: ['rust-for-linux', 'abstractions', 'drop'],
  },
  {
    id: 'lx-ch16-t-015',
    chapter: 16,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'What Makes A Wrapper Unsound',
    prompt: `A wrapper hands out a plain Rust reference &T into a C-owned buffer, but the C side may free that buffer at any time from another context. Explain why this abstraction is unsound even though every individual line looks fine.`,
    hints: ['Safe Rust assumes a reference stays valid for its lifetime.', 'C can invalidate it behind Rust’s back.'],
    solution: `Safe Rust references carry a hard guarantee: while a &T exists, the referent stays valid and (for shared references) is not mutated in conflicting ways. If the wrapper produces such a reference into memory that C can free or relocate independently, that guarantee can be violated without any unsafe block in the caller, because the caller is allowed to assume the reference is good for its whole lifetime. A use-after-free then becomes reachable from entirely safe code, which is exactly what soundness forbids. The wrapper is unsound because it exports a safe API that does not actually uphold the invariant the type system relies on; the unsafe code inside it made a promise to callers that it cannot keep. A correct design must tie the reference's lifetime to something that keeps the buffer alive, or expose the data through a guarded, lifetime-bound API.`,
    tags: ['rust-for-linux', 'soundness', 'unsafe'],
  },
  {
    id: 'lx-ch16-t-016',
    chapter: 16,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Send, Sync, And Kernel Concurrency',
    prompt: `The kernel runs the same code across CPUs and contexts, and Rust expresses thread-safety with the Send and Sync marker traits. Explain why correctly choosing whether a kernel abstraction is Send and/or Sync is a safety decision, not a cosmetic one.`,
    hints: ['Send = can move to another thread; Sync = can be shared by reference across threads.', 'A wrong impl lets safe code create a data race.'],
    solution: `Send means a value may be moved to another execution context, and Sync means a shared reference to it may be used from multiple contexts at once; together they are how Rust knows what is safe to use concurrently. The compiler relies on these traits to permit or forbid sharing across CPUs, so a wrapper that is wrongly marked Send or Sync tells the compiler a lie. If an abstraction over a non-thread-safe C object is incorrectly declared Sync, safe code can then access it from several CPUs simultaneously and create a data race with no unsafe block in sight. Choosing these impls therefore decides whether the kernel's concurrency is actually sound, so they must reflect the real thread-safety of the underlying object, often justified with an explicit unsafe impl and a safety comment.`,
    tags: ['rust-for-linux', 'send-sync', 'concurrency'],
  },
  {
    id: 'lx-ch16-t-017',
    chapter: 16,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Reference Counting The Rust Way',
    prompt: `Much kernel C uses kref-style manual reference counting with explicit get and put calls. Compare that with how a Rust abstraction would expose a refcounted object, and explain what kind of mistake the Rust approach removes.`,
    hints: ['Think of an Arc-like type whose clone is get and drop is put.', 'No manual pairing of get/put.'],
    solution: `A Rust abstraction wraps the refcounted object in an Arc-like smart pointer where cloning the pointer performs the get (increment) and dropping it performs the put (decrement), with the final drop triggering the object's release. The programmer never calls get or put directly; they just clone when they need another owner and let values go out of scope when done. This removes the classic manual-refcount bugs of forgetting a put (a leak) or doing an extra put (a premature free and use-after-free), because increments and decrements are now driven by ownership and Drop rather than by the developer remembering to pair them. The count is therefore correct by construction for any code path the compiler accepts.`,
    tags: ['rust-for-linux', 'refcount', 'arc'],
  },
  {
    id: 'lx-ch16-t-018',
    chapter: 16,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'When To Reach For Unsafe Yourself',
    prompt: `You are writing a brand-new abstraction and the safe APIs in the kernel crate do not yet cover the C function you must call. Explain when it is appropriate to write your own unsafe block, and what obligations come with doing so.`,
    hints: ['Unsafe is fine inside a reviewed abstraction with documented invariants.', 'You must prove the safety contract holds.'],
    solution: `It is appropriate to write unsafe when you are building the abstraction layer itself and there is genuinely no safe wrapper for the C facility you need; that is exactly where unsafe is meant to live. The obligation is that you become responsible for the invariants the unsafe operation requires: you must ensure pointers are valid, lifetimes are respected, and concurrency rules hold, and you must document this with a safety comment explaining why the call is sound. You then expose only a safe API on top, so callers cannot misuse it. What is not appropriate is reaching for unsafe in ordinary driver code to bypass the borrow checker for convenience; that defeats the entire point and should instead motivate adding a proper safe abstraction.`,
    tags: ['rust-for-linux', 'unsafe', 'abstractions'],
  },
  {
    id: 'lx-ch16-t-019',
    chapter: 16,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Crossing The FFI Boundary Both Ways',
    prompt: `Rust kernel code both calls into C and is called from C (for example, callbacks the C core invokes). Explain the two directions of this interop and one hazard that is specific to being called from C.`,
    hints: ['Rust to C: through bindings/abstractions. C to Rust: extern "C" callbacks.', 'C may pass you a raw or null pointer with no Rust guarantees.'],
    solution: `In the outward direction, Rust calls C functions through the generated bindings, usually hidden behind a safe abstraction so driver code stays safe. In the inward direction, the C core calls back into Rust: Rust exposes functions with the C ABI (extern "C") that the kernel invokes for things like probe, file operations, or interrupt handlers. The hazard specific to being called from C is that the arguments arrive with no Rust guarantees at all: a pointer might be null, dangling, unaligned, or aliased, and there is no borrow checker on the C side to have ensured otherwise. So the entry-point shim must treat those inputs with suspicion, validate them, and convert raw pointers into safe references only after checking the invariants, before handing control to the safe Rust body.`,
    tags: ['rust-for-linux', 'ffi', 'interop'],
  },
  {
    id: 'lx-ch16-t-020',
    chapter: 16,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Why Pin Shows Up In Kernel Abstractions',
    prompt: `Some kernel abstractions wrap data in Pin and forbid moving it after creation. Without going deep into the type, explain at a conceptual level why an abstraction over certain C objects must promise the object never moves in memory.`,
    hints: ['C structures may be linked into lists or referenced by their address.', 'Moving them would invalidate those addresses.'],
    solution: `Many C kernel objects are self-referential or are registered by address: they get linked into intrusive lists, stored in trees, or handed to the C core which remembers their pointer. Such an object must stay at one fixed memory address for as long as it is registered, because something elsewhere holds that address. Plain Rust values, however, can be moved freely (a move is just a memory copy), which would leave those external pointers dangling. Pinning expresses the promise that, once created, the value will not move, so the abstraction can safely register the object's address with C. That is why abstractions over address-sensitive C structures expose them as pinned: it encodes the no-move invariant the C side depends on.`,
    tags: ['rust-for-linux', 'pin', 'invariants'],
  },
  {
    id: 'lx-ch16-t-021',
    chapter: 16,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Two Drivers, One Abstraction Decision',
    prompt: `You could either give each new Rust driver its own ad hoc unsafe calls into a C subsystem, or invest in one shared safe abstraction over that subsystem. Discuss the trade-off and explain which choice the project's philosophy favors and why.`,
    hints: ['Shared abstraction costs more up front but pays off across drivers.', 'Ad hoc unsafe spreads the audit burden.'],
    solution: `Ad hoc unsafe in each driver is faster to write for the first driver but spreads unsafe code and its invariants across every consumer, multiplying the audit burden and the chance that one driver gets the safety contract subtly wrong. A shared safe abstraction costs more up front to design and review, but afterward every driver gets a safe, idiomatic API and the dangerous code exists in exactly one reviewed place. The project's philosophy strongly favors the shared abstraction, because its whole value proposition is keeping driver code safe and concentrating unsafe in a small, audited layer. So unless something is a genuine one-off, the right move is to build (or extend) the abstraction, which also benefits the next contributor who needs the same subsystem.`,
    tags: ['rust-for-linux', 'abstractions', 'design'],
  },
  {
    id: 'lx-ch16-t-022',
    chapter: 16,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Status Honesty: What Rust Cannot Do Yet',
    prompt: `A manager wants to ship a major production subsystem entirely in Rust next quarter and asks if the kernel's Rust support is ready. Give a balanced, accurate picture of the current status and set realistic expectations.`,
    hints: ['Rust support is merged but still maturing; abstraction coverage is partial.', 'Drivers exist but breadth is limited.'],
    solution: `Rust support is officially in the mainline kernel and is real, with working sample modules and a growing number of actual drivers, but it is still maturing rather than a drop-in replacement for C across the board. The biggest practical limit is abstraction coverage: many C subsystems still have no safe Rust bindings, so writing in Rust there would mean building those abstractions first. Tooling, supported architectures, and the set of stabilized APIs are also still expanding. A realistic expectation is that targeted new drivers in areas with existing abstractions are feasible, but committing a large production subsystem entirely to Rust on a tight deadline would likely require significant abstraction work and carries schedule risk; an incremental approach is safer.`,
    tags: ['rust-for-linux', 'status', 'expectations'],
  },
  {
    id: 'lx-ch16-t-023',
    chapter: 16,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'A Safe API That Is Still Unsound',
    prompt: `A reviewer rejects an abstraction whose every public function is marked safe, saying it is unsound. The author protests that no caller can write unsafe code. Explain how an all-safe API can still be unsound, and what the real test of soundness is.`,
    hints: ['Soundness is about whether safe code can trigger UB, not about keywords.', 'The internal unsafe must uphold its contract for all inputs.'],
    solution: `Marking functions safe only declares that callers need no unsafe block; it does not make the code actually safe. An API is unsound if there exists any sequence of calls using only safe code that can trigger undefined behavior. That can happen when the unsafe operations inside the abstraction rely on invariants the public API does not actually guarantee, so an ordinary caller can drive the object into a state where an internal unsafe call dereferences a bad pointer, races, or violates aliasing. The real test of soundness is therefore not whether callers write unsafe, but whether the abstraction's internal unsafe code upholds its safety contract for every possible safe use, including adversarial sequences, error paths, and concurrency. If a safe caller can ever reach UB, the abstraction is unsound regardless of how its signatures are annotated.`,
    tags: ['rust-for-linux', 'soundness', 'review'],
  },
  {
    id: 'lx-ch16-t-024',
    chapter: 16,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Panic Strategy Inside The Kernel',
    prompt: `Rust normally responds to a failed bounds check or a failed unwrap by panicking. In the kernel this is dangerous, yet the language still has panics. Discuss how kernel Rust treats panics and why the design still pushes hard toward Result-based error handling.`,
    hints: ['A kernel panic is roughly oops/abort, not graceful.', 'Recoverable conditions must not panic; reserve panic for genuine bugs.'],
    solution: `In the kernel a Rust panic cannot unwind to a recovering caller the way it might in userspace; it maps to a kernel-level failure (an oops or abort) that may kill the task or destabilize the system, so it is effectively a last resort for unrecoverable logic bugs. The kernel build is configured so panics abort rather than unwind, which keeps behavior predictable. Because of this severity, the design treats panicking paths, like unwrap, indexing that can go out of bounds, or unchecked arithmetic assumptions, as code smells in normal flow, and pushes recoverable conditions (allocation failure, bad input, device errors) through Result with kernel error codes. The aim is that panics should fire only when an invariant the programmer guaranteed has actually been broken, signaling a real bug, while everything that can legitimately fail is handled explicitly and gracefully.`,
    tags: ['rust-for-linux', 'panic', 'error-handling'],
  },
  {
    id: 'lx-ch16-t-025',
    chapter: 16,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Modeling C Lifetimes That Rust Cannot See',
    prompt: `A C API hands your callback a pointer that is valid only for the duration of that one call and must not be stored. Explain how a Rust abstraction can encode this "valid only during the call" rule so that safe code physically cannot stash the pointer, and why a raw pointer field would fail.`,
    hints: ['Use a borrowed reference whose lifetime is bounded by the call.', 'Storing it would outlive the borrow and not compile.'],
    solution: `The abstraction should turn the transient C pointer into a Rust reference whose lifetime is tied to the callback invocation, for example by passing a borrow whose lifetime parameter cannot escape the function call. Because the borrow's lifetime ends when the call returns, safe code that tried to store it in a longer-lived structure would not compile: the borrow checker sees the reference outliving its permitted lifetime and rejects it. This encodes the "valid only during the call" rule directly in the type system, so the prohibition is mechanically enforced rather than left to documentation. A raw pointer field would fail because raw pointers carry no lifetime information; the compiler would happily let you store it, and it would dangle the moment C reclaimed the memory, reintroducing exactly the use-after-free the abstraction was supposed to prevent.`,
    tags: ['rust-for-linux', 'lifetimes', 'abstractions'],
  },
  {
    id: 'lx-ch16-t-026',
    chapter: 16,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'The Trust Boundary Of An Abstraction',
    prompt: `Explain the idea that each safe abstraction defines a "trust boundary," and use it to argue why a single incorrect unsafe block in one abstraction can undermine the safety guarantees of completely unrelated, fully safe driver code.`,
    hints: ['Safe callers trust the abstraction to have upheld its contract.', 'UB is global; one unsound spot can corrupt anything.'],
    solution: `A safe abstraction is a contract: callers above it write safe code and trust that the abstraction has correctly discharged all the unsafe obligations beneath it. That trust is what lets the compiler reason about safe code as if no unsafe existed. But undefined behavior is not locally contained; if one unsafe block gets its contract wrong, it can corrupt memory, violate aliasing, or race, and the consequences (a freed object reused, a stack smashed, an optimizer assumption broken) can manifest anywhere in the kernel, including in unrelated drivers that never touched the buggy code. So the safety of all the safe code resting on an abstraction is only as strong as that abstraction's weakest unsafe block. This is precisely why unsafe must be minimal, concentrated, and rigorously reviewed: a single unsound spot breaks the guarantee globally, not just locally.`,
    tags: ['rust-for-linux', 'soundness', 'trust-boundary'],
  },
  {
    id: 'lx-ch16-t-027',
    chapter: 16,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Interior Mutability Under Kernel Locks',
    prompt: `Rust's borrow rules forbid mutating data through a shared reference, yet kernel data behind a lock is shared by many CPUs and still mutated. Explain conceptually how a safe lock abstraction reconciles these two facts and why ordinary &mut sharing would be wrong here.`,
    hints: ['The lock provides interior mutability: shared outside, exclusive inside the guard.', 'The lock, not the borrow checker, enforces exclusivity at runtime.'],
    solution: `The lock type provides interior mutability: the lock itself is shared (many CPUs hold a shared reference to it), but the protected data is only reachable through the guard returned by lock, and that guard grants exclusive, mutable access for as long as it is held. So exclusivity is enforced at runtime by the lock rather than statically by a single &mut, which is what makes it sound to mutate shared data. Ordinary &mut sharing would be wrong because you cannot hand a unique mutable borrow to many CPUs at once; the whole point is that several contexts hold the data concurrently and take turns. The abstraction bridges this by combining a runtime mutual-exclusion mechanism with the guard's compile-time exclusive borrow, so the compiler still prevents you from using the data outside the lock while the lock provides the actual serialization across CPUs.`,
    tags: ['rust-for-linux', 'interior-mutability', 'locking'],
  },
  {
    id: 'lx-ch16-t-028',
    chapter: 16,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'When A Drop That Can Fail Meets The Kernel',
    prompt: `Rust runs Drop automatically and Drop cannot return an error or be skipped, but some kernel teardown operations can fail or must happen in a specific context (for example, not in atomic context). Discuss the tension this creates for abstraction designers and one reasonable way to handle it.`,
    hints: ['Drop is infallible and may run anywhere; some cleanup needs to report errors or run in process context.', 'Offer an explicit close that returns Result, with Drop as a best-effort fallback.'],
    solution: `Automatic Drop is wonderful for guaranteeing cleanup, but it is infallible and the abstraction cannot fully control where it runs, so cleanup that can fail or that must occur in a particular context (such as something that may sleep and therefore cannot run in atomic context) does not map cleanly onto Drop. The tension is that Drop will silently run teardown wherever the value happens to be dropped and has no way to report a problem. A reasonable design is to provide an explicit consuming method, for example a close or shutdown that takes the value by value and returns Result, so callers who care can perform teardown in the right context and observe failures; Drop then remains as a best-effort safety net that frees resources but may only log or ignore errors. This gives correctness-conscious callers a fallible, context-aware path while still guaranteeing that nothing leaks if they forget.`,
    tags: ['rust-for-linux', 'drop', 'design'],
  },
  {
    id: 'lx-ch16-t-029',
    chapter: 16,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Why Coexistence Beats A Rust-Only Boundary',
    prompt: `Some proposals imagine isolating Rust code behind a strict boundary so it never directly shares data structures with C. Argue why deep coexistence, where Rust abstractions wrap and embed real C structures, is the approach Rust-for-Linux actually takes, and what the cost of that choice is.`,
    hints: ['Drivers must integrate with C subsystems and their structs to be useful.', 'The cost is that abstractions must precisely model C invariants.'],
    solution: `A driver is only useful if it plugs into the existing C subsystems, which means registering C structures, being called through C callback tables, and sharing data the C core owns; a strict isolation boundary would prevent exactly this integration and make Rust drivers second-class or impossible. So Rust-for-Linux chooses deep coexistence: abstractions directly embed and wrap real C structures and participate in C lifecycles, while presenting safe APIs upward. The cost is that each abstraction must faithfully and completely model the C side's invariants, including initialization order, locking rules, lifetime and pinning requirements, and concurrency expectations, because the safety of the safe API depends on getting that model exactly right. This is harder and demands careful review, but it is the price of letting Rust be a first-class participant in the kernel rather than a sandboxed island.`,
    tags: ['rust-for-linux', 'coexistence', 'interop'],
  },
  {
    id: 'lx-ch16-t-030',
    chapter: 16,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Evaluating The Long-Term Case For Rust',
    prompt: `A skeptic concedes Rust prevents memory bugs but argues the cost (a second toolchain, a steeper contributor learning curve, and the work of building abstractions) outweighs the benefit for a project as mature as Linux. Construct a reasoned response that takes the costs seriously while making the long-term case.`,
    hints: ['Weigh up-front and ongoing costs against the lifetime cost of memory-safety CVEs.', 'Consider where the benefit is concentrated and that it compounds.'],
    solution: `The costs are real and should not be dismissed: maintaining a Rust toolchain alongside C, training reviewers who must judge both unsafe Rust and C, and the labor of building abstractions before drivers can be written are genuine ongoing burdens, and Rust does not fix logic bugs or design errors. The counterweight is that memory-safety defects dominate serious kernel vulnerabilities and each one carries a heavy lifetime cost in incident response, patching, and exploitation, so eliminating whole classes of them has compounding value as more code is written. The benefit is concentrated where it matters most, in new drivers and subsystems that are bug-prone and security-sensitive, and the abstraction investment is paid once but reused by every later driver. A reasoned conclusion is that the strategy is justified precisely because it is incremental and targeted: the kernel keeps its proven C core, pays the Rust cost only where the safety return is highest, and lets the benefits accumulate over time rather than betting everything on a rewrite.`,
    tags: ['rust-for-linux', 'tradeoffs', 'motivation'],
  },
]

export default problems
