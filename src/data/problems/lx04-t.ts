import type { Problem } from '../../types'

const problems: Problem[] = [
  {
    id: 'lx-ch04-t-001',
    chapter: 4,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'What A File Descriptor Really Is',
    prompt: `A new programmer says "a file descriptor is the file itself." Explain why that is inaccurate. What does the small non-negative integer returned by open() actually denote, and where does the real per-open state (like the current offset and the access mode) live?`,
    hints: [
      'The integer is just an index.',
      'Think about a per-process table pointing at kernel objects.',
    ],
    solution: `A file descriptor is a small non-negative integer that indexes into the process's file descriptor table; it is a handle, not the file. The fd entry points to a kernel "open file description" (struct file) which holds the per-open state: the current offset, the access mode (read/write), and the status flags (e.g. O_NONBLOCK, O_APPEND). That open file description in turn refers to the underlying file (its inode). So the integer is just a reference: closing it removes the table entry, and the same numeric value can later refer to a completely different file. Two different fds can even share one open file description (after dup), or two opens of the same path produce two independent offsets.`,
    tags: ['files', 'fd', 'vfs'],
  },
  {
    id: 'lx-ch04-t-002',
    chapter: 4,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'The Three Standard Descriptors',
    prompt: `Every freshly started program normally has three file descriptors already open: 0, 1, and 2. Name what each conventionally is, and explain why a printf and a perror can end up in different places on your terminal even though both "go to the screen".`,
    hints: [
      'stdin, stdout, stderr.',
      'Two of them are output, but distinct streams.',
    ],
    solution: `By convention fd 0 is standard input (stdin), fd 1 is standard output (stdout), and fd 2 is standard error (stderr). printf writes to fd 1 while perror writes to fd 2. On a plain terminal both happen to appear on the screen because both descriptors point at the terminal device, but they are independent descriptors: if you redirect stdout to a file (program > out.txt) the printf output goes into the file while the perror message still appears on the terminal. Keeping errors on fd 2 is what lets users separate normal output from diagnostics.`,
    tags: ['files', 'fd', 'stdio'],
  },
  {
    id: 'lx-ch04-t-003',
    chapter: 4,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'Reading Permission Bits As rwx',
    prompt: `ls -l shows a file as "-rw-r--r--". Read this string aloud: what is the file type, and what may the owner, the group, and others each do? Then state which single octal number encodes those nine permission bits.`,
    hints: [
      'The first character is the type, then three groups of three.',
      'r=4, w=2, x=1 per group.',
    ],
    solution: `The leading "-" means it is a regular file (a directory would show "d", a symlink "l"). The next nine characters are three triplets owner/group/other: "rw-" means the owner can read and write but not execute; "r--" means the group can only read; "r--" means others can only read. Converting each triplet with r=4, w=2, x=1: owner is 4+2=6, group is 4, other is 4, so the mode is 644 (octal). That is the typical permission set for a non-executable data file.`,
    tags: ['permissions', 'mode', 'octal'],
  },
  {
    id: 'lx-ch04-t-004',
    chapter: 4,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'Everything Is A File',
    prompt: `Linux is often described with the slogan "everything is a file." Reading /proc/cpuinfo with the same read() you would use on a text file is an example. Explain in your own words what this slogan really means for the programmer and give two kinds of things, besides regular files, that you can talk to through this uniform interface.`,
    hints: [
      'It is about a uniform API, not about literal disk files.',
      'Think devices, pipes, sockets, kernel info under /proc and /sys.',
    ],
    solution: `The slogan means that a wide variety of resources expose the same uniform interface: you obtain a file descriptor and then use open/read/write/close (and sometimes lseek) on it, regardless of what sits behind it. The kernel's VFS layer hides the differences. Besides regular disk files, this interface also covers device nodes (e.g. /dev/null or a disk under /dev), pipes between processes, kernel-exported information under /proc and /sys, and network sockets. The payoff is that one small set of syscalls and one mental model work everywhere, so tools like cat can read a sysfs attribute and a real file with identical code.`,
    tags: ['vfs', 'files', 'proc'],
  },
  {
    id: 'lx-ch04-t-005',
    chapter: 4,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Why open() Can Fail With A Valid Path',
    prompt: `You call open("/etc/shadow", O_RDONLY) as a normal (non-root) user and get -1 with errno set. The path clearly exists. Give two distinct reasons open() can fail even though the named file is really there, and explain how the program is supposed to learn which one happened.`,
    hints: [
      'Existence is not the same as access rights.',
      'open returns -1 and sets errno.',
    ],
    solution: `Existence does not guarantee you may open it. Common reasons include: permission denied (errno EACCES) because the file's mode and your uid/gid do not grant the requested access; too many open descriptors (EMFILE for this process or ENFILE system-wide); a component of the path is not a directory or lacks search (x) permission; or the file is a special file that cannot be opened in the requested mode. The program learns which happened by checking that open returned -1 and then reading errno (e.g. printing it with perror or strerror). For /etc/shadow as a normal user the typical result is EACCES, since only privileged users may read it.`,
    tags: ['open', 'errno', 'permissions'],
  },
  {
    id: 'lx-ch04-t-006',
    chapter: 4,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'The mode Argument To open()',
    prompt: `Two students call open("log.txt", O_WRONLY | O_CREAT, 0644). One insists the new file will always have permissions 644. Explain when the mode argument is even consulted, why the resulting permissions may not be exactly 0644, and what would happen if O_CREAT were omitted.`,
    hints: [
      'The third argument matters only when a file is created.',
      'Remember the umask.',
    ],
    solution: `The third (mode) argument is consulted only when open actually creates a new file, i.e. when O_CREAT is given and the file did not already exist; if the file already exists, mode is ignored and the existing permissions stay. Even when it is used, the final permissions are mode AND-NOT umask: the process umask clears bits, so with the common umask 022 a requested 0644 yields 0644 (no change there) but a requested 0666 would become 0644. If O_CREAT were omitted entirely, the mode argument is meaningless and, if the file does not exist, open fails with ENOENT rather than creating it.`,
    tags: ['open', 'mode', 'umask'],
  },
  {
    id: 'lx-ch04-t-007',
    chapter: 4,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'O_TRUNC Versus O_APPEND',
    prompt: `Compare opening an existing non-empty file with O_WRONLY | O_TRUNC against opening it with O_WRONLY | O_APPEND. Describe what each does to the existing contents and where the next write lands, and give a situation where choosing the wrong one silently destroys data.`,
    hints: [
      'One empties the file; one seeks to the end before every write.',
      'Think about a shared log file.',
    ],
    solution: `O_TRUNC truncates the file to zero length at open time, so the previous contents are immediately discarded and the offset starts at 0. O_APPEND keeps the existing contents and arranges that every write atomically seeks to the current end of file first, so new data is added after what is already there. A classic data-loss mistake is opening a log file with O_TRUNC (or plain "w" in fopen terms) when you meant to keep history: the moment you open it the old log is gone. O_APPEND is also the safe choice when several processes write the same log, because each write goes to the true end without races over the offset.`,
    tags: ['open', 'flags', 'append'],
  },
  {
    id: 'lx-ch04-t-008',
    chapter: 4,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'read() Returns Zero Versus Negative One',
    prompt: `A read loop on a file descriptor can return a positive number, 0, or -1. Explain what each of these three return values means, and why treating a return of 0 the same as an error is a bug.`,
    hints: [
      'Positive is a byte count.',
      '0 is end of file, -1 is an error with errno.',
    ],
    solution: `A positive return is the number of bytes actually read into the buffer (which may be fewer than you asked for). A return of 0 means end of file: there is no more data and there never will be on this descriptor, so the loop should stop normally. A return of -1 means a real error occurred and errno tells you which (for example EINTR or EBADF). Treating 0 as an error is wrong because EOF is the expected, successful way a file ends; and treating -1 as just "0 bytes" is wrong because you would lose the error and possibly spin forever. A correct loop continues on positive counts, stops cleanly on 0, and handles -1 (retrying on EINTR, failing otherwise).`,
    tags: ['read', 'eof', 'errno'],
  },
  {
    id: 'lx-ch04-t-009',
    chapter: 4,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Why close() Still Matters',
    prompt: `"The kernel closes all my file descriptors when the process exits, so calling close() is pointless." Explain why this reasoning is dangerous for a long-running program, and name two concrete bad outcomes of never closing descriptors you are done with.`,
    hints: [
      'Descriptors are a finite per-process resource.',
      'Think buffered writes and stale references.',
    ],
    solution: `It is true the kernel reclaims descriptors at exit, but a long-running program (a server, a daemon) may never exit for days, so leaked descriptors accumulate. Concrete harms: (1) descriptor exhaustion, the process hits its open-file limit and subsequent open/accept calls fail with EMFILE; (2) resources held open longer than needed, e.g. a deleted file's disk space is not freed until the last descriptor closes, or a device/lock stays busy. Closing also lets you detect write errors (close can surface a delayed error), and it lets dup2-style descriptor reuse work cleanly. So close() is part of correct resource management, not a no-op.`,
    tags: ['close', 'fd', 'resources'],
  },
  {
    id: 'lx-ch04-t-010',
    chapter: 4,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'What lseek Moves',
    prompt: `You open a 100-byte file, read 10 bytes, then call lseek(fd, 0, SEEK_SET). Explain exactly what state lseek changed, what the next read() will return, and clarify whether lseek touched any bytes on disk.`,
    hints: [
      'lseek changes the offset, not the data.',
      'SEEK_SET sets an absolute position.',
    ],
    solution: `lseek changes only the current file offset stored in the open file description, not the file's contents on disk. After reading 10 bytes the offset was 10; lseek(fd, 0, SEEK_SET) sets it back to absolute position 0. The next read() therefore starts again from the beginning of the file and returns the first bytes a second time. No data was written or moved; lseek is a pure bookkeeping operation on the offset, which is why it is cheap and why it can even position past end of file without doing any I/O.`,
    tags: ['lseek', 'offset', 'read'],
  },
  {
    id: 'lx-ch04-t-011',
    chapter: 4,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Hard Link Versus Symbolic Link, First Look',
    prompt: `You run "ln file a" and "ln -s file b". Both a and b let you reach the data of file. Explain the fundamental difference between what a (a hard link) and b (a symbolic link) actually contain, and what happens to each if you then delete the original name "file".`,
    hints: [
      'A hard link is another directory entry for the same inode.',
      'A symlink stores a path string.',
    ],
    solution: `A hard link (a) is simply another directory entry pointing at the very same inode as "file"; there is no notion of an "original" once it exists, and the two names are equal peers. A symbolic link (b) is a small special file whose contents are the path string "file"; it points by name, not by inode. If you delete "file": the hard link a still works, because the inode survives as long as its link count is above zero and a is still a reference to it. The symlink b becomes dangling/broken, because the name it stored no longer resolves, and opening b now fails with ENOENT.`,
    tags: ['links', 'inode', 'symlink'],
  },
  {
    id: 'lx-ch04-t-012',
    chapter: 4,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Inspecting Metadata With stat',
    prompt: `You want to know a file's size, its permission bits, and how many names point at it, without opening or reading its contents. Which call gives you this, and from its result, which field tells you the size, which tells you the type-and-permissions, and which tells you the number of hard links?`,
    hints: [
      'stat fills a struct of metadata.',
      'Look for st_size, st_mode, st_nlink.',
    ],
    solution: `stat (or fstat on an already-open fd, or lstat to inspect a symlink itself) fills in a struct stat with the file's metadata, without reading the data blocks. In that struct, st_size gives the size in bytes, st_mode encodes both the file type and the permission bits (you use the S_ISREG/S_ISDIR macros for type and mask with 0777 for the permission bits), and st_nlink gives the number of hard links (directory entries) referring to that inode. Other fields cover ownership (st_uid, st_gid) and timestamps. The key point is that all of this lives in the inode and stat reads it directly.`,
    tags: ['stat', 'metadata', 'inode'],
  },
  {
    id: 'lx-ch04-t-013',
    chapter: 4,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Reading A /sys Attribute Like A File',
    prompt: `To learn whether a network interface is up you can read /sys/class/net/eth0/operstate and get the text "up" plus a newline. A coworker is surprised there is no special API. Explain why a plain open/read works here, why you should read the whole short value in one go rather than seeking around, and why you generally do not cache the result.`,
    hints: [
      'sysfs exposes kernel values as tiny pseudo-files.',
      'Each read snapshots a live value.',
    ],
    solution: `sysfs presents kernel attributes as ordinary-looking pseudo-files, so the uniform VFS interface (open, read, close) just works, no special API needed. These attributes are typically one small value, generated on demand when you read; you open, read the short string (often ending in a newline) once, and close. Seeking and partial re-reading is usually pointless because the value is produced freshly each open, not stored as a seekable byte stream. You normally do not cache the result because it reflects live kernel state that can change at any moment; to see the current state you read the file again rather than trusting a stale copy.`,
    tags: ['sysfs', 'proc', 'vfs'],
  },
  {
    id: 'lx-ch04-t-014',
    chapter: 4,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Why A Single write() Is Not Enough',
    prompt: `A program does write(fd, buf, 4096) to a socket or pipe and assumes all 4096 bytes were sent because it did not get -1. Explain how this assumption breaks, what a "short write" is, and sketch the loop structure that correctly writes the entire buffer.`,
    hints: [
      'write may report fewer bytes than requested.',
      'Advance the pointer and decrease the count, then loop.',
    ],
    solution: `write() may perform a short write: it can return a positive count smaller than what you asked for, especially on pipes, sockets, or when interrupted, and that is not an error. So checking only for -1 misses the case where, say, only 1000 of 4096 bytes were accepted; the remaining 3096 bytes are silently lost if you do not retry. The correct pattern loops: keep a pointer and a remaining count, call write on what is left, and on a positive return advance the pointer by n and subtract n from the remaining count, repeating until remaining reaches 0. Handle -1 by retrying on EINTR and failing otherwise. In sketch: written = 0; while (written < len) { n = write(fd, buf+written, len-written); if (n < 0) { if (errno==EINTR) continue; else fail; } written += n; }`,
    tags: ['write', 'short-count', 'loops'],
  },
  {
    id: 'lx-ch04-t-015',
    chapter: 4,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Two Opens Versus One dup',
    prompt: `Compare two ways to have "two descriptors for the same file": (A) call open() on the same path twice, getting fd1 and fd2; (B) call open() once and then dup() it. In each case, if you read 10 bytes through the first descriptor, where is the offset for the second descriptor? Explain why the two cases differ.`,
    hints: [
      'open creates a new open file description each time.',
      'dup shares the existing open file description.',
    ],
    solution: `Each open() creates a brand-new open file description with its own offset, so in case A fd1 and fd2 have independent offsets: reading 10 bytes through fd1 leaves fd2's offset at 0, and a read through fd2 starts at the beginning. dup() does not create a new open file description; fd2 is a second descriptor pointing at the same open file description as fd1, so they share one offset. In case B, reading 10 bytes through fd1 advances the shared offset to 10, and a subsequent read through fd2 continues at offset 10. The difference is exactly whether the offset (and the status flags) are shared, which is determined by whether they reference the same struct file.`,
    tags: ['dup', 'open', 'offset'],
  },
  {
    id: 'lx-ch04-t-016',
    chapter: 4,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'How Shell Redirection Uses dup2',
    prompt: `The shell runs the command "ls > out.txt". Explain, in terms of file descriptors and dup2, the sequence the shell performs in the child process before exec so that ls's normal output lands in out.txt, even though ls itself just writes to fd 1.`,
    hints: [
      'Open the target file, then make fd 1 refer to it.',
      'dup2(newfd, 1) closes 1 and aliases it.',
    ],
    solution: `In the child (after fork, before exec) the shell opens out.txt for writing, getting some fd, say 3. It then calls dup2(3, 1), which closes the current fd 1 if open and makes fd 1 a copy referring to the same open file description as fd 3, so fd 1 now points at out.txt. It closes fd 3 since it is no longer needed (fd 1 already aliases the file). Finally it execs ls. Because exec keeps the descriptor table, ls inherits this arrangement and its ordinary writes to fd 1 (stdout) go straight into out.txt, all without ls knowing anything about redirection. That decoupling of the program from where stdout actually goes is exactly what redirection provides.`,
    tags: ['dup2', 'redirection', 'shell'],
  },
  {
    id: 'lx-ch04-t-017',
    chapter: 4,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'A Pipe Is A One-Way Byte Stream',
    prompt: `pipe(fds) gives you fds[0] for reading and fds[1] for writing. In "ls | wc -l", explain which process writes which end and reads which end, why both processes must close the pipe ends they do not use, and what a reader sees when all write ends are closed.`,
    hints: [
      'A pipe has a read end and a write end.',
      'EOF on the read end requires every write end to be closed.',
    ],
    solution: `A pipe is a unidirectional byte stream: fds[1] is the write end and fds[0] is the read end. For "ls | wc -l", ls writes its stdout into the write end, and wc reads its stdin from the read end (the shell wires these up with pipe and dup2). Each process must close the ends it does not use so that the descriptor reference counts are correct. Crucially, the reader only sees end of file (read returns 0) once every write end has been closed; if the writer process leaks an open write end, the reader can hang forever waiting for data that will never come. Conversely, writing to a pipe whose read end is all closed delivers SIGPIPE / EPIPE.`,
    tags: ['pipe', 'redirection', 'eof'],
  },
  {
    id: 'lx-ch04-t-018',
    chapter: 4,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Permissions Checked At Open, Not At Each Read',
    prompt: `A process opens a file for reading. While the file is still open, the owner runs chmod to remove read permission. The process keeps read()-ing successfully. Explain why removing the permission did not stop the ongoing reads, and what determines whether a future open() of the same file would succeed.`,
    hints: [
      'Access permission is checked when you open.',
      'The open file description already grants the mode.',
    ],
    solution: `The kernel checks permission at open() time, when it decides whether to grant a descriptor with the requested access mode. Once the open file description exists with read access, subsequent read() calls do not re-check the file's permission bits, so changing the mode afterward does not revoke the already-open descriptor; the reads keep working. What the chmod affects is future attempts: a new open() for reading would now consult the current (reduced) permission bits and fail with EACCES for a user no longer allowed to read. This is why "secure" code relies on the access decision made at open, not on the mode staying constant during use.`,
    tags: ['permissions', 'open', 'mode'],
  },
  {
    id: 'lx-ch04-t-019',
    chapter: 4,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Why Directory x Permission Is Surprising',
    prompt: `A directory has mode r-- (read but not execute) for you. You can run ls on it and see the names, but you cannot open or stat any file inside, even files you own. Explain the distinct roles of the r and x bits on a directory, and which one you actually need to reach a file inside.`,
    hints: [
      'On a directory, x means "search/traverse".',
      'r only lets you list names.',
    ],
    solution: `On a directory the permission bits mean something different from a regular file. The r bit lets you list the directory's entries (read the names), and the x (search/execute) bit lets you traverse into it, i.e. resolve a name within it to reach the inode behind it. With r but not x you can see the names via ls but cannot actually use any name, because every path lookup that descends through that directory needs the x bit; so open, stat, or cd on anything inside fails with EACCES even for files you own. To reach a file inside a directory you need x on the directory (and x on every ancestor directory in the path); r alone is not enough.`,
    tags: ['permissions', 'directory', 'mode'],
  },
  {
    id: 'lx-ch04-t-020',
    chapter: 4,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Blocking Versus Non-Blocking read',
    prompt: `Reading from a terminal or an empty pipe normally makes read() wait until data arrives. With O_NONBLOCK set, read() returns immediately. Explain the behavioral difference, what a non-blocking read returns when no data is ready, and why you must not confuse that with end of file.`,
    hints: [
      'Blocking puts the process to sleep until data is ready.',
      'Non-blocking returns -1 with EAGAIN, not 0.',
    ],
    solution: `In blocking mode (the default), if no data is currently available on something like a pipe, socket, or terminal, read() puts the process to sleep until data arrives (or EOF), then returns. With O_NONBLOCK the call never sleeps: if data is ready it returns it, but if no data is ready right now it returns -1 with errno EAGAIN (a.k.a. EWOULDBLOCK), meaning "try again later." This must not be confused with a return of 0, which means true end of file (no data ever again). So a robust non-blocking loop treats EAGAIN as "nothing yet, come back later" (often after waiting for readiness), while a 0 means stop. Mixing them up either causes a busy spin or a premature stop.`,
    tags: ['blocking', 'nonblocking', 'read'],
  },
  {
    id: 'lx-ch04-t-021',
    chapter: 4,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'lstat Versus stat On A Symlink',
    prompt: `You have a symlink "cur" pointing to "data-2026". Compare what stat("cur", &st) reports against lstat("cur", &st). Which one tells you that cur is itself a symlink, and which reports the size and type of the target? When would using the wrong one cause a bug?`,
    hints: [
      'stat follows the link; lstat does not.',
      'Think about a backup tool walking a tree.',
    ],
    solution: `stat() follows the symlink and reports metadata about the target ("data-2026"): its type (e.g. regular file or directory), its size, its permissions. lstat() does not follow the link; it reports metadata about the symlink itself, so st_mode shows it is a symlink (S_ISLNK is true) and st_size is the length of the stored path string. A tool that walks a directory tree must use lstat to detect symlinks; if it used stat instead it would see only the target and could, for example, follow a symlink that points back up the tree and loop forever, or recurse into a directory it should not have entered. So choosing stat vs lstat is exactly the choice of "follow the link or examine the link."`,
    tags: ['stat', 'symlink', 'metadata'],
  },
  {
    id: 'lx-ch04-t-022',
    chapter: 4,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Link Count And Deleting Open Files',
    prompt: `A program creates a temporary file, opens it, then immediately unlinks (removes) its name while keeping the descriptor open. The program keeps reading and writing the file successfully, and only when it exits is the space freed. Explain how this works in terms of the inode's link count and the open descriptor.`,
    hints: [
      'Unlink removes a name, not necessarily the inode.',
      'The inode survives while link count or open count is above zero.',
    ],
    solution: `unlink() removes a directory entry and decrements the inode's link count; it does not by itself destroy the file. An inode (and its data) is only actually freed when both its hard-link count reaches zero and no process still has it open. Here, after unlink the link count is 0, so the name is gone and no one else can reach the file by path, but the program's open descriptor keeps an internal reference to the inode, so the data stays alive and reads/writes continue to work normally. When the program exits (or calls close), that last reference drops, the open count hits zero, and the kernel finally reclaims the space. This is the classic "unlink an open temp file" trick: an invisible, self-cleaning scratch file.`,
    tags: ['unlink', 'inode', 'links'],
  },
  {
    id: 'lx-ch04-t-023',
    chapter: 4,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Why O_APPEND Beats lseek-Then-write',
    prompt: `Two processes both append lines to the same shared log file. One implementation does lseek(fd, 0, SEEK_END) followed by write(); the other opens with O_APPEND and just calls write(). Explain why the lseek-then-write version can corrupt the log under concurrency and why O_APPEND does not.`,
    hints: [
      'lseek and write are two separate syscalls.',
      'O_APPEND makes the seek-to-end and the write atomic.',
    ],
    solution: `In the lseek-then-write version there are two separate syscalls with a gap in between. Process A seeks to the end (say offset 1000), then before A's write runs, process B also seeks to 1000 and writes there; now both believe the end is 1000 and one write overwrites the other, corrupting or losing a record. With O_APPEND the kernel guarantees that each write atomically repositions to the current end of file and writes in one indivisible step, so two concurrent appends never target the same offset; they are serialized and both records survive intact. That is exactly why log files and other multi-writer files should be opened with O_APPEND rather than emulating append with a manual seek.`,
    tags: ['append', 'concurrency', 'atomicity'],
  },
  {
    id: 'lx-ch04-t-024',
    chapter: 4,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Sparse Files And Seeking Past The End',
    prompt: `You open a new empty file, lseek to offset 1,000,000, write one byte, and close. stat then reports st_size as 1000001, yet "du" reports the file uses almost no disk blocks. Explain how seeking past end of file plus a write produces this, and why the apparent size and the actual disk usage disagree.`,
    hints: [
      'lseek can move the offset beyond current end.',
      'Unwritten gaps become holes that consume no blocks.',
    ],
    solution: `lseek can position the offset beyond the current end of file without writing anything. When you then write one byte at offset 1,000,000, the file's logical size (st_size) becomes 1,000,001, because size is just the highest written position plus the bytes written. But the gap from offset 0 up to 1,000,000 was never written, so the filesystem does not allocate disk blocks for it; it records a hole. Reading that hole back yields zero bytes on demand without storing them. That is why st_size (a logical length) is large while du (which measures allocated blocks) shows almost nothing: this is a sparse file, and the two numbers measure different things, logical extent versus physical allocation.`,
    tags: ['lseek', 'sparse', 'stat'],
  },
  {
    id: 'lx-ch04-t-025',
    chapter: 4,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Handling EINTR In An I/O Loop',
    prompt: `A blocking read() on a slow device returns -1 with errno EINTR partway through, even though nothing is actually wrong. Explain what causes EINTR, why simply treating any -1 as fatal is incorrect, and how a correct loop distinguishes EINTR from a genuine error.`,
    hints: [
      'A signal can interrupt a blocked syscall.',
      'EINTR means "retry", real errors do not.',
    ],
    solution: `EINTR occurs when a signal is delivered to the process while it is blocked inside a slow syscall like read() on a pipe, terminal, or socket; the kernel returns early with -1 and errno EINTR so the signal handler can run, but no data error happened. Treating every -1 as fatal is wrong because EINTR is a benign "you were interrupted, try again" condition, not a failure of the device or descriptor. A correct loop checks errno: if it is EINTR it simply retries the read; for any other errno (EBADF, EIO, EFAULT, etc.) it reports a real error. So the rule is: on -1, look at errno; loop on EINTR, fail on the rest.`,
    tags: ['read', 'eintr', 'signals'],
  },
  {
    id: 'lx-ch04-t-026',
    chapter: 4,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Cross-Filesystem Hard Links And Why They Fail',
    prompt: `Creating a hard link with "ln a /othermount/b" fails with EXDEV ("Invalid cross-device link"), but a symlink to the same target works fine. Explain why hard links cannot cross filesystem boundaries while symlinks can, in terms of what each link actually references.`,
    hints: [
      'A hard link points to an inode number.',
      'Inode numbers are only meaningful within one filesystem.',
    ],
    solution: `A hard link is just another directory entry that refers to an inode by its inode number, and inode numbers are only unique and meaningful within a single filesystem. A directory on filesystem X therefore cannot hold an entry pointing at an inode that lives on filesystem Y, because the inode number would be ambiguous or nonexistent over there; the kernel rejects the attempt with EXDEV. A symbolic link sidesteps this entirely because it does not reference an inode number at all, it stores a path string. A path is resolved at use time by the VFS, so it can name a target on any mounted filesystem. That is the core trade-off: hard links are tight, inode-level aliases confined to one filesystem, while symlinks are loose, path-level pointers that span filesystems but break if the target name moves.`,
    tags: ['links', 'inode', 'filesystem'],
  },
  {
    id: 'lx-ch04-t-027',
    chapter: 4,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Why A /proc File Has Size Zero',
    prompt: `You can read /proc/meminfo with read(), but treating it like a normal byte-addressable file (seeking around assuming stable offsets, or expecting a fixed size from stat) leads to bugs. Explain why /proc entries are generated on the fly and what that implies for seeking, size, and re-reading.`,
    hints: [
      'proc files are produced by kernel functions, not stored bytes.',
      'st_size is often 0 and contents are a live snapshot.',
    ],
    solution: `Most /proc and /sys entries are not stored byte streams; their contents are produced by a kernel function each time you open and read them, reflecting live kernel state. So stat often reports st_size as 0 (or a placeholder) even though read returns plenty of bytes, because the real length is not known until the text is generated. Seeking to a saved offset and expecting the same bytes is unreliable: the underlying values can change between reads, and the generated text can shift in length, so offsets are not stable. The correct usage is to open, read the whole thing in one pass into a buffer, parse it, and close; to get fresh values you re-open and re-read rather than reusing offsets or caching. They behave like a live report, not a fixed file.`,
    tags: ['proc', 'sysfs', 'vfs'],
  },
  {
    id: 'lx-ch04-t-028',
    chapter: 4,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Race Between access() And open()',
    prompt: `A setuid program checks access(path, W_OK) to confirm the real user may write, and only then opens the file for writing. Explain the time-of-check-to-time-of-use race this creates, how an attacker can exploit it with a symlink, and why this pattern is considered unsafe.`,
    hints: [
      'There is a gap between the check and the use.',
      'access uses real uid; the path can be swapped to a symlink.',
    ],
    solution: `access() answers the question for the path as it exists at that instant, using the real uid, and then open() acts on the path again a moment later; in the gap between them the meaning of the path can change. This is a TOCTOU (time-of-check-to-time-of-use) race: an attacker who controls the directory can, after access succeeds on an innocent file, replace that name with a symlink pointing at a sensitive file (say /etc/passwd). The subsequent open then follows the symlink and writes where the attacker wants, with the program's elevated privileges. The pattern is unsafe because the check and the use are not atomic and the two calls can resolve the same path to different objects. Safer approaches drop privileges, open first and then check the opened descriptor (fstat on the fd), or use openat with O_NOFOLLOW so the check and the use refer to one object.`,
    tags: ['toctou', 'access', 'security'],
  },
  {
    id: 'lx-ch04-t-029',
    chapter: 4,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Shared Offset After Fork',
    prompt: `A process opens a file, then fork()s. Both parent and child write to that descriptor. Explain why their writes do not overwrite each other at offset 0 but instead advance through increasing offsets, and contrast this with what happens if each had independently opened the same path.`,
    hints: [
      'fork duplicates descriptors but not the open file description.',
      'A shared offset advances for both.',
    ],
    solution: `fork() duplicates the descriptor table, so the child's fd and the parent's fd both refer to the same open file description that existed before the fork. They therefore share one current offset. When the parent writes, the shared offset advances; when the child writes, it continues from that advanced offset, so their output lands at increasing positions and does not all stomp on offset 0. (The interleaving order between them is still nondeterministic, but the offset itself is shared and advances monotonically.) By contrast, if parent and child had each called open() on the same path independently, each would have its own open file description with its own offset starting at 0, so without O_APPEND their writes would both begin at 0 and overwrite each other. The deciding factor is whether the offset is shared, which fork preserves and a second open does not.`,
    tags: ['fork', 'offset', 'fd'],
  },
  {
    id: 'lx-ch04-t-030',
    chapter: 4,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Designing A Robust File Copy',
    prompt: `Design, at the level of syscalls and error handling, a correct routine that copies a file's bytes from one descriptor to another using only open, read, write, and close. Describe the loop, how you handle short reads and short writes, how you detect EOF versus error, and what you must do on close.`,
    hints: [
      'Outer loop on read; inner loop to fully write each chunk.',
      'Distinguish 0 (EOF), -1 (error), and positive counts.',
    ],
    solution: `Open the source with O_RDONLY and the destination with O_WRONLY | O_CREAT | O_TRUNC and a sensible mode (subject to umask). Loop: call read(src, buf, sizeof buf). If it returns 0, that is EOF, so stop successfully. If it returns -1, retry on EINTR else fail. On a positive count n, you must write exactly n bytes, which can take several writes because write may be short: run an inner loop that writes from buf+written for n-written bytes, advancing written by each positive return and retrying on EINTR, until written equals n (or a real write error fails). Repeat the outer read loop until EOF. Finally close both descriptors and check the close return value on the destination, because buffered or delayed write errors can surface only at close; if close reports an error the copy may be incomplete and should be treated as a failure. This carefully separates 0 (EOF, normal stop), -1 (error, with EINTR retried), and positive (data, possibly partial).`,
    tags: ['read', 'write', 'design'],
  },
]

export default problems
