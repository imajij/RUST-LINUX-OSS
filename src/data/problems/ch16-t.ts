import type { Problem } from '../../types'

const problems: Problem[] = [
  {
    id: 'rs-ch16-t-001',
    chapter: 16,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'What thread::spawn Returns',
    prompt: `You call thread::spawn(|| { /* some work */ }) and store the result in a variable called handle. What type of value is handle, and in one sentence, what is the main thing you can do with it later?`,
    hints: [
      'The closure runs on a brand new thread.',
      'Think about how the main thread waits for the new one.',
    ],
    solution: `thread::spawn returns a JoinHandle. It is an owned value that represents the newly spawned thread and lets the rest of your program keep a reference to that thread. The main thing you do with it is call handle.join() later, which blocks the calling thread until the spawned thread finishes its work. Without holding onto the handle you would have no way to wait for that thread to complete.`,
    tags: ['thread-spawn', 'join-handle', 'basics'],
  },
  {
    id: 'rs-ch16-t-002',
    chapter: 16,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'Why Join the Handle',
    prompt: `A beginner spawns a thread that prints the numbers 1 through 10, then immediately ends main() without calling join on the handle. They are surprised that only a few numbers print before the program exits. Explain in plain words why this happens.`,
    hints: [
      'What happens to spawned threads when main returns?',
      'join is what makes the main thread wait.',
    ],
    solution: `When the main thread finishes and main() returns, the whole program shuts down, and any threads that are still running are stopped immediately, whether or not they finished. The spawned thread runs concurrently with main, so there is no guarantee it gets to print all ten numbers before main exits. Calling handle.join() forces the main thread to pause and wait for the spawned thread to finish before continuing. That is exactly why join exists: it guarantees the spawned thread runs to completion.`,
    tags: ['thread-spawn', 'join-handle', 'basics'],
  },
  {
    id: 'rs-ch16-t-003',
    chapter: 16,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'What an mpsc Channel Is',
    prompt: `The letters mpsc stand for "multiple producer, single consumer." In one or two sentences, describe what the two halves returned by mpsc::channel() are called and what each half is for.`,
    hints: [
      'The function returns a pair of values.',
      'One end sends, the other receives.',
    ],
    solution: `mpsc::channel() returns a tuple of two values, conventionally named (tx, rx), short for transmitter and receiver. The transmitter is the sending end: you call tx.send(value) to put a value into the channel. The receiver is the consuming end: you call rx.recv() to take a value out. "Multiple producer, single consumer" means you can clone the transmitter so many threads send, but there is only one receiver taking values out.`,
    tags: ['channels', 'mpsc', 'basics'],
  },
  {
    id: 'rs-ch16-t-004',
    chapter: 16,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'What a Mutex Guards',
    prompt: `The name Mutex comes from "mutual exclusion." In plain language, what problem does a Mutex solve when several threads want to use the same piece of data, and what must a thread do before it is allowed to touch that data?`,
    hints: [
      'Only one thread at a time should be allowed in.',
      'Think about the word lock.',
    ],
    solution: `A Mutex lets multiple threads share one piece of data while making sure only one thread accesses it at any given moment, so they do not stomp on each other. Before a thread can read or change the protected data it must acquire the lock by calling lock(). While one thread holds the lock, any other thread that calls lock() waits until the first thread releases it. This mutual exclusion is what prevents two threads from changing the same value at the same time.`,
    tags: ['mutex', 'shared-state', 'basics'],
  },
  {
    id: 'rs-ch16-t-005',
    chapter: 16,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Move Closures and Borrowed Data',
    prompt: `This does not compile:

    let v = vec![1, 2, 3];
    let handle = thread::spawn(|| {
        println!("{:?}", v);
    });

The fix is to write thread::spawn(move || ...). Explain why the borrow checker rejects the version without move, and what the move keyword changes.`,
    hints: [
      'How long might the new thread live compared to v?',
      'A plain closure borrows; move takes ownership.',
    ],
    solution: `Without move, the closure only borrows v, but Rust cannot prove the spawned thread will finish before v goes out of scope in the outer function. If the borrow outlived v, the thread would hold a dangling reference, so the compiler refuses. Adding move forces the closure to take ownership of v, moving it into the thread. Now the data lives as long as the closure does, regardless of what the outer scope does, so there is no possibility of a dangling reference.`,
    tags: ['move-closure', 'thread-spawn', 'ownership'],
  },
  {
    id: 'rs-ch16-t-006',
    chapter: 16,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Using v After Moving It',
    prompt: `Suppose you fix the previous problem with move:

    let v = vec![1, 2, 3];
    let handle = thread::spawn(move || println!("{:?}", v));
    println!("{:?}", v);

Now a different error appears on the last line. What is wrong, and why does move cause it?`,
    hints: [
      'move transfers ownership of v into the closure.',
      'What is left in the outer scope after a move?',
    ],
    solution: `Once move takes ownership of v, the vector belongs to the closure and the outer scope no longer owns it. The final println! tries to use v in main after it has been moved out, which is a use-after-move error: the value is no longer valid in that scope. This is the normal ownership rule, not anything special to threads. To use the data in both places you would need to clone it before moving, or only access it through the thread.`,
    tags: ['move-closure', 'ownership', 'borrowing'],
  },
  {
    id: 'rs-ch16-t-007',
    chapter: 16,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'recv Blocks, the Channel Closes',
    prompt: `A thread is spawned that sends three values over tx and then the thread ends. The main thread calls rx.recv() in a loop forever. Describe what rx.recv() does on each of the first three calls, and what it returns once the sender end has been dropped.`,
    hints: [
      'recv waits when nothing is available yet.',
      'recv returns a Result.',
    ],
    solution: `rx.recv() blocks the calling thread until a value is available, then returns Ok(value) with the next item from the channel. So the first three calls each return Ok with one of the three sent values, waiting if necessary. Once every transmitter has been dropped and the channel is empty, recv() returns Err, because no more values can ever arrive. An infinite loop that ignores that Err would spin or panic, so you normally handle the Err by breaking out of the loop.`,
    tags: ['channels', 'recv', 'blocking'],
  },
  {
    id: 'rs-ch16-t-008',
    chapter: 16,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Iterating Over a Receiver',
    prompt: `Instead of calling rx.recv() in a manual loop, the book writes for received in rx { ... }. Explain what treating the receiver as an iterator does, including when the for loop ends.`,
    hints: [
      'A for loop over rx calls something like recv under the hood.',
      'What ends a normal iterator?',
    ],
    solution: `Treating rx as an iterator lets a for loop pull values out one at a time as they arrive; each iteration effectively waits for and yields the next received value. The loop blocks when nothing is available yet, just like recv. The iteration ends naturally when the channel closes, that is, when all transmitters have been dropped and no more values can come. This is cleaner than a manual loop because you do not have to match on Ok and Err yourself; the for loop simply stops when the channel is done.`,
    tags: ['channels', 'iterator', 'recv'],
  },
  {
    id: 'rs-ch16-t-009',
    chapter: 16,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'send Moves the Value',
    prompt: `In the book's channel example, after a thread runs tx.send(val).unwrap(), a later attempt to use val in that same thread would not compile. What does send do with the value it is given, and why does that protect you?`,
    hints: [
      'The value travels to another thread.',
      'Think about ownership crossing a thread boundary.',
    ],
    solution: `send takes ownership of the value and moves it into the channel, so it can be handed to whatever thread is receiving. After the move, the sending thread no longer owns val, so using it again is a use-after-move error. This is protective: if both threads could touch the same value, the receiver might read or modify it while the sender also did, causing a data race. By moving the value out, Rust guarantees only one side owns it at a time.`,
    tags: ['channels', 'send', 'ownership'],
  },
  {
    id: 'rs-ch16-t-010',
    chapter: 16,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Locking Returns a Result',
    prompt: `The book writes let mut num = m.lock().unwrap(); when using a Mutex. Why does lock() return a Result rather than the guarded data directly, and what situation does that Result protect against?`,
    hints: [
      'A thread holding the lock could panic.',
      'Think about what happens to the lock then.',
    ],
    solution: `lock() returns a Result because acquiring the lock can fail. If another thread panicked while holding the lock, the Mutex becomes "poisoned," because the protected data might be in a half-updated, inconsistent state. lock() then returns Err to warn you of that. Calling unwrap() says "I expect the lock to be healthy; panic if it is poisoned." So the Result exists specifically to surface the case where a previous lock holder crashed mid-update.`,
    tags: ['mutex', 'lock', 'poisoning'],
  },
  {
    id: 'rs-ch16-t-011',
    chapter: 16,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'The Guard and Automatic Unlock',
    prompt: `When you call m.lock().unwrap(), the value you get back is a MutexGuard, not the plain data. Two questions: how do you read or write the data through the guard, and when exactly is the lock released?`,
    hints: [
      'A guard behaves like a smart pointer to the inner value.',
      'Think about scope and Drop.',
    ],
    solution: `The MutexGuard acts like a smart pointer to the protected data: you read or write the inner value by dereferencing the guard, for example *num += 1. The lock is released automatically when the guard goes out of scope, because the guard's Drop implementation unlocks the Mutex for you. This means you never call an explicit unlock; you simply let the guard drop at the end of the block. It is the same RAII pattern that frees other resources at scope exit.`,
    tags: ['mutex', 'guard', 'drop'],
  },
  {
    id: 'rs-ch16-t-012',
    chapter: 16,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Cloning the Transmitter',
    prompt: `You want two separate spawned threads to both send messages into the same channel. The receiver rx can only be owned by one place. How do you give each sending thread its own way to send, and what does that have to do with the "mp" in mpsc?`,
    hints: [
      'You can make extra senders without making a new channel.',
      'Look at what clone does to a transmitter.',
    ],
    solution: `You call tx.clone() to make a second transmitter that feeds into the same channel, then move one transmitter into each thread. Both clones send into the one shared receiver. This is exactly the "multiple producer" part of mpsc: any number of cloned senders can push values, but there is still a single consumer reading them. The channel stays open as long as at least one transmitter exists, and closes only when every clone has been dropped.`,
    tags: ['channels', 'clone', 'multiple-producers'],
  },
  {
    id: 'rs-ch16-t-013',
    chapter: 16,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'join Returns the Thread Result',
    prompt: `A spawned thread's closure ends with the expression 42 (no semicolon), so the thread "produces" a value. When you later call handle.join(), what kind of value comes back, and how would you get the 42 out of it?`,
    hints: [
      'join can report that a thread panicked.',
      'The good case is wrapped in something.',
    ],
    solution: `handle.join() returns a Result. The Ok variant holds whatever value the thread's closure produced, so here it would be Ok(42); you get the 42 out by unwrapping or matching on that Result. The Err variant carries the panic payload if the thread panicked instead of finishing normally. So join both waits for the thread and tells you whether it succeeded, letting you recover the returned value or detect a crash.`,
    tags: ['join-handle', 'thread-spawn', 'result'],
  },
  {
    id: 'rs-ch16-t-014',
    chapter: 16,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Predict the Print Order',
    prompt: `Consider:

    let handle = thread::spawn(|| {
        for i in 1..5 { println!("spawned: {i}"); }
    });
    for i in 1..5 { println!("main: {i}"); }
    handle.join().unwrap();

Will the output be exactly main lines first then spawned lines, exactly interleaved, or something not guaranteed? Explain your reasoning.`,
    hints: [
      'The two threads run concurrently.',
      'Does join at the end control ordering of the prints above it?',
    ],
    solution: `The exact ordering is not guaranteed. Both loops run on separate threads at the same time, and the operating system scheduler decides when each thread actually runs, so the spawned and main lines may interleave in any order and differently on each run. Placing join after both loops only guarantees the program waits for the spawned thread before exiting; it does not order the prints that happen before it. The only thing you can rely on is that within a single thread its own lines stay in 1,2,3,4 order.`,
    tags: ['thread-spawn', 'scheduling', 'predict-output'],
  },
  {
    id: 'rs-ch16-t-015',
    chapter: 16,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Join in the Middle Kills Concurrency',
    prompt: `A developer moves handle.join().unwrap() to the line immediately after thread::spawn, before the main thread's own loop runs. How does this change the program's behavior, and why is it usually not what you want?`,
    hints: [
      'join blocks until the thread finishes.',
      'What runs while the main thread is blocked?',
    ],
    solution: `Calling join right after spawning makes the main thread block and wait for the spawned thread to completely finish before it runs its own loop. So instead of the two loops running concurrently, the spawned thread fully completes, then main proceeds, producing strictly sequential output. This usually defeats the purpose of spawning a thread at all, because you get no parallelism. You join early only when you specifically need that thread's work done before continuing.`,
    tags: ['join-handle', 'concurrency', 'scheduling'],
  },
  {
    id: 'rs-ch16-t-016',
    chapter: 16,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Why Rc Is Not Thread-Safe',
    prompt: `You try to share an Rc<Mutex<i32>> across several spawned threads by cloning the Rc and moving a clone into each thread. The compiler rejects it, mentioning that Rc cannot be sent between threads safely. Explain the underlying reason Rc is unsafe across threads, and what type fixes it.`,
    hints: [
      'Rc keeps a reference count it updates on clone and drop.',
      'Is that count updated atomically?',
    ],
    solution: `Rc maintains a reference count that it increments on clone and decrements on drop, but it updates that count with ordinary, non-atomic operations for speed. If two threads cloned or dropped the same Rc at once, those updates could interleave and corrupt the count, leading to a value freed too early or never freed. Rust encodes this by not implementing Send for Rc, so the compiler forbids moving it into a thread. The fix is Arc, the atomically reference counted pointer, whose count is updated with thread-safe atomic operations.`,
    tags: ['rc', 'arc', 'send', 'thread-safety'],
  },
  {
    id: 'rs-ch16-t-017',
    chapter: 16,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Why Arc and Mutex Together',
    prompt: `To build a counter that many threads increment, the book uses Arc<Mutex<i32>>, not just Arc<i32> and not just Mutex<i32>. Explain the distinct job each of the two wrappers does, and why neither one alone is enough.`,
    hints: [
      'One wrapper handles ownership across threads.',
      'The other handles safe mutation.',
    ],
    solution: `Arc handles shared ownership: it lets many threads each hold a clone pointing at the same value, with a thread-safe reference count keeping it alive until the last owner drops it. Mutex handles safe mutation: it ensures only one thread at a time can change the value, preventing data races. Arc<i32> alone gives shared ownership but no way to mutate, since Arc only hands out shared references. Mutex<i32> alone allows safe mutation but cannot be shared by multiple owning threads. Combining them, Arc<Mutex<i32>>, gives both shared ownership and exclusive, race-free mutation.`,
    tags: ['arc', 'mutex', 'shared-state', 'counter'],
  },
  {
    id: 'rs-ch16-t-018',
    chapter: 16,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Clone the Arc, Not the Mutex',
    prompt: `In the shared-counter example, inside the loop that spawns threads the code writes let counter = Arc::clone(&counter); before each spawn. What is actually being cloned here, what is shared rather than copied, and why is this clone necessary?`,
    hints: [
      'Cloning an Arc does not copy the inner data.',
      'Each thread needs its own owner of the pointer.',
    ],
    solution: `Arc::clone copies only the smart pointer and bumps the reference count; the Mutex and the integer inside it are not duplicated, they stay shared. Every thread that you move a clone into ends up pointing at the same single Mutex<i32>. The clone is necessary because each spawned thread takes ownership of whatever it captures, and a single Arc cannot be moved into many threads. Making a fresh clone per thread gives each one its own owning handle to the one shared value.`,
    tags: ['arc', 'clone', 'shared-state'],
  },
  {
    id: 'rs-ch16-t-019',
    chapter: 16,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Predict the Counter Total',
    prompt: `Ten threads are spawned; each one locks an Arc<Mutex<i32>>, adds 1, and the guard drops. The main thread joins all ten handles, then locks and prints the counter. What value is printed, and is it deterministic or subject to a race? Explain.`,
    hints: [
      'Each increment happens while holding the lock.',
      'Does locking remove the race condition?',
    ],
    solution: `The printed value is exactly 10, and it is deterministic. Although the threads run concurrently and may increment in any order, each increment is protected by the Mutex, so only one thread reads-and-writes the counter at a time. There is no lost update or torn value: every one of the ten additions is applied. Joining all the handles before printing guarantees all increments are finished. So the order is nondeterministic but the final total is reliably 10.`,
    tags: ['arc', 'mutex', 'counter', 'predict-output'],
  },
  {
    id: 'rs-ch16-t-020',
    chapter: 16,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'try_recv Versus recv',
    prompt: `A thread runs a game loop that must keep redrawing the screen many times per second, and it occasionally checks a channel for player input. Would you use rx.recv() or rx.try_recv() in that loop, and what is the key behavioral difference that drives the choice?`,
    hints: [
      'One of them blocks; the other does not.',
      'A game loop cannot afford to stop and wait.',
    ],
    solution: `You would use try_recv() in that loop. The key difference is that recv() blocks the thread until a value arrives, while try_recv() returns immediately: it gives Ok(value) if a message is waiting, or an Err right away if the channel is currently empty. A game loop must keep running and redrawing even when there is no input, so it cannot afford recv's blocking. With try_recv the loop peeks for a message, handles it if present, and otherwise continues drawing without stalling.`,
    tags: ['channels', 'try-recv', 'recv', 'design'],
  },
  {
    id: 'rs-ch16-t-021',
    chapter: 16,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'When the Receiver Loop Ends',
    prompt: `You write for msg in rx { ... } in the main thread, and you have cloned the transmitter so two threads each send. The main thread seems to hang forever after both threads finish sending. What common mistake keeps the for loop from ending, given that it ends only when the channel closes?`,
    hints: [
      'The channel closes when all transmitters are dropped.',
      'Did the original tx get dropped, or is it still alive in main?',
    ],
    solution: `The for loop over a receiver ends only when every transmitter has been dropped. The common mistake is that the original tx (the one you cloned from) is still alive in main's scope, so the channel never closes and the loop waits forever for a message that will never come. The fix is to make sure all transmitters are dropped: typically you move clones into the threads and drop or let the original tx go out of scope before the loop, so once the threads finish, no senders remain and the loop exits.`,
    tags: ['channels', 'iterator', 'clone', 'drop'],
  },
  {
    id: 'rs-ch16-t-022',
    chapter: 16,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Channels Versus Shared State',
    prompt: `You are designing a pipeline where one thread reads lines from a file and several worker threads each transform a line and report a result. Would you lean toward message passing with channels or shared state with Arc<Mutex<...>> for moving the lines to the workers? Give the main reasoning, citing the book's slogan about sharing.`,
    hints: [
      'The book quotes: do not communicate by sharing memory.',
      'Lines are owned, single-use units of work.',
    ],
    solution: `For handing off lines of work, channels are the natural fit. Each line is an owned unit of work that should be processed by exactly one worker, and send moves ownership cleanly to whichever worker receives it, so there is no contention over shared memory. This matches the book's slogan, "do not communicate by sharing memory; instead, share memory by communicating." A Mutex would force workers to fight over a lock just to grab the next line, which is more complex and more contention-prone. You would reach for Arc<Mutex<...>> only when threads must read and update one shared piece of state, such as an aggregate total.`,
    tags: ['channels', 'shared-state', 'design', 'arc-mutex'],
  },
  {
    id: 'rs-ch16-t-023',
    chapter: 16,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'The Double-Lock Deadlock',
    prompt: `A thread holds a MutexGuard from m.lock().unwrap() in variable g, and while g is still in scope it calls m.lock().unwrap() again on the same Mutex. The program freezes. Explain precisely why it hangs, in terms of the guard's lifetime and the lock's state.`,
    hints: [
      'The first guard has not been dropped yet.',
      'lock waits for the lock to become free.',
    ],
    solution: `The first lock() succeeded and returned a guard g that still owns the lock, and the guard releases the lock only when it is dropped at the end of its scope. Since g is still in scope, the lock is still held. The second lock() call then waits for the lock to become available, but the only thread that could release it is this very thread, which is now blocked waiting on itself. That self-wait is a deadlock, and the program freezes. The fix is to drop or scope the first guard before locking again.`,
    tags: ['mutex', 'deadlock', 'guard', 'lifetime'],
  },
  {
    id: 'rs-ch16-t-024',
    chapter: 16,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Holding the Lock Too Long',
    prompt: `Inside a thread you write let data = shared.lock().unwrap(); then do a long, slow computation that does not touch data, and only at the end use *data. Other threads grind to a halt during the slow part. Explain the problem and how restructuring the code fixes it, referring to when the guard drops.`,
    hints: [
      'The guard holds the lock for its whole scope.',
      'How could you shorten the time the lock is held?',
    ],
    solution: `The guard holds the lock from the moment lock() returns until the guard is dropped at the end of the block, so the slow computation runs while the lock is still held, blocking every other thread that wants it. The lock is being held far longer than necessary. The fix is to minimize the critical section: do the slow work without the lock, and only lock around the brief moment you actually read or write data, or copy out what you need and let the guard drop immediately by giving it a tiny scope. Shrinking how long the guard lives shrinks how long other threads are blocked.`,
    tags: ['mutex', 'guard', 'performance', 'contention'],
  },
  {
    id: 'rs-ch16-t-025',
    chapter: 16,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'What Send and Sync Mean',
    prompt: `Rust has two marker traits central to concurrency: Send and Sync. State precisely what it means for a type T to be Send, and separately what it means for T to be Sync. Then explain the relationship: how is "&T is Send" connected to "T is Sync"?`,
    hints: [
      'Send is about transferring ownership across threads.',
      'Sync is about sharing references across threads.',
    ],
    solution: `A type T is Send if it is safe to transfer ownership of a value of that type to another thread, that is, the value can be moved across a thread boundary. T is Sync if it is safe for multiple threads to hold shared references to the same value at the same time, that is, &T can be shared across threads. The two connect through a precise rule: T is Sync exactly when &T is Send. In other words, a type is safe to share by reference across threads if and only if its shared references can themselves be sent to other threads.`,
    tags: ['send', 'sync', 'marker-traits'],
  },
  {
    id: 'rs-ch16-t-026',
    chapter: 16,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Auto-Derived Marker Traits',
    prompt: `The book notes that you rarely write impl Send or impl Sync yourself. How do most types come to be Send and Sync, and what determines whether a struct you define gets them? Use the phrase "made up entirely of" in your answer.`,
    hints: [
      'These traits are automatically derived.',
      'A composite type is only as thread-safe as its parts.',
    ],
    solution: `Send and Sync are automatically derived (they are auto traits): a type made up entirely of fields that are themselves Send is automatically Send, and likewise for Sync. So a struct you define gets Send and Sync for free as long as every one of its fields is Send (or Sync). You do not implement them by hand in ordinary code. A type loses these traits only when it contains something not thread-safe, such as a raw pointer or an Rc, and implementing them manually is unsafe and reserved for rare low-level cases.`,
    tags: ['send', 'sync', 'auto-traits', 'composition'],
  },
  {
    id: 'rs-ch16-t-027',
    chapter: 16,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Mutex Versus RefCell Across Threads',
    prompt: `Earlier the book introduced RefCell<T> for interior mutability in single-threaded code, and Rc<RefCell<T>> for shared mutable state in one thread. The threaded analog is Arc<Mutex<T>>. Map each single-threaded piece to its threaded counterpart and explain why the single-threaded versions cannot simply be used across threads.`,
    hints: [
      'Rc maps to one thing, RefCell maps to another.',
      'Neither Rc nor RefCell is thread-safe.',
    ],
    solution: `Rc corresponds to Arc, and RefCell corresponds to Mutex; together Rc<RefCell<T>> in one thread maps to Arc<Mutex<T>> across threads. RefCell enforces the borrow rules at runtime but with non-atomic bookkeeping, and it is not Sync, so two threads borrowing through it could race on those checks; Mutex enforces exclusive access with real locking that is safe across threads. Rc's reference count is non-atomic and not Send, so it cannot cross threads, whereas Arc uses atomic counting. So you swap each single-threaded tool for its thread-safe equivalent.`,
    tags: ['mutex', 'refcell', 'arc', 'rc'],
  },
  {
    id: 'rs-ch16-t-028',
    chapter: 16,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Mutex Does Not Make Data Send',
    prompt: `A developer reasons: "Mutex makes access exclusive, so wrapping any value in a Mutex should make it safe to share across threads." They wrap an Rc in a Mutex, put it in an Arc, and the compiler still refuses. Explain why wrapping in Mutex does not rescue an Rc.`,
    hints: [
      'Mutex<T> is Send/Sync only when T meets certain bounds.',
      'The inner Rc is still moved and dropped by whichever thread holds the lock.',
    ],
    solution: `A Mutex guarantees only one thread touches the inner value at a time, but it does not change what that inner value is or make a non-Send type safe to move between threads. Mutex<T> is Send and Sync only when T itself is Send. An Rc is not Send because its reference count is non-atomic, and even under a lock, different threads would still clone and drop that Rc and corrupt the count. So the Mutex cannot launder an Rc into something thread-safe; you must use Arc for the inner shared ownership too.`,
    tags: ['mutex', 'rc', 'arc', 'send'],
  },
  {
    id: 'rs-ch16-t-029',
    chapter: 16,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Combining Channels and Shared State',
    prompt: `Design question: a server has many worker threads handling requests, and it must keep a single running count of total requests handled plus stream each completed request's id back to a logging thread. For each of the two needs (the running count, and the stream of ids), say whether you would use shared state or a channel, and justify the split.`,
    hints: [
      'A running total is read-modify-write on one value.',
      'A stream of one-time events flows in one direction.',
    ],
    solution: `Use shared state, an Arc<Mutex<u64>>, for the running count, because that is a single value many workers must read and update in place, which is exactly what a Mutex protects. Use a channel for the stream of completed ids, because each id is a one-way, one-time message handed from a worker to the single logging thread, and the mpsc receiver naturally serializes them for one consumer. Splitting them this way matches each tool to its job: shared mutation for one aggregate value, message passing for a flow of discrete events, rather than forcing everything through one mechanism.`,
    tags: ['channels', 'shared-state', 'design', 'arc-mutex'],
  },
  {
    id: 'rs-ch16-t-030',
    chapter: 16,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Why move Is Needed but the Compiler Is the Hero',
    prompt: `The book stresses that Rust's concurrency safety comes from the ownership and type system, not a special runtime. Tie three things together: the move keyword on a thread closure, the Send trait, and the borrow checker. Explain how these jointly guarantee no data races at compile time, using a one-line scenario.`,
    hints: [
      'move decides what data the thread owns.',
      'Send decides whether that data may cross the thread boundary.',
    ],
    solution: `When you write thread::spawn(move || ...), move decides exactly which values the new thread takes ownership of, and the borrow checker verifies the thread does not also hold borrows that could outlive their owners. The Send bound on spawn then requires every captured value to be a type that is safe to transfer to another thread, so a non-Send type like Rc is rejected outright. Together: ownership says only one thread owns the data, Send says the data may legally cross the boundary, and the borrow checker forbids dangling or aliased access. For example, moving a Vec into a thread compiles and is race-free, while moving an Rc<i32> fails to compile, all checked before the program ever runs.`,
    tags: ['move-closure', 'send', 'borrow-checker', 'thread-safety'],
  },
]

export default problems
