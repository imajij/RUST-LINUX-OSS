import type { Problem } from '../../types'

const problems: Problem[] = [
  {
    id: 'lx-ch13-t-001',
    chapter: 13,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'What A kobject Is For',
    prompt: `A newcomer browsing /sys asks what the kernel data structure behind that directory tree is called and what its basic job is. In one short paragraph, explain what a struct kobject represents and name two things it ties together.`,
    hints: ['Every directory under /sys is backed by a kobject.', 'Think reference count plus a place in a hierarchy.'],
    solution: `A struct kobject is the fundamental building block of the device model: it is the kernel object that, when registered, appears as a directory under /sys. Its core jobs are to carry a reference count (a struct kref) so the object stays alive exactly as long as someone is using it, and to hold a name plus a parent pointer so kobjects form the hierarchical tree you see in sysfs. It also links to a ktype (which defines its behavior, including how it is freed) and optionally to a kset (a collection of related kobjects). So a kobject ties together lifetime management and the sysfs hierarchy; higher-level structures like struct device embed a kobject to get both for free.`,
    tags: ['kernel', 'kobject', 'sysfs'],
  },
  {
    id: 'lx-ch13-t-002',
    chapter: 13,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'Why sysfs Files Are One Value Each',
    prompt: `A student notices that files under /sys typically hold a single small value, like the number "1" or a device name, rather than long structured text. Explain the rule of thumb for what belongs in a sysfs attribute and why this convention exists.`,
    hints: ['One value per file.', 'sysfs is text, human and script readable.'],
    solution: `The sysfs convention is "one value per file": each attribute should expose a single, simple, ideally ASCII value, and complex structured data should be split across multiple files rather than packed into one. This keeps sysfs easy to read and write from the shell and from scripts (cat one file, echo into another) and keeps each show/store handler trivial. It also makes the interface stable and self-documenting, since each file has one clear meaning. Things that need rich or binary formats generally belong elsewhere (debugfs, a char device, or a netlink interface), not in a sysfs attribute.`,
    tags: ['kernel', 'sysfs', 'attribute'],
  },
  {
    id: 'lx-ch13-t-003',
    chapter: 13,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'The Bus, Device, Driver Triangle',
    prompt: `The Linux device model is often described with three roles: a bus, a device, and a driver. Briefly state what each of the three represents, and which one is responsible for deciding that a particular device and driver belong together.`,
    hints: ['Bus = a kind of connection/namespace.', 'Matching is the bus’s job.'],
    solution: `A bus (struct bus_type) represents a category of connection or a namespace of devices, such as the PCI bus, the I2C bus, or the platform "pseudo-bus"; it owns the matching policy. A device (struct device) represents one piece of hardware present in the system, attached to some bus. A driver (struct device_driver) represents code that knows how to operate a class of devices. The bus is responsible for matching: its .match callback compares each device against each registered driver, and when they pair up the driver's probe function is called to bind them. So devices and drivers are independent lists, and the bus is the matchmaker between them.`,
    tags: ['kernel', 'device-model', 'bus'],
  },
  {
    id: 'lx-ch13-t-004',
    chapter: 13,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'What udev Does',
    prompt: `When you plug in a USB stick, a device node like /dev/sdb appears almost instantly and a desktop pop-up may show up. A user thinks the kernel itself creates these reactions. Explain at a high level the division of labor between the kernel and udev here.`,
    hints: ['Kernel sends events (uevents); userspace acts on them.', 'udev runs in userspace.'],
    solution: `The kernel does the low-level work: it detects the new hardware, creates the device in the device model, and emits a "uevent" (a small event message describing what happened) over a netlink socket. It does not, by itself, create /dev nodes or run desktop notifications. udev is a userspace daemon that listens for those uevents and reacts according to rules: it creates or removes the /dev node, sets its name, owner, and permissions, may load firmware or run helper programs, and can notify the desktop. So the kernel announces device events and exposes information through sysfs, while udev is the userspace policy engine that turns those announcements into device nodes and actions.`,
    tags: ['kernel', 'udev', 'uevent'],
  },
  {
    id: 'lx-ch13-t-005',
    chapter: 13,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Reference Counting With kref',
    prompt: `Two parts of the kernel each hold a pointer to the same kobject-backed object. Explain how reference counting keeps the object alive for both of them, and predict what happens to the object's memory when each of them is done with it. What call frees the object?`,
    hints: ['kobject_get / kobject_put adjust the count.', 'Release happens when the count hits zero.'],
    solution: `Each holder should take its own reference with kobject_get(), which increments the embedded kref, and drop it with kobject_put() when done, which decrements it. As long as either holder still holds a reference the count is above zero and the object stays alive, so neither can pull the memory out from under the other. When the last holder calls kobject_put() and the count reaches zero, the kobject's ktype->release() callback is invoked, which is where the containing structure is actually kfree'd. The object is freed exactly once, by that release callback, not directly by the callers; this is why you must never kfree a kobject-backed structure by hand.`,
    tags: ['kernel', 'kobject', 'refcount'],
  },
  {
    id: 'lx-ch13-t-006',
    chapter: 13,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'The Role Of ktype',
    prompt: `Two different subsystems both embed a struct kobject but want different sysfs attributes and different cleanup behavior. Explain what part of the kobject infrastructure lets them differ, and name the single most important field it must provide.`,
    hints: ['Behavior is pulled out into struct kobj_type.', 'release() is mandatory.'],
    solution: `The differing behavior lives in the struct kobj_type (the "ktype") that each kobject points to. A ktype bundles the per-type behavior: the default set of attributes exposed in sysfs, the sysfs_ops show/store dispatch functions, and most importantly the release() callback. The release() function is the mandatory field, because it is what gets called when the reference count drops to zero and is responsible for freeing the enclosing structure; a kobject without a release method is a bug the kernel will warn about. By pointing two kobjects at different ktypes, the two subsystems get different attributes and different cleanup while sharing the same generic kobject core.`,
    tags: ['kernel', 'ktype', 'kobject'],
  },
  {
    id: 'lx-ch13-t-007',
    chapter: 13,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Show Versus Store',
    prompt: `A read-write sysfs attribute has two handler functions. Describe what triggers the show() handler versus the store() handler from userspace, and state which direction each one moves data (toward the kernel or toward userspace).`,
    hints: ['Reading a file vs writing a file.', 'show fills a buffer; store parses one.'],
    solution: `The show() handler runs when userspace reads the file (for example cat /sys/.../value), and the store() handler runs when userspace writes the file (for example echo 5 > /sys/.../value). show() moves data toward userspace: the kernel formats the current value into the supplied page-sized buffer and returns the number of bytes written, conventionally with scnprintf. store() moves data toward the kernel: it receives the bytes userspace wrote plus a count, parses them (for example with kstrtoint), updates the kernel state, and returns the number of bytes consumed (usually the full count) or a negative errno on bad input. A read-only attribute has only show(); a write-only one has only store().`,
    tags: ['kernel', 'sysfs', 'show-store'],
  },
  {
    id: 'lx-ch13-t-008',
    chapter: 13,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Reading A sysfs File From The Shell',
    prompt: `You want to know a driver's current setting, exposed as /sys/devices/platform/mydev/mode. Give the shell command to read it, and explain why no special tool is needed compared with, say, an ioctl-based interface.`,
    hints: ['cat is enough.', 'sysfs files are plain text.'],
    solution: `You simply run cat /sys/devices/platform/mydev/mode, and to change it (if writable) echo newval > /sys/devices/platform/mydev/mode. No special tool is needed because sysfs presents each attribute as an ordinary text file whose read calls the kernel's show() handler and whose write calls store(); the VFS plumbing makes it look like any file. An ioctl interface, by contrast, requires a program that opens a device node and issues a specific numeric command with a typed argument structure, so you cannot drive it from the shell. This shell-friendliness is a major reason sysfs is preferred for simple per-device tunables and status.`,
    tags: ['kernel', 'sysfs', 'userspace'],
  },
  {
    id: 'lx-ch13-t-009',
    chapter: 13,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Why Probe Is Separate From Registration',
    prompt: `A platform driver author expects that calling platform_driver_register() will run their probe() immediately. Sometimes it does, sometimes it does not. Explain when probe() actually runs and why registration alone is not enough to guarantee it.`,
    hints: ['Probe needs a matching device.', 'Registration triggers a match attempt against existing devices.'],
    solution: `platform_driver_register() does not call probe() unconditionally; probe() runs only when the bus finds a device that matches the driver. At registration time the bus walks the existing list of devices on that bus and, for each one that matches (by name, of_match_table compatible string, ACPI id, etc.), calls the driver's probe(). If a matching device is already present, probe() runs right away; if not, nothing happens until such a device is added later, at which point the bus matches it and probe() runs then. So binding is event-driven on both sides: it can be triggered by registering the driver or by adding the device, whichever happens second.`,
    tags: ['kernel', 'platform', 'probe'],
  },
  {
    id: 'lx-ch13-t-010',
    chapter: 13,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Symmetry Of Probe And Remove',
    prompt: `A driver's probe() requests a memory region, maps registers, and allocates a private struct. Describe what its remove() function is expected to do, and explain the general rule relating probe and remove.`,
    hints: ['Remove undoes probe.', 'Reverse order of acquisition.'],
    solution: `remove() must undo everything probe() did, in roughly the reverse order: stop the device, unmap the registers, release the memory region, and free or otherwise release the private structure (or rely on devm-managed resources to be auto-released). The general rule is symmetry: every resource a probe acquires must be released exactly once when the device is unbound, whether unbinding happens at module unload, hot-unplug, or manual unbind. Doing the teardown in reverse order of acquisition avoids using a resource after a dependency of it has already been torn down. Getting this wrong leaks memory or leaves hardware in a bad state, and using device-managed (devm_) helpers in probe is the common way to make the cleanup automatic and hard to get wrong.`,
    tags: ['kernel', 'probe', 'remove'],
  },
  {
    id: 'lx-ch13-t-011',
    chapter: 13,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'The Platform Bus Versus A Real Bus',
    prompt: `PCI and USB devices announce themselves to software automatically, but the platform bus is described as being for devices that cannot do that. Explain what the platform bus is for and how those platform devices become known to the kernel instead.`,
    hints: ['Think SoC peripherals soldered on a board.', 'They are described, not discovered.'],
    solution: `The platform bus is a "pseudo-bus" for devices that are directly addressable and not enumerable over a discoverable bus, typically peripherals integrated into an SoC or wired onto a board: timers, GPIO controllers, simple memory-mapped UARTs, and so on. Such hardware cannot tell the system it exists, so it must be described rather than discovered. On modern systems that description comes from a device tree (or ACPI) that the firmware passes to the kernel; the kernel parses it and instantiates a platform_device for each node, which the platform bus then tries to match against registered platform drivers. So discovery is replaced by an external description, but from the driver's side the bus/device/driver model works just the same.`,
    tags: ['kernel', 'platform', 'device-tree'],
  },
  {
    id: 'lx-ch13-t-012',
    chapter: 13,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'What A Compatible String Is',
    prompt: `A device tree node contains compatible = "vendor,widget-v2";. Explain what a compatible string is, how a driver advertises that it can handle it, and what convention the vendor prefix follows.`,
    hints: ['of_match_table holds compatible strings.', 'Format is "vendor,model".'],
    solution: `A compatible string is the device tree's way of naming what kind of hardware a node is, written as "vendor,model" (a lowercase vendor prefix, a comma, then a model name), here "vendor,widget-v2". A driver advertises which strings it supports by filling a struct of_device_id array and pointing its driver's of_match_table at it; the OF matching logic compares each node's compatible list against those ids. The vendor prefix is a registered short name (documented in the kernel's vendor-prefixes list) so different companies' parts do not collide. A node may list several compatible strings from most- to least-specific so a generic driver can match if no specific one is present.`,
    tags: ['kernel', 'device-tree', 'compatible'],
  },
  {
    id: 'lx-ch13-t-013',
    chapter: 13,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Where Device Nodes Come From',
    prompt: `A driver creates a struct device with a major/minor number, yet /dev/mything does not exist until a moment later. A colleague claims the kernel writes into /dev directly. Explain who actually creates the node and how they learn it should exist.`,
    hints: ['udev creates nodes from a uevent.', 'sysfs exposes the dev attribute (major:minor).'],
    solution: `The kernel does not normally write into /dev itself; it registers the device in the model, which causes a "dev" attribute holding major:minor to appear in sysfs and an "add" uevent to be broadcast. udev (running in userspace) receives that uevent, reads the major:minor and name from sysfs, and creates the /dev node with mknod-equivalent logic, applying naming and permission rules. That is why there is a brief delay and why the node appears even though the driver only registered an in-kernel device. On a system without udev you would create the node manually or rely on devtmpfs, but the conceptual split is the same: kernel announces, userspace materializes the node.`,
    tags: ['kernel', 'udev', 'devnode'],
  },
  {
    id: 'lx-ch13-t-014',
    chapter: 13,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Sizing The show() Buffer',
    prompt: `A show() handler does sprintf(buf, "%s\\n", some_long_string) where some_long_string is read from device memory. Explain the latent bug, what guarantee the kernel gives about buf, and the safe way to write the value.`,
    hints: ['buf is exactly one page.', 'Use scnprintf and bound the length.'],
    solution: `The kernel passes show() a buffer that is exactly PAGE_SIZE bytes (typically 4 KB) and no more, so writing an unbounded string with sprintf risks overflowing that page if some_long_string is large, corrupting kernel memory. The safe pattern is to bound the write, e.g. return scnprintf(buf, PAGE_SIZE, "%s\\n", some_long_string);. scnprintf never writes past PAGE_SIZE and returns the number of characters actually stored (clamped), which is exactly what show() must return. Relying on "the value is probably short" is not a guarantee, especially when the value comes from device memory you do not fully control, so always cap the output at the page size.`,
    tags: ['kernel', 'sysfs', 'buffer'],
  },
  {
    id: 'lx-ch13-t-015',
    chapter: 13,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Parsing Input In store()',
    prompt: `A store() handler does kstrtoint(buf, 10, &val) and then uses val with no further checks, returning count. Two reviewers disagree: one says the input is fine because the shell only sends numbers. Critique store()'s robustness and list what it should validate before acting.`,
    hints: ['Userspace can write anything to a sysfs file.', 'Check the parse result and the value range.'],
    solution: `The reviewer who trusts the shell is wrong: a sysfs file can be written by any program, not just an interactive shell, so the bytes in buf are fully attacker-controlled and may be non-numeric, empty, or out of range. store() must check kstrtoint's return value (a negative errno means the input was not a valid integer, in which case store() should return that error, not count) and then validate that val falls within the legal range for the field before applying it. Only after both checks pass should it update state and return count to acknowledge it consumed the input. Skipping these checks lets bad input set the device to an invalid state or trigger out-of-range hardware accesses.`,
    tags: ['kernel', 'sysfs', 'validation'],
  },
  {
    id: 'lx-ch13-t-016',
    chapter: 13,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'GFP Flags Inside probe()',
    prompt: `Inside a platform driver's probe(), the author hesitates between kzalloc(sizeof(*priv), GFP_KERNEL) and GFP_ATOMIC for the private structure. Which is correct, and why does the answer depend on what context probe() runs in?`,
    hints: ['probe runs in process context.', 'GFP_KERNEL may sleep; GFP_ATOMIC must not.'],
    solution: `GFP_KERNEL is the correct choice: probe() runs in normal process context (the bus calls it from a kernel thread / the driver-core machinery, not from an interrupt handler and not while holding a spinlock), so it is allowed to sleep. GFP_KERNEL lets the allocator block to reclaim memory, making it far more likely to succeed under pressure, which is exactly what you want for a one-time setup allocation. GFP_ATOMIC would only be needed if you were allocating from atomic context such as an interrupt handler or under a spinlock, where sleeping is forbidden; using it in probe() needlessly weakens the allocation. So the rule is contextual: pick GFP_ATOMIC only when you truly cannot sleep, and probe() can sleep.`,
    tags: ['kernel', 'probe', 'gfp'],
  },
  {
    id: 'lx-ch13-t-017',
    chapter: 13,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'copy_to_user Versus A sysfs Buffer',
    prompt: `A developer copies their char-driver read() habit into a sysfs show() handler and calls copy_to_user(ubuf, value, len). Explain why show() does not (and must not) use copy_to_user, and what it does instead to deliver data to userspace.`,
    hints: ['show() writes into a kernel buffer, not a user pointer.', 'The VFS layer does the user copy.'],
    solution: `In a sysfs show() handler the buf argument is an ordinary kernel buffer (one page) that the sysfs/VFS layer already allocated; show() just formats text into it and returns the length. The copy across the user/kernel boundary is done by the sysfs machinery after show() returns, not by show() itself, so calling copy_to_user inside show() is wrong: buf is not a user pointer, and treating it as one would access the wrong address. By contrast, a raw char-driver read() does receive a real __user pointer and must use copy_to_user (which can fault and may return a short count). The takeaway is that the two interfaces have opposite buffer ownership conventions, and you must not transplant copy_to_user into a show() handler.`,
    tags: ['kernel', 'sysfs', 'copy_to_user'],
  },
  {
    id: 'lx-ch13-t-018',
    chapter: 13,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Devices And Drivers As Independent Lists',
    prompt: `A driver for "acme,gizmo" is loaded an hour before the gizmo hardware is described and added to the system. With no extra code, the driver binds correctly the moment the device appears. Explain the mechanism that makes ordering between driver load and device add not matter.`,
    hints: ['Both registrations trigger a match scan.', 'The bus keeps both lists.'],
    solution: `The bus maintains two independent lists, one of registered devices and one of registered drivers, and runs the match step whenever either list changes. When the driver registers first, the bus scans existing devices, finds none matching, and leaves the driver waiting. Later, when the gizmo device is added, the bus scans the registered drivers, matches the "acme,gizmo" driver via its of_match_table, and calls probe() right then. Symmetrically, if the device had been added first, registering the driver would have triggered the match. Because matching is re-run on every add of either kind, the relative order of driver load and device appearance does not matter; whichever comes second triggers the bind.`,
    tags: ['kernel', 'device-model', 'binding'],
  },
  {
    id: 'lx-ch13-t-019',
    chapter: 13,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Creating Attributes After Userspace Sees The Device',
    prompt: `A driver registers its device first, then in a later step adds several sysfs attributes one by one with device_create_file(). Explain the race this opens with udev and what kind of attribute grouping the kernel provides to avoid it.`,
    hints: ['udev runs on the add uevent.', 'attribute_group / dev_groups appear atomically with the device.'],
    solution: `When the device is registered, the kernel immediately emits an "add" uevent and udev may start reading sysfs attributes right away. If the driver then creates attributes one at a time afterward, udev rules can run before some attributes exist, so they intermittently fail to see values that "should" be there, a classic startup race. The fix is to declare the attributes up front as a struct attribute_group (or via the driver's dev_groups / the device's default groups) so the driver core creates them as part of device registration, atomically with the device, before the uevent is sent. That way udev never observes a half-populated device, and you also avoid manual cleanup since the core removes the group when the device goes away.`,
    tags: ['kernel', 'sysfs', 'udev'],
  },
  {
    id: 'lx-ch13-t-020',
    chapter: 13,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Locking Between store() And An IRQ',
    prompt: `A store() handler and a device interrupt handler both modify the same shared state. The author guards store() with a mutex. Explain why a mutex is the wrong lock to also take in the IRQ handler, and what the two sides should use instead.`,
    hints: ['Interrupt context cannot sleep; mutexes can sleep.', 'Use a spinlock with the irq-safe variant.'],
    solution: `A mutex can sleep when contended, but an interrupt handler runs in atomic context where sleeping is forbidden, so taking a mutex in the IRQ handler is illegal and can deadlock or crash the machine. When shared state is touched by both an interrupt handler and process-context code like store(), they must coordinate with a spinlock, and the process-context side must use the irq-saving variant (spin_lock_irqsave / spin_unlock_irqrestore) so the interrupt cannot fire and try to grab the same lock while store() already holds it on the same CPU. store() runs in sleepable context, so it may hold a mutex for other purposes, but the lock that is shared with the IRQ must be a spinlock taken with interrupts disabled on the process side.`,
    tags: ['kernel', 'locking', 'interrupt'],
  },
  {
    id: 'lx-ch13-t-021',
    chapter: 13,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Multiple Compatible Strings, One Driver',
    prompt: `A device tree node lists compatible = "acme,gizmo-v3", "acme,gizmo";. A driver's of_match_table contains only "acme,gizmo". Predict whether it binds, and explain how the kernel's preference ordering interacts with the node's list.`,
    hints: ['Match if any node string equals any driver id.', 'Earlier in the node list is more specific/preferred.'],
    solution: `It binds. OF matching succeeds if any compatible string in the node's list equals any entry in the driver's of_match_table; the node lists "acme,gizmo" as its second (more generic) string, which the driver supports, so they match. The ordering in the node's list expresses preference from most specific to least specific: the kernel will prefer a driver that matches the earlier "acme,gizmo-v3" if one exists, but since none does it falls back to the generic match. This is exactly the design intent: hardware advertises a specific model plus a generic family so a generic driver can still drive a newer revision it has never explicitly heard of, while leaving room for a specialized driver to take precedence later.`,
    tags: ['kernel', 'device-tree', 'matching'],
  },
  {
    id: 'lx-ch13-t-022',
    chapter: 13,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Getting Private Data In remove()',
    prompt: `In probe() a driver allocates a private struct and needs it again in remove() (and in its sysfs handlers). A novice stores it in a single global pointer. Explain why the global is wrong for a driver that may bind to two devices, and the device-model mechanism that solves it.`,
    hints: ['One driver can bind many device instances.', 'platform_set_drvdata / dev_set_drvdata per device.'],
    solution: `A single global pointer assumes there is only ever one bound device, but one driver can match and bind to several device instances at once (two identical UARTs, say); the second probe() would overwrite the global and the two instances would share or corrupt each other's state. The device model solves this with per-device driver data: in probe() call platform_set_drvdata(pdev, priv) (or dev_set_drvdata), and elsewhere retrieve it with platform_get_drvdata(pdev) / dev_get_drvdata(dev). Because the pointer is stored on each struct device, every bound instance keeps its own private struct, and remove() and the sysfs handlers (which receive the relevant device) can fetch the right one. This is the standard, instance-safe way to thread context through the driver.`,
    tags: ['kernel', 'drvdata', 'probe'],
  },
  {
    id: 'lx-ch13-t-023',
    chapter: 13,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'sysfs Tunable Versus ioctl',
    prompt: `You must expose one integer threshold that admins occasionally tweak, and separately a high-rate command channel that passes a packed binary struct hundreds of times per second. Argue which of sysfs and ioctl fits each, and why mixing them up would be poor design.`,
    hints: ['sysfs: simple, text, scriptable, one value.', 'ioctl: structured, binary, high-rate.'],
    solution: `The integer threshold fits sysfs: it is a single simple value, changed rarely, and exposing it as a text file means admins and scripts can read or set it with cat and echo and udev can even apply defaults, with no custom program. The high-rate binary command channel fits an ioctl (or a char-device read/write protocol): it carries a structured, packed argument and is called extremely often, which sysfs's one-value-per-file text model and per-call parsing handle poorly, and which would force awkward encoding of binary data into text. Forcing the binary channel through sysfs would be slow and ugly, while forcing the simple tunable through an ioctl would needlessly require a custom userspace tool. Matching the mechanism to the access pattern keeps each interface clean.`,
    tags: ['kernel', 'sysfs', 'ioctl'],
  },
  {
    id: 'lx-ch13-t-024',
    chapter: 13,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Use-After-Free On A Plain kfree',
    prompt: `A subsystem embeds a kobject in struct widget, registers it so it appears in /sys, and on shutdown calls kfree(w) directly after the last user is "done." Userspace still has the sysfs file open. Explain the use-after-free, and the correct teardown sequence.`,
    hints: ['Open sysfs files hold references; kfree ignores the refcount.', 'kobject_put plus a release() callback.'],
    solution: `Calling kfree(w) directly bypasses the reference count: an open sysfs file (or any other holder) still has a reference to the embedded kobject, so freeing the memory immediately leaves dangling references, and the next access through the sysfs file is a use-after-free that can corrupt memory or crash. The correct lifetime is driven entirely by the refcount: the subsystem must not free the structure itself but instead drop its own reference with kobject_put(&w->kobj), and the actual kfree(w) must happen inside the ktype's release() callback, which the kobject core invokes only when the count truly reaches zero. That way the structure survives until the last reference (including the open file's) is gone. The rule is: never kfree a kobject-embedding structure; only kobject_put it and free in release().`,
    tags: ['kernel', 'kobject', 'use-after-free'],
  },
  {
    id: 'lx-ch13-t-025',
    chapter: 13,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'probe() Returns Success But Setup Failed',
    prompt: `A probe() requests an IRQ, and if request_irq() fails it logs an error but still does return 0. Predict the consequences for binding, for remove(), and for the hardware. What is the correct error-path discipline in probe()?`,
    hints: ['Return value of probe decides bind success.', 'On error, undo what you already acquired, then return the errno.'],
    solution: `probe()'s return value is how the driver tells the core whether binding succeeded: returning 0 marks the device as successfully bound even though the IRQ was never obtained. The core therefore considers the device driven, will later call remove() expecting a fully set-up device, and the hardware runs without a working interrupt, producing mysterious hangs or lost events; remove() may also try to free_irq an IRQ it never got. The correct discipline is that probe() must return a negative errno on any failure (here, propagate request_irq's error), and before returning it must unwind everything it already acquired in reverse order (or rely on devm_ helpers that auto-unwind on probe failure). Returning the error keeps the device unbound and consistent rather than half-initialized.`,
    tags: ['kernel', 'probe', 'error-handling'],
  },
  {
    id: 'lx-ch13-t-026',
    chapter: 13,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'store() Triggering A Hotplug Event',
    prompt: `Writing to a sysfs "rescan" attribute causes the store() handler to add a new child device, which makes the kernel emit a uevent and udev create a node. A reviewer worries about doing such heavy work in store(). Explain why store() is allowed to sleep and call into the device core, and what to still be careful about.`,
    hints: ['store() runs in process context (a write syscall).', 'Watch locking/recursion and unbounded work.'],
    solution: `store() runs in ordinary process context as the back end of a userspace write() syscall, so it is allowed to sleep: it may take mutexes, allocate with GFP_KERNEL, and call into the device core to register a new device, which in turn emits the uevent that udev consumes. That is fundamentally different from an interrupt handler, where none of this would be legal. What to watch for is more subtle: store() should not hold a spinlock across these sleeping calls, must avoid lock-ordering inversions or recursion if device registration calls back into the same subsystem, and should bound the work so a single write does not stall for an unreasonable time or let userspace trigger unlimited device creation. So the heavy work is permitted by context, but correctness still depends on disciplined locking and resource limits.`,
    tags: ['kernel', 'sysfs', 'context'],
  },
  {
    id: 'lx-ch13-t-027',
    chapter: 13,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Deferred Probe',
    prompt: `Driver A's probe() needs a clock that is provided by driver B, but at boot A is probed before B exists, so A cannot get the clock. Returning a hard error would permanently fail A. Explain the device-core feature that lets A retry later and how A signals it.`,
    hints: ['A returns -EPROBE_DEFER.', 'The core re-queues the device for another probe attempt.'],
    solution: `The feature is deferred probing. When probe() cannot get a mandatory resource because the provider is not ready yet, it returns -EPROBE_DEFER instead of a fatal errno. The driver core treats that specially: rather than marking the device as failed, it puts the device on a deferred list and retries its probe later, typically after some other driver successfully probes (for example once driver B registers and provides the clock). On the retry A can obtain the clock and bind. This decouples probe order from dependency order at boot, so the system does not need to load drivers in a precise sequence; the only requirement is that A unwind anything it acquired before returning -EPROBE_DEFER so each retry starts clean.`,
    tags: ['kernel', 'probe', 'deferred-probe'],
  },
  {
    id: 'lx-ch13-t-028',
    chapter: 13,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Removing An Attribute With A Reader Mid-Read',
    prompt: `A driver is being unbound. remove() tears down state that show() dereferences, while a userspace process is in the middle of reading that attribute. Explain how the sysfs/kernfs layer prevents show() from running on freed state, and what remove() must still not do.`,
    hints: ['sysfs removal drains active show/store calls.', 'Removal waits; do not block forever holding it up.'],
    solution: `The sysfs (kernfs) layer reference-counts active attribute operations: while a show() or store() call is in flight it holds the attribute "active," and removing the attribute (which happens as the device's groups are torn down during unbind) blocks until all in-flight calls have drained, and prevents new ones from starting. So once removal of the file completes, no show() can be running, which is what lets remove() safely free the state that show() touched without racing an in-progress read. What remove() must still avoid is making its own teardown depend on something that cannot complete while a show() is stuck, and it must not free the show()-referenced state before the attribute removal that drains those calls has finished, otherwise the ordering guarantee is lost and a use-after-free returns.`,
    tags: ['kernel', 'sysfs', 'teardown'],
  },
  {
    id: 'lx-ch13-t-029',
    chapter: 13,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Why udev Rules Use sysfs Attributes, Not Device Names',
    prompt: `An admin writes a udev rule that acts on a device whose kernel name happened to be sdb. After a reboot the same disk is sdc and the rule misfires. Explain why kernel device names are a fragile match key and what udev matches on instead for stability.`,
    hints: ['Enumeration order is not stable.', 'Match persistent sysfs attributes (serial, by-id, etc.).'],
    solution: `Kernel device names like sdb are assigned in discovery order, which depends on probe timing, hub topology, and boot-to-boot races, so the same physical disk can get a different name on the next boot; matching a rule on the name is inherently fragile. udev solves this by matching on stable identifying attributes exposed in sysfs (and walking up the parent chain), such as a device serial number, model, WWN, or bus path, via rule keys like ATTRS{serial}, ATTRS{idVendor}, and so on. From those it can also create persistent symlinks (the /dev/disk/by-id and by-path schemes). The lesson is to key udev rules on intrinsic, sysfs-visible properties of the hardware rather than on the volatile kernel-assigned name, so the rule follows the device regardless of enumeration order.`,
    tags: ['kernel', 'udev', 'sysfs'],
  },
  {
    id: 'lx-ch13-t-030',
    chapter: 13,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Designing A Driver For Two Hardware Revisions',
    prompt: `You must support an "acme,uart-v1" and an "acme,uart-v2" that differ only in a register offset, with one driver binary. Sketch how to use the OF match table and probe() so the right offset is selected per device without runtime version-sniffing hacks. Why is the match table the natural place for this?`,
    hints: ['of_device_id has a .data pointer.', 'Stash per-variant config and read it in probe via of_device_get_match_data.'],
    solution: `Put both compatible strings in one struct of_device_id table and attach per-variant configuration to each entry's .data field, for example pointing at a small struct holding the register offset for v1 and another for v2. In probe(), retrieve the matched entry's data with of_device_get_match_data(dev) (or by reading the matched of_device_id), and use the offset from it; no string comparison or runtime sniffing is needed. The match table is the natural home because matching has already identified exactly which variant this device is, so the per-variant data rides along with the decision the core just made, keeping probe() generic and data-driven. Adding a future v3 then means appending one table entry and one config struct rather than editing branching logic, which is cleaner and less error-prone.`,
    tags: ['kernel', 'device-tree', 'match-data'],
  },
]

export default problems
