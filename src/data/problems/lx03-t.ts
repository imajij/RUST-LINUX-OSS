import type { Problem } from '../../types'

const problems: Problem[] = [
  {
    id: 'lx-ch03-t-001',
    chapter: 3,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'What Is a PID',
    prompt: `A program prints a number like 4271 obtained from getpid(). What does that number identify, is it the same for two different running programs at the same time, and roughly when does the kernel hand it out?`,
    hints: [
      'PID stands for process identifier.',
      'Each running process gets its own identity.',
    ],
    solution: `The number is the process ID (PID): a small integer the kernel assigns to uniquely identify a running process. At any single moment no two live processes share the same PID, so 4271 names exactly one process. The kernel hands out a fresh PID when a new process is created (for example by fork or by exec'ing a program), and that PID is associated with the process until it terminates. PIDs can be reused later for new processes once an old one has fully exited and been reaped, but never for two processes alive simultaneously.`,
    tags: ['process', 'pid', 'intro'],
  },
  {
    id: 'lx-ch03-t-002',
    chapter: 3,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'User Mode Versus Kernel Mode',
    prompt: `Your normal C program runs its own loops and arithmetic, but when it calls write() to print to the screen something different happens. Explain the difference between "user mode" and "kernel mode", and which one ordinary application code runs in.`,
    hints: [
      'Application code is restricted; the kernel is privileged.',
      'A system call is the doorway between the two.',
    ],
    solution: `Ordinary application code runs in user mode, a restricted CPU privilege level where the program cannot directly touch hardware, other processes' memory, or protected kernel data. Kernel mode is the privileged level where the kernel code runs and can do those things. When the program calls write(), it cannot perform the device I/O itself, so it issues a system call that traps into the kernel; the CPU switches to kernel mode, the kernel does the work on the process's behalf, and then control returns to user mode. This separation protects the system: a buggy or malicious user program is confined and must ask the kernel, through well-defined syscalls, to do anything privileged.`,
    tags: ['syscall', 'privilege', 'intro'],
  },
  {
    id: 'lx-ch03-t-003',
    chapter: 3,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'What fork Returns',
    prompt: `After a successful fork() you suddenly have two processes running. In the parent, what does fork() return, and what does it return in the child? How does the code tell which process it is?`,
    hints: [
      'fork returns twice, once in each process.',
      'One return value is 0, the other is positive.',
    ],
    solution: `fork() returns twice: once in the parent and once in the child. In the parent it returns the PID of the newly created child (a positive number), and in the child it returns 0. Code distinguishes the two by inspecting the return value: a return of 0 means "I am the child", and a positive return means "I am the parent and this is my child's PID". (On failure fork returns -1 in the single original process and no child is created.) This single return value is the standard way each side of the fork picks its own code path.`,
    tags: ['fork', 'process', 'intro'],
  },
  {
    id: 'lx-ch03-t-004',
    chapter: 3,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'What Is a File Descriptor',
    prompt: `open() gives you back a small non-negative integer like 3, and you pass it to read() and write(). What is this integer conceptually, why does a fresh process usually have 0, 1, and 2 already in use, and is it meaningful in another unrelated process?`,
    hints: [
      'It is a per-process index, not a global handle.',
      'Standard input, output, and error are pre-opened.',
    ],
    solution: `A file descriptor is a small non-negative integer that indexes into the kernel's per-process table of open files; it is the handle a process uses to refer to an open file, pipe, socket, or device. A fresh process normally already has descriptors 0, 1, and 2 open as standard input, standard output, and standard error, so the first file you open typically gets descriptor 3. Because the table is per-process, the same number means different things in different processes: descriptor 3 in your program and descriptor 3 in an unrelated program point to entirely separate open files. So a bare descriptor number is only meaningful within the process that owns it.`,
    tags: ['file-descriptor', 'process', 'intro'],
  },
  {
    id: 'lx-ch03-t-005',
    chapter: 3,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Why Check errno Only After Failure',
    prompt: `A student writes: "I called read(), then printed errno, and it said 'No such file or directory' even though the read worked fine." What is the bug in this reasoning, and what is the correct rule for using errno?`,
    hints: [
      'errno is only meaningful after an error indication.',
      'Successful calls do not clear errno.',
    ],
    solution: `errno is not "the status of the last call"; it is a thread-local variable that the C library sets only when a call indicates failure, and successful calls generally leave it unchanged. So errno can hold a leftover value from some earlier failed call, which is exactly what the student saw. The correct rule is: first check the call's own return value (for read, a return of -1 means error), and only then read errno to find out why. Reading errno after a successful call is meaningless. Code like "if (read(...) == -1) perror(\"read\");" follows the rule correctly.`,
    tags: ['errno', 'syscall', 'error-handling'],
  },
  {
    id: 'lx-ch03-t-006',
    chapter: 3,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'fork Then Both Print',
    prompt: `Consider this sketch (error handling omitted):

    printf("start\\n");
    pid_t p = fork();
    printf("after fork, p=%d\\n", p);

Assuming fork succeeds, how many times does "start" print, how many times does the second printf run, and why are the printed p values different?`,
    hints: [
      'fork happens after the first printf.',
      'The child is a copy continuing from the fork point.',
    ],
    solution: `"start" prints once, because fork happens after that line, so only the original process executes it. The second printf runs twice, once in the parent and once in the child, because after fork both processes continue from the same point with their own copies of the code and data. The printed p values differ because fork returns the child's PID (a positive number) in the parent and 0 in the child. So you see two "after fork" lines, one with a positive p and one with p=0, in an order the kernel's scheduler decides.`,
    tags: ['fork', 'predict-behavior', 'process'],
  },
  {
    id: 'lx-ch03-t-007',
    chapter: 3,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'What exec Replaces',
    prompt: `A process calls fork(), and in the child calls execlp("ls", "ls", NULL). Right after a successful execlp, what happened to the child's original program code, and does any line of code written after the execlp call in the child run? Explain.`,
    hints: [
      'exec replaces the running image in place.',
      'On success exec does not return.',
    ],
    solution: `A successful exec replaces the entire program image of the calling process with the new program (here ls), keeping the same PID but throwing away the old code, data, heap, and stack. Because the old image is gone, code written after the execlp call does not run on success: control never returns to it. The only time a line after exec executes is when exec fails (returns -1), which is why exec is normally immediately followed by error handling like perror plus _exit. So in the child the original program effectively becomes ls, while the parent keeps running its own code unchanged.`,
    tags: ['exec', 'process', 'predict-behavior'],
  },
  {
    id: 'lx-ch03-t-008',
    chapter: 3,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Why wait After fork',
    prompt: `A parent forks a child to run a task and then immediately moves on without ever calling wait() or waitpid(), and the parent keeps running for a long time. The child finishes quickly. Describe the state the finished child ends up in and why calling wait fixes it.`,
    hints: [
      'A terminated but un-reaped child has a name.',
      'The kernel keeps a little info until the parent collects it.',
    ],
    solution: `When the child terminates, the kernel cannot fully discard it until the parent collects its exit status, so the child becomes a "zombie": a process that has exited but whose entry lingers holding its exit status. Because the parent never calls wait/waitpid and keeps running, the zombie persists, consuming a slot in the process table. Calling wait() (or waitpid()) makes the parent collect the child's exit status, which lets the kernel "reap" the zombie and free its resources. This is why a parent that creates children should wait for them, or otherwise arrange for them to be reaped.`,
    tags: ['wait', 'zombie', 'process'],
  },
  {
    id: 'lx-ch03-t-009',
    chapter: 3,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Reading exit Status Bits',
    prompt: `A parent does: waitpid(pid, &status, 0). It wants to know "did the child exit normally, and with what number did it call exit()?" Why is it wrong to just print status directly as the exit code, and which macros should it use instead?`,
    hints: [
      'status packs several pieces of information.',
      'There are WIFEXITED and WEXITSTATUS macros.',
    ],
    solution: `The status word filled in by waitpid is not the exit code itself; it encodes several things, including whether the child exited normally or was killed by a signal, and the exit number or signal number. Printing status raw would give a meaningless packed value. The parent should first test WIFEXITED(status) to confirm a normal exit, and only then use WEXITSTATUS(status) to extract the low-byte value the child passed to exit() or returned from main. (Correspondingly, WIFSIGNALED and WTERMSIG describe the case where a signal killed the child.) Using the macros keeps the code correct regardless of how the status bits are laid out.`,
    tags: ['wait', 'exit-status', 'process'],
  },
  {
    id: 'lx-ch03-t-010',
    chapter: 3,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'A System Call Is Not a Function Call',
    prompt: `From a C program, calling getpid() looks just like calling any ordinary function. Explain what is actually different under the hood when a system call runs compared with calling a plain function in your own program.`,
    hints: [
      'A plain function call stays in user mode.',
      'A syscall crosses the user/kernel boundary.',
    ],
    solution: `A plain function call stays entirely within your process in user mode: it just jumps to another address in the same privilege level. A system call is fundamentally different because the work must be done by the kernel: the C wrapper sets up the syscall number and arguments and executes a special trap instruction that switches the CPU into kernel mode and transfers control to the kernel's syscall handler. The kernel validates the request, does the privileged work, and then returns to user mode with a result. So although getpid() reads like a function, it crosses a protection boundary, which is more expensive and is the only way user code can get the kernel to act on its behalf.`,
    tags: ['syscall', 'privilege', 'compare'],
  },
  {
    id: 'lx-ch03-t-011',
    chapter: 3,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Peeking at a Process via /proc',
    prompt: `You know a process has PID 1830. Without using any special tool, you want to see its command line and which files it has open. Which directory would you look in, and what does the fact that this information appears as files tell you about how Linux exposes process state?`,
    hints: [
      'There is a per-PID directory under one virtual filesystem.',
      'These files are generated on read, not stored on disk.',
    ],
    solution: `You look under /proc/1830, the per-process directory in the /proc virtual filesystem. There you find entries like /proc/1830/cmdline for the command line and /proc/1830/fd/, a directory of symlinks for each open file descriptor. The key idea is that /proc is not a real on-disk filesystem; it is a kernel-provided view in which reading a file causes the kernel to produce up-to-date process information on the fly. This lets ordinary file operations (open/read) inspect live process state, which is exactly how tools like ps and lsof work internally.`,
    tags: ['proc', 'process', 'introspection'],
  },
  {
    id: 'lx-ch03-t-012',
    chapter: 3,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Why fork Returns -1 Sometimes',
    prompt: `A long-running server forks a child per request and never reaps them, and after a while fork() starts returning -1 with errno set. Without naming the exact error constant, explain two distinct reasons fork can fail, and why "always assume fork succeeds" is a bug.`,
    hints: [
      'Process creation needs free resources.',
      'There are system-wide and per-user limits.',
    ],
    solution: `fork can fail because creating a process needs resources that may be exhausted: for example, hitting the limit on the number of processes (a per-user or system-wide cap on process slots, which un-reaped zombies count against), or running out of memory needed to set up the new process. When fork fails it returns -1, sets errno, and creates no child. "Always assume fork succeeds" is a bug because the code would then treat -1 as if it were a valid child or proceed as the "parent" with no child existing, corrupting its logic; worse, in a fork that forgot to handle -1 you can end up doing child-only work in the parent. Correct code checks for -1 and handles the failure.`,
    tags: ['fork', 'error-handling', 'process'],
  },
  {
    id: 'lx-ch03-t-013',
    chapter: 3,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'What a Pipe Connects',
    prompt: `pipe(fds) gives you two file descriptors, fds[0] and fds[1]. After a fork, the parent wants to send bytes to the child. Which descriptor is for reading and which for writing, and what should each process do with the descriptor it is not going to use?`,
    hints: [
      'A pipe is one-directional: a read end and a write end.',
      'Both ends are inherited by the child across fork.',
    ],
    solution: `A pipe is unidirectional with two ends: fds[0] is the read end and fds[1] is the write end. After fork, both processes inherit both ends. To send from parent to child, the parent writes to fds[1] and the child reads from fds[0]. Each process should close the end it will not use: the parent closes fds[0] (its read end) and the child closes fds[1] (its write end). Closing the unused ends matters because, for example, the reader only sees end-of-file once all write ends are closed; if the parent left fds[1] open, the child's read might never report EOF.`,
    tags: ['pipe', 'file-descriptor', 'ipc'],
  },
  {
    id: 'lx-ch03-t-014',
    chapter: 3,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'fork With a Pending Buffer',
    prompt: `A program does:

    printf("hello");          /* no newline */
    fork();

and the program's stdout is a file (not a terminal). The string "hello" ends up in the file twice. Explain why, and how adding fflush(stdout) before fork changes the outcome.`,
    hints: [
      'stdio buffers output in user-space memory.',
      'fork copies the process memory, including that buffer.',
    ],
    solution: `printf writes into a user-space stdio buffer rather than immediately to the kernel; when stdout is a file the stream is fully buffered, so "hello" is still sitting in that buffer at the time of fork. fork duplicates the whole process memory, including the not-yet-flushed buffer, so both parent and child end up with their own copy of "hello" pending. When each later flushes (at exit), the bytes are written, producing "hello" twice in the file. Calling fflush(stdout) before fork forces the buffer out to the kernel first, so it is empty when copied, and "hello" appears only once. This is a classic reason to flush before forking.`,
    tags: ['fork', 'buffering', 'predict-behavior'],
  },
  {
    id: 'lx-ch03-t-015',
    chapter: 3,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Shared Open File Across fork',
    prompt: `A process opens a file, gets descriptor 4, then forks. Both parent and child now have descriptor 4. They both write to it. Compared with each process opening the same path independently, why do the writes interleave cleanly without overwriting each other through the inherited descriptor?`,
    hints: [
      'fork duplicates descriptors but they share one open file description.',
      'The file offset lives in the shared open file description.',
    ],
    solution: `When you fork, the child's descriptor 4 and the parent's descriptor 4 are duplicates that refer to the same underlying open file description in the kernel, which holds the current file offset. Because the offset is shared, after either process writes, the offset advances for both, so their writes append one after another rather than landing on top of each other. If instead each process called open() on the same path independently, they would get separate open file descriptions with independent offsets both starting at 0, so their writes would clobber the same region. This shared-offset behavior of inherited descriptors is exactly why a parent and child can cooperatively write to one file.`,
    tags: ['fork', 'file-descriptor', 'compare'],
  },
  {
    id: 'lx-ch03-t-016',
    chapter: 3,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Async-Signal-Safety in a Handler',
    prompt: `A developer installs a SIGINT handler that calls printf("caught it\\n") and malloc() to log the event. Reviewers say this is unsafe even though it "usually works". Explain why calling printf or malloc inside a signal handler is dangerous, and what kinds of functions are safe there.`,
    hints: [
      'A handler can interrupt the program at any instant.',
      'Think about what state printf and malloc might have mid-operation.',
    ],
    solution: `A signal handler can run at an arbitrary moment, interrupting the main program even in the middle of a library call. If the signal arrives while the program is inside malloc (holding the heap's internal lock or mid-update of its data structures) and the handler also calls malloc, it can deadlock or corrupt the heap; printf has the same problem because stdio keeps internal buffers and locks that are not re-entrant. These functions are not async-signal-safe. Only functions on the async-signal-safe list (such as write(), _exit(), and a few others) may be called from a handler. A common safe pattern is to just set a volatile sig_atomic_t flag in the handler and do the real work back in the main loop. "Usually works" is exactly the trap: it fails only when the signal lands at the bad instant.`,
    tags: ['signals', 'async-safety', 'unsafe'],
  },
  {
    id: 'lx-ch03-t-017',
    chapter: 3,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'EINTR On a Slow Syscall',
    prompt: `A program calls read() on a terminal and blocks waiting for input. While it is blocked, a SIGCHLD arrives and a handler runs. After the handler returns, read() comes back with -1. The data was never read. What likely happened, and what should robust code do about it?`,
    hints: [
      'A signal can interrupt a blocked system call.',
      'There is a specific errno for "interrupted system call".',
    ],
    solution: `The blocked read() was interrupted by the delivery of SIGCHLD; when a signal handler runs during a slow (blocking) system call, that call can return -1 with errno set to EINTR, meaning "interrupted system call", without having transferred data. This is normal, not a real I/O failure. Robust code distinguishes EINTR from genuine errors and retries the call, typically with a loop like: do { n = read(...); } while (n == -1 && errno == EINTR);. (Alternatively, installing the handler with SA_RESTART asks the kernel to automatically restart many such calls.) The key lesson is that signals can cut short blocking syscalls, so error handling must account for EINTR.`,
    tags: ['signals', 'syscall', 'errno'],
  },
  {
    id: 'lx-ch03-t-018',
    chapter: 3,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'fork Versus a New Thread',
    prompt: `For a task that runs a separate flow of execution alongside the main one and shares the same variables, a colleague says "just fork a process". Compare creating a new process with fork against creating a thread in terms of memory sharing and isolation, and say when fork is the better choice.`,
    hints: [
      'fork gives a separate address space; threads share one.',
      'Think about both safety and the cost of communication.',
    ],
    solution: `fork creates a new process with its own copy of the address space, so after fork the child and parent do not share variables; changes in one are invisible to the other, and they communicate through explicit channels like pipes. Threads, by contrast, live inside one process and share the same memory, so they see the same global variables directly but must use synchronization to avoid races. If the task needs to share variables in-place with the main flow, threads fit better. fork is the better choice when you want isolation (a crash or corruption in the child cannot scribble on the parent's memory), when you intend to exec a different program, or when you want the OS's process-level protection and independent resource limits. So the colleague's advice is wrong if the requirement is shared variables, and right if the requirement is an isolated worker.`,
    tags: ['fork', 'threads', 'compare'],
  },
  {
    id: 'lx-ch03-t-019',
    chapter: 3,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Orphan Adopted by init',
    prompt: `A parent forks a child, then the parent exits immediately while the child keeps running for another minute. The child is now an "orphan". What process becomes its new parent, and why does this matter for who eventually reaps the child's exit status?`,
    hints: [
      'An orphan does not just disappear; it is re-parented.',
      'Reaping requires some parent to call wait.',
    ],
    solution: `When the parent exits first, the still-running child becomes an orphan and is re-parented (adopted) by init/PID 1 (or a subreaper). This matters because a terminated child must be reaped by its parent via wait/waitpid, and the original parent is gone. init's job includes periodically reaping the orphans it has adopted, so when the orphan eventually exits, init collects its status and the zombie does not linger. This is the safety net that prevents orphaned, finished processes from piling up as permanent zombies. Note it is different from the zombie case where a living parent simply neglects to call wait.`,
    tags: ['process', 'orphan', 'wait'],
  },
  {
    id: 'lx-ch03-t-020',
    chapter: 3,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'waitpid With WNOHANG',
    prompt: `An event-loop server cannot afford to block. It still must reap children that exit. A blocking wait() would freeze the loop. What variant lets the loop check for finished children without blocking, how do you process several at once, and how do you know there are none left right now?`,
    hints: [
      'There is a flag that makes waitpid return immediately.',
      'Loop until it reports "nobody ready".',
    ],
    solution: `Use waitpid(-1, &status, WNOHANG): the WNOHANG flag makes it return immediately instead of blocking. If a child has terminated, it reaps one and returns that child's PID; if children exist but none have exited yet, it returns 0; on real error it returns -1. To reap several at once, call it in a loop: while ((pid = waitpid(-1, &status, WNOHANG)) > 0) { /* handle each */ }. The loop stops when it returns 0 (children still running but none finished) or -1 (no children, errno ECHILD). This non-blocking polling, often triggered by a SIGCHLD notification, lets the event loop collect exit statuses without ever freezing.`,
    tags: ['wait', 'process', 'design'],
  },
  {
    id: 'lx-ch03-t-021',
    chapter: 3,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Why the Kernel Copies User Buffers',
    prompt: `When user code calls write(fd, buf, n), the kernel does not simply dereference buf and trust it. From the topic's perspective on user versus kernel mode, explain why kernel code must treat a user-supplied pointer with suspicion and what could go wrong if it dereferenced it blindly.`,
    hints: [
      'The pointer comes from untrusted user mode.',
      'It might be invalid, unmapped, or point at kernel memory.',
    ],
    solution: `The pointer buf is supplied by untrusted user-mode code, so the kernel cannot assume it is valid: it could be NULL, point to an unmapped or not-yet-faulted page, or be a malicious attempt to point at kernel memory the process should not access. If kernel code dereferenced it blindly, a bad or unmapped address would fault inside the kernel (a serious bug), and a pointer aimed at kernel memory could leak or corrupt privileged data, breaking the whole protection model that separates user and kernel mode. That is why the kernel accesses user buffers only through checked accessors (the copy_to_user / copy_from_user family) that validate the address range and safely handle a page fault during the copy, returning an error like EFAULT rather than crashing. The user/kernel boundary means user pointers are input to be validated, never trusted.`,
    tags: ['syscall', 'privilege', 'safety'],
  },
  {
    id: 'lx-ch03-t-022',
    chapter: 3,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Negative Return Convention in Syscalls',
    prompt: `Inside the kernel, system-call implementations typically "return -EFAULT" or "return -EINVAL" on failure, yet a user-space program that called that syscall sees a return of -1 and errno set to EINVAL. Explain how the kernel's negative-errno convention turns into the user-space "-1 plus errno" pattern.`,
    hints: [
      'The kernel encodes errors as small negative numbers.',
      'The C library translates that for user space.',
    ],
    solution: `Inside the kernel, syscall handlers signal failure by returning a small negative error code such as -EINVAL or -EFAULT, and success by returning zero or a non-negative result. This negative-errno convention lets one return value carry both outcomes. When control returns to user space, the C library wrapper around the syscall checks whether the raw return is a negative value in the reserved error range; if so, it negates it, stores it in errno, and returns -1 to the caller. That is why kernel code writes "return -EINVAL" but the application sees the familiar pattern of a -1 return with errno == EINVAL. The two views are the same information, translated at the user/kernel boundary by the library.`,
    tags: ['syscall', 'errno', 'kernel'],
  },
  {
    id: 'lx-ch03-t-023',
    chapter: 3,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Double-Fork To Avoid Zombies',
    prompt: `A daemon wants to launch a worker but never wants to call wait() for it, and never wants a zombie. A common trick is the "double fork": fork a child, that child forks a grandchild, the child exits immediately, and the grandchild does the real work. Explain step by step why this leaves no zombie for the original daemon to reap.`,
    hints: [
      'Reaping who? The daemon only ever has to reap its direct child.',
      'When the middle process exits, what happens to the grandchild?',
    ],
    solution: `The daemon's only direct child is the middle process, so that is the only one the daemon would ever need to reap. In the double-fork, the middle child forks the grandchild and then exits right away; the daemon does a single quick wait on that middle child, which finishes almost immediately, so it leaves no lingering zombie. The grandchild, which does the long-running work, is now an orphan because its parent (the middle child) has exited, so it gets re-parented to init/PID 1. When the grandchild eventually terminates, init reaps it automatically. Thus the daemon reaps the short-lived middle child once, the grandchild is reaped by init, and no zombie ever accumulates against the daemon, even though the daemon never waits on the actual worker.`,
    tags: ['fork', 'zombie', 'design'],
  },
  {
    id: 'lx-ch03-t-024',
    chapter: 3,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Pipeline With a Deadlock Risk',
    prompt: `A parent creates a pipe, forks, and intends: child runs a program whose stdout is the pipe's write end (via dup2), parent reads all of the child's output from the pipe's read end. The author dup2's the write end onto the child's stdout but forgets to close the parent's own copy of the pipe's write end. The parent's read loop then hangs forever after the child exits. Explain the cause.`,
    hints: [
      'read returns EOF only when all write ends are closed.',
      'The parent is holding a write end open.',
    ],
    solution: `A read on a pipe returns end-of-file (read returns 0) only when there are no remaining open write ends across all processes. Here, even after the child exits and its write end is gone, the parent still holds its own un-closed copy of the pipe's write descriptor. From the kernel's point of view at least one write end is still open (the parent's), so the pipe is not considered done; the parent's read blocks waiting for more data that will never come, and the loop hangs forever. The fix is for the parent to close its copy of the write end right after the fork, so that once the child finishes, no write end remains and the parent's read finally returns 0 (EOF) and the loop ends.`,
    tags: ['pipe', 'deadlock', 'file-descriptor'],
  },
  {
    id: 'lx-ch03-t-025',
    chapter: 3,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Signal Coalescing And Lost Children',
    prompt: `A server installs a SIGCHLD handler that calls waitpid exactly once (reaping a single child) per delivery. Under heavy load several children exit at nearly the same time, yet later the process still has zombies. Explain why "one waitpid per SIGCHLD" is broken, given how standard signals are delivered.`,
    hints: [
      'Standard signals are not queued; they can merge.',
      'How many SIGCHLDs do you actually get for N near-simultaneous exits?',
    ],
    solution: `Standard (non-real-time) signals like SIGCHLD are not queued: if several children exit while a SIGCHLD is already pending and not yet handled, those multiple events collapse into a single pending SIGCHLD. So the handler may run just once even though, say, three children exited. If the handler reaps exactly one child per invocation, the other two are never collected and become lingering zombies. The correct pattern is for the handler to loop, reaping every available child each time it runs: while (waitpid(-1, &status, WNOHANG) > 0) { } so it drains all finished children regardless of how many signals were coalesced. Relying on a one-to-one signal-to-child correspondence is the bug.`,
    tags: ['signals', 'wait', 'zombie'],
  },
  {
    id: 'lx-ch03-t-026',
    chapter: 3,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Why vfork And Plain fork Differ For exec',
    prompt: `A classic pattern is "fork then immediately exec a new program". Some code uses fork, some uses a leaner primitive that shares the parent's address space until exec. Explain the danger of doing ordinary work (allocating memory, modifying variables, returning from the function) in the child between such a lean fork and the exec, and why the simple fork-then-exec is safer.`,
    hints: [
      'The lean variant lets the child run in the parent address space.',
      'Only a tiny, careful set of actions is safe before exec.',
    ],
    solution: `The lean primitive (vfork-style) is built on the assumption that the child will do almost nothing except immediately exec or _exit, because until then it can be sharing or borrowing the parent's address space and execution context. If the child instead allocates memory, modifies variables, or returns from the calling function before exec, it can corrupt the parent's state or leave both processes in an undefined condition, since they are not yet truly independent. Ordinary fork gives the child its own copy of the address space immediately (logically, with copy-on-write under the hood), so the child can safely do setup work (close descriptors, dup2, set up the environment) before exec without disturbing the parent. So fork-then-exec is safer and more flexible; the lean variant trades that safety for speed and must be used with extreme discipline, doing nothing but exec/_exit in between.`,
    tags: ['fork', 'exec', 'compare'],
  },
  {
    id: 'lx-ch03-t-027',
    chapter: 3,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Reading /proc Atomically',
    prompt: `A monitoring tool opens /proc/1830/status, reads a few fields, then a moment later opens /proc/1830/fd and counts entries, and combines the two into one "snapshot". Why can this combined snapshot be internally inconsistent or even refer to a different process, and what does that reveal about /proc as a process view?`,
    hints: [
      'The process keeps running between your two reads.',
      'A PID can be reused after the process exits.',
    ],
    solution: `Each read from /proc reflects the kernel's state at the instant of that read, and the target process keeps running between your separate opens, so the status fields and the fd count are sampled at different moments and may not agree; the process could have opened or closed files, or changed state, in between. Worse, if process 1830 exits between the two reads and the PID is recycled, your second read could be describing a completely different process that now holds PID 1830. This reveals that /proc is a live, point-in-time window, not a frozen transactional snapshot: there is no atomic "read all of a process's state at once" guarantee across multiple files. Tools that need consistency must read quickly, re-check identity (for example via start time), or accept that combined cross-file snapshots are inherently best-effort.`,
    tags: ['proc', 'process', 'introspection'],
  },
  {
    id: 'lx-ch03-t-028',
    chapter: 3,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Self-Pipe For Safe Signal Handling',
    prompt: `An event-loop program must react to SIGTERM, but its loop blocks waiting on file descriptors and cannot poll a flag. A known technique is the "self-pipe trick": the SIGTERM handler writes one byte to a pipe whose read end is watched by the event loop. Explain why this is both async-signal-safe and integrates the signal into the loop cleanly.`,
    hints: [
      'What is the only real work the handler does?',
      'write is on the async-signal-safe list; the loop already watches descriptors.',
    ],
    solution: `The handler's only action is a single write() of one byte to the pipe's write end. write() is on the async-signal-safe list, so it is legal to call from a signal handler, unlike printf or malloc; the handler does no allocation, no stdio, and touches no complex shared state, so it cannot deadlock or corrupt anything even if the signal lands at an arbitrary instant. Meanwhile the event loop already blocks waiting on a set of file descriptors, and the pipe's read end is one of them, so when the byte arrives the loop wakes up just as it would for any I/O event and then does the real shutdown work in normal (non-handler) context. This converts an asynchronous, restricted signal into an ordinary, safely-handled readable-descriptor event, giving clean integration without doing unsafe work inside the handler.`,
    tags: ['signals', 'pipe', 'design'],
  },
  {
    id: 'lx-ch03-t-029',
    chapter: 3,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'exec Preserves Some Descriptors',
    prompt: `A process opens a sensitive file (descriptor 5), then forks and exec's an untrusted helper program. To everyone's surprise the helper can still read from descriptor 5. Explain why exec did not close it, and what mechanism is meant to prevent this leak.`,
    hints: [
      'exec replaces the image but not the descriptor table by default.',
      'Descriptors carry a flag that controls behavior across exec.',
    ],
    solution: `exec replaces the program image but, by default, keeps the process's open file descriptors intact, because that is exactly how a shell hands stdin/stdout/stderr (descriptors 0,1,2) and redirections to the new program. So descriptor 5 survives the exec and is inherited by the untrusted helper, which can read the sensitive file: a descriptor leak. The intended mechanism to prevent this is the close-on-exec flag (FD_CLOEXEC, settable with fcntl or by opening with O_CLOEXEC): a descriptor marked close-on-exec is automatically closed by the kernel during a successful exec, so it is not passed to the new program. The fix is to set close-on-exec on descriptor 5 (ideally at open time with O_CLOEXEC) so that only the descriptors you deliberately want to share survive into the exec'd helper.`,
    tags: ['exec', 'file-descriptor', 'security'],
  },
  {
    id: 'lx-ch03-t-030',
    chapter: 3,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Shell Implementation Of cd Versus ls',
    prompt: `In a simple shell, running "ls" works by fork+exec of /bin/ls, but "cd somedir" cannot be implemented that way; it must be a built-in handled by the shell itself. Using only the process model and exec semantics, explain precisely why fork+exec'ing a "cd" program would not change the shell's directory.`,
    hints: [
      'The current working directory is per-process state.',
      'What happens to the child after it exits?',
    ],
    solution: `The current working directory is per-process state inherited across fork but private to each process. If the shell forked and exec'd a hypothetical "cd" program, that change of directory would happen in the child process, not in the shell. When the child exits, its directory change vanishes with it, and the shell, which kept running unchanged, is still in the old directory. There is no way for the short-lived child to reach back and modify the parent shell's working directory, because processes do not share that state and a child cannot alter a parent's. Therefore cd must be a shell built-in that calls chdir() directly in the shell process, whereas ls is fine as an external command because it only produces output and its effects do not need to persist in the shell after it exits.`,
    tags: ['process', 'exec', 'shell'],
  },
]

export default problems
