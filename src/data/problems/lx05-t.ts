import type { Problem } from '../../types'

const problems: Problem[] = [
  {
    id: 'lx-ch05-t-001',
    chapter: 5,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'What .config Actually Is',
    prompt: `After running make menuconfig and saving, you see a file named .config appear at the top of the kernel source tree. A teammate deletes it because "it is just a dotfile clutter," then runs make and is confused. In plain terms, what is .config, and why does removing it derail the build?`,
    hints: ['It records every CONFIG option choice.', 'Kbuild reads it to decide what to compile.'],
    solution: `.config is the saved record of every Kconfig choice for this build: each option is a line like CONFIG_EXT4_FS=y, CONFIG_FOO=m, or "# CONFIG_BAR is not set". The whole Kbuild system reads it to decide which files compile, which features are built in (=y), built as modules (=m), or left out. Deleting it throws away all your configuration, so the next make has nothing to build from and will ask you to configure again (or it regenerates defaults), losing your choices. That is why you configure first (menuconfig/defconfig) to produce .config, and only then run make.`,
    tags: ['kconfig', 'config', 'kbuild'],
  },
  {
    id: 'lx-ch05-t-002',
    chapter: 5,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'Why menuconfig Exists',
    prompt: `The kernel has tens of thousands of configuration options. A newcomer asks why anyone uses menuconfig instead of just hand-editing .config in a text editor. Give two concrete reasons menuconfig is safer and easier than raw editing.`,
    hints: ['Dependencies between options.', 'Help text and search.'],
    solution: `menuconfig understands the Kconfig dependency graph, so when you enable an option it automatically pulls in (or greys out) the options it depends on, and prevents you from creating an inconsistent .config that will not build. Hand-editing a single CONFIG line can silently violate a "depends on" rule, leaving a setting that the build quietly drops or that breaks the configuration step. menuconfig also shows help text for each symbol and lets you search ("/") for an option by name, so you can discover what a setting does and where it lives. In short, it gives you guided, dependency-aware, documented editing instead of error-prone manual edits.`,
    tags: ['kconfig', 'menuconfig', 'config'],
  },
  {
    id: 'lx-ch05-t-003',
    chapter: 5,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'What dmesg Shows You',
    prompt: `Right after a kernel boots, you run dmesg and see a stream of timestamped lines describing CPU detection, memory layout, and driver initialization. What is this buffer called, who writes to it, and why is it the first place to look when a freshly built kernel misbehaves at boot?`,
    hints: ['The kernel ring buffer.', 'printk writes there.'],
    solution: `dmesg prints the kernel ring buffer, a fixed-size in-memory log that the kernel itself writes to via printk() during boot and runtime. Every subsystem and driver logs its progress and any warnings or errors there, with timestamps relative to boot. When a new kernel misbehaves, this log is the earliest and most direct evidence of what the kernel actually did: which devices it found, which drivers loaded, and where something failed (a panic, a missing driver, a wrong command-line option). Because it is the kernel's own narration of boot, reading it usually points straight at the failing step.`,
    tags: ['dmesg', 'boot', 'printk'],
  },
  {
    id: 'lx-ch05-t-004',
    chapter: 5,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'Why QEMU For Kernel Testing',
    prompt: `A student wants to test their freshly built kernel but is nervous about installing it on their only laptop. Explain why booting the new kernel inside QEMU is a sensible first step, and what bad outcome it protects them from compared to installing it on the real machine.`,
    hints: ['QEMU is an emulated/virtual machine.', 'A kernel panic in a VM is harmless.'],
    solution: `QEMU runs a complete emulated machine as an ordinary program, so the new kernel boots inside a sandbox instead of on the bare hardware. If the kernel panics, fails to find its root filesystem, or hangs, only the QEMU process is affected; you just close it and try again, and your real system is untouched. Installing an unbootable kernel directly on the laptop could leave the machine unable to boot, forcing a painful recovery. QEMU also makes iteration fast: you can pass a fresh bzImage and command line on each run without touching a bootloader. It is the standard, low-risk way to validate a kernel before trusting it on real hardware.`,
    tags: ['qemu', 'boot', 'testing'],
  },
  {
    id: 'lx-ch05-t-005',
    chapter: 5,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'vmlinux Versus bzImage',
    prompt: `Your build produces both vmlinux at the top of the tree and arch/x86/boot/bzImage. A colleague asks which one to hand to QEMU's -kernel option and why the two files differ so much in size and format. Explain the relationship between vmlinux and bzImage.`,
    hints: ['One is the raw ELF, one is compressed and bootable.', 'Bootloaders load bzImage.'],
    solution: `vmlinux is the full, uncompressed kernel as a raw ELF object: it contains all the symbols and is what you use for debugging and as the input to later steps, but it is not itself a directly bootable image. bzImage is the packaged, compressed kernel produced for x86 from vmlinux, wrapped with the small setup/boot code a bootloader (or QEMU's -kernel) needs to load and start it. So you hand bzImage to QEMU's -kernel option because that is the bootable artifact. The size difference comes from bzImage being compressed (plus stripped of debug info), while vmlinux carries the full uncompressed image and symbol table.`,
    tags: ['vmlinux', 'bzimage', 'artifacts'],
  },
  {
    id: 'lx-ch05-t-006',
    chapter: 5,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Built-In Versus Module',
    prompt: `In menuconfig many options can be set to "y" or "m". For the ext4 filesystem driver, what is the practical difference between CONFIG_EXT4_FS=y and CONFIG_EXT4_FS=m at boot time, and when might =y be the only choice that works?`,
    hints: ['y means compiled into the kernel image; m means a separate .ko.', 'Think about the root filesystem.'],
    solution: `=y compiles ext4 directly into the kernel image, so the code is present the instant the kernel starts. =m builds it as a loadable module (a .ko file) that lives outside the image and must be inserted later, typically by userspace or an initramfs after boot has progressed. If ext4 is the filesystem that holds your root partition, you cannot mount root to then load the module from that same disk, a chicken-and-egg problem, so the driver must either be built in (=y) or already present in the initramfs. That is why core, boot-critical drivers are usually built in, while optional or rarely used drivers are good candidates for =m to keep the image small.`,
    tags: ['kconfig', 'modules', 'builtin'],
  },
  {
    id: 'lx-ch05-t-007',
    chapter: 5,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'What initramfs Is For',
    prompt: `When booting a kernel under QEMU you often pass an initramfs (an -initrd image). What problem does the initramfs solve during early boot, and what would go wrong if the root filesystem driver were a module and you booted with no initramfs and no built-in driver for it?`,
    hints: ['It is a tiny userspace available before the real root is mounted.', 'It can hold modules needed to reach root.'],
    solution: `The initramfs is a small, self-contained root filesystem the kernel unpacks into memory very early, giving you a working userspace (an init program plus any tools and modules) before the real root device is mounted. Its job is to bridge the gap: load whatever drivers are needed to find and mount the actual root filesystem, then hand off to it. If the root filesystem's driver is only a module and it is neither built in nor present in an initramfs, the kernel reaches the point of mounting root, has no way to read that device, and panics with "VFS: Unable to mount root fs." The initramfs (or a built-in driver) is exactly what prevents that.`,
    tags: ['initramfs', 'boot', 'rootfs'],
  },
  {
    id: 'lx-ch05-t-008',
    chapter: 5,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Why make Only Rebuilds Some Files',
    prompt: `You change one line in a single .c file deep in the tree and run make again. Instead of recompiling the whole kernel, it rebuilds only a handful of objects and relinks. Explain how the Kbuild/make system knows to do so little work, and why that is correct.`,
    hints: ['Make compares timestamps against dependencies.', 'Only changed files and their dependents rebuild.'],
    solution: `make tracks dependencies and modification times: each object file depends on its source and on the headers it includes, recorded by Kbuild. When you run make, it compares the timestamp of each target against its prerequisites and only rebuilds targets whose inputs are newer. Editing one .c file marks just that file (and anything that includes a changed header) as out of date, so only those objects recompile, and then the final image relinks. This is correct because unchanged source produces identical object files, so recompiling them would waste time without changing the result. It is what makes incremental kernel builds fast after the first full build.`,
    tags: ['kbuild', 'make', 'incremental'],
  },
  {
    id: 'lx-ch05-t-009',
    chapter: 5,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Passing root= On The Command Line',
    prompt: `Two engineers boot the same bzImage in QEMU. One passes the kernel command line root=/dev/sda1 and the other passes root=/dev/vda1, and only one boots. Explain what the root= parameter tells the kernel, and why the correct device name depends on how QEMU exposes the disk.`,
    hints: ['root= names the device holding the real root filesystem.', 'Different disk buses get different device names.'],
    solution: `The root= kernel command-line parameter tells the kernel which block device contains the real root filesystem to mount once early boot is done. The kernel does not guess; it uses exactly the name you give. The right name depends on the emulated hardware: a disk attached as a traditional SATA/IDE controller appears as /dev/sdaN, while a virtio-blk disk (QEMU's -drive if=virtio) appears as /dev/vdaN. So if QEMU presents the disk over virtio, root=/dev/sda1 names a device that does not exist and the mount fails, whereas root=/dev/vda1 works, and vice versa. The parameter is correct only when it matches how QEMU actually exposes the disk.`,
    tags: ['cmdline', 'qemu', 'rootfs'],
  },
  {
    id: 'lx-ch05-t-010',
    chapter: 5,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Reading The First Boot Lines',
    prompt: `The very first lines of dmesg often include a line beginning "Linux version ..." followed by "Command line: ...". As a debugging habit, why are these two lines the most valuable to read first when a build seems to behave unexpectedly?`,
    hints: ['One confirms which kernel actually ran.', 'One shows the exact parameters it received.'],
    solution: `The "Linux version" line records the exact version string, compiler, and build timestamp of the kernel that is actually running, so it confirms you booted the image you think you did and not a stale one. The "Command line" line echoes back the full kernel command line the kernel received, letting you verify that the parameters you intended (root=, console=, custom options) actually arrived intact and were not mangled by the bootloader or QEMU invocation. Many "mysterious" failures are really the wrong kernel or a typo'd parameter, and these two lines catch both immediately. Confirming them first saves you from debugging behavior that was never going to happen with the inputs you assumed.`,
    tags: ['dmesg', 'boot', 'cmdline'],
  },
  {
    id: 'lx-ch05-t-011',
    chapter: 5,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'defconfig As A Starting Point',
    prompt: `Instead of answering thousands of questions in menuconfig, many people start a build with make defconfig (or an arch-specific defconfig) and then make small changes. Explain what defconfig gives you and why it is a smarter starting point than configuring from a blank slate.`,
    hints: ['defconfig is a sane, maintainer-provided baseline.', 'It produces a known-good .config.'],
    solution: `make defconfig generates a .config from a default configuration the kernel maintainers ship for that architecture: a sane, known-to-boot baseline with the common drivers and core options enabled. Starting from it means you begin with a configuration that is already consistent and builds a working kernel, so you only need to toggle the handful of options you actually care about. Configuring from a blank slate risks missing a critical option (for example the driver for your root device or console) and producing a kernel that compiles but will not boot. defconfig turns "get thousands of choices right" into "tweak a known-good baseline," which is far less error-prone.`,
    tags: ['kconfig', 'defconfig', 'config'],
  },
  {
    id: 'lx-ch05-t-012',
    chapter: 5,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Why You Need console= In QEMU',
    prompt: `A student boots a kernel in QEMU using -nographic but sees no kernel messages at all, even though the kernel is clearly running. They add console=ttyS0 to the kernel command line and the boot log appears. Explain what console= controls and why the messages were missing before.`,
    hints: ['console= chooses where printk output is directed.', '-nographic routes the serial port to your terminal.'],
    solution: `The console= parameter tells the kernel which device should act as the system console, i.e. where early printk/boot messages are written. With QEMU's -nographic, there is no graphical display and the emulated serial port (ttyS0) is wired to your terminal, but the kernel by default may direct its console to the (non-visible) VGA console, so its messages go somewhere you cannot see. Adding console=ttyS0 redirects kernel output to the serial port, which -nographic has connected to your terminal, so the boot log finally shows up. Nothing was wrong with the kernel; the output was simply being sent to a console you were not watching.`,
    tags: ['cmdline', 'qemu', 'console'],
  },
  {
    id: 'lx-ch05-t-013',
    chapter: 5,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Enable A Driver And Rebuild Correctly',
    prompt: `You forgot to enable the virtio network driver and your QEMU guest has no network. You run make menuconfig, set CONFIG_VIRTIO_NET=y, save, and then run make. Walk through what actually has to happen for the running test to pick up the change, and explain why simply editing the source files would not have been enough.`,
    hints: ['Changing .config changes which files compile.', 'You must rebuild the image and reboot QEMU with it.'],
    solution: `Setting CONFIG_VIRTIO_NET=y changes .config so that the virtio-net source is now selected to compile into the image. Running make then recompiles the newly enabled object(s), links them into a fresh vmlinux, and regenerates bzImage. The change does not affect the kernel currently running in QEMU; you must stop that VM and relaunch QEMU pointing -kernel at the newly built bzImage so the guest boots the kernel that contains the driver. Editing source files alone would not help because the file was never being compiled in the first place: the Kconfig symbol gates whether that code is part of the build, so the option must be turned on, the image rebuilt, and QEMU rebooted with it.`,
    tags: ['kconfig', 'kbuild', 'rebuild'],
  },
  {
    id: 'lx-ch05-t-014',
    chapter: 5,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'VFS Unable To Mount Root',
    prompt: `Your kernel boots, prints lots of messages, then ends with: "VFS: Cannot open root device or unknown-block(0,0)" followed by a panic. The disk is fine and the filesystem is ext4. List the two or three most likely causes that are fully explained by topic-5 concepts, and how you would distinguish them.`,
    hints: ['Wrong root= device name.', 'Missing filesystem/block driver (not built in, not in initramfs).'],
    solution: `Because the kernel got far enough to try mounting root and then could not, the failure is at the "find and mount root" step. The likely causes are: (1) the root= parameter names a device that does not exist for this QEMU setup (e.g. root=/dev/sda1 when the disk is virtio /dev/vda1), distinguishable by checking the "Command line" line and how QEMU attaches the disk; (2) the driver for the root block device is not built in and not in an initramfs, so the kernel cannot see the disk at all; (3) the ext4 filesystem driver itself is a module that is missing, so the device is found but cannot be mounted. You distinguish them by reading dmesg for whether the disk/partition was detected, and by confirming CONFIG_EXT4_FS and the relevant block driver are =y (or present in the initramfs). The unknown-block(0,0) hint typically means the device named by root= was never found.`,
    tags: ['boot', 'rootfs', 'panic'],
  },
  {
    id: 'lx-ch05-t-015',
    chapter: 5,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Compare Built-In Init Versus initramfs Init',
    prompt: `Two QEMU setups both boot the same kernel. Setup A passes a full disk image as root with an init program on it; Setup B passes only an initramfs containing /init and no separate root disk. Compare what runs as the first userspace process in each case, and explain how the kernel decides what to execute first.`,
    hints: ['init= and the default init paths.', 'initramfs /init runs before any root is mounted.'],
    solution: `In both cases, once early kernel setup is done the kernel must launch a first userspace process (PID 1). If an initramfs is present, the kernel runs /init inside that unpacked in-memory filesystem first; in Setup B that /init is the entire userspace, so it stays the running system (or could mount something later). In Setup A, the typical flow is that an initramfs (or built-in logic) eventually mounts the real root disk and then executes its init program (commonly /sbin/init), and the init= command-line parameter, if given, overrides which program is run. So the difference is just which userspace the kernel hands control to: the in-memory /init from the initramfs, versus an init binary on the mounted root filesystem. The kernel always needs exactly one first process, and panics if it cannot start one ("No working init found").`,
    tags: ['initramfs', 'init', 'boot'],
  },
  {
    id: 'lx-ch05-t-016',
    chapter: 5,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Stale bzImage Trap',
    prompt: `You edit code, run make, and reboot QEMU, but your change does not appear. The "Linux version" line in dmesg shows a build timestamp from yesterday. Explain what most likely happened, and how the version/timestamp line let you diagnose it without re-reading your source changes.`,
    hints: ['QEMU may be loading an old image path.', 'The build timestamp proves which kernel ran.'],
    solution: `The build timestamp in the "Linux version" line is set when that kernel image was compiled, so a yesterday's date means QEMU booted an old bzImage, not the one you just built. The usual cause is that QEMU's -kernel is pointing at a stale path (or you copied the image to the wrong place, or make actually failed and the old image remained), so today's compile never reached the running kernel. This diagnosis comes entirely from the version line: it tells you which binary ran, independent of what your source now says. The fix is to confirm make succeeded, locate the freshly produced bzImage, and make sure QEMU's -kernel points exactly at it before rebooting.`,
    tags: ['dmesg', 'qemu', 'rebuild'],
  },
  {
    id: 'lx-ch05-t-017',
    chapter: 5,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Parallel Build With -j',
    prompt: `Someone builds the kernel with plain make on an 8-core machine and it takes a long time, while a colleague uses make -j8 and finishes much faster, with identical resulting images. Explain what -j does, why the kernel build is well suited to it, and why the output kernel is the same either way.`,
    hints: ['-j sets parallel jobs.', 'Compiling independent .c files is parallelizable.'],
    solution: `make -j8 lets make run up to eight compile/link jobs at once instead of one at a time. The kernel build is well suited to this because most of the work is compiling thousands of independent .c files into object files, and those compilations have no ordering dependency on each other, so they can run in parallel across cores. make still respects real dependencies (it links only after the needed objects exist), so it never reorders steps that actually depend on one another. The resulting image is identical because parallelism only changes the order and concurrency of independent steps, not the inputs or the compiler output; each object file comes out the same regardless of how many ran simultaneously.`,
    tags: ['kbuild', 'make', 'parallel'],
  },
  {
    id: 'lx-ch05-t-018',
    chapter: 5,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Why quiet Changes The Log',
    prompt: `Adding the word "quiet" to the kernel command line dramatically shortens what scrolls by during boot, yet dmesg afterward still shows the full detailed log. Explain how a single command-line word can suppress messages on screen without actually losing them, in terms of where messages go.`,
    hints: ['quiet raises the console loglevel threshold.', 'The ring buffer still receives everything.'],
    solution: `Every printk message carries a log level (severity). The "quiet" parameter raises the console loglevel threshold so that only high-severity messages are printed to the console during boot, while lower-severity informational messages are filtered out from the screen. Crucially, all messages are still written to the kernel ring buffer regardless of the console threshold, so nothing is actually discarded, the console is simply not showing the less important lines. That is why dmesg, which reads the ring buffer, still displays the complete detailed log afterward. So "quiet" only changes what is displayed live, not what is recorded.`,
    tags: ['cmdline', 'dmesg', 'loglevel'],
  },
  {
    id: 'lx-ch05-t-019',
    chapter: 5,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Where The Command Line Comes From',
    prompt: `In a real boot, the bootloader (GRUB) passes the kernel command line; in QEMU you pass it with -append. The kernel itself does not invent these parameters. Trace, at a high level, how a string like "console=ttyS0 root=/dev/vda1 quiet" reaches and affects the kernel, and why this separation is useful.`,
    hints: ['Bootloader/QEMU hands the string to the kernel at load time.', 'The kernel parses it early and configures itself.'],
    solution: `The command-line string is supplied by whatever loads the kernel: GRUB on real hardware, or QEMU's -append in emulation. That loader places the string where the kernel's boot setup code can find it, and very early in boot the kernel parses it and lets subsystems consume their parameters: console= sets the console device, root= selects the root device, quiet adjusts the console loglevel. The kernel then echoes the whole string in the "Command line" dmesg line. This separation is useful because the same kernel image can be booted with different behavior, different console, root device, or verbosity, just by changing the externally supplied line, without recompiling. It keeps boot policy (set by the loader/operator) distinct from the kernel binary itself.`,
    tags: ['cmdline', 'boot', 'qemu'],
  },
  {
    id: 'lx-ch05-t-020',
    chapter: 5,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'depends On Greys Out An Option',
    prompt: `In menuconfig you try to enable a driver but its entry is greyed out and you cannot select it. The help text mentions it "depends on" another symbol. Explain what is happening in Kconfig terms, and the correct way to make the driver selectable rather than hand-editing .config to force it on.`,
    hints: ['A depends-on prerequisite is not satisfied.', 'Enable the prerequisite first.'],
    solution: `In Kconfig, a symbol can declare "depends on SOMETHING," meaning it only becomes selectable when that prerequisite option is enabled. If the prerequisite is off, menuconfig greys out the dependent option because choosing it would be meaningless or unbuildable, the code it gates needs the prerequisite's support. The correct fix is to find and enable the prerequisite symbol first (often the bus or framework the driver needs), after which the driver entry becomes selectable. Hand-editing .config to force CONFIG_DRIVER=y while its dependency is unset is wrong: the configuration step will detect the unmet dependency and drop or override your line, so the option will not actually take effect. Satisfying dependencies through the menu is the reliable path.`,
    tags: ['kconfig', 'menuconfig', 'dependencies'],
  },
  {
    id: 'lx-ch05-t-021',
    chapter: 5,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Read A Driver Init Failure',
    prompt: `Late in dmesg you see: "myeth: probe of 0000:00:03.0 failed with error -12". You know -12 is ENOMEM. Reasoning only from boot-log and topic-5 concepts, what is this line telling you about the driver, and what general category of problem does an error during probe represent?`,
    hints: ['probe runs when the driver binds to a device.', 'A negative errno means initialization failed.'],
    solution: `This line is the kernel reporting that the myeth driver tried to initialize (probe) a specific device, the PCI device at 0000:00:03.0, and that initialization returned a failure code. A driver's probe runs when it is matched to a device during boot, and returning a negative errno means the driver could not bring the device up. Here -12 (ENOMEM) means it failed because a memory allocation during setup did not succeed. The general category is a driver-initialization failure: the device was detected and the driver attempted to claim it, but setup did not complete, so that device will not be usable. From the log alone you know which driver, which device, and the errno reason, which is enough to direct further investigation.`,
    tags: ['dmesg', 'boot', 'drivers'],
  },
  {
    id: 'lx-ch05-t-022',
    chapter: 5,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Bisecting Config Versus Code',
    prompt: `A kernel built from a known-good commit boots fine, but after you change only .config (no source edits) it no longer boots and panics trying to mount root. A colleague insists "you must have broken the code." Argue, using topic-5 reasoning, why the cause is almost certainly configuration, and outline a disciplined way to find the offending option.`,
    hints: ['Source is unchanged, so behavior change comes from what was compiled in or out.', 'Compare against the known-good .config.'],
    solution: `Since not a single source line changed, the kernel's logic is identical to the known-good build; the only variable is .config, which controls which code is compiled in, as a module, or omitted. A panic at mount-root after a config change is the classic signature of having turned off something boot-critical, for example the root filesystem driver or the relevant block driver, so the kernel can no longer reach root. The disciplined approach is to diff the new .config against the known-good one to see exactly which symbols changed, then re-enable the boot-critical ones (filesystem and block/console drivers as built-in), rebuild, and retest, narrowing down which toggled option breaks boot. Because the code is constant, this is a configuration bisection, not a code bug, and the boot log (which driver/mount step fails) tells you which option to look at first.`,
    tags: ['kconfig', 'boot', 'debugging'],
  },
  {
    id: 'lx-ch05-t-023',
    chapter: 5,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Design A Minimal Bootable QEMU Setup',
    prompt: `You want the smallest possible setup that boots your custom kernel in QEMU to a shell, with no real disk involved. Describe the pieces you need (kernel artifact, what provides userspace, what the command line must specify) and explain why an initramfs containing a static /init lets you skip a root disk entirely.`,
    hints: ['bzImage plus an initramfs is enough.', '/init in the initramfs becomes PID 1; no root= disk needed.'],
    solution: `The minimal setup is: a bootable kernel artifact (bzImage) passed to QEMU's -kernel, an initramfs image passed as -initrd that contains a working userspace, and a command line that directs the console somewhere you can see it (console=ttyS0 with -nographic). The initramfs needs at minimum an executable /init, ideally a statically linked one so it has no library dependencies. Because the kernel unpacks the initramfs into memory and runs its /init as PID 1 before any real root must be mounted, that /init can simply give you a shell, and you never need a root disk or a root= parameter at all. This works precisely because the initramfs is the userspace; the chicken-and-egg of "need a driver to mount root before you have userspace" disappears when the entire userspace already lives in the in-memory image.`,
    tags: ['initramfs', 'qemu', 'design'],
  },
  {
    id: 'lx-ch05-t-024',
    chapter: 5,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Why Compressing Saves vmlinux',
    prompt: `bzImage is essentially a compressed vmlinux plus a small decompressor and setup code. Explain the boot-time mechanism by which a compressed image still ends up running as ordinary kernel code in memory, and what tradeoff compression buys versus simply loading the raw uncompressed image.`,
    hints: ['A small stub decompresses the kernel into memory at boot.', 'Smaller image to load, slight decompression cost.'],
    solution: `bzImage carries the compressed kernel together with a small self-extracting stub. When the bootloader (or QEMU) loads bzImage and jumps into it, that stub runs first and decompresses the real kernel image into memory, then transfers control to the now-expanded, ordinary kernel code, which proceeds with normal boot. So at runtime the kernel is the same uncompressed code that vmlinux represents; compression only affects how it was stored and transported. The tradeoff is that the on-disk/loaded image is much smaller (faster to read from disk or load over a wire, and it fits constrained boot paths), at the cost of a brief, one-time decompression step early in boot. For almost all systems the smaller image is well worth the negligible decompression time.`,
    tags: ['bzimage', 'vmlinux', 'boot'],
  },
  {
    id: 'lx-ch05-t-025',
    chapter: 5,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'No Output At All In QEMU',
    prompt: `You launch QEMU with -kernel bzImage and -nographic and see absolutely nothing, not even an error, and QEMU appears to hang. Enumerate the distinct topic-5 reasons "no output" can occur, and explain how you would tell a silent-but-running kernel apart from a kernel that never started.`,
    hints: ['Wrong/missing console= so output goes elsewhere.', 'Image not actually bootable, or no init so it panics silently.'],
    solution: `"No output" has several distinct causes. First, the kernel may be running but sending its messages to the wrong console: with -nographic you must pass console=ttyS0, otherwise printk goes to the unseen VGA console and your terminal stays blank. Second, the command line might include quiet (or a high loglevel) suppressing console output even though the ring buffer fills. Third, the image may not be a valid bootable kernel for that setup, so it never really starts. Fourth, the kernel may boot but fail to find a first userspace process and panic. To distinguish a silent-but-running kernel from one that never started, add console=ttyS0 and remove quiet first; if messages now appear, it was a console issue. If still nothing, verify the artifact is the freshly built bzImage and that QEMU's machine/disk options are sane. A kernel that got far enough usually prints at least the early banner once the console is correct, whereas a non-starting image yields no banner regardless of console settings.`,
    tags: ['qemu', 'console', 'debugging'],
  },
  {
    id: 'lx-ch05-t-026',
    chapter: 5,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Trust The Timestamps Or Force A Rebuild',
    prompt: `After pulling new commits, you run make and it finishes suspiciously fast, and you doubt it rebuilt what it should. Explain when make's timestamp-based incremental logic can legitimately do almost nothing, when it might be fooled, and why "make clean" or removing .o files is the blunt but reliable remedy.`,
    hints: ['Few changed files means little to rebuild.', 'Clock skew or odd timestamps can confuse make.'],
    solution: `make decides what to rebuild by comparing each target's modification time to its prerequisites, so a fast build can be perfectly legitimate: if the new commits touched only a few files, only those objects and their dependents recompile, and a near-instant make is the expected, correct outcome of incremental building. It can be fooled, though, when timestamps lie, for example clock skew between machines or filesystems, files restored with old mtimes, or a generated header whose dependency was not tracked, so make sees a target as newer than a source that actually changed. The blunt remedy is make clean (or deleting the .o files), which removes the built artifacts so every target is missing and must be regenerated from scratch. That sacrifices the speed of incremental builds but guarantees the output reflects the current source, which is why it is the go-to when you suspect the incremental logic was misled.`,
    tags: ['kbuild', 'make', 'incremental'],
  },
  {
    id: 'lx-ch05-t-027',
    chapter: 5,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Module Versus Built-In For A Boot Driver',
    prompt: `A reviewer suggests building the SATA controller driver as a module (=m) "to keep the kernel small," but your test system boots its root filesystem from a SATA disk and you use no initramfs. Explain the boot-time consequence of taking that advice, and articulate the general rule for deciding =y versus =m for any given driver.`,
    hints: ['The driver needed to reach root cannot be loaded from root.', 'Boot-critical drivers must be built in or in initramfs.'],
    solution: `If the SATA controller driver is =m and there is no initramfs, the kernel reaches the point of mounting root, has no driver to see the SATA disk (the module lives on a filesystem it cannot yet read), and panics, a classic chicken-and-egg failure. So the reviewer's suggestion would make the system unbootable in this configuration. The general rule: any driver required to reach and mount the root filesystem, plus the root filesystem driver itself and the console driver, should be built in (=y) or supplied via an initramfs that the kernel unpacks before mounting root. Drivers that are optional or only needed after userspace is up are good candidates for =m, which keeps the core image smaller without endangering boot. The deciding question is simply: "Is this needed before root is mounted?" If yes, it must not be a bare module.`,
    tags: ['kconfig', 'modules', 'boot'],
  },
  {
    id: 'lx-ch05-t-028',
    chapter: 5,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Interpreting A Panic Stack In dmesg',
    prompt: `Your QEMU boot ends with "Kernel panic - not syncing: Attempted to kill init!" along with a register dump. From topic-5 boot concepts alone, explain what the kernel was doing when this happened, why losing init is fatal, and two configuration/command-line causes consistent with this exact message.`,
    hints: ['init is PID 1; the kernel cannot continue without it.', 'Wrong init= path or an init that immediately exits/crashes.'],
    solution: `This panic happens after the kernel finished its own setup and handed control to the first userspace process, init (PID 1). The message means that PID 1 died or was about to be killed, and the kernel refuses to continue because init is the ancestor of all userspace; with no PID 1 there is nothing to run, so the only safe action is to panic. Causes consistent with topic-5 concepts include: an init= command-line path (or default init binary) that points to a program that crashes or immediately exits, for example a non-executable or dynamically linked /init in an initramfs with no libraries; or a userspace that is fundamentally broken/missing so the started process exits right away. In short, the kernel booted fine but the very first userspace program failed to keep running, and that is unrecoverable by definition.`,
    tags: ['boot', 'init', 'panic'],
  },
  {
    id: 'lx-ch05-t-029',
    chapter: 5,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'One Image, Many Boots',
    prompt: `Your team keeps a single bzImage but boots it three ways: a developer's noisy debug boot, a fast quiet boot, and a recovery boot to a shell. Explain how this is achievable without three separate kernel builds, and what specifically differs between the three, framing your answer around what is fixed in the image versus what is chosen at boot.`,
    hints: ['The image is fixed; the command line and initrd vary.', 'loglevel/quiet, init=, root= are boot-time choices.'],
    solution: `What the image fixes is the compiled-in code and configuration: which drivers and features are present is decided by .config at build time and is the same in all three boots. What varies is everything supplied at boot, principally the kernel command line and optionally a different initramfs. The noisy debug boot can drop quiet and raise the console loglevel to print verbose messages; the fast boot adds quiet to suppress them; the recovery boot can use init=/bin/sh (or an initramfs whose /init is a shell) to land directly at a prompt instead of normal startup, and can point root= at a different device. So one bzImage serves all three because boot policy, verbosity, which init runs, and which root to mount, is chosen by the loader/operator at boot time, not baked into the binary. This is exactly the value of keeping kernel code separate from boot-time parameters.`,
    tags: ['cmdline', 'boot', 'design'],
  },
  {
    id: 'lx-ch05-t-030',
    chapter: 5,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Where A Build Error Versus Boot Error Lives',
    prompt: `A teammate conflates two very different failures: "the kernel did not build" and "the kernel did not boot." Using topic-5 concepts, contrast where each failure surfaces, what artifacts exist (or do not) in each case, and why fixing a boot failure never involves the compiler while fixing a build failure never involves QEMU.`,
    hints: ['Build errors come from make/compiler before any image exists.', 'Boot errors come from running the produced image, seen in dmesg/panic.'],
    solution: `A build failure happens during make: the compiler or linker reports an error in the terminal, the process stops, and no usable new bzImage/vmlinux is produced (or the old one is left untouched). You fix it by correcting source or configuration so the code compiles, entirely in the build environment, QEMU is never involved because there is no new image to run yet. A boot failure happens only after a successful build, when you actually run the produced image: it surfaces as kernel messages, a hang, or a panic in dmesg/console under QEMU, and the artifacts (bzImage, vmlinux) exist and are valid binaries. You fix a boot failure by changing runtime inputs, command line (root=, console=), initramfs, or which boot-critical drivers were configured in, not by invoking the compiler, because the code already compiled correctly. Keeping these straight tells you immediately whether to look at the make output or at the boot log.`,
    tags: ['kbuild', 'boot', 'debugging'],
  },
]

export default problems
