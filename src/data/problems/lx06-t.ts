import type { Problem } from '../../types'

const problems: Problem[] = [
  {
    id: 'lx-ch06-t-001',
    chapter: 6,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'What module_init Registers',
    prompt: `A newcomer writes a kernel module with a function named setup() that prints a greeting, but nothing happens when they insmod it. In plain terms, what does the module_init() macro do, and why is it required for setup() to run on load?`,
    hints: ['The kernel needs to know which function to call.', 'A plain function name is not special by itself.'],
    solution: `module_init() registers a function as the module's entry point: it tells the kernel "call this function when the module is loaded." Without it, the kernel has no idea that setup() is meant to run at load time, so it is just an ordinary unused function. You fix it by writing module_init(setup); the kernel then invokes setup() during insmod and uses its return value (0 for success, negative errno for failure) to decide whether the module stays loaded. The matching module_exit() registers the function to run at unload.`,
    tags: ['module', 'init', 'lifecycle'],
  },
  {
    id: 'lx-ch06-t-002',
    chapter: 6,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'Why printk Instead of printf',
    prompt: `A C programmer new to the kernel asks why module code calls printk() (or pr_info()) instead of the familiar printf(). Explain why printf is unavailable in a kernel module and where printk's output actually goes.`,
    hints: ['The kernel does not link against the C standard library.', 'Output goes to the kernel ring buffer, not your terminal.'],
    solution: `A kernel module runs in kernel space and is not linked against the userspace C standard library, so printf and stdio simply do not exist there. printk is the kernel's own logging function; it writes into the kernel ring buffer (a fixed-size in-memory log) rather than to a process's stdout. You read that output with dmesg or via /var/log/kern.log, not in the terminal where you ran insmod. printk also understands log-level prefixes like KERN_INFO so messages can be filtered by severity.`,
    tags: ['printk', 'logging', 'module'],
  },
  {
    id: 'lx-ch06-t-003',
    chapter: 6,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'The Tainted Kernel Warning',
    prompt: `You build a tiny module that omits the MODULE_LICENSE() macro. On insmod, dmesg prints "module verification failed: signature and/or required key missing - tainting kernel" and a "module license 'unspecified' taints kernel" message. Why does leaving out MODULE_LICENSE cause the kernel to mark itself tainted?`,
    hints: ['The kernel must know if the module is GPL-compatible.', 'Some symbols are only available to GPL modules.'],
    solution: `MODULE_LICENSE() declares the module's license so the kernel can decide whether it is GPL-compatible. If it is missing, the kernel assumes the worst (license "unspecified"), which means it might be proprietary, and it sets the taint flag to record that a non-GPL-compatible module was loaded. Tainting does not stop the module from loading, but it marks the kernel so bug reports show that an unknown-license module was present, and it also blocks the module from using EXPORT_SYMBOL_GPL symbols. Declaring MODULE_LICENSE("GPL") states the module is GPL-compatible and avoids the taint.`,
    tags: ['module', 'license', 'taint'],
  },
  {
    id: 'lx-ch06-t-004',
    chapter: 6,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'insmod Versus modprobe',
    prompt: `Two ways to load a module are insmod ./mymod.ko and modprobe mymod. A beginner uses them interchangeably. Describe the key difference in what each tool does, especially regarding dependencies and where it looks for the file.`,
    hints: ['One takes a path, the other a module name.', 'One resolves dependencies, the other does not.'],
    solution: `insmod loads exactly the .ko file you point it at by path and does nothing else; if that module depends on symbols from another module that is not loaded, insmod fails. modprobe instead takes a module name, looks it up under /lib/modules/$(uname -r)/, reads the precomputed modules.dep dependency database, and automatically loads any modules it depends on first, in the right order. So modprobe is the high-level, dependency-aware tool meant for installed modules, while insmod is the low-level tool useful for an out-of-tree .ko you just built and have not installed.`,
    tags: ['insmod', 'modprobe', 'tools'],
  },
  {
    id: 'lx-ch06-t-005',
    chapter: 6,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Return Value of the Init Function',
    prompt: `An init function does its setup and then ends with return 1; thinking "1 means it worked." After insmod, the module is not loaded and dmesg shows nothing obviously wrong. Explain what the return value of a module init function actually means and why returning 1 misbehaves.`,
    hints: ['Kernel convention: 0 is success.', 'Nonzero is treated as an error code.'],
    solution: `A module init function returns an int using the kernel convention: 0 means success, and any nonzero value is treated as a failure with that value interpreted as a negative errno (you should return something like -ENOMEM or -EINVAL). Returning 1 is nonzero, so the kernel concludes init failed, immediately tears the module back down, and insmod reports an error; the module does not stay loaded. The correct success path is return 0. To signal a real failure you return a negative error code, for example return -ENODEV.`,
    tags: ['init', 'errno', 'module'],
  },
  {
    id: 'lx-ch06-t-006',
    chapter: 6,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Choosing a printk Log Level',
    prompt: `A module logs a recoverable hardware probe failure with printk(KERN_DEBUG "probe failed\\n") but the message never appears in the default dmesg view, while routine status messages logged at KERN_ERR flood the console. Explain what is wrong with these two log-level choices and how to fix them.`,
    hints: ['Log level controls both filtering and console printing.', 'KERN_ERR is for errors, KERN_DEBUG is usually suppressed.'],
    solution: `The log level both classifies severity and controls whether a message reaches the console based on the console_loglevel threshold. KERN_DEBUG is the lowest priority and is filtered out by default, so the genuinely important probe failure is hidden; it should be logged at a higher level such as KERN_WARNING or KERN_ERR (pr_warn/pr_err). Conversely, routine status is not an error, so logging it at KERN_ERR wrongly marks it critical and pushes it to the console, causing the flood; it belongs at KERN_INFO or KERN_DEBUG. Matching the level to the actual severity makes filtering work as intended.`,
    tags: ['printk', 'log-level', 'logging'],
  },
  {
    id: 'lx-ch06-t-007',
    chapter: 6,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'A module_param You Can Read at Runtime',
    prompt: `A module declares an int parameter with module_param(debug, int, 0644). A colleague claims the 0644 just documents that the value is an integer. Explain what the third argument to module_param actually controls and what 0644 specifically allows.`,
    hints: ['It is a permission mode for a sysfs file.', 'The type was already given by the second argument.'],
    solution: `The third argument is the permission mode for the parameter's file under /sys/module/<name>/parameters/, not a type hint (the type is the second argument, int here). 0644 means owner read+write and group/other read-only, so root can both read and change debug at runtime by writing to that sysfs file, while everyone can read it. Passing 0 would mean no sysfs file is created at all, so the parameter could only be set at load time via insmod debug=1. So 0644 makes the parameter both visible and runtime-tunable by root.`,
    tags: ['module_param', 'sysfs', 'permissions'],
  },
  {
    id: 'lx-ch06-t-008',
    chapter: 6,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Passing a Parameter at Load Time',
    prompt: `A module has module_param(count, int, 0644) with a default of int count = 3;. You want it to start with the value 10 for one load. Show how you pass that on the command line for both insmod and modprobe, and state what value count holds inside the init function.`,
    hints: ['Parameters are name=value pairs after the module.', 'The default applies only if you do not override it.'],
    solution: `You pass it as a name=value pair: insmod ./mymod.ko count=10 or modprobe mymod count=10. The kernel parses that before calling init, so by the time the init function runs, count holds 10, not the default 3. The default of 3 is only used when you do not supply count= at load. This is why module_param-declared variables should be read inside init rather than captured at compile time: their final value is set by the load command line.`,
    tags: ['module_param', 'insmod', 'modprobe'],
  },
  {
    id: 'lx-ch06-t-009',
    chapter: 6,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Why rmmod Fails with Busy',
    prompt: `You try rmmod mymod and get "ERROR: Module mymod is in use." The module's exit function has no bug. Explain in terms of the module reference count why the kernel refuses to unload, and name one common reason the count is above zero.`,
    hints: ['The kernel tracks who is using the module.', 'An open file or a dependent module can hold a reference.'],
    solution: `Each module has a reference (use) count; the kernel only allows rmmod when that count is zero, because unloading a module whose code is still in use would leave dangling pointers and crash the kernel. "In use" means something is currently relying on the module: for example a userspace process still has an open file descriptor to a device the module created, or another loaded module depends on a symbol this module exported, or a timer/work item it registered is still pending. Until those references are released (close the fd, unload the dependent module), the count stays above zero and rmmod safely refuses.`,
    tags: ['rmmod', 'refcount', 'unload'],
  },
  {
    id: 'lx-ch06-t-010',
    chapter: 6,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'What lsmod Reads',
    prompt: `lsmod prints columns for Module, Size, and "Used by." A student assumes lsmod queries the .ko files on disk. Explain what lsmod's source of information actually is and what the "Used by" column tells you.`,
    hints: ['It reflects currently loaded modules, not disk files.', 'It is a formatted view of a /proc file.'],
    solution: `lsmod reports modules currently loaded into the running kernel, not files on disk; it is essentially a nicely formatted view of /proc/modules. Each row is one loaded module with its in-memory size and a use count. The "Used by" column lists the reference count followed by the names of other loaded modules that depend on this one. That is why a module with nonzero use-by entries cannot be removed until its dependents are unloaded first, which directly explains many rmmod "in use" failures.`,
    tags: ['lsmod', 'proc', 'tools'],
  },
  {
    id: 'lx-ch06-t-011',
    chapter: 6,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Out-of-Tree Kbuild Makefile Basics',
    prompt: `For an out-of-tree module the canonical Makefile contains obj-m += mymod.o and a line invoking make -C /lib/modules/$(shell uname -r)/build M=$(PWD) modules. Explain the roles of obj-m, the -C option, and M= in getting your module built against the running kernel.`,
    hints: ['obj-m tells Kbuild what to build as a module.', 'The kernel build system actually does the compiling.'],
    solution: `obj-m += mymod.o tells the kernel build system (Kbuild) to compile mymod.o (from mymod.c) into a loadable module mymod.ko rather than build it into the kernel. The -C /lib/modules/$(uname -r)/build switches into the prepared kernel source/headers tree for the running kernel, so the module is compiled with the right configuration and headers. M=$(PWD) then tells that kernel Makefile to come back out to your directory and build the modules found there. So your Makefile is just a thin wrapper that hands control to the real kernel build infrastructure, pointed at your source via M=.`,
    tags: ['kbuild', 'makefile', 'out-of-tree'],
  },
  {
    id: 'lx-ch06-t-012',
    chapter: 6,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'MODULE_AUTHOR and MODULE_DESCRIPTION',
    prompt: `Besides MODULE_LICENSE, modules commonly include MODULE_AUTHOR and MODULE_DESCRIPTION. A reviewer says these "do nothing at runtime so delete them." Are they functionally required, and what practical value do they provide? Where can a user see them?`,
    hints: ['They are metadata embedded in the .ko.', 'modinfo reads them.'],
    solution: `MODULE_AUTHOR and MODULE_DESCRIPTION are not functionally required; unlike MODULE_LICENSE they do not affect tainting or symbol access, so the module loads fine without them. Their value is documentation: they embed human-readable metadata into the .ko that anyone can read with modinfo mymod.ko (or via /sys/module/...) without the source. That helps users and maintainers identify what a module does and who wrote it, which is why kernel style strongly encourages including them even though they are optional. Deleting them loses self-documentation for no real gain.`,
    tags: ['module', 'metadata', 'modinfo'],
  },
  {
    id: 'lx-ch06-t-013',
    chapter: 6,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Where init Messages Show Up',
    prompt: `After a successful insmod, a student opens the terminal expecting the pr_info() message from their init function and sees nothing. The module did load correctly. Explain why the terminal is empty and the one command they should run to see the message.`,
    hints: ['printk does not write to your shell.', 'The ring buffer is read with a tool.'],
    solution: `printk/pr_info writes to the kernel ring buffer, which is independent of the shell where you typed insmod, so the terminal stays empty even though the message was logged. To see it, run dmesg (often dmesg | tail or dmesg -w to follow). Depending on console_loglevel and your distro, the message may also appear on the system console or in /var/log/kern.log, but dmesg is the reliable way. The module loading silently in the terminal is expected behavior, not a sign that init did not run.`,
    tags: ['printk', 'dmesg', 'logging'],
  },
  {
    id: 'lx-ch06-t-014',
    chapter: 6,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'What __init Buys You',
    prompt: `A module marks its init function with __init: static int __init mymod_init(void). A teammate says "that annotation is just decoration." Explain what __init actually instructs the linker/kernel to do, and the consequence if you accidentally call an __init function later, after load completes.`,
    hints: ['__init code lives in a special section.', 'That memory is freed after init runs.'],
    solution: `__init places the function in a special .init.text section. Once the kernel (for built-in code) or the module loader has finished running all init code, that memory is freed and reclaimed, because one-time setup code is not needed afterward. For a module this also lets the kernel discard the init section after the module loads. The danger is that if you keep a pointer to an __init function and call it after init has completed, the code may have been freed, so you would jump into reclaimed memory and likely crash. So __init is a real optimization with a rule: only call __init functions during initialization, never store them for later use.`,
    tags: ['init', 'sections', 'memory'],
  },
  {
    id: 'lx-ch06-t-015',
    chapter: 6,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'When __exit Code Gets Discarded',
    prompt: `A module's cleanup function is marked __exit. A developer notices that if the kernel is built without module unloading support (CONFIG_MODULE_UNLOAD off), or the code is built into the kernel rather than as a module, the __exit function seems to vanish. Explain the rule behind __exit and why this disappearance is correct, not a bug.`,
    hints: ['__exit code is only useful if unloading can happen.', 'Built-in code never unloads.'],
    solution: `__exit marks code that is only ever needed to undo a module's setup when the module is unloaded. If unloading can never happen, that code is dead weight, so the kernel is free to drop it: when the same source is compiled built-in (=y) it never unloads, and when CONFIG_MODULE_UNLOAD is disabled modules cannot be removed at all, so the __exit section is discarded to save memory. This is why you must register the cleanup with module_exit() and never call an __exit function from normal (non-exit) paths: it may not exist. The disappearance is the intended optimization, matching __init's freeing of init-only code.`,
    tags: ['exit', 'sections', 'unload'],
  },
  {
    id: 'lx-ch06-t-016',
    chapter: 6,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Unwinding on Init Failure with goto',
    prompt: `An init function allocates a buffer, registers a device, then starts a timer. If starting the timer fails, the code simply does return -EIO; without undoing the earlier two steps. Explain what resource problem this creates and why the kernel idiom of goto error labels is the correct fix.`,
    hints: ['Anything you set up before the failure must be torn down.', 'Order the cleanup in reverse.'],
    solution: `Returning early without undoing the buffer allocation and device registration leaks them: the module did not load, but the allocated memory is never freed and the device stays registered, so you have a memory leak and a stale registration that can later collide or crash. The standard fix is a cascade of goto error labels: on each failure you jump to a label that unwinds exactly the resources acquired so far, in reverse order (unregister the device, then free the buffer), then return the error. For example on timer failure goto err_unreg; which unregisters and falls through to err_free; which frees the buffer. This keeps every partial-success path clean without duplicating cleanup code.`,
    tags: ['error-handling', 'cleanup', 'goto'],
  },
  {
    id: 'lx-ch06-t-017',
    chapter: 6,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'EXPORT_SYMBOL Versus a Plain Global',
    prompt: `Module A defines a global function helper_calc() and module B wants to call it. The author of A makes helper_calc non-static and assumes B can just declare and call it. Why is a non-static global function still not callable from B, and what does EXPORT_SYMBOL change?`,
    hints: ['Each module is a separate linkable object.', 'The kernel symbol table is the gatekeeper.'],
    solution: `Being non-static only makes helper_calc visible within module A's own object; it does not place the symbol into the kernel's global symbol table that the module loader uses to resolve cross-module references. When B is loaded, the loader looks up helper_calc in the kernel symbol table, does not find it, and fails to load B with an "unknown symbol" error. EXPORT_SYMBOL(helper_calc) explicitly publishes the symbol into that table, so B can resolve and link against it at load time (and A becomes "used by" B). EXPORT_SYMBOL_GPL does the same but only for GPL-compatible modules.`,
    tags: ['export_symbol', 'linking', 'symbols'],
  },
  {
    id: 'lx-ch06-t-018',
    chapter: 6,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Module Load Order and Dependencies',
    prompt: `Module B uses a symbol exported by module A. With both freshly built out of tree, insmod ./B.ko fails with "Unknown symbol" while modprobe B (if installed) would succeed. Explain the dependency relationship, why insmod fails here, and the two ways to make the load work.`,
    hints: ['B cannot resolve A symbols until A is loaded.', 'insmod does no dependency resolution.'],
    solution: `B depends on A because it references a symbol A exports, so A must be in the kernel symbol table before B can be linked. insmod loads exactly the file you name and does no dependency resolution, so loading B first leaves its reference to A's symbol unresolved and the load fails with "Unknown symbol." Two fixes: either insmod A.ko first and then insmod B.ko (manually satisfy the dependency in order), or use modprobe B against installed modules so it reads modules.dep and pulls in A automatically. The first works for raw out-of-tree .ko files; the second needs the modules installed and depmod run.`,
    tags: ['dependencies', 'insmod', 'modprobe'],
  },
  {
    id: 'lx-ch06-t-019',
    chapter: 6,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Static Versus Exported Symbol Pollution',
    prompt: `A module has many helper functions, all non-static "in case we need them later," and exports a few with EXPORT_SYMBOL. A reviewer asks you to make every helper that is not exported static. Explain the downside of leaving internal helpers non-static and how it interacts with the global namespace and EXPORT_SYMBOL.`,
    hints: ['Non-static names can collide across the kernel.', 'static gives the function internal linkage.'],
    solution: `Even without EXPORT_SYMBOL, a non-static function has external linkage within its object, which clutters the symbol space and risks name collisions with identically named functions elsewhere in the kernel, and it can confuse tooling and debugging. Marking internal helpers static gives them internal linkage so they are private to the file, cannot collide, and let the compiler optimize more aggressively (e.g., inline them). Exporting is a deliberate, separate decision: only the handful of functions other modules truly need should be non-static and wrapped in EXPORT_SYMBOL. So the rule is static by default, and make a symbol global only when you intend to export it.`,
    tags: ['symbols', 'static', 'export_symbol'],
  },
  {
    id: 'lx-ch06-t-020',
    chapter: 6,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'charp Parameter Lifetime',
    prompt: `A module declares a string parameter with module_param(name, charp, 0444) and stores the pointer in a struct during init for later use. Reason about whether holding onto that char* is safe, and what the charp type actually points to after load.`,
    hints: ['charp is a char pointer set by the loader.', 'The string buffer is owned by the module loader, not allocated by you.'],
    solution: `With charp, the kernel module loader parses name=... from the load command line and points your char* at a string it keeps for the life of the module load; you did not allocate it and should not free it. Storing the pointer for later read access is generally fine because that buffer persists while the module is loaded. The hazards are that the parameter can be empty/NULL if not supplied (with 0444 it is read-only in sysfs so it will not change underneath you), and you must never free() it or assume you own the memory. So treat a charp parameter as a borrowed, read-mostly string owned by the loader.`,
    tags: ['module_param', 'charp', 'lifetime'],
  },
  {
    id: 'lx-ch06-t-021',
    chapter: 6,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'permission 0444 Versus 0644 on a Param',
    prompt: `Two parameters use the same int type but one is module_param(max, int, 0444) and the other module_param(level, int, 0644). A user reports they can echo a new value into level's sysfs file but get "Permission denied" for max. Explain the design intent behind each permission and which to choose for a tunable versus a read-only config.`,
    hints: ['The mode is the sysfs file permission.', 'Write permission means runtime-changeable.'],
    solution: `The mode controls the /sys/module/<name>/parameters/ file's permissions. 0444 is read-only for everyone, so max can be inspected at runtime but never changed after load; that is right for a value that must stay fixed once the module is up (changing it later could be unsafe or meaningless). 0644 grants the owner (root) write access, so level can be retuned at runtime by writing to its sysfs file, appropriate for a knob like a debug verbosity that is safe to adjust live. So choose 0644 for things you intend operators to tune live, and 0444 (or 0) for fixed configuration you only want to expose for inspection.`,
    tags: ['module_param', 'permissions', 'sysfs'],
  },
  {
    id: 'lx-ch06-t-022',
    chapter: 6,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Why the Exit Function Must Mirror Init',
    prompt: `An init function registers a chardev, allocates a DMA-capable buffer, and starts a periodic timer. The exit function only frees the buffer. Predict what goes wrong after rmmod and explain the principle that the exit path must undo everything init created.`,
    hints: ['Leftover registrations and timers outlive the module code.', 'The module text is freed on unload.'],
    solution: `On rmmod the module's code is unloaded and its memory freed, but the chardev is still registered and the timer is still armed pointing at functions that no longer exist. When that timer next fires, it calls into freed code and crashes the kernel; the orphaned chardev registration also leaves a stale entry that can collide or be opened into nothing. The principle is symmetry: the exit function must reverse every resource init acquired, ideally in reverse order (stop/del the timer, unregister the chardev, then free the buffer). Forgetting any one leaves a resource that outlives the module and turns into a use-after-free or leak.`,
    tags: ['exit', 'cleanup', 'use-after-free'],
  },
  {
    id: 'lx-ch06-t-023',
    chapter: 6,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'GFP_KERNEL Allocation in Init Context',
    prompt: `An init function allocates with kmalloc(size, GFP_KERNEL). A reviewer asks: is GFP_KERNEL safe here, and would the answer change if this same allocation were performed inside an interrupt handler the module registers? Reason about process versus atomic context.`,
    hints: ['GFP_KERNEL may sleep.', 'Init runs in process context; interrupt handlers do not.'],
    solution: `A module's init function runs in process context (the insmod thread), where sleeping is allowed, so GFP_KERNEL is correct: it may block to reclaim memory if needed. Inside an interrupt handler the context is atomic, you cannot sleep, and using GFP_KERNEL would be a bug because the allocator might try to sleep and deadlock or trigger a "scheduling while atomic" warning. In atomic context you must use GFP_ATOMIC, which does not sleep (at the cost of being more likely to fail under pressure). So the flag choice depends on context: GFP_KERNEL in sleepable process context like init, GFP_ATOMIC in interrupt or other atomic context.`,
    tags: ['gfp', 'atomic-context', 'init'],
  },
  {
    id: 'lx-ch06-t-024',
    chapter: 6,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'A Param Write Handler That Can Sleep',
    prompt: `A module exposes module_param(level, int, 0644). The author wants writing the sysfs file to trigger a custom callback that calls copy_from_user-style logic and may allocate with GFP_KERNEL. Reason about what context a sysfs parameter store runs in and whether sleeping there is permitted.`,
    hints: ['sysfs store happens on a userspace write syscall.', 'That is process context.'],
    solution: `Writing a sysfs parameter file is driven by a userspace write() syscall, so the store callback runs in process context on behalf of that process, not in interrupt or atomic context. Therefore sleeping is allowed: GFP_KERNEL allocations and routines that may block are fine in that path. This is unlike, say, a timer or interrupt callback. The real caution is not the sleeping but correctness: validate the written value, and remember the simple module_param already handles parsing an int safely, so a custom store is only needed when you want side effects on change. Context-wise, though, a param store is sleepable process context.`,
    tags: ['module_param', 'context', 'sysfs'],
  },
  {
    id: 'lx-ch06-t-025',
    chapter: 6,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Race Between Init Registration and Callback',
    prompt: `An init function registers an interrupt handler or timer early, then continues allocating a buffer that the handler dereferences. Explain the race this ordering creates if the interrupt fires (or timer expires) before init finishes, and the ordering principle that avoids it.`,
    hints: ['Once registered, a callback can fire immediately.', 'Do not expose a resource before it is fully ready.'],
    solution: `As soon as you register the handler or arm the timer, it can be invoked at any moment, including before the rest of init runs. If the handler dereferences the buffer that init has not yet allocated (still NULL or uninitialized), it will null-deref or read garbage and crash. The ordering principle is to fully initialize everything a callback touches before you register/arm the callback, and on the error path unwind in the reverse order. Concretely: allocate and set up the buffer first, then register the handler last; on failure after registration, unregister before freeing. Never publish a resource that something else can act on until it is completely ready.`,
    tags: ['init-ordering', 'race', 'cleanup'],
  },
  {
    id: 'lx-ch06-t-026',
    chapter: 6,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'EXPORT_SYMBOL_GPL and a Non-GPL Module',
    prompt: `Module A exports a function with EXPORT_SYMBOL_GPL. Module B declares MODULE_LICENSE("Proprietary") and tries to use it; B fails to load with an unknown-symbol error even though the symbol clearly exists. Explain why, and how the result would differ if A had used plain EXPORT_SYMBOL.`,
    hints: ['GPL-only symbols check the consumer license.', 'License is read from MODULE_LICENSE.'],
    solution: `EXPORT_SYMBOL_GPL publishes a symbol that the loader only resolves for modules whose MODULE_LICENSE is GPL-compatible. When B declares a proprietary license, the loader treats the GPL-only symbol as invisible to B, so the reference is unresolved and B fails to load with an unknown-symbol error, even though the symbol physically exists. If A had used plain EXPORT_SYMBOL instead, the symbol would be available to any module regardless of license, so B would load (though loading a non-GPL module still taints the kernel). So the GPL variant is an intentional license gate enforced at symbol-resolution time.`,
    tags: ['export_symbol', 'license', 'symbols'],
  },
  {
    id: 'lx-ch06-t-027',
    chapter: 6,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Built-In Versus Module: Same Code, Different Lifecycle',
    prompt: `The same driver source is compiled =y (built into the kernel) in one config and =m (a loadable module) in another. Reason about what differs in how module_init/module_exit, __init/__exit, and unloading behave between the two builds.`,
    hints: ['Built-in code runs init at boot and never unloads.', 'module_exit and __exit only matter for =m.'],
    solution: `When built in (=y), module_init becomes a boot-time initcall run during kernel startup, the __init section is freed after boot, and the code can never be unloaded, so module_exit is never called and the __exit cleanup section is dropped entirely. When built as a module (=m), module_init runs at insmod (its __init section freed after the module loads) and module_exit runs at rmmod, so __exit cleanup is real and needed. This is exactly why __init code must not be kept around and __exit must register through module_exit: the macros and section annotations let one source adapt to both lifecycles correctly. The behavioral split is: built-in is one-shot at boot with no teardown; module supports load/unload cycles.`,
    tags: ['builtin', 'module', 'lifecycle'],
  },
  {
    id: 'lx-ch06-t-028',
    chapter: 6,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Vermagic and Module Load Refusal',
    prompt: `You build a module against one kernel's headers, then try to insmod it on a machine running a different kernel version and get "version magic ... should be ...". Explain what version magic (vermagic) protects against and why the kernel refuses rather than just loading the .ko anyway.`,
    hints: ['Modules are tied to the kernel they were built for.', 'Internal layouts and configs must match.'],
    solution: `A module embeds a vermagic string recording the kernel version and key build options it was compiled against, and the loader compares it to the running kernel. The kernel refuses a mismatch because module code is tightly coupled to the running kernel's internal structure layouts, symbol versions, and config options; loading a module built for a different kernel could read or write the wrong offsets in shared structures and corrupt memory or crash. Rather than risk silent corruption, the loader rejects the module up front. The fix is to rebuild the module against the exact running kernel's headers (the /lib/modules/$(uname -r)/build tree), which is why the out-of-tree Makefile points -C at that tree.`,
    tags: ['vermagic', 'compatibility', 'kbuild'],
  },
  {
    id: 'lx-ch06-t-029',
    chapter: 6,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Idempotent and Failure-Safe Exit',
    prompt: `An init function fails partway and uses goto labels to unwind, then the same cleanup helpers are also called from the __exit function on normal unload. A bug appears where a pointer freed during init-failure unwind is freed again at exit. Explain how to design the cleanup so partial init and full exit share code without double-free.`,
    hints: ['Track which resources were actually acquired.', 'Set pointers to NULL after freeing and guard frees.'],
    solution: `The trouble is that init-failure unwinding frees only what was acquired so far, while the exit path assumes everything was acquired; sharing helpers naively can free the same resource twice. The standard discipline is to make cleanup reflect actual state: after freeing a resource set its pointer to NULL (and gate kfree-style helpers so freeing NULL is a no-op), and have the goto error ladder jump only to the label matching the last successful step so each resource is undone exactly once. Then the __exit function can reuse the same tail labels safely because everything not acquired is NULL. This keeps a single cleanup ladder correct for both partial-init failure and full unload, avoiding double-free.`,
    tags: ['cleanup', 'double-free', 'error-handling'],
  },
  {
    id: 'lx-ch06-t-030',
    chapter: 6,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Designing a Configurable Logging Module',
    prompt: `Design (in prose) a small module that on load takes an integer verbosity parameter tunable at runtime, logs at the appropriate printk level, exports one helper for other modules, and cleans up correctly. List the specific macros/APIs you would use and one correctness pitfall for each of: parameter, logging, export, and cleanup.`,
    hints: ['Map each requirement to a topic-6 mechanism.', 'Think about runtime tunability, log levels, symbol visibility, and unwinding.'],
    solution: `Use module_param(verbosity, int, 0644) so the value can be set at load (insmod verbosity=2) and retuned at runtime via sysfs; the pitfall is choosing the permission wrongly (0 hides it, 0444 makes it read-only) or not validating the value. For logging, gate pr_info/pr_warn/pr_err (or printk with KERN_* levels) on verbosity and pick the level by severity; the pitfall is logging routine info at KERN_ERR or hiding important messages at KERN_DEBUG. For sharing, mark the helper non-static and wrap it in EXPORT_SYMBOL (or _GPL with MODULE_LICENSE("GPL")); the pitfall is forgetting the export so dependents fail with "unknown symbol," or exporting GPL-only and using it from a non-GPL module. For cleanup, register module_init/module_exit with __init/__exit, allocate with GFP_KERNEL in the sleepable init context, and unwind via goto labels in reverse order; the pitfall is leaving a registration or timer alive past unload, causing a use-after-free.`,
    tags: ['design', 'module_param', 'export_symbol'],
  },
]

export default problems
