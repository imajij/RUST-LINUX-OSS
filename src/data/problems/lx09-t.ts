import type { Problem } from '../../types'

const problems: Problem[] = [
  {
    id: 'lx-ch09-t-001',
    chapter: 9,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'Virtual Versus Physical Memory',
    prompt: `A newcomer asks why a kernel pointer like the one returned by kmalloc() is described as a "virtual address" rather than just "the memory." In one short paragraph, explain the difference between a virtual address and a physical address, and why code in the kernel almost always works with virtual addresses.`,
    hints: ['The CPU runs code through the MMU and page tables.', 'Devices and page tables care about physical addresses; CPU instructions use virtual ones.'],
    solution: `Every memory access the CPU makes goes through the MMU, which translates a virtual address into a physical address using the page tables, so the addresses your code dereferences are virtual, not the literal RAM locations. The kernel works in virtual addresses because that is what the CPU's load/store instructions consume, and because virtual mapping lets the kernel present memory contiguously even when the underlying RAM is scattered. Physical addresses still matter, but mainly to hardware that bypasses the CPU's view, such as DMA-capable devices, and to the page-table machinery itself. kmalloc returns a kernel virtual address you can dereference directly; if you need the physical address you must translate it explicitly (for example with virt_to_phys).`,
    tags: ['kernel', 'memory', 'virtual'],
  },
  {
    id: 'lx-ch09-t-002',
    chapter: 9,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'Why Pages Are The Unit',
    prompt: `Physical memory is managed in fixed-size chunks called pages (commonly 4 KB). Explain what a page is, and give one reason the hardware and kernel manage memory in page-sized units instead of byte by byte.`,
    hints: ['The MMU maps memory at page granularity.', 'Per-byte bookkeeping would be enormous.'],
    solution: `A page is the smallest unit of memory that the MMU maps and that the page tables track; on most architectures it is 4 KB. The hardware translates virtual to physical addresses one page at a time, so a single page-table entry controls protections and the mapping for a whole page rather than for individual bytes. Managing memory at page granularity keeps the bookkeeping tractable: tracking permissions, presence, and ownership for every byte would require absurd amounts of metadata, while one entry per 4 KB page is manageable. Allocators like kmalloc and the slab layer are built on top of this page foundation, carving pages into smaller objects.`,
    tags: ['kernel', 'memory', 'pages'],
  },
  {
    id: 'lx-ch09-t-003',
    chapter: 9,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'kmalloc And Its Matching Free',
    prompt: `You write u8 *buf = kmalloc(256, GFP_KERNEL); and use it. A reviewer asks what call must eventually pair with this, and what happens if you never make that call. Answer both, and name the function that frees kmalloc memory.`,
    hints: ['kmalloc pairs with kfree.', 'Unfreed kernel memory does not get garbage collected.'],
    solution: `Memory from kmalloc must eventually be released with kfree(buf); kmalloc and kfree are a matched pair. The kernel has no garbage collector, so if a driver allocates with kmalloc and never calls kfree, that memory stays allocated for the lifetime of the system (or until the module is unloaded, and even then only if the module frees it) — a memory leak. Repeated leaks, for example one per request, will slowly exhaust kernel memory and can eventually bring the machine down. Always pair every successful kmalloc with exactly one kfree, including on every error path.`,
    tags: ['kernel', 'kmalloc', 'kfree'],
  },
  {
    id: 'lx-ch09-t-004',
    chapter: 9,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'What GFP_KERNEL Means',
    prompt: `Most allocations in ordinary driver code use kmalloc(size, GFP_KERNEL). In plain terms, what does the GFP_KERNEL flag tell the allocator it is allowed to do, and why is that fine in normal process context?`,
    hints: ['GFP stands for "get free pages" flags.', 'GFP_KERNEL permits sleeping.'],
    solution: `GFP_KERNEL tells the allocator it is allowed to block (sleep) while satisfying the request — for instance to wait for the page reclaim machinery to free up memory, or even to start I/O writing dirty pages to disk. That is acceptable in ordinary process context, such as a syscall handler or a kernel thread, because in that context the kernel is allowed to put the current task to sleep and reschedule. Because it can wait, GFP_KERNEL is the most likely flag to actually succeed under memory pressure. The catch is that it must never be used where sleeping is forbidden, such as interrupt handlers or code holding a spinlock.`,
    tags: ['kernel', 'gfp', 'gfp_kernel'],
  },
  {
    id: 'lx-ch09-t-005',
    chapter: 9,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'GFP_KERNEL In An Interrupt Handler',
    prompt: `A driver's interrupt handler calls kmalloc(512, GFP_KERNEL). Explain why this is a bug, what specifically could go wrong, and what one-word change fixes it.`,
    hints: ['Interrupt context cannot sleep.', 'GFP_KERNEL allows the allocator to block.'],
    solution: `Interrupt handlers run in atomic context where the kernel must not sleep, but GFP_KERNEL explicitly permits the allocator to block while it waits for memory. If kmalloc with GFP_KERNEL is hit in an interrupt and memory is tight, it may try to sleep — which in atomic context is illegal and triggers a "scheduling while atomic" / "sleeping function called from invalid context" bug, potentially deadlocking or crashing the machine. The fix is to pass GFP_ATOMIC instead: kmalloc(512, GFP_ATOMIC), which tells the allocator it must not sleep and must satisfy the request immediately or fail. The price is that GFP_ATOMIC is more likely to return NULL, so the caller must handle failure.`,
    tags: ['kernel', 'gfp_atomic', 'interrupt'],
  },
  {
    id: 'lx-ch09-t-006',
    chapter: 9,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Always Check For NULL',
    prompt: `Two engineers argue about whether kmalloc can really fail in practice. One says "it basically always works, so checking the return is busywork." Explain why every kmalloc return value must be checked, and what the correct response to a NULL return is.`,
    hints: ['Allocation can fail under memory pressure.', 'Dereferencing NULL in the kernel is fatal.'],
    solution: `kmalloc can return NULL whenever the allocator cannot satisfy the request — under memory pressure, for large sizes, and especially with GFP_ATOMIC, which cannot wait for reclaim. In the kernel a NULL dereference is not a catchable exception; it is typically an oops or a hard crash that can take down the whole system, so skipping the check is genuinely dangerous, not busywork. The correct response is to detect NULL and bail out gracefully: free anything already allocated, undo partial state, and return an error code such as -ENOMEM. A typical pattern is "if (!buf) return -ENOMEM;" placed immediately after the allocation.`,
    tags: ['kernel', 'kmalloc', 'enomem'],
  },
  {
    id: 'lx-ch09-t-007',
    chapter: 9,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Physical Contiguity Of kmalloc',
    prompt: `A device needs a buffer that its DMA engine will read directly from RAM, so the buffer must be physically contiguous. A colleague proposes using kmalloc for it. Is kmalloc's memory physically contiguous? Explain the guarantee kmalloc provides.`,
    hints: ['kmalloc is backed by the page allocator and slab.', 'Contrast this with vmalloc later.'],
    solution: `Yes — memory returned by kmalloc is contiguous in physical RAM, not just in the virtual address space. That is because kmalloc is built on the slab allocator and the page allocator, which hand out runs of physically adjacent pages. This is exactly why kmalloc (or a DMA-specific allocator) is the right choice for DMA buffers that hardware reads through physical addresses: the device sees one unbroken physical region. By contrast vmalloc gives only virtual contiguity, so it would be wrong for a DMA buffer that bypasses the CPU's page tables.`,
    tags: ['kernel', 'kmalloc', 'dma'],
  },
  {
    id: 'lx-ch09-t-008',
    chapter: 9,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'When To Reach For vmalloc',
    prompt: `You need a 2 MB scratch buffer inside a driver. It is only ever touched by CPU code (no DMA), and you do not care whether it is physically contiguous. Explain why vmalloc is a reasonable choice here and what it gives you that a large kmalloc might not.`,
    hints: ['Large physically contiguous allocations can fail.', 'vmalloc stitches scattered pages into one virtual range.'],
    solution: `vmalloc allocates several individual pages and maps them into one contiguous virtual address range, even though the underlying physical pages are scattered. That makes it well suited to large buffers like 2 MB, because finding that many physically adjacent pages can fail under fragmentation, whereas vmalloc only needs the pages to be available somewhere. Since this buffer is CPU-only and contiguity in physical memory does not matter, the lack of physical contiguity is harmless. The trade-offs are extra page-table setup, slightly slower access, and consumption of the limited vmalloc virtual area, but for a big CPU-only scratch buffer vmalloc is the natural fit. Remember to release it with vfree, not kfree.`,
    tags: ['kernel', 'vmalloc', 'memory'],
  },
  {
    id: 'lx-ch09-t-009',
    chapter: 9,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'vfree Versus kfree',
    prompt: `A patch allocates with p = vmalloc(size); and later releases it with kfree(p);. Explain why mixing the allocator with the wrong free function is a bug, and state the correct pairing for vmalloc, kmalloc, and __get_free_pages.`,
    hints: ['Each allocator has its own free.', 'The free must match the alloc that produced the pointer.'],
    solution: `Each allocator tracks its memory differently, so you must release a pointer with the free function that matches the allocator that produced it; calling kfree on a vmalloc pointer corrupts internal bookkeeping and causes undefined behavior, often a crash. The correct pairings are: vmalloc with vfree, kmalloc (and kzalloc/krealloc) with kfree, and __get_free_pages/__get_free_page with free_pages/free_page (and alloc_pages with __free_pages). The right fix for the patch is to call vfree(p). Mixing them is never acceptable even if it appears to "work" once.`,
    tags: ['kernel', 'vmalloc', 'vfree'],
  },
  {
    id: 'lx-ch09-t-010',
    chapter: 9,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'kzalloc Versus kmalloc',
    prompt: `A struct allocated with kmalloc has a few fields that the code reads before they are ever written, and a subtle bug appears because those fields hold garbage. A reviewer suggests kzalloc. Explain the difference between kmalloc and kzalloc and when zeroing matters.`,
    hints: ['kmalloc does not clear the memory it returns.', 'kzalloc = kmalloc + memset 0.'],
    solution: `kmalloc returns a block of the requested size but does not initialize its contents, so the bytes contain whatever was last there — effectively garbage. If code reads a field before assigning it, that garbage is used, which is exactly the bug described. kzalloc is equivalent to kmalloc followed by zeroing the whole block, so every field starts at 0/NULL. Use kzalloc whenever you cannot guarantee that every field will be explicitly initialized before use (common for structs with optional pointers or flags); use plain kmalloc only when you will immediately overwrite the entire buffer, since zeroing has a small cost.`,
    tags: ['kernel', 'kzalloc', 'kmalloc'],
  },
  {
    id: 'lx-ch09-t-011',
    chapter: 9,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Raw Pages With __get_free_pages',
    prompt: `Instead of kmalloc, some code calls __get_free_pages(GFP_KERNEL, 3) to obtain memory. Explain what the second argument (the "order") means, roughly how much memory this returns, and when working in whole pages is preferable to kmalloc.`,
    hints: ['Order is a power-of-two count of pages.', 'Think page-aligned, page-multiple buffers.'],
    solution: `__get_free_pages allocates 2^order physically contiguous pages, so order 3 means 2^3 = 8 pages, which on a 4 KB-page system is 32 KB. It returns the kernel virtual address of the first page, and that address is page-aligned. Working in whole pages is preferable when you need a naturally page-aligned, page-multiple region — for example backing memory you intend to mmap, page tables, or buffers sized in pages — because kmalloc rounds up to its own bucket sizes and does not guarantee page alignment for arbitrary requests. The matching free is free_pages(addr, order), and you must pass the same order back.`,
    tags: ['kernel', 'pages', 'get_free_pages'],
  },
  {
    id: 'lx-ch09-t-012',
    chapter: 9,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Freeing With The Wrong Size',
    prompt: `Code does pages = __get_free_pages(GFP_KERNEL, 2); and later free_pages(pages, 0);. The author thinks "the address is the same, so the free is fine." Explain why this is wrong and what the rule about order is for the page allocator.`,
    hints: ['The page allocator is not size-tracking like kfree.', 'order on free must equal order on alloc.'],
    solution: `Unlike kfree, which can recover the size of a kmalloc block from its own metadata, the page allocator's free_pages requires you to tell it how many pages you are returning via the order argument, and that order must match the one you allocated with. Here the code allocated order 2 (4 pages) but freed order 0 (1 page), so 3 pages are leaked and the allocator's free lists are left inconsistent. The rule is simple: the order passed to free_pages must equal the order passed to __get_free_pages. The fix is free_pages(pages, 2). Mismatched orders cause leaks or corruption, not a harmless no-op.`,
    tags: ['kernel', 'pages', 'free_pages'],
  },
  {
    id: 'lx-ch09-t-013',
    chapter: 9,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Double Free Hazard',
    prompt: `A cleanup path calls kfree(buf); and then, on a later error branch that is also reachable, calls kfree(buf); again on the same pointer without re-allocating. Explain what a double free is and why it is dangerous, and give one simple coding habit that prevents it.`,
    hints: ['Freeing the same block twice corrupts the allocator.', 'Consider setting the pointer to NULL after freeing.'],
    solution: `A double free occurs when the same allocation is handed to kfree more than once. After the first kfree the block is returned to the allocator and may be reused for something else; freeing it again corrupts the slab/heap metadata or frees memory now owned by another caller, leading to crashes or exploitable corruption that often surfaces far from the bug. A simple, robust habit is to null the pointer right after freeing — kfree(buf); buf = NULL; — because kfree(NULL) is explicitly defined to be a safe no-op, so a second free becomes harmless. Structuring cleanup so each resource is freed on exactly one path (for example with goto-style unwinding) also avoids the problem.`,
    tags: ['kernel', 'kfree', 'double-free'],
  },
  {
    id: 'lx-ch09-t-014',
    chapter: 9,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Why A Slab Cache For Fixed-Size Objects',
    prompt: `A subsystem allocates and frees millions of identically sized struct conn objects per second using kmalloc/kfree. A senior engineer suggests creating a dedicated kmem_cache for them instead. Explain what advantage a kmem_cache gives over generic kmalloc for a high-churn, fixed-size object.`,
    hints: ['The slab allocator specializes per object size/type.', 'Think about reuse, fragmentation, and per-CPU free lists.'],
    solution: `kmalloc serves arbitrary sizes by rounding each request up to the nearest of a fixed set of buckets, which wastes memory and mixes many object types in the same pools. A kmem_cache created with kmem_cache_create for one specific object size packs those objects tightly into slabs dedicated to that type, reducing internal fragmentation and improving cache locality. The slab layer keeps per-CPU free lists of recently freed objects, so allocation and free become very cheap pointer operations that frequently hand back a still-warm, already-constructed object, which is exactly what you want for millions of alloc/free cycles per second. It can also run a constructor once per object. For high-churn, single-size, hot-path objects, a dedicated cache is a clear win over generic kmalloc.`,
    tags: ['kernel', 'slab', 'kmem_cache'],
  },
  {
    id: 'lx-ch09-t-015',
    chapter: 9,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Using A kmem_cache Correctly',
    prompt: `You create a cache with cache = kmem_cache_create("conn", sizeof(struct conn), 0, 0, NULL);. Explain how you then allocate and free individual objects from it, and what mistake happens if you accidentally kfree() one of those objects instead.`,
    hints: ['Objects come from kmem_cache_alloc.', 'They must return to the same cache.'],
    solution: `Objects are taken from the cache with kmem_cache_alloc(cache, GFP_KERNEL), which returns a pointer to one object-sized slot, and they are returned with kmem_cache_free(cache, obj). The allocation and free must both reference the same cache, because the slab metadata that tracks the object lives in that cache's slabs. If you call kfree(obj) instead of kmem_cache_free, you hand a cache-owned object to the generic kmalloc free path, which does not own it and cannot correctly account for it — corrupting allocator state and risking a crash. When the subsystem shuts down, free all outstanding objects and then destroy the cache with kmem_cache_destroy(cache).`,
    tags: ['kernel', 'kmem_cache', 'slab'],
  },
  {
    id: 'lx-ch09-t-016',
    chapter: 9,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'kmalloc Holding A Spinlock',
    prompt: `This code runs in process context but holds a spinlock:

  spin_lock(&lock);
  p = kmalloc(64, GFP_KERNEL);
  ...
  spin_unlock(&lock);

Explain why GFP_KERNEL is wrong here even though we are in process context, and describe the two acceptable ways to fix it.`,
    hints: ['Holding a spinlock disables preemption / is atomic.', 'Either change the flag or move the allocation out of the locked region.'],
    solution: `While a spinlock is held the code is in atomic context — preemption (and possibly interrupts) are disabled — so sleeping is forbidden even though the surrounding function is in process context. GFP_KERNEL permits the allocator to sleep, which inside the critical section can deadlock the CPU or trigger a "sleeping in atomic context" bug. One fix is to use GFP_ATOMIC, which never sleeps, at the cost of a higher chance of returning NULL. The cleaner fix is usually to move the allocation outside the lock: allocate with GFP_KERNEL before spin_lock, then take the lock only for the short critical section that uses the memory. Allocating before locking is preferred when possible because GFP_KERNEL is more reliable than GFP_ATOMIC.`,
    tags: ['kernel', 'gfp', 'spinlock'],
  },
  {
    id: 'lx-ch09-t-017',
    chapter: 9,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'kmalloc Versus vmalloc Trade-Offs',
    prompt: `Compare kmalloc and vmalloc along three axes: physical contiguity, suitability for large allocations, and access/setup overhead. Then state which you would pick for (a) a small per-request control block and (b) a 4 MB CPU-only ring buffer, and why.`,
    hints: ['kmalloc: physically contiguous, fast, size-limited.', 'vmalloc: virtually contiguous, good for big buffers, pricier.'],
    solution: `kmalloc returns physically contiguous memory, is fast to allocate and access, and is ideal for small-to-moderate sizes, but large physically contiguous requests become unreliable as memory fragments. vmalloc returns only virtually contiguous memory (scattered physical pages mapped into one virtual range), which makes large allocations far more likely to succeed, but it costs extra page-table setup, slightly slower access via those mappings, and uses the limited vmalloc address area. For (a) a small per-request control block, use kmalloc: it is fast, cheap, and the size is tiny. For (b) a 4 MB CPU-only ring buffer, use vmalloc: it is large enough that physical contiguity is hard to guarantee, contiguity is not needed since only the CPU touches it, and the per-access overhead is negligible relative to its size.`,
    tags: ['kernel', 'kmalloc', 'vmalloc'],
  },
  {
    id: 'lx-ch09-t-018',
    chapter: 9,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'copy_from_user Cannot Run Atomically',
    prompt: `An interrupt handler tries to copy data from a user pointer with copy_from_user() into a kmalloc(GFP_ATOMIC) buffer. Separate from the allocation, explain why copy_from_user is itself unsafe in this context.`,
    hints: ['User pages may be paged out.', 'A fault triggers handling that can sleep.'],
    solution: `copy_from_user (like copy_to_user) accesses user-space memory, which may not currently be resident — the target page can be paged out or not yet faulted in. Touching it can trigger a page fault that the kernel resolves by potentially performing I/O and sleeping. Sleeping is illegal in an interrupt handler (atomic context), so the copy can crash or hang the system regardless of which GFP flag the buffer used. Beyond that, an interrupt does not even run in the address space of any particular user process, so "the user pointer" is meaningless there. User-copy routines belong in process context (such as a syscall or ioctl handler), not in interrupt context; deferring the work to a process-context mechanism is the correct approach.`,
    tags: ['kernel', 'copy_from_user', 'atomic'],
  },
  {
    id: 'lx-ch09-t-019',
    chapter: 9,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Sizing With sizeof Versus Hard-Coding',
    prompt: `One driver writes p = kmalloc(sizeof(struct widget), GFP_KERNEL); another writes p = kmalloc(64, GFP_KERNEL); intending to hold a struct widget. Explain why the sizeof form is safer, and what specifically can go wrong with the hard-coded 64.`,
    hints: ['Struct size changes with edits and padding/alignment.', 'Undersized allocations lead to out-of-bounds writes.'],
    solution: `sizeof(struct widget) always evaluates to the struct's true current size, including any compiler-inserted padding for alignment, and it tracks automatically if a field is ever added or the layout changes. The hard-coded 64 is a guess frozen in time: if struct widget is actually larger (now or after a future edit), the buffer is too small, and writing the whole struct overruns it, corrupting adjacent heap memory in ways that crash unpredictably. Even today, padding can make the real size differ from a naive field-by-field count. Prefer sizeof(*p) over sizeof(struct widget) so the size always matches the pointer's type, eliminating an entire class of undersizing bugs.`,
    tags: ['kernel', 'sizeof', 'kmalloc'],
  },
  {
    id: 'lx-ch09-t-020',
    chapter: 9,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Multiplication Overflow In Allocation Size',
    prompt: `Code computes a size as kmalloc(n * sizeof(struct item), GFP_KERNEL) where n comes from user input. Explain the security/stability problem with this multiplication and what kernel helper exists to allocate an array safely.`,
    hints: ['n is attacker-controlled; the product can overflow.', 'There is a dedicated array-allocation helper.'],
    solution: `If n is large enough, the product n * sizeof(struct item) can overflow the size_t computation and wrap around to a small value. kmalloc then allocates a tiny buffer, but the code believes it has room for n items and writes past the end — a classic heap buffer overflow that is both a crash and an exploitable security hole, especially since n is attacker-controlled. The kernel provides kmalloc_array(n, sizeof(struct item), GFP_KERNEL) (and kcalloc for the zeroed variant), which performs the multiplication with an explicit overflow check and returns NULL instead of wrapping. Always use the array helpers when the count is variable or untrusted rather than multiplying by hand.`,
    tags: ['kernel', 'overflow', 'kmalloc_array'],
  },
  {
    id: 'lx-ch09-t-021',
    chapter: 9,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Alignment Guarantees Of kmalloc',
    prompt: `A driver needs a buffer whose start address is aligned to a hardware requirement. The author assumes "kmalloc always returns 8-byte aligned memory, so any alignment I need is covered." Explain what alignment kmalloc actually guarantees and why that assumption can be unsafe for stricter requirements.`,
    hints: ['kmalloc aligns to at least the natural word/ARCH alignment.', 'Specific hardware may need page or cache-line alignment.'],
    solution: `kmalloc guarantees that the returned pointer is aligned at least to the architecture's natural alignment (commonly the word size / ARCH_KMALLOC_MINALIGN), which is enough for normal C objects of any built-in type. It does not promise arbitrary stronger alignment such as page alignment or a specific 4 KB / cache-line boundary. So assuming "any alignment I need is covered" is unsafe when the hardware demands stricter alignment than kmalloc's minimum: the buffer may start misaligned for that device, causing it to malfunction. For page alignment, use the page allocator (__get_free_pages returns page-aligned memory); for other special alignment, use an allocator or technique that lets you request it explicitly rather than relying on kmalloc's baseline.`,
    tags: ['kernel', 'alignment', 'kmalloc'],
  },
  {
    id: 'lx-ch09-t-022',
    chapter: 9,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Why Memory Zones Exist',
    prompt: `The page allocator divides physical memory into zones such as ZONE_DMA, ZONE_NORMAL, and (on 32-bit) ZONE_HIGHMEM. In conceptual terms, why does the kernel split RAM into zones instead of treating all physical memory identically?`,
    hints: ['Some hardware can only address low physical memory.', 'Not all physical memory is permanently mapped on 32-bit.'],
    solution: `Zones exist because not all physical memory is equally usable for every purpose, due to hardware and addressing constraints. ZONE_DMA (and DMA32) covers low physical addresses that older or limited DMA-capable devices can reach, so allocations destined for such hardware must come from that range. ZONE_HIGHMEM exists mainly on 32-bit systems, where the kernel cannot keep all physical RAM permanently mapped into its limited virtual address space, so high memory needs temporary mapping before use. ZONE_NORMAL is the directly-mapped memory used for most ordinary allocations. By tagging memory with zones, the allocator can honor constraints — for example using a GFP flag like GFP_DMA to force a DMA-reachable page — while still pooling the rest for general use.`,
    tags: ['kernel', 'zones', 'memory'],
  },
  {
    id: 'lx-ch09-t-023',
    chapter: 9,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'GFP_ATOMIC Failure Strategy',
    prompt: `A network RX interrupt path must allocate a small buffer with GFP_ATOMIC on every received packet, and under load these allocations sometimes return NULL. The author wants to "fix" this by looping: while ((p = kmalloc(len, GFP_ATOMIC)) == NULL) { /* retry */ }. Critique this approach and propose a better failure strategy.`,
    hints: ['GFP_ATOMIC cannot sleep, so it cannot wait for reclaim.', 'Busy-looping in an interrupt makes the pressure worse.'],
    solution: `The retry loop is wrong because GFP_ATOMIC deliberately does not sleep or reclaim; if there is no free memory now, spinning will not make any appear, since the very code that could free memory cannot run while you busy-loop in the interrupt handler with preemption/interrupts effectively stalled. So the loop can hang the CPU and worsens contention precisely when the system is already low on memory. The correct strategy is to treat NULL as an expected, recoverable condition: drop the packet (incrementing a drop counter), since networking tolerates loss and the sender will retransmit, and return promptly. Better still, reduce pressure by pre-allocating a pool of receive buffers ahead of time (or using a dedicated kmem_cache / reserved skb pool) so the interrupt path rarely needs a fresh atomic allocation. In short: never loop forever on GFP_ATOMIC — handle failure and move on.`,
    tags: ['kernel', 'gfp_atomic', 'failure'],
  },
  {
    id: 'lx-ch09-t-024',
    chapter: 9,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Partial Allocation Cleanup On Error',
    prompt: `A probe function allocates three resources in sequence: a kmalloc buffer, a kmem_cache, and a vmalloc region. The third allocation fails. Describe what the error-handling code must do at that point, and explain the common goto-based unwinding pattern that makes this correct and maintainable.`,
    hints: ['Free in reverse order of acquisition.', 'Use labeled goto targets, one per resource.'],
    solution: `When the third allocation fails, the function must undo the work it already did before returning the error: free the kmem_cache it created and free the kmalloc buffer, but it must NOT free the vmalloc region because that allocation never succeeded. The standard kernel pattern is staged goto unwinding: on each failure you jump to a label that releases exactly the resources acquired so far, with labels ordered so they free in reverse order of acquisition. For example, on vmalloc failure goto err_cache (which kmem_cache_destroy then falls through to kfree the buffer), on cache failure goto err_buf (kfree only), and the success path skips all the error labels. This avoids both leaks (forgetting to free an earlier resource) and double frees / freeing-the-unallocated (freeing something that was never created), and it keeps the cleanup logic in one readable place instead of duplicating frees at every return.`,
    tags: ['kernel', 'cleanup', 'error-handling'],
  },
  {
    id: 'lx-ch09-t-025',
    chapter: 9,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Large Contiguous Allocation Fails Over Time',
    prompt: `A driver does kmalloc(1 << 20, GFP_KERNEL) (1 MB) once at module load and it works on a freshly booted machine. After the system has been up for weeks, reloading the module sometimes fails the same allocation with NULL. Explain why a large kmalloc can succeed early and fail later, and what design change avoids the problem.`,
    hints: ['kmalloc needs physically contiguous pages.', 'Memory fragments as the system runs.'],
    solution: `kmalloc must return physically contiguous memory, which means the page allocator has to find a large run of adjacent free pages (a high-order allocation). On a freshly booted machine, free physical memory is mostly unfragmented, so a contiguous megabyte is easy to find. After weeks of uptime, memory becomes fragmented: plenty of pages may be free overall, but they are scattered, so no single contiguous 1 MB run exists and the high-order allocation fails even though total free memory is ample. The robust design is to avoid requiring such a large physically contiguous block: if the buffer is CPU-only, use vmalloc, which only needs scattered pages stitched into a contiguous virtual range; if you genuinely need contiguity (for DMA), reserve it early at boot or use a contiguous-memory mechanism, and handle allocation failure gracefully rather than assuming it will succeed.`,
    tags: ['kernel', 'fragmentation', 'kmalloc'],
  },
  {
    id: 'lx-ch09-t-026',
    chapter: 9,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Use-After-Free Through A Stale Pointer',
    prompt: `Two code paths share a pointer to a kmalloc'd struct. Path A calls kfree(obj) when it is done. Path B, running slightly later, still holds the same pointer and reads obj->state. Explain precisely why this read is dangerous even though obj is not NULL, and what ownership discipline prevents it.`,
    hints: ['Freed memory can be reused immediately.', 'A non-NULL pointer is not a valid pointer.'],
    solution: `After kfree(obj) the block is returned to the allocator and may be handed out to a completely different caller at any moment; the bytes that were obj->state can now belong to another object. Path B's pointer is still non-NULL — kfree does not change the pointer value — but it is now dangling, so reading obj->state is a use-after-free that returns garbage, or worse, lets one subsystem read/write another's live data, causing corruption and security vulnerabilities. The danger is exactly that a non-NULL pointer says nothing about whether the memory is still yours. The discipline that prevents it is clear single ownership and lifetime rules: there must be one well-defined owner responsible for freeing, and no other path may use the pointer after that owner frees it. When sharing is genuine, the object's lifetime must be managed by reference counting so it is freed only after the last user is done, not by whichever path happens to finish first.`,
    tags: ['kernel', 'use-after-free', 'ownership'],
  },
  {
    id: 'lx-ch09-t-027',
    chapter: 9,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Choosing The Right Allocator: A Decision',
    prompt: `For each case, name the most appropriate allocator (kmalloc, vmalloc, __get_free_pages, or a dedicated kmem_cache) and justify in one sentence: (a) millions of identical 96-byte objects on a hot path; (b) a physically contiguous DMA buffer of 16 KB; (c) a 10 MB CPU-only decompression workspace; (d) a single page-aligned 4 KB buffer to back an mmap.`,
    hints: ['Match each requirement: churn, contiguity, size, alignment.', 'One allocator is clearly best per case.'],
    solution: `(a) A dedicated kmem_cache: a single fixed object size at very high churn benefits from the slab's tight packing and per-CPU free lists, far better than generic kmalloc. (b) kmalloc (or a DMA helper built on it): the buffer must be physically contiguous for the device's DMA engine, 16 KB is small enough to allocate contiguously, and kmalloc guarantees physical contiguity, whereas vmalloc would not. (c) vmalloc: 10 MB is too large to count on a physically contiguous block, the workspace is CPU-only so physical contiguity is irrelevant, and vmalloc only needs scattered pages. (d) __get_free_pages with order 0: it returns exactly one page, naturally page-aligned, which is what mmap-backing memory needs, while kmalloc does not guarantee page alignment.`,
    tags: ['kernel', 'allocator', 'design'],
  },
  {
    id: 'lx-ch09-t-028',
    chapter: 9,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Why You Cannot vmalloc A DMA Buffer',
    prompt: `An engineer allocates a buffer with vmalloc and programs its address into a device's DMA controller so the device can write incoming data straight into it. Explain the two distinct problems this causes, relating them to virtual versus physical addresses and to physical contiguity.`,
    hints: ['DMA controllers use physical addresses, not kernel virtual ones.', 'vmalloc pages are scattered in physical RAM.'],
    solution: `First, a DMA controller typically addresses memory by physical address (it bypasses the CPU's MMU), but the pointer vmalloc returns is a kernel virtual address; programming that virtual value into the device makes it write to the wrong physical location entirely, scribbling over unrelated memory. Second, even after translating to physical addresses, vmalloc's pages are scattered across physical RAM and only made contiguous in virtual space, so a device that expects one contiguous physical region cannot use it correctly — consecutive offsets in the buffer map to non-adjacent physical pages. A correct DMA buffer must be physically contiguous and described by a physical (DMA) address, which is why you use kmalloc or, better, the dedicated DMA allocation APIs, never vmalloc.`,
    tags: ['kernel', 'vmalloc', 'dma'],
  },
  {
    id: 'lx-ch09-t-029',
    chapter: 9,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'kmem_cache Object Reuse Surprise',
    prompt: `A team replaces kzalloc with a kmem_cache (allocated via kmem_cache_alloc with no constructor and without a zeroing flag) for a hot struct. Suddenly a field that "used to be zero" sometimes contains stale data. Explain what changed about initialization, and the two correct ways to make objects start in a known state.`,
    hints: ['kzalloc zeroed every block; kmem_cache_alloc does not by default.', 'A slab reuses freed objects in place.'],
    solution: `kzalloc zeroed every block it returned, so each struct started clean. A plain kmem_cache_alloc returns an object slot from the slab without zeroing it, and slabs deliberately reuse freed objects in place to stay hot in cache — so a freshly allocated object often contains the leftover bytes of a previously freed object of the same type, which is exactly the stale data observed. The code was implicitly relying on the allocator to clear fields, and the cache does not. The two correct fixes are: explicitly initialize every field after kmem_cache_alloc (or request zeroing by passing a flag like __GFP_ZERO to the alloc), or give the cache a constructor at kmem_cache_create time that initializes the invariant parts once. Either way, do not assume slab objects come back zeroed.`,
    tags: ['kernel', 'kmem_cache', 'initialization'],
  },
  {
    id: 'lx-ch09-t-030',
    chapter: 9,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Internal Fragmentation From Bucket Rounding',
    prompt: `A driver allocates one million objects, each via kmalloc(33, GFP_KERNEL). A profiler reports the memory used is far more than 33 MB. Explain why kmalloc(33) wastes memory per object, and how restructuring the allocation could reclaim most of the waste.`,
    hints: ['kmalloc rounds up to fixed power-of-two-ish bucket sizes.', 'Just over a boundary is the worst case.'],
    solution: `kmalloc does not hand out exactly 33 bytes; it rounds each request up to the next available slab bucket size, which are coarse (roughly powers of two, e.g. 8, 16, 32, 64, ...). A 33-byte request just exceeds the 32-byte bucket, so it is served from the 64-byte bucket, meaning 31 of every 64 bytes — nearly half — are wasted internal fragmentation. Across a million objects that lost ~31 bytes each adds up to tens of megabytes of overhead, explaining the profiler result. To reclaim it, either shrink the object to fit a smaller bucket (for example trim the struct to <= 32 bytes), or, if the size is fixed and the count huge, create a dedicated kmem_cache sized to exactly 33 bytes so the slab packs objects tightly with little per-object slack instead of paying the kmalloc bucket penalty.`,
    tags: ['kernel', 'fragmentation', 'slab'],
  },
]

export default problems
