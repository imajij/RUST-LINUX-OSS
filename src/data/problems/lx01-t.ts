import type { Problem } from '../../types'

const problems: Problem[] = [
  {
    id: 'lx-ch01-t-001',
    chapter: 1,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'What Does a Pointer Actually Hold?',
    prompt: `A teammate says "a pointer is just a number." Is that an accurate mental model for kernel C? Explain what value a pointer variable stores and what its type adds on top of that raw number.`,
    hints: [
      'Think about what the CPU sees versus what the compiler tracks.',
      'Why does int* and char* matter if both "hold an address"?',
    ],
    solution: `It is partly right: at the machine level a pointer holds a memory address, which is just an unsigned integer of the machine's address width (e.g. 64 bits). But the type is not cosmetic. The pointer's type tells the compiler the size and layout of the object it points at, which controls dereferencing (how many bytes to read) and pointer arithmetic (p + 1 advances by sizeof(*p), not by 1 byte). So an int* and a char* may hold the same address yet behave completely differently when dereferenced or incremented. Treating a pointer as "just a number" is exactly the kind of thinking that produces wrong arithmetic and type-punning bugs.`,
    tags: ['c', 'pointers', 'types'],
  },
  {
    id: 'lx-ch01-t-002',
    chapter: 1,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'Stack, Heap, or Data?',
    prompt: `For each of these, say whether it lives on the stack, on the heap, or in the static/data area of a program, and why: (a) a local int counter; declared inside a function, (b) memory returned by kmalloc, (c) a variable declared static int boot_count;.`,
    hints: ['Where does automatic storage go?', 'Who frees heap memory, and when does static storage exist?'],
    solution: `(a) A plain local variable like int counter; has automatic storage: it lives on the stack and disappears when the function returns. (b) Memory from kmalloc is dynamically allocated from the kernel heap; it persists until you explicitly kfree it. (c) A static variable lives in the static/data area (the .data segment if initialized non-zero, .bss if zero/uninitialized); it exists for the whole lifetime of the module/kernel and keeps its value across calls. The key distinctions are lifetime (automatic vs explicit vs whole-program) and who is responsible for reclaiming the memory.`,
    tags: ['c', 'memory-layout', 'stack'],
  },
  {
    id: 'lx-ch01-t-003',
    chapter: 1,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'Why u32 Instead of int?',
    prompt: `Kernel code is full of u8, u16, u32, u64 (and uint8_t style names) instead of char, short, int, long. Give two concrete reasons the kernel prefers these fixed-width types.`,
    hints: ['How big is a long on different platforms?', 'Think about hardware registers and on-disk/on-wire formats.'],
    solution: `First, the widths of char, short, int, long are only minimums in the C standard and actually vary by platform and compiler (notably long is 32 bits on 32-bit systems but 64 bits on 64-bit Linux). Fixed-width types like u32 guarantee an exact size everywhere the kernel builds, which is essential for portability. Second, kernel code constantly maps structures onto hardware registers, device descriptors, and on-disk or on-wire formats where the exact byte count and layout are fixed by a spec; using u32 makes those sizes explicit and self-documenting. The u8/u32 names are the kernel's own spelling of the same idea as uint8_t/uint32_t.`,
    tags: ['c', 'fixed-width', 'portability'],
  },
  {
    id: 'lx-ch01-t-004',
    chapter: 1,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'What Include Guards Prevent',
    prompt: `Header files in the kernel begin with #ifndef _SOMETHING_H and #define _SOMETHING_H and end with #endif. What problem does this pattern solve, and what would go wrong without it?`,
    hints: ['What happens if two .c files include the same header chain?', 'Think about a struct definition appearing twice.'],
    solution: `This is an include guard. Without it, if a header is included more than once in a single translation unit (which happens easily through transitive includes), its contents would be textually pasted multiple times. For things like struct, enum, or typedef definitions that is a redefinition error, and even for harmless declarations it bloats compilation. The guard works because the first inclusion defines the macro; on any later inclusion the #ifndef is false, so the body is skipped. The effect is that the header's body is compiled at most once per translation unit. (Many kernel headers also use #pragma once for the same purpose.)`,
    tags: ['c', 'preprocessor', 'headers'],
  },
  {
    id: 'lx-ch01-t-005',
    chapter: 1,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Pointer Arithmetic by Element, Not Byte',
    prompt: `Given u32 *p; where p holds the address 0x1000, what address does p + 1 evaluate to, and what address does ((char *)p) + 1 evaluate to? Explain the difference.`,
    hints: ['Pointer arithmetic scales by sizeof of the pointed-to type.', 'How many bytes is a u32?'],
    solution: `p is a u32 pointer and a u32 is 4 bytes, so p + 1 advances by sizeof(u32) = 4 bytes, giving 0x1004. ((char *)p) + 1 first reinterprets the address as a char pointer, and char is 1 byte, so adding 1 advances by a single byte, giving 0x1001. The rule is that p + n moves by n * sizeof(*p) bytes, so the pointed-to type, not the raw address, determines the stride. This is exactly why casting to char* (or u8*) is the idiom when you want to step through raw bytes.`,
    tags: ['c', 'pointers', 'arithmetic'],
  },
  {
    id: 'lx-ch01-t-006',
    chapter: 1,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Arrow Versus Dot',
    prompt: `You have struct task t; and struct task *tp = &t;. When do you write t.pid versus tp->pid, and what is tp->pid shorthand for? Could you ever use the dot operator with tp?`,
    hints: ['One operand is a struct value, the other is a pointer to a struct.', 'Rewrite the arrow using dereference and dot.'],
    solution: `The dot operator . accesses a member of a struct value, so you write t.pid because t is a struct task. The arrow operator -> accesses a member through a pointer to a struct, so you write tp->pid because tp is a struct task *. The arrow is pure shorthand: tp->pid is exactly (*tp).pid, that is, dereference the pointer and then take the member. You could write (*tp).pid with the dot, but you cannot write tp.pid directly because tp is a pointer, not a struct; -> exists precisely to make the common pointer case readable.`,
    tags: ['c', 'structs', 'pointers'],
  },
  {
    id: 'lx-ch01-t-007',
    chapter: 1,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Setting, Clearing, and Testing a Flag',
    prompt: `A status field u32 flags; uses bit 3 (value 1 << 3) as a "ready" flag. Write the expression to (a) set the ready bit, (b) clear it, and (c) test whether it is set, without disturbing any other bits.`,
    hints: ['OR to set, AND with a complemented mask to clear.', 'AND to test; remember ~ flips every bit.'],
    solution: `Define the mask once, e.g. #define READY (1u << 3). (a) To set: flags |= READY; OR forces that bit to 1 and leaves the rest unchanged. (b) To clear: flags &= ~READY; ~READY is all ones except bit 3, so ANDing forces bit 3 to 0 while preserving every other bit. (c) To test: if (flags & READY) ... ; the AND yields nonzero exactly when the bit is set. The whole point of using a single-bit mask plus |=, &= ~, and & is that each operation touches only the bit of interest and leaves the other flags intact.`,
    tags: ['c', 'bitwise', 'flags'],
  },
  {
    id: 'lx-ch01-t-008',
    chapter: 1,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Const Pointer Versus Pointer to Const',
    prompt: `Distinguish these three declarations and say what each forbids: const char *a; char *const b; const char *const c;`,
    hints: ['Read declarations right-to-left around the *.', 'Which const protects the pointee, which protects the pointer itself?'],
    solution: `const char *a is a pointer to const char: you may change which char a points to (a = other; is fine) but you may not modify the pointed-to data through a (*a = 'x'; is an error). char *const b is a const pointer to non-const char: you may modify the data (*b = 'x'; is fine) but you may not repoint it (b = other; is an error). const char *const c is both: neither the data nor the pointer may be changed through c. Reading right-to-left around the star makes this consistent, and the distinction matters in APIs that promise not to mutate a buffer versus those that must keep pointing at the same place.`,
    tags: ['c', 'const', 'pointers'],
  },
  {
    id: 'lx-ch01-t-009',
    chapter: 1,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Where Is the Null Terminator?',
    prompt: `In C a string is just a char array ending in a '\\0' byte. If you have char name[8] = "kernel";, how many bytes of the array are used, what is at index 6, and why does strlen return 6 rather than 7?`,
    hints: ['Count the characters plus the terminator.', 'strlen counts up to but not including the terminator.'],
    solution: `The literal "kernel" is 6 characters, and the compiler appends a terminating '\\0', so 7 of the 8 bytes are used: indices 0..5 hold 'k','e','r','n','e','l' and index 6 holds '\\0' (index 7 is left as 0 too here). strlen returns 6 because it counts characters up to but not including the terminating '\\0'; it walks the array until it hits the null byte. This is why a buffer must always be sized for the characters plus one extra byte for the terminator, and why functions that forget the terminator (or overflow past it) are a classic source of kernel string bugs.`,
    tags: ['c', 'strings', 'arrays'],
  },
  {
    id: 'lx-ch01-t-010',
    chapter: 1,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Why volatile Exists',
    prompt: `A driver polls a hardware status register in a loop: while ((reg & DONE) == 0) ; where reg is read from a memory-mapped address. Why might the compiler turn this into an infinite loop, and how does volatile fix it?`,
    hints: ['What does the compiler assume about a value nothing in the code writes?', 'volatile changes the assumptions about reads.'],
    solution: `If reg is an ordinary variable, the compiler sees that nothing in the loop changes it, so it is allowed to read it once, cache the value in a register, and reuse that cached value forever, producing an infinite loop even after the hardware sets the DONE bit. Declaring the underlying access as volatile (or using the kernel's readl/ioread accessors, which do this for you) tells the compiler the value can change outside the program's control, so it must re-read from the actual memory location every time. volatile forbids caching and reordering of those accesses, which is exactly what hardware register polling needs. Note volatile is about visibility of reads/writes, not atomicity or locking.`,
    tags: ['c', 'volatile', 'hardware'],
  },
  {
    id: 'lx-ch01-t-011',
    chapter: 1,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'The Classic Macro Pitfall',
    prompt: `Consider #define SQUARE(x) x * x and then int r = SQUARE(a + 1);. What does this expand to, and why is the result usually wrong? How do you fix the macro?`,
    hints: ['The preprocessor substitutes text, it does not evaluate.', 'Add parentheses generously.'],
    solution: `The preprocessor does pure textual substitution, so SQUARE(a + 1) expands to a + 1 * a + 1, which by precedence is a + (1*a) + 1 = 2a + 1, not (a+1)^2. The fix is to parenthesize both the whole expression and each parameter use: #define SQUARE(x) ((x) * (x)), which expands to ((a + 1) * (a + 1)) as intended. A second subtlety is double evaluation: if you pass SQUARE(i++) it increments twice, so function-like macros should avoid arguments with side effects (or use a static inline function instead, which the kernel often prefers for type safety).`,
    tags: ['c', 'preprocessor', 'macros'],
  },
  {
    id: 'lx-ch01-t-012',
    chapter: 1,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Array Name Decays to a Pointer',
    prompt: `Inside a function with int buf[16];, the expression sizeof(buf) gives 64 on a system with 4-byte ints. But if you pass buf to a function declared as void f(int *p), then sizeof(p) inside f gives 8. Explain the "decay" rule that causes this.`,
    hints: ['What is the type of an array when used in most expressions?', 'What does the callee actually receive?'],
    solution: `An array name is not a pointer, but in most expression contexts it decays to a pointer to its first element. sizeof and & are exceptions, so sizeof(buf) sees the real array type int[16] and reports 16*4 = 64 bytes. When you pass buf to f, the argument decays to an int* pointing at buf[0], so the function only receives an address, not the array. Inside f, sizeof(p) is the size of a pointer (8 on a 64-bit system), with no knowledge of the original length. This is precisely why kernel functions that take arrays must also take a separate length parameter; the size information is lost at the call.`,
    tags: ['c', 'arrays', 'pointers'],
  },
  {
    id: 'lx-ch01-t-013',
    chapter: 1,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Reading a Function Pointer Declaration',
    prompt: `What does int (*op)(int, int); declare, and how is it different from int *op(int, int);? Show how you would assign a real function to the function-pointer version and then call it.`,
    hints: ['Parentheses around *op bind the star to the name.', 'Without them, the * applies to the return type.'],
    solution: `int (*op)(int, int); declares op as a pointer to a function taking two ints and returning int; the parentheses around (*op) bind the star to the name. In contrast int *op(int, int); declares op as a function taking two ints and returning int* (a pointer to int) -- a function declaration, not a variable. To use the pointer: given int add(int a, int b){...}, you write op = add; (the function name decays to its address; op = &add; is equivalent), then call it with op(2, 3); (or the older (*op)(2,3)). Function pointers are how the kernel implements dispatch tables like struct file_operations.`,
    tags: ['c', 'function-pointers', 'declarations'],
  },
  {
    id: 'lx-ch01-t-014',
    chapter: 1,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Why Callback Tables Use Function Pointers',
    prompt: `The kernel registers a driver by filling a struct full of function pointers (for example file_operations with .read, .write, .open). Why is a struct of function pointers a better design than, say, a giant switch statement in the core that knows about every driver?`,
    hints: ['Think about adding a new driver without touching core code.', 'What does the core need to know about a specific driver?'],
    solution: `A table of function pointers decouples the generic core from the specific implementations. The VFS core can call f->f_op->read(...) without knowing or being compiled against any particular driver; each driver supplies its own behavior by filling in the pointers. This means a new driver can be added (even as a loadable module at runtime) without modifying or recompiling the core, which a central switch would require for every new case. It also lets unsupported operations be left NULL so the core can detect and handle "not implemented" uniformly. This is the C idiom for polymorphism/virtual dispatch: the struct of function pointers is effectively a vtable, enabling open-ended extension and clean separation of concerns.`,
    tags: ['c', 'function-pointers', 'design'],
  },
  {
    id: 'lx-ch01-t-015',
    chapter: 1,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Unsigned Overflow and a Broken Loop',
    prompt: `A loop is written as for (size_t i = count - 1; i >= 0; i--) to iterate backwards, where count is a size_t. When count is 0, and even normally, this loop misbehaves. Explain both problems and how unsigned wraparound is involved.`,
    hints: ['size_t is unsigned; what is an unsigned value never?', 'What is 0 - 1 for an unsigned type?'],
    solution: `size_t is unsigned, so the condition i >= 0 is always true -- an unsigned value can never be negative -- making the loop's exit test useless and the loop run "forever" (until something else stops it). The wraparound makes it worse: when i is 0 and i-- runs, it does not go to -1 but wraps to SIZE_MAX (a huge value), so the loop keeps going and indexes far out of bounds. And if count is 0 to begin with, count - 1 is already SIZE_MAX, starting the loop catastrophically out of range. Safe patterns are to use a signed index, or write for (size_t i = count; i-- > 0; ) which tests before decrementing and handles count == 0 correctly. Unsigned overflow is defined (it wraps modulo 2^N), which is exactly why it silently produces these huge values instead of trapping.`,
    tags: ['c', 'overflow', 'unsigned'],
  },
  {
    id: 'lx-ch01-t-016',
    chapter: 1,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'How container_of Works',
    prompt: `The kernel macro container_of(ptr, type, member) recovers a pointer to an enclosing struct given a pointer to one of its members. Conceptually, how can it compute the struct's address from a member's address, and why is this valid C rather than a hack?`,
    hints: ['Members sit at fixed offsets inside a struct.', 'offsetof gives the byte distance from the struct start to the member.'],
    solution: `Every member of a struct sits at a fixed, compile-time-known byte offset from the start of the struct, given by offsetof(type, member). So if you know the address of the member and that offset, the struct's start is simply member_address - offset. container_of casts ptr to char* (so the subtraction is in bytes), subtracts offsetof(type, member), and casts the result back to type*. It is valid, not a hack, because the C layout rules guarantee members occupy stable offsets within a struct, and offsetof is a standard, portable way to obtain that offset (the compiler computes it accounting for any padding). This is what lets a single generic list/rbtree embed itself in many different structs: from the embedded node you recover the owner.`,
    tags: ['c', 'container-of', 'offsetof'],
  },
  {
    id: 'lx-ch01-t-017',
    chapter: 1,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Detecting the Machine Byte Order',
    prompt: `You store the 32-bit value 0x01020304 in a u32, then read the four bytes of that variable through a u8* starting at the lowest address. On a little-endian machine versus a big-endian machine, what byte sequence do you see, and why?`,
    hints: ['Endianness is about which byte goes at the lowest address.', 'Little-endian puts the least significant byte first.'],
    solution: `Endianness determines which byte of a multi-byte integer is stored at the lowest memory address. On a little-endian machine the least significant byte comes first, so reading the bytes low-to-high gives 0x04, 0x03, 0x02, 0x01. On a big-endian machine the most significant byte comes first, so you see 0x01, 0x02, 0x03, 0x04. Same numeric value, different in-memory byte order. This is exactly the trick used to probe byte order at runtime, and it is why on-disk and on-wire formats (which fix a byte order) require explicit conversion such as le32_to_cpu/be32_to_cpu rather than just reinterpreting the bytes.`,
    tags: ['c', 'endianness', 'byte-order'],
  },
  {
    id: 'lx-ch01-t-018',
    chapter: 1,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Union for Type Punning',
    prompt: `Compare a struct and a union that both contain a u32 i and a float f. How do their sizes and memory layouts differ, and what does it mean that writing one union member and reading the other reinterprets the same bytes?`,
    hints: ['Struct members each get their own storage; union members share.', 'Total size: sum versus max.'],
    solution: `In a struct, each member gets its own distinct storage laid out sequentially (with possible padding), so the struct is at least the sum of the members' sizes; i and f are independent. In a union, all members share the same storage starting at the same address, so the union's size is the size of its largest member (here 4 bytes), and only one member is meaningfully "active" at a time. Because they overlay the same bytes, writing u.i and then reading u.f reinterprets the identical bit pattern as a float -- this is type punning. Unions save space and are used in the kernel for tagged variants and register overlays, but the programmer must track which member is currently valid, since the bytes carry no tag of their own.`,
    tags: ['c', 'unions', 'memory-layout'],
  },
  {
    id: 'lx-ch01-t-019',
    chapter: 1,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Extracting a Bit Field by Hand',
    prompt: `A 32-bit hardware register packs a 4-bit field at bit positions 8..11. Given u32 reg;, write the expression to extract just that 4-bit field as a number 0..15, and explain the roles of the shift and the mask.`,
    hints: ['Shift the field down to bit 0 first.', 'Then mask off the bits above it.'],
    solution: `Use (reg >> 8) & 0xF. The right shift by 8 moves the field down so its lowest bit lands at bit 0, discarding bits 0..7. The mask & 0xF (binary 1111) then keeps only the lowest 4 bits and zeroes everything above, so bits 12..31 cannot contaminate the result. Order matters conceptually but both orders work if the mask is shifted accordingly; the shift-then-mask form is the common idiom. To build a value into that field you would do the reverse: reg = (reg & ~(0xF << 8)) | ((val & 0xF) << 8), clearing the field then OR-ing the new bits in.`,
    tags: ['c', 'bitwise', 'masks'],
  },
  {
    id: 'lx-ch01-t-020',
    chapter: 1,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Returning a Pointer to a Local',
    prompt: `This function looks reasonable but is badly broken: char *make_label(void) { char buf[16]; snprintf(buf, sizeof buf, "id"); return buf; } Explain why returning buf is a use-after-return bug and list two correct alternatives.`,
    hints: ['What is the lifetime of an automatic array?', 'What is the caller pointing at after the function returns?'],
    solution: `buf is an automatic (stack) array whose lifetime ends when make_label returns; its stack space is reclaimed and may be overwritten by the next call. Returning &buf[0] hands the caller a pointer to memory that no longer logically exists, so any use is undefined behavior (it might appear to work, then corrupt). Correct alternatives: (1) have the caller pass in a buffer, e.g. void make_label(char *out, size_t n), so the storage outlives the function; (2) allocate on the heap with kmalloc/kstrdup and return that, documenting that the caller must kfree it; or (3) for truly constant text, return a pointer to a string literal or static storage. The rule is that a returned pointer must point to storage that survives the return.`,
    tags: ['c', 'stack', 'lifetime'],
  },
  {
    id: 'lx-ch01-t-021',
    chapter: 1,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Const Is a Promise, Not Armor',
    prompt: `A function takes const char *src and promises not to modify the data. Does const guarantee the underlying bytes cannot change while the function runs? Contrast this with volatile, and explain why something can be both const and volatile.`,
    hints: ['const restricts what this code may do through this pointer.', 'Who else might write to that memory?'],
    solution: `const is a compile-time promise about what your code may do through that particular pointer: you may not write through src. It does not guarantee the bytes are immutable -- another pointer, another thread, or hardware could legitimately change that memory, and const says nothing about them. volatile is the orthogonal concern: it tells the compiler the value may change outside this code's control, so reads must not be optimized away. Therefore const volatile makes sense, e.g. for a read-only hardware status register: your code must not write it (const), and its value can change underneath you so every read must hit memory (volatile). They answer different questions -- "may I write?" versus "must I re-read?"`,
    tags: ['c', 'const', 'volatile'],
  },
  {
    id: 'lx-ch01-t-022',
    chapter: 1,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'sizeof a Struct Versus Its Fields',
    prompt: `struct s { u8 a; u32 b; u8 c; }; On a typical machine sizeof(struct s) is 12, even though the fields total only 6 bytes. Explain padding and alignment, and how reordering the fields could shrink the struct.`,
    hints: ['Each type wants to sit at an address that is a multiple of its size.', 'Padding is inserted to satisfy alignment.'],
    solution: `The compiler aligns each member to its natural boundary -- a u32 typically must start at a 4-byte-aligned offset -- and pads the struct so arrays of it stay aligned too. With order a(1), b(4), c(1): a is at 0, then 3 bytes of padding push b to offset 4, b occupies 4..7, c is at 8, and the struct is padded to a multiple of 4 (its strictest alignment), giving 12. Reordering to put the largest/strictest member first, e.g. { u32 b; u8 a; u8 c; }, lets a and c pack into the bytes after b: b at 0..3, a at 4, c at 5, pad to 8 -- only 8 bytes. This is why kernel structs are often arranged largest-to-smallest, and why you should never assume a struct's size equals the sum of its fields.`,
    tags: ['c', 'structs', 'alignment'],
  },
  {
    id: 'lx-ch01-t-023',
    chapter: 1,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Signed Shift and Sign Extension',
    prompt: `Compare (-1) >> 1 for a signed int versus 0xFFFFFFFFu >> 1 for an unsigned 32-bit value. Why is left-shifting a signed value into or past the sign bit undefined or implementation-defined, and what does the kernel do to stay safe?`,
    hints: ['Right shift of a negative value: logical or arithmetic?', 'Think about what bit is shifted into the top.'],
    solution: `Right-shifting a negative signed int is implementation-defined but in practice arithmetic: (-1) >> 1 keeps the sign bit, so it stays -1 (the vacated high bit is filled with the sign). For unsigned, 0xFFFFFFFFu >> 1 is a logical shift that brings in a 0, giving 0x7FFFFFFF. Left shifts are the sharper trap: shifting a 1 into or past the sign bit of a signed type is undefined behavior in C, as is shifting by an amount >= the type's width. The kernel sidesteps all of this by doing bit manipulation on unsigned fixed-width types (u32, u64) where shifts have well-defined modular/logical behavior, and by using helpers like BIT(n) and explicit masks so the shift count and signedness are always controlled.`,
    tags: ['c', 'bitwise', 'undefined-behavior'],
  },
  {
    id: 'lx-ch01-t-024',
    chapter: 1,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Strict Aliasing and Type Punning Through Pointers',
    prompt: `A developer reinterprets a float as bits with float x; u32 bits = *(u32 *)&x;. Beyond endianness, why can this be unsafe under C's aliasing rules, and what are the kernel-friendly safe ways to do the same reinterpretation?`,
    hints: ['Can the compiler assume a float* and a u32* never refer to the same object?', 'What about memcpy or a union?'],
    solution: `C's strict aliasing rule lets the compiler assume that pointers of incompatible types (here float* and u32*) never refer to the same object, so it may reorder or cache the float and the u32 read independently, producing wrong or surprising results -- accessing an object through an incompatible pointer type is undefined behavior. The fix is to make the byte reinterpretation explicit in a way the standard blesses: use memcpy(&bits, &x, sizeof bits), which copies the representation without an aliasing violation, or use a union and write one member then read the other (well-supported in GNU C, which the kernel relies on). The kernel also commonly compiles with -fno-strict-aliasing, which relaxes the assumption, but writing memcpy/union-based punning is the portable, intention-revealing choice. Endianness is a separate concern about byte order, not about whether the access is legal.`,
    tags: ['c', 'aliasing', 'type-punning'],
  },
  {
    id: 'lx-ch01-t-025',
    chapter: 1,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Why do-while(0) Wraps Multi-Statement Macros',
    prompt: `Kernel multi-statement macros are written as #define FOO(x) do { stmt1; stmt2; } while (0). Why not just wrap the body in { }? Show a concrete case where braces alone break and the do/while form does not.`,
    hints: ['Think about a macro call followed by a semicolon inside an if/else.', 'What does a trailing brace plus semicolon do to else?'],
    solution: `A bare braced block leaves a dangling problem when the macro is used like a statement with a trailing semicolon. Consider if (cond) FOO(x); else bar();. With #define FOO(x) { a; b; } this expands to if (cond) { a; b; }; else bar();. The stray semicolon after the closing brace is an empty statement that terminates the if, so the else has no matching if -- a syntax error. Wrapping the body in do { a; b; } while (0) makes the macro a single statement that correctly consumes the caller's semicolon: if (cond) do { a; b; } while (0); else bar(); parses fine and works in every context (after if without braces, as the body of a loop, etc.). The while (0) ensures the body runs exactly once and the construct behaves like one ordinary statement.`,
    tags: ['c', 'preprocessor', 'macros'],
  },
  {
    id: 'lx-ch01-t-026',
    chapter: 1,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Reading and Writing Hardware Registers Safely',
    prompt: `Why is it wrong to access a memory-mapped device register with a plain dereference like u32 v = *(u32 *)reg_addr;, even if you make the pointer volatile? Explain what the kernel accessors (readl/writel, ioread32/iowrite32) add beyond a volatile access.`,
    hints: ['volatile handles compiler caching, but not byte order or CPU/compiler ordering between accesses.', 'Think portability across architectures.'],
    solution: `A volatile dereference only stops the compiler from caching or eliding the access; it does not handle several things device I/O needs. The kernel accessors readl/writel (and ioread32/iowrite32) also: convert byte order so the value matches the device's documented little-endian (on most buses) regardless of CPU endianness; insert the architecture-required memory barriers so register accesses are not reordered with respect to each other or to surrounding memory in ways that break the device protocol; and abstract over architectures where MMIO uses special instructions rather than plain loads/stores. A raw volatile pointer compiles but is non-portable and can silently misbehave on big-endian or weakly-ordered systems. So you use the accessors because correct device access is about ordering and byte order, not just "don't optimize the read away."`,
    tags: ['c', 'volatile', 'mmio'],
  },
  {
    id: 'lx-ch01-t-027',
    chapter: 1,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'A Calling Convention via Function-Pointer Table',
    prompt: `You are designing a small subsystem where each registered handler is a function pointer stored in a table, and one handler may run from interrupt context. What signature and behavioral constraints should you put on these callbacks, and why does the calling context restrict what their implementations may do?`,
    hints: ['Callbacks invoked from interrupt/atomic context cannot sleep.', 'Think about return values for "not handled" and about allocation flags.'],
    solution: `Give the callbacks a uniform, explicit signature -- for example int (*handle)(struct ctx *, void *priv) returning a status (0/handled vs an error/"not mine") and taking a context pointer plus an opaque private pointer -- so the dispatcher can iterate the table generically and pass per-handler state. The crucial behavioral constraint is the calling context: a handler that may run in interrupt (atomic) context must not sleep, so it cannot call blocking allocators (must use GFP_ATOMIC, not GFP_KERNEL), cannot take sleeping locks like a mutex (use spinlocks), and cannot call anything that may block such as copy_to_user (which can fault and sleep). You should document the context contract as part of the callback's interface, because the function pointer hides the implementation from the core and the core has no other way to enforce that the handler obeys the no-sleep rule. Keeping such handlers short and non-blocking, deferring heavy work to a process-context path, is the standard design.`,
    tags: ['c', 'function-pointers', 'atomic-context'],
  },
  {
    id: 'lx-ch01-t-028',
    chapter: 1,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Integer Promotion in a Mask Expression',
    prompt: `On a 32-bit-int machine, a developer writes u16 a = 0xFFFF; u16 b = 0xFFFF; u32 wide = a + b; expecting wraparound to 0xFFFE, but gets 0x1FFFE. Separately, ~a does not yield 0 even though a is all ones. Explain integer promotion and why it changes both results.`,
    hints: ['What type are u16 operands converted to before arithmetic?', 'How wide is the intermediate result of + and ~?'],
    solution: `Before almost any arithmetic, operands narrower than int undergo integer promotion to int (since int can represent all u16 values). So a + b is computed as int + int: 0xFFFF + 0xFFFF = 0x1FFFE, which fits in a 32-bit int and is assigned to wide unchanged -- there is no u16 wraparound because the addition never happened in 16 bits. Likewise ~a promotes a to int 0x0000FFFF first, and ~0x0000FFFF is 0xFFFF0000 (as an int, that is -65536), not 0; only the low 16 bits are the "complement of a." If you truly want 16-bit results you must mask back down, e.g. (u16)(a + b) or (a + b) & 0xFFFF, and for the complement (u16)~a. This promotion surprise is a frequent source of bugs in mask and checksum code, which is why kernel bit math is careful about casting results back to the intended width.`,
    tags: ['c', 'integer-promotion', 'overflow'],
  },
  {
    id: 'lx-ch01-t-029',
    chapter: 1,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Endianness of On-Disk and On-Wire Data',
    prompt: `A filesystem stores a 32-bit block count on disk in little-endian. A driver reads it into a u32 with a plain copy and uses it directly. On a big-endian kernel this silently gives wrong numbers. Explain the bug and how the kernel's typed endian helpers prevent it at compile time.`,
    hints: ['The disk format fixes a byte order; the CPU may differ.', 'Think about __le32 and le32_to_cpu, and sparse checking.'],
    solution: `On-disk and on-wire formats fix a byte order independent of the CPU. If the disk stores the count little-endian and a big-endian CPU copies those four bytes into a native u32 and uses it directly, the CPU interprets the bytes in its own order and reads a byte-swapped, wrong value -- on a little-endian CPU it happens to work, which is why the bug hides. The fix is to declare the on-disk field with a tagged type like __le32 and always convert with le32_to_cpu() when reading (and cpu_to_le32() when writing); these are no-ops on little-endian and byte-swap on big-endian, so the same code is correct everywhere. Crucially the __le32/__be32 tags let the kernel's sparse static checker flag any place that mixes a tagged endian value with native arithmetic without a conversion, turning a runtime, architecture-specific bug into a compile-time/analysis error. The lesson: never treat externally-defined data as native integers; convert through the typed helpers.`,
    tags: ['c', 'endianness', 'on-disk-format'],
  },
  {
    id: 'lx-ch01-t-030',
    chapter: 1,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Pointer Subtraction, void*, and Element Counts',
    prompt: `Given two pointers p and q into the same int array, ptrdiff_t n = q - p; gives an element count, not a byte count. Yet you cannot do arithmetic on a void* in standard C. Explain both facts and why the kernel sometimes casts to char*/uintptr_t for raw address math.`,
    hints: ['Pointer subtraction divides by the element size.', 'What is sizeof(void)?'],
    solution: `Subtracting two pointers into the same array yields the number of elements between them, because the language divides the byte distance by sizeof(*p); so for int* with 4-byte ints, a 12-byte gap is reported as 3, not 12. void* cannot be used in pointer arithmetic in standard C because arithmetic needs the element size and sizeof(void) is undefined (void is an incomplete type); GCC permits void* arithmetic as an extension treating it like char*, but standard code should not rely on it. When the kernel needs raw byte-level address math -- computing offsets, aligning addresses, or implementing container_of -- it casts to char*/u8* (so each step is one byte) or to an integer type like uintptr_t/unsigned long, does the arithmetic explicitly, then casts back. Doing it explicitly keeps the units unambiguous and avoids relying on the void* extension. (Subtraction is also only well-defined when both pointers are within, or one past the end of, the same array object.)`,
    tags: ['c', 'pointers', 'arithmetic'],
  },
]

export default problems
