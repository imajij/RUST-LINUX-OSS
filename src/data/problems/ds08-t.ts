import type { Problem } from '../../types'

const problems: Problem[] = [
  {
    id: 'ds-ch08-t-001',
    chapter: 8,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'What Is a Prefix Query?',
    prompt: `A contacts app stores a few thousand names. The moment you type "Ja" it must show Jacob, Jade, James, and Jasmine, refreshing on every keystroke. In one or two sentences, explain what kind of question this is and why it is different from asking "is there a contact named exactly Ja?".`,
    hints: [
      'Think about the difference between "starts with" and "is exactly equal to".',
      'The user almost never has a contact whose full name is just the letters they typed so far.',
    ],
    solution: `This is a prefix query: the app is asking "which stored keys start with the letters Ja?", not "is the exact key Ja present?". The two are fundamentally different questions. Exact membership wants one specific full word, while a prefix query wants every word that begins with a given run of characters. The user almost never has a contact whose full name is just "Ja", so answering with exact membership would return nothing useful; the feature only makes sense as a prefix query.`,
    tags: ['prefix-query', 'trie', 'hash-set'],
  },
  {
    id: 'ds-ch08-t-002',
    chapter: 8,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'Where Do the Characters Live?',
    prompt: `A classmate says "each node of a trie holds one character, and the root holds the first letter of every word." Two things in that sentence are off. Correct them, and say what the root actually represents.`,
    hints: [
      'In a trie the information rides on the edges, not inside the nodes.',
      'What had you typed before you typed any letter at all?',
    ],
    solution: `First, characters live on the edges, not inside the nodes. Each edge is labelled with a single character, and a word is spelled out by the sequence of edge labels you read walking from the root down to a node; a node is best thought of as a position between characters. Second, the root does not hold the first letter of every word. The root holds no character at all: it is the empty starting position, the "" before you have typed anything. The first letters of the stored words are the labels on the edges leaving the root.`,
    tags: ['trie', 'nodes', 'edges'],
  },
  {
    id: 'ds-ch08-t-003',
    chapter: 8,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Why is_end Cannot Be Skipped',
    prompt: `You insert only the single word "cat" into a fresh trie, then call search on "ca". A buggy implementation returns true. The author argues: "I successfully walked c then a, so the path exists, so it is a word." What is wrong with that reasoning, and what one piece of information distinguishes "ca" from "cat" here?`,
    hints: [
      'Walking the path proves the prefix exists, not that a word ends there.',
      'Every node carries a single boolean flag for exactly this purpose.',
    ],
    solution: `Walking c then a only proves the prefix "ca" exists; it does not prove a complete word ends on that node. When you inserted "cat", you created the path c, a, t and set the is_end flag true only on the final t node. The a node was created along the way but never flagged, so it is a mere stop on the road to "cat". The one piece of information that distinguishes them is the is_end boolean: search must return the final node's is_end, not just "did the walk arrive". Here is_end is false on the a node, so search("ca") must return false.`,
    tags: ['trie', 'is-end', 'search'],
  },
  {
    id: 'ds-ch08-t-004',
    chapter: 8,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Counting New Nodes on Insert',
    prompt: `Starting from an empty trie, you insert the words in this order: "car", "card", "cart", "dog". How many brand-new nodes does each insert allocate, and how many nodes does the whole trie contain at the end (not counting the root)? Explain where the savings come from.`,
    hints: [
      'A new node is created only for a character whose edge does not already exist on the current path.',
      'After "car" exists, inserting "card" reuses c, a, and r.',
    ],
    solution: `insert("car") allocates 3 new nodes (c, a, r). insert("card") reuses c, a, r and allocates only 1 new node (the d). insert("cart") again reuses c, a, r and allocates 1 new node (the t, a second child of r). insert("dog") shares nothing with the existing tree, so it allocates 3 new nodes (d, o, g). The total is 3 + 1 + 1 + 3 = 8 nodes (excluding the root). The savings come from shared prefixes: car, card, and cart all reuse the single path c -> a -> r, so each of card and cart costs only one extra node instead of four.`,
    tags: ['trie', 'insert', 'shared-prefix'],
  },
  {
    id: 'ds-ch08-t-005',
    chapter: 8,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'search and starts_with: the One Difference',
    prompt: `A trie stores exactly "car" and "card". Predict the result of each call and give a one-line reason:
search("car"), search("ca"), search("care"), starts_with("ca"), starts_with("cb").`,
    hints: [
      'Both operations do the identical walk; only the final check differs.',
      'starts_with ignores is_end entirely; search checks it.',
    ],
    solution: `search("car") is true: the walk c, a, r arrives and that node has is_end true (a word ends there). search("ca") is false: the walk arrives on the a node, but is_end is false there, so no word ends on "ca". search("care") is false: after c, a, r there is no child for 'e', so the path breaks. starts_with("ca") is true: the walk c, a arrives, and a prefix query only needs the walk to arrive, ignoring is_end. starts_with("cb") is false: after c there is no child for 'b', so the path breaks. The only difference between the two operations is that search checks is_end at the end while starts_with just checks that the walk arrived.`,
    tags: ['trie', 'search', 'starts-with'],
  },
  {
    id: 'ds-ch08-t-006',
    chapter: 8,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Why Box Is Required',
    prompt: `A student writes the trie node as struct TrieNode { children: HashMap<char, TrieNode>, is_end: bool } and the compiler rejects it. The note instead uses HashMap<char, Box<TrieNode>>. Explain why the first version cannot compile and what the Box fixes.`,
    hints: [
      'A struct that contains itself by value would need to know its own size up front.',
      'A Box is just a fixed-size pointer to data on the heap.',
    ],
    solution: `The first version is recursive by value: a TrieNode would directly contain TrieNodes inside its children, which would contain more TrieNodes, and so on. To lay this out the compiler would need a finite, known size, but a type that contains itself by value has no finite size, so Rust rejects it. A Box<TrieNode> is a smart pointer that puts the child node on the heap and is itself just a fixed-size pointer. So the struct's size becomes known and finite (a HashMap plus a bool plus pointer-sized child slots), which breaks the infinite-size cycle and lets the type compile.`,
    tags: ['trie', 'box', 'rust-ownership'],
  },
  {
    id: 'ds-ch08-t-007',
    chapter: 8,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'The entry API Versus Two Lookups',
    prompt: `Inside insert, the note steps into a child with node.children.entry(ch).or_default(). A classmate instead writes: "if children does not contain ch, insert an empty node; then get_mut(ch) and step in." Both reach the same child. Why does the note prefer the single entry call?`,
    hints: [
      'Count how many times each version hashes the character ch.',
      'One version walks the HashMap structure twice for the same key.',
    ],
    solution: `The classmate's version does the work twice: contains_key hashes ch and probes the HashMap once, then get_mut hashes ch and probes again, even when the key was just inserted. The entry(ch).or_default() call does the lookup a single time: it finds the slot for ch, inserts a Default (an empty boxed node) only if it is missing, and hands back a mutable reference, all in one pass. So entry is faster (one hash and one probe instead of two) and more idiomatic. The double-lookup pattern is a classic beginner inefficiency the note flags as a pitfall.`,
    tags: ['trie', 'entry-api', 'hash-map'],
  },
  {
    id: 'ds-ch08-t-008',
    chapter: 8,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Why Prefix Queries Beat a HashSet',
    prompt: `You store n words in a HashSet and in a trie. For exact membership both answer in roughly the length of the query word. But to answer "does any stored word start with this prefix?" the HashSet is O(n times L) while the trie is O(L). Explain why the HashSet cannot do better, and why the trie's cost has no n in it.`,
    hints: [
      'Hashing deliberately scrambles bytes, which destroys any relationship between similar keys.',
      'In a trie, reaching the prefix is just walking down a few edges.',
    ],
    solution: `A HashSet stores each word by running it through a hash function that deliberately scrambles the bytes to spread keys evenly across buckets. That scrambling is exactly what makes exact lookup fast, but it also destroys any relationship between similar keys: "James" and "Jasmine" share the prefix "Ja" yet land in unrelated buckets. So to find every key with a given prefix the set has no choice but to scan all n keys and compare each (up to L characters), giving O(n times L). A trie keeps the prefix structure as the literal shape of the tree, so reaching a prefix is just walking one edge per prefix character, O(L). That walk steps down a fixed number of edges regardless of how many words are stored, so n never enters the cost.`,
    tags: ['trie', 'hash-set', 'complexity'],
  },
  {
    id: 'ds-ch08-t-009',
    chapter: 8,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'The Two Phases of Autocomplete',
    prompt: `A trie holds car, card, cat, and dog. You call autocomplete on the prefix "ca". Describe the two phases of the algorithm, then state which words come back and why the dog branch is never even looked at. What does each phase cost in big-O terms?`,
    hints: [
      'Phase one walks to the prefix node; phase two explores the subtree below it.',
      'You start the depth-first search from the "ca" node, not from the root.',
    ],
    solution: `Phase one walks from the root to the node at the end of the prefix: c then a lands on the "ca" node. This is the same O(L) walk as starts_with, where L is the prefix length. Phase two does a depth-first search (DFS) of the entire subtree below that node, collecting every node flagged is_end and rebuilding each word from the path taken. From "ca" the DFS finds car, card, and cat. The dog branch is a sibling of c hanging off the root; because phase one moved us below "ca" before the DFS started, the search never climbs back up to the root, so the dog branch is never visited. Phase two costs O(k) where k is the size of the matched subtree (the number of completions plus their internal nodes), so the total is O(L) to reach the prefix plus O(k) to list the answers, which is optimal.`,
    tags: ['trie', 'autocomplete', 'dfs'],
  },
  {
    id: 'ds-ch08-t-010',
    chapter: 8,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'The Forgotten pop in a DFS',
    prompt: `An autocomplete DFS keeps a shared String buffer, pushing a character before descending into each child. A student forgets to pop the character after the recursive call returns. On a trie holding car and cat, calling autocomplete on "ca" returns the wrong list. Explain what the buffer contains when the DFS visits the second child, and why the missing pop corrupts the words.`,
    hints: [
      'The buffer is shared across sibling branches, so it must be restored on the way back up.',
      'After exploring the r branch, what should the buffer be before exploring the t branch?',
    ],
    solution: `The buffer is meant to always spell the current path, so before exploring each child it must hold just the prefix. The DFS starts with the buffer at "ca". It pushes 'r', descends, and emits "car". With the pop missing, the buffer is still "car" when control returns to the "ca" node and it tries the second child. It then pushes 't', making the buffer "cart", and emits the garbage word "cart" instead of "cat". The pop is the backtracking step: it undoes the character added for the branch you just finished so sibling branches start from the correct shared prefix. Without it, siblings inherit each other's characters and you collect corrupted words.`,
    tags: ['trie', 'dfs', 'backtracking'],
  },
  {
    id: 'ds-ch08-t-011',
    chapter: 8,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'HashMap Children Versus a 26-Slot Array',
    prompt: `You are building a trie that only ever stores lowercase English words. You are choosing between HashMap<char, Box<TrieNode>> children and a fixed [Option<Box<TrieNode>>; 26] array indexed by the letter minus 'a'. State the main advantage of each and one situation where you would clearly prefer the array.`,
    hints: [
      'Following an edge in the array is a plain index; following one in the HashMap involves hashing.',
      'Think about what every array node carries even for letters it never uses.',
    ],
    solution: `The array's advantage is speed and cache-friendliness: following an edge is a single plain array index with no hashing at all, and each step is exactly O(1). Its cost is memory: every node carries 26 slots whether or not it uses them, so a sparse trie wastes space. The HashMap's advantage is the opposite: it allocates memory only for children that actually exist and works for any alphabet, including Unicode, but each edge step pays the cost of a hash lookup. You would clearly prefer the array when the alphabet is small and dense, exactly the 26 lowercase letters case, especially if the trie is well filled so most slots are used and edge-following speed matters most.`,
    tags: ['trie', 'array-node', 'trade-offs'],
  },
  {
    id: 'ds-ch08-t-012',
    chapter: 8,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Why a Hash Table Cannot Do Longest-Prefix Match',
    prompt: `An IP router stores a set of network prefixes and, for each incoming address, must find the longest stored prefix that matches the start of that address (the most specific route wins). The note says a hash table simply cannot express this, while a trie answers it in one O(L) walk. Explain why the hash table fails and how the trie finds the longest match.`,
    hints: [
      'A hash table can only ask "is this exact key present?"; it has no notion of "longest matching start".',
      'Walk the address down the trie and remember the deepest is_end you pass through.',
    ],
    solution: `A hash table only answers exact membership: "is this exact key present?". To find the longest matching prefix of an address it would have to guess candidate prefix lengths and test each one, and it has no way to know which lengths are stored, so it cannot answer "the longest stored prefix of X" as a single operation. A trie keeps prefixes as paths, so it walks the address bit by bit from the root and, every time it passes through a node flagged is_end, it records that as a matching stored prefix. When the walk ends (or breaks), the deepest is_end it saw is the longest matching prefix, which is the most specific route. This is one O(L) walk in the address length, with no need to enumerate candidate lengths, because the trie structure encodes all stored prefixes at once.`,
    tags: ['trie', 'longest-prefix-match', 'hash-set'],
  },
]

export default problems
