import type { Problem } from '../../types'

const problems: Problem[] = [
  {
    id: 'lx-ch07-t-001',
    chapter: 7,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'What Intrusive Means',
    prompt: `The kernel's linked list is "intrusive": instead of the list allocating little node boxes that point at your data, you embed a struct list_head field directly inside your own struct. A newcomer asks why the kernel does it this way instead of the textbook approach of a node that holds a void *data pointer. Give two concrete advantages of embedding the list_head in the data structure.`,
    hints: ['Think about extra allocations per element.', 'Think about how many lists one object can belong to.'],
    solution: `With an intrusive list, the link fields (prev/next) live inside your object, so adding an element to a list needs no separate node allocation: the memory is already there as part of the struct. That avoids a second kmalloc per insertion (and the matching free), which removes a failure point and reduces fragmentation and cache misses. It also lets one object sit on several lists at once just by embedding several list_head fields, each threading a different list, without duplicating the object. The trade-off is that you must use container_of to get back from a list_head to the enclosing struct, but you gain zero-allocation, multi-membership linking.`,
    tags: ['kernel', 'list', 'intrusive'],
  },
  {
    id: 'lx-ch07-t-002',
    chapter: 7,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'Initializing A List Head',
    prompt: `You declare a list anchor with struct list_head my_list; inside a struct, but forget to initialize it before calling list_add() on it. What is wrong, and what does a correctly initialized empty list_head look like (what do its prev and next point at)?`,
    hints: ['An empty list head points at itself.', 'Look at LIST_HEAD_INIT / INIT_LIST_HEAD.'],
    solution: `A list_head must be initialized so that both next and prev point back at the head itself; that self-referential state is exactly what "empty list" means and is what the list operations assume. If you leave it as uninitialized stack/heap garbage, the first list_add() will dereference junk prev/next pointers and corrupt memory. Initialize it with INIT_LIST_HEAD(&my_list) at runtime, or use LIST_HEAD(my_list) for a static one (or LIST_HEAD_INIT in a struct initializer). After that, list_empty() returns true and insertions work correctly.`,
    tags: ['kernel', 'list', 'init'],
  },
  {
    id: 'lx-ch07-t-003',
    chapter: 7,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'Why container_of Exists',
    prompt: `When you iterate a kernel list you get pointers to embedded struct list_head fields, not to your own objects. Explain in plain terms what container_of(ptr, type, member) computes, and why it is needed for intrusive lists.`,
    hints: ['It does pointer arithmetic with offsetof.', 'It recovers the outer struct from an inner field.'],
    solution: `Because the list threads through a list_head field embedded inside your struct, a list pointer aims at that inner field, not at the start of your object. container_of takes the address of the member, the type of the enclosing struct, and the member name, and subtracts the member's offset (offsetof) within that type to recover a pointer to the whole struct. So from &obj->link it gives you back obj. It is essentially "given the address of a field, find the object that contains it," which is the inverse of the embedding and is the only way to get your data back from an intrusive node.`,
    tags: ['kernel', 'container_of', 'list'],
  },
  {
    id: 'lx-ch07-t-004',
    chapter: 7,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'List Versus Array Trade-Off',
    prompt: `A coworker stores a small, fixed-size table of items that is built once at init and only ever read afterward, but reaches for a struct list_head linked list out of habit. When is a doubly linked list a poor choice compared to a plain array, and what does an array give you that a list does not?`,
    hints: ['Think about random access and cache behavior.', 'Lists shine at cheap insert/delete in the middle.'],
    solution: `Linked lists are good when you frequently insert or delete elements at arbitrary positions in O(1) once you hold the node, and when you do not know the size up front. They are a poor fit for a small, mostly-read, fixed table: each element costs two pointers, traversal chases pointers all over memory (cache-unfriendly), and you cannot index directly, so any "give me element N" is O(n). An array stores elements contiguously, giving O(1) indexed access and excellent cache locality, with no per-element link overhead. For a build-once, read-many table, an array is simpler and faster; reserve lists for collections with dynamic, positional mutation.`,
    tags: ['kernel', 'list', 'array'],
  },
  {
    id: 'lx-ch07-t-005',
    chapter: 7,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'list_for_each_entry Decoded',
    prompt: `You see this loop: list_for_each_entry(p, &mylist, node) { ... } where p is a struct task *p and node is the struct list_head member of struct task. Explain what each of the three arguments is, and what container_of work the macro is quietly doing on every iteration.`,
    hints: ['Argument order is cursor, head, member name.', 'The macro hides container_of for you.'],
    solution: `The three arguments are the loop cursor (p, a pointer to your object type), the address of the list head to walk (&mylist), and the name of the embedded struct list_head member inside your struct (node). On each iteration the macro advances an internal list_head pointer to the next link and then applies container_of(that_link, struct task, node) to set p to the enclosing object. So you write loop bodies in terms of p->field directly and never touch the raw list_head. That is the whole point: list_for_each_entry combines the traversal with the container_of recovery so your code stays readable.`,
    tags: ['kernel', 'list', 'iteration'],
  },
  {
    id: 'lx-ch07-t-006',
    chapter: 7,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Deleting While Iterating',
    prompt: `You walk a list with list_for_each_entry(p, &head, node) and, inside the loop, call list_del(&p->node) and kfree(p) when p matches some condition. Predict what goes wrong, and name the macro you should use instead.`,
    hints: ['How does the loop find the next element?', 'Look at list_for_each_entry_safe.'],
    solution: `list_for_each_entry computes the next cursor from p->node.next at the top of each iteration, but you just called list_del (which poisons or repoints p->node) and kfree(p), so when the loop tries to advance it reads freed/poisoned memory — a use-after-free and likely a crash. The fix is list_for_each_entry_safe(p, n, &head, node), which takes an extra temporary cursor n that holds the next entry before the body runs, so deleting and freeing p is safe. Always use the _safe variant whenever the loop body may remove the current element.`,
    tags: ['kernel', 'list', 'use-after-free'],
  },
  {
    id: 'lx-ch07-t-007',
    chapter: 7,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'list_add Versus list_add_tail',
    prompt: `Two functions add to a list: list_add(new, head) and list_add_tail(new, head). For a list used as a FIFO queue (oldest item dequeued first), which one do producers use to enqueue, and from which end do consumers remove? Explain how the two combine to give FIFO ordering.`,
    hints: ['list_add inserts right after the head (front).', 'list_add_tail inserts right before the head (back).'],
    solution: `list_add inserts the new node immediately after the head, i.e. at the front, while list_add_tail inserts it immediately before the head, i.e. at the back. For a FIFO you want producers to enqueue at the tail with list_add_tail so newer items pile up at the back, and consumers to dequeue from the front (the entry right after the head, e.g. via list_first_entry then list_del). That way the element that has waited longest is always nearest the front and comes out first. Using list_add to enqueue would instead give LIFO/stack ordering.`,
    tags: ['kernel', 'list', 'fifo'],
  },
  {
    id: 'lx-ch07-t-008',
    chapter: 7,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'list_move Instead Of del Plus add',
    prompt: `You want to take an element off list A and put it onto list B. You write list_del(&p->node); list_add_tail(&p->node, &listB);. A reviewer says "just use list_move_tail." What does list_move_tail do, and is there any correctness difference between the two approaches?`,
    hints: ['list_move is del followed by add.', 'Think about re-initializing the node.'],
    solution: `list_move_tail(&p->node, &listB) is exactly a list_del followed by a list_add_tail onto listB, done as one operation, so functionally your two-line version is equivalent. The advantage of the helper is that it is clearer, atomic in intent, and avoids the trap of using the plain list_del (which poisons next/prev) and then reading them — list_move re-links cleanly. The one real bug to avoid is calling list_del and then list_add_tail with a stale or wrong node pointer in between; list_move removes that window. So prefer list_move/list_move_tail for moving between lists.`,
    tags: ['kernel', 'list', 'list_move'],
  },
  {
    id: 'lx-ch07-t-009',
    chapter: 7,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'list_del Poison Values',
    prompt: `After list_del(&p->node), if your code accidentally tries to walk p->node.next, the kernel often crashes with an address like LIST_POISON1. Why does list_del set next and prev to poison values instead of leaving them or zeroing them, and what does that buy you during debugging?`,
    hints: ['Poison is a deliberately invalid address.', 'It turns silent bugs into loud crashes.'],
    solution: `list_del sets the deleted node's next and prev to special poison constants (LIST_POISON1/LIST_POISON2) that are deliberately invalid addresses in kernel space. The point is fail-fast debugging: if some code uses the node after deletion (a use-after-delete or double-delete), dereferencing the poison faults immediately at a recognizable address, instead of silently following a stale-but-valid pointer into corrupted state. Zeroing would risk being mistaken for a legitimately empty/NULL link, hiding the bug. So poisoning converts an invisible logic error into an obvious, traceable crash. If you intend to re-add the node, use list_del_init, which leaves it as a valid empty node instead.`,
    tags: ['kernel', 'list', 'debugging'],
  },
  {
    id: 'lx-ch07-t-010',
    chapter: 7,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Why hlist For Hash Buckets',
    prompt: `Kernel hash tables use struct hlist_head for buckets and struct hlist_node for elements, rather than an array of regular struct list_head. Given that a hash table may have thousands of buckets, explain the memory motivation for hlist and how its head differs from a normal list_head.`,
    hints: ['A list_head is two pointers; an hlist_head is one.', 'Multiply by the number of buckets.'],
    solution: `A struct list_head is two pointers (next and prev), so an array of thousands of such heads wastes one pointer per bucket on a back-pointer that an unordered hash bucket does not need. struct hlist_head is a single pointer (just first), halving the per-bucket cost across the whole table — significant when buckets number in the thousands. The list elements use struct hlist_node, which keeps next plus a pointer-to-pointer pprev so that O(1) removal still works without a full doubly linked head. So hlist trades the symmetric two-pointer head for a one-pointer head, optimizing the common many-buckets hash-table layout.`,
    tags: ['kernel', 'hlist', 'hashtable'],
  },
  {
    id: 'lx-ch07-t-011',
    chapter: 7,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'kfifo Is Single Producer Single Consumer Free',
    prompt: `A driver uses a struct kfifo with exactly one thread writing (kfifo_in) and one thread reading (kfifo_out). A reviewer notes you can do this without taking a lock. Explain why one-producer/one-consumer kfifo access can be lock-free, and what changes the moment you add a second producer.`,
    hints: ['One index is owned by the writer, one by the reader.', 'Multiple writers race on the same index.'],
    solution: `A kfifo is a ring buffer with separate in (write) and out (read) indices. With a single producer, only the writer ever modifies the in index, and with a single consumer only the reader ever modifies out; each side reads the other's index but never writes it, so with appropriate memory barriers (which kfifo provides) there is no shared-mutable-state race, and no lock is needed. The instant you add a second producer, both writers update the in index and reserve buffer space concurrently, which is a genuine data race; now you must serialize the producers with a lock (or use the locked kfifo variants). The lock-free property is specifically the single-producer/single-consumer case.`,
    tags: ['kernel', 'kfifo', 'concurrency'],
  },
  {
    id: 'lx-ch07-t-012',
    chapter: 7,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Bitmap For Allocation State',
    prompt: `You manage 1024 hardware DMA channels and need to track which are free or in use. A colleague suggests an array of 1024 bools; you suggest a bitmap with DECLARE_BITMAP(channels, 1024) and set_bit/clear_bit/test_bit. Give two reasons the bitmap is the better representation here.`,
    hints: ['One bit versus one byte per channel.', 'find_first_zero_bit scans fast.'],
    solution: `A bitmap packs each channel's state into a single bit, so 1024 channels fit in 128 bytes (1024 bits) instead of 1024 bytes for a bool array — eight times less memory and far better cache density. The bitmap API also gives you fast whole-word scans: find_first_zero_bit/find_next_zero_bit locate a free channel in O(words) by testing 32 or 64 bits at a time, instead of looping byte-by-byte over bools. set_bit/clear_bit/test_bit additionally provide atomic, well-defined operations on individual bits, which matters if multiple CPUs claim channels. So the bitmap wins on memory, scan speed, and atomicity.`,
    tags: ['kernel', 'bitmap', 'allocation'],
  },
  {
    id: 'lx-ch07-t-013',
    chapter: 7,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Why idr Beats An Array Of Pointers',
    prompt: `A subsystem hands out small integer IDs to userspace and must map an ID back to its kernel object. A new contributor proposes a fixed global array struct foo *table[65536] indexed by ID. Explain two problems with that and why idr (or ida) is the kernel's answer.`,
    hints: ['IDs can be sparse; the array reserves all slots.', 'idr grows on demand and allocates IDs for you.'],
    solution: `A fixed array reserves memory for every possible ID even when only a handful are live, so a 65536-entry pointer table wastes hundreds of kilobytes for, say, ten open objects, and it hard-caps the ID range at compile time. It also makes you invent your own free-slot search and reuse policy. idr is a radix-tree-based map from integer ID to pointer that grows on demand, so it is memory-proportional to the number of live IDs, supports a large/sparse ID space, and idr_alloc both picks a free ID and stores the pointer in one call (idr_find looks it up, idr_remove frees it). When you only need the IDs and not a stored pointer, ida does just the ID allocation. So idr/ida give scalable, sparse, self-managing ID maps.`,
    tags: ['kernel', 'idr', 'ida'],
  },
  {
    id: 'lx-ch07-t-014',
    chapter: 7,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'container_of On The Wrong Member',
    prompt: `A struct has two embedded list_heads: struct list_head active and struct list_head dirty. A pointer p threads the dirty list, but a bug calls container_of(p, struct buf, active). The result does not crash immediately but reads garbage fields. Explain precisely why, in terms of offsets.`,
    hints: ['container_of subtracts the named member offset.', 'The two members live at different offsets.'],
    solution: `container_of recovers the object by subtracting the offset of the named member from the pointer: object = p - offsetof(struct buf, member). Here p actually points at the dirty member, so the correct recovery subtracts offsetof(...,dirty), but the code subtracts offsetof(...,active). Since active and dirty sit at different offsets in the struct, the computed "object" address is wrong by exactly (offsetof dirty - offsetof active) bytes. That bogus pointer still lies in valid memory near the real object, so dereferencing it does not fault; it just lands on the wrong fields and returns garbage. The member name passed to container_of must match the list the pointer came from.`,
    tags: ['kernel', 'container_of', 'bug'],
  },
  {
    id: 'lx-ch07-t-015',
    chapter: 7,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'list_empty Race Without A Lock',
    prompt: `Two CPUs share a list protected by a spinlock. One CPU does if (!list_empty(&q)) { /* later */ p = list_first_entry(&q, ...); list_del(&p->node); } but takes the lock only around the list_del, not around the list_empty check. Describe the race and the correct locking discipline.`,
    hints: ['The check and the use must be one critical section.', 'Another CPU can empty the list in between.'],
    solution: `Reading list_empty and then acting on the result are two separate steps; if the lock is not held across both, another CPU can pop the last element between your check and your list_first_entry/list_del. Then list_first_entry returns the head sentinel cast as an object (garbage) and list_del corrupts the now-empty list — a classic time-of-check-to-time-of-use race. The correct discipline is to take the spinlock before list_empty and hold it through list_first_entry and list_del, releasing only after the element is fully removed. The check and the mutation must live inside one critical section so the list cannot change underneath you.`,
    tags: ['kernel', 'list', 'locking'],
  },
  {
    id: 'lx-ch07-t-016',
    chapter: 7,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'rbtree Insert Needs Manual Linking',
    prompt: `Unlike list_add, the rbtree API makes you write the search-and-link loop yourself before calling rb_insert_color. Explain why the kernel cannot offer a single generic rb_insert(root, node) the way it offers list_add, and what your insert loop must do.`,
    hints: ['The tree has no idea how to compare your keys.', 'rb_link_node then rb_insert_color.'],
    solution: `A linked list has a fixed insertion position (front or back), so list_add needs no knowledge of your data. A balanced binary search tree must place each node according to key order, but struct rb_node carries no key and the kernel has no generic comparator for your type. So you write the loop: walk from the root comparing your key against each node's key, following left or right, until you find the empty child slot, remembering the parent and the address of that child link. Then you call rb_link_node(new, parent, link) to attach the raw node and rb_insert_color(new, root) to recolor/rotate for balance. The kernel supplies the balancing; you supply the ordering.`,
    tags: ['kernel', 'rbtree', 'insert'],
  },
  {
    id: 'lx-ch07-t-017',
    chapter: 7,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'When rbtree Beats A Sorted List',
    prompt: `You keep a large, frequently-updated set of items that must always be queryable by key in sorted order and looked up by key. Compare a sorted doubly linked list with an rbtree for this workload, focusing on lookup and insertion cost, and say which the kernel uses for things like the CFS runqueue or VMA trees.`,
    hints: ['Sorted-list lookup/insert is O(n).', 'rbtree is O(log n) for both.'],
    solution: `A sorted linked list keeps order but offers only linear traversal: finding a key, or finding the insertion point to keep it sorted, is O(n) because you must walk node by node. An rbtree is a self-balancing binary search tree, so lookup, insertion, and deletion are all O(log n), and in-order traversal still yields sorted output via rb_next. For a large, churn-heavy keyed set this is a decisive win, which is why the kernel uses rbtrees for exactly such cases — the CFS scheduler's runqueue keyed by vruntime, and a process's VMAs keyed by address. Use a sorted list only for small sets where O(n) is cheap and code simplicity matters.`,
    tags: ['kernel', 'rbtree', 'comparison'],
  },
  {
    id: 'lx-ch07-t-018',
    chapter: 7,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'kfifo In Interrupt Context',
    prompt: `An interrupt handler produces data with kfifo_in into a single-producer/single-consumer kfifo, and a workqueue consumes it with kfifo_out. Why is kfifo a particularly good fit for handing data from hardirq context to process context, given the rules about what you may do in an interrupt handler?`,
    hints: ['No sleeping or allocation allowed in hardirq.', 'kfifo_in does not allocate and is bounded-time.'],
    solution: `Code running in hardirq context must not sleep and should not block or allocate with sleeping GFP flags; it must run quickly and finish. kfifo_in on a preallocated kfifo does none of those things: the ring buffer is allocated once up front, so the producer just copies bytes into reserved space and bumps an index in bounded time, never sleeping or allocating. Because the single-producer/single-consumer case is lock-free, the IRQ side does not even need a lock that the consumer also holds. The slow, sleepable work (further processing) is deferred to the workqueue consumer in process context. So kfifo cleanly bridges the atomic producer and the sleepable consumer.`,
    tags: ['kernel', 'kfifo', 'interrupt'],
  },
  {
    id: 'lx-ch07-t-019',
    chapter: 7,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'set_bit Versus __set_bit',
    prompt: `The kernel provides both set_bit() and __set_bit() (and similarly clear_bit/__clear_bit). They do the same logical thing to a bitmap word. Explain the difference and give a rule for choosing between them, including the performance angle.`,
    hints: ['One is atomic, one is not.', 'Non-atomic needs external serialization.'],
    solution: `set_bit() is atomic: it performs a locked, indivisible read-modify-write so that concurrent set_bit/clear_bit on the same word from other CPUs do not lose updates. __set_bit() is the non-atomic variant — a plain read-modify-write that is faster (no locked instruction, no bus/cacheline locking) but is safe only if no one else can touch that word concurrently, i.e. you already hold a lock that serializes access or the bitmap is CPU-local. The rule: use the atomic set_bit/clear_bit when other contexts may concurrently modify the same word; use the __ versions only when you have established exclusive access, to avoid paying for an atomic you do not need. Choosing the non-atomic version without that guarantee is a lost-update bug.`,
    tags: ['kernel', 'bitmap', 'atomic'],
  },
  {
    id: 'lx-ch07-t-020',
    chapter: 7,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'idr Allocation And GFP Flags',
    prompt: `You call idr_alloc(&my_idr, ptr, 0, 0, GFP_KERNEL) while holding a spinlock, and occasionally see "scheduling while atomic" warnings. What is the connection between GFP_KERNEL, the spinlock, and idr's internal growth, and how do you fix it?`,
    hints: ['idr may allocate radix-tree nodes internally.', 'GFP_KERNEL can sleep; spinlocks forbid sleeping.'],
    solution: `idr_alloc may need to grow its internal radix tree, and to do so it allocates memory using the GFP flags you pass. GFP_KERNEL permits the allocator to sleep (reclaim, wait for pages). But you are holding a spinlock, which creates an atomic, non-sleepable context, so if the allocation blocks you get "scheduling while atomic" or worse. The fixes are to either not hold the spinlock across idr_alloc, or to use a non-sleeping flag like GFP_ATOMIC (with its higher failure risk), or to use the idr_preload()/idr_preload_end() pattern that pre-reserves memory before you take the lock so the in-lock idr_alloc cannot need to sleep. The root rule: never use a sleeping GFP flag while holding a spinlock.`,
    tags: ['kernel', 'idr', 'gfp'],
  },
  {
    id: 'lx-ch07-t-021',
    chapter: 7,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Hash Bucket Sizing',
    prompt: `Two designs for a kernel hash table: (A) a small number of buckets, each a long hlist chain; (B) a large number of buckets, each a short chain. Discuss the trade-off between lookup time and memory, and what a degenerate hash function does to design A.`,
    hints: ['Lookup cost is roughly chain length.', 'Too few buckets means long chains.'],
    solution: `Hash-table lookup cost is dominated by the average chain length you must walk after hashing to a bucket. Design A (few buckets) saves memory on the bucket array but produces longer chains, so lookups degrade toward O(n/buckets); Design B (many buckets) spends more memory on hlist_head pointers but keeps chains short, giving near-O(1) lookups. The right choice balances the expected element count against acceptable chain length and memory budget. A degenerate hash (e.g. one that maps most keys to the same bucket) is catastrophic for A: even with "enough" buckets nominally, all elements pile into one chain and lookups become O(n), so a good, well-distributed hash function matters as much as bucket count.`,
    tags: ['kernel', 'hlist', 'hashtable'],
  },
  {
    id: 'lx-ch07-t-022',
    chapter: 7,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'list_first_entry On An Empty List',
    prompt: `A consumer does p = list_first_entry(&queue, struct job, node); list_del(&p->node); without first checking list_empty. The queue is sometimes empty. Predict the failure mode and explain what list_first_entry returns on an empty list. What is the safe variant?`,
    hints: ['An empty list head points at itself.', 'container_of of the head is not a real object.'],
    solution: `list_first_entry assumes the list is non-empty: it takes head->next and applies container_of to get an object. On an empty list, head->next is the head itself, so list_first_entry returns container_of(&head, struct job, node) — a bogus "object" pointer computed from the head sentinel, not a real job. Dereferencing it reads garbage fields, and list_del(&p->node) then mangles the list head, corrupting the queue. The safe approaches are to guard with if (!list_empty(&queue)) first, or to use list_first_entry_or_null(&queue, struct job, node), which returns NULL when the list is empty so you can check before dereferencing.`,
    tags: ['kernel', 'list', 'empty'],
  },
  {
    id: 'lx-ch07-t-023',
    chapter: 7,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Why hlist_node Uses pprev',
    prompt: `struct list_head stores prev as a direct pointer to the previous node, but struct hlist_node stores pprev as a "pointer to the previous node's next pointer" (struct hlist_node **pprev). Explain why this indirection is necessary for O(1) deletion given that the bucket head is only one pointer wide.`,
    hints: ['The first element must update the head, not a node.', 'pprev can point at hlist_head->first uniformly.'],
    solution: `For O(1) deletion you must, given only the node, fix up whatever pointer was pointing at it. In a normal doubly linked list the head is a full list_head, so the previous "thing" is always a node and a direct prev pointer works. In an hlist the head is just a single first pointer, not a node, so the predecessor of the first element is the bucket head itself, which has no next field to mirror a node's. By storing pprev as the address of the previous element's next pointer (or of the head's first pointer for the first element), deletion uniformly does *(node->pprev) = node->next and updates node->next's pprev — no special case for "am I the first element?". The double indirection is exactly what lets a one-pointer head and node-to-node links be unlinked with the same two assignments.`,
    tags: ['kernel', 'hlist', 'pprev'],
  },
  {
    id: 'lx-ch07-t-024',
    chapter: 7,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'rbtree Rebalance After Key Change',
    prompt: `An object sits in an rbtree keyed by a timestamp. Code updates the object's timestamp field in place but leaves the rb_node where it is. Later searches sometimes fail to find objects that are present. Explain the invariant that was broken and the correct way to change a key.`,
    hints: ['The tree assumes keys never move while linked.', 'Search relies on the BST ordering invariant.'],
    solution: `An rbtree is a binary search tree: every node's position encodes that its key is greater than everything in its left subtree and less than everything in its right subtree. Searches navigate left/right by comparing the target key against node keys, trusting that ordering. Mutating the key in place while the node stays put can violate the ordering relative to the node's ancestors and subtrees, so a search takes a wrong branch and walks right past an object that is physically in the tree but now mis-positioned. The correct procedure is to remove the node with rb_erase, update its key, then re-insert it (search for the new slot, rb_link_node, rb_insert_color) so the tree's ordering invariant is restored. Never edit a live key in place.`,
    tags: ['kernel', 'rbtree', 'invariant'],
  },
  {
    id: 'lx-ch07-t-025',
    chapter: 7,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'kfifo Power-Of-Two Masking',
    prompt: `kfifo requires its buffer size to be a power of two and lets its in/out indices grow monotonically (it does not reset them to zero at wrap). Explain how the power-of-two size enables cheap index wrapping with a bitwise AND, and why letting indices run past the buffer size (relying on unsigned overflow) simplifies the empty/full distinction.`,
    hints: ['idx & (size - 1) replaces idx % size.', 'Used count is in - out; equal means empty.'],
    solution: `When size is a power of two, the buffer position for an index is index & (size - 1), which is identical to index % size but is a single cheap AND instead of a division — important on hot data paths. Because the in and out indices are free-running unsigned counters (never reset on wrap) the number of bytes currently stored is simply in - out, computed in unsigned arithmetic that wraps correctly even when the raw indices overflow. This makes empty (in == out) and full (in - out == size) trivially distinguishable, which the classic "reset to zero on wrap" scheme cannot do without an extra flag or a wasted slot, since there in == out is ambiguous between empty and full. So power-of-two sizing plus monotonic indices give branch-free wrapping and an unambiguous, lock-free fill computation.`,
    tags: ['kernel', 'kfifo', 'ringbuffer'],
  },
  {
    id: 'lx-ch07-t-026',
    chapter: 7,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'kfifo_in With User Data',
    prompt: `A write() handler copies data from a userspace buffer into a kfifo. A contributor uses kfifo_in(&fifo, user_ptr, len) directly with the raw __user pointer. Explain why this is wrong and how kfifo_from_user differs, including what can go wrong when touching user memory.`,
    hints: ['User pointers must go through copy_from_user.', 'copy_from_user can fault and may sleep.'],
    solution: `kfifo_in expects a valid kernel pointer and does a plain memcpy from it; passing a raw __user pointer is a bug because user addresses must never be dereferenced directly from the kernel — the page may be unmapped, swapped out, or malicious, so a direct access can fault, leak data, or be a security hole. The correct API is kfifo_from_user, which internally uses copy_from_user to safely move bytes from user space, validating access and handling page faults. Note copy_from_user can fault and may sleep while faulting in a page, so this must run in process context (a write() handler qualifies), never in atomic/hardirq context, and you must check its return value for a partial/failed copy. Symmetrically, kfifo_to_user copies out to user space.`,
    tags: ['kernel', 'kfifo', 'copy_from_user'],
  },
  {
    id: 'lx-ch07-t-027',
    chapter: 7,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'idr Lifetime And Lookup Race',
    prompt: `Userspace passes an ID; the kernel does obj = idr_find(&idr, id); then uses obj. Meanwhile another thread may call idr_remove(&idr, id) and kfree(obj). Even if idr_find itself is protected by a lock, describe the lifetime hazard and the general technique used to keep obj valid after the lookup.`,
    hints: ['Removing from the idr is not the same as freeing safely.', 'Take a reference before dropping the lock.'],
    solution: `idr_find only returns the pointer stored for that ID; it does nothing about the object's lifetime. If you drop the lookup lock and then use obj, another thread can idr_remove and kfree it in between, leaving you with a dangling pointer (use-after-free). Locking idr_find alone is not enough because the danger is after the lookup. The standard technique is reference counting: while still holding the lock that protects the idr (so the object cannot be removed/freed yet), take a reference on obj (e.g. via its kref/get function); only then release the lock and use it, dropping your reference when done. The reference keeps the object alive even after it is removed from the idr, and the final put frees it. Lookup-then-get-ref-under-lock is the idiom.`,
    tags: ['kernel', 'idr', 'refcount'],
  },
  {
    id: 'lx-ch07-t-028',
    chapter: 7,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'One Object On Two Lists',
    prompt: `A buffer object must simultaneously be on an LRU list (ordered by recency) and on a per-owner list (all buffers owned by one process). Describe the intrusive-list design that supports this, how you remove the object from just one of the two lists, and a locking hazard when two different locks protect the two lists.`,
    hints: ['Embed two list_head members.', 'Each list_head is independent.'],
    solution: `Embed two separate struct list_head members in the buffer, say lru and owner, and thread each through its own list; because the links are independent fields, the same object lives on both lists at once with no duplication. To remove it from just the LRU you call list_del(&buf->lru) (or list_del_init if you may re-add it), which touches only the LRU links and leaves the owner membership intact, and vice versa. The locking hazard is lock ordering: the LRU list and the owner list are protected by different locks, so any code path that must modify both (e.g. freeing the buffer) acquires both locks and must always take them in the same global order on every path, or two CPUs grabbing them in opposite orders will deadlock. So embed two heads, delete per-list independently, and impose a consistent lock-acquisition order.`,
    tags: ['kernel', 'list', 'locking'],
  },
  {
    id: 'lx-ch07-t-029',
    chapter: 7,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Resizing A Hash Table Live',
    prompt: `A static kernel hash table built with a fixed number of hlist buckets is suffering long chains under unexpectedly high load. Explain why you cannot simply realloc the bucket array bigger in place, and outline what a correct rehash must do to every element.`,
    hints: ['Bucket index depends on table size.', 'Every element may move to a different bucket.'],
    solution: `An element's bucket is chosen by hashing its key modulo the number of buckets, so changing the bucket count changes which bucket nearly every element belongs to. You cannot just grow the array in place because the existing hlist_node links point into the old buckets and the old hash mapping; after resizing, those placements are stale and lookups using the new modulus would miss them. A correct rehash allocates a new, larger bucket array, then walks every element in the old table, recomputes its bucket with the new size, and re-inserts it (hlist_del from the old chain, hlist_add_head into the new bucket). This must be done under the table's lock (or with an RCU/seqlock scheme) so concurrent lookups never see a half-migrated table, after which you free the old array. In practice the kernel offers rhashtable for exactly this resizable, concurrent-safe behavior.`,
    tags: ['kernel', 'hlist', 'rehash'],
  },
  {
    id: 'lx-ch07-t-030',
    chapter: 7,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Choosing The Right Structure',
    prompt: `For each requirement, name the kernel data structure from this topic that fits best and justify in one line: (a) FIFO byte stream from an IRQ to a worker; (b) map a sparse 32-bit integer ID to a pointer; (c) keyed set needing sorted iteration and O(log n) lookup; (d) tracking which of 4096 fixed slots are in use; (e) many short collision chains indexed by a hash.`,
    hints: ['Match each to kfifo, idr, rbtree, bitmap, hlist.', 'Think about access pattern, not familiarity.'],
    solution: `(a) kfifo: a lock-free single-producer/single-consumer ring buffer is the canonical IRQ-to-worker byte queue, no allocation or sleeping on the producer side. (b) idr (or ida if you only need the IDs): it maps a sparse, possibly large integer ID to a pointer with memory proportional to live IDs. (c) rbtree: a self-balancing BST gives O(log n) insert/lookup and in-order (sorted) traversal via rb_next. (d) a bitmap (DECLARE_BITMAP with set_bit/clear_bit/find_first_zero_bit): one bit per slot, fast word-at-a-time free-slot scans. (e) an hlist-based hash table: single-pointer hlist_head buckets keep the bucket array compact while short hlist_node chains handle collisions. Matching the structure to the access pattern is the whole skill of this topic.`,
    tags: ['kernel', 'design', 'data-structures'],
  },
]

export default problems
