import type { Problem } from '../../types'

const problems: Problem[] = [
  {
    id: 'rs-ch20-t-001',
    chapter: 20,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'What TcpListener::bind Does',
    prompt: `Your web server starts with TcpListener::bind("127.0.0.1:7878"). In plain words, what does this call accomplish, and what does the address "127.0.0.1:7878" mean? Why might bind return a Result rather than a plain TcpListener?`,
    hints: [
      'bind ties your program to an address and a port.',
      'Binding can fail for real-world reasons.',
    ],
    solution: `TcpListener::bind reserves a network address and port on your machine so that your program can listen for incoming TCP connections there. "127.0.0.1" is the loopback address meaning "this same computer," and "7878" is the port number that clients must connect to. It returns a Result because binding can fail at runtime, for example if another program is already using that port or if you lack permission to bind it. You handle that Result, usually with unwrap or proper error handling, before you can start accepting connections.`,
    tags: ['tcplistener', 'bind', 'basics'],
  },
  {
    id: 'rs-ch20-t-002',
    chapter: 20,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'What incoming Yields',
    prompt: `After binding, the server loops with for stream in listener.incoming(). What does incoming() give you on each iteration, and why is each item wrapped in a Result?`,
    hints: [
      'Each loop turn corresponds to one client trying to connect.',
      'Opening a connection is an operation that can fail.',
    ],
    solution: `listener.incoming() returns an iterator that produces a value for each connection attempt a client makes to your bound address. Each item is a Result<TcpStream, std::io::Error>, where the Ok value is a TcpStream representing the open connection between the client and your server. It is a Result because accepting a connection is an operating-system operation that can fail, for example if too many connections are open at once. You typically unwrap or match on each item before reading from or writing to the stream.`,
    tags: ['incoming', 'tcpstream', 'basics'],
  },
  {
    id: 'rs-ch20-t-003',
    chapter: 20,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'Anatomy of an HTTP Response',
    prompt: `A minimal HTTP response your server writes looks like "HTTP/1.1 200 OK\\r\\n\\r\\n". Name the three logical parts of an HTTP response and say which of them this minimal response actually contains.`,
    hints: [
      'Think status line, headers, body.',
      'The blank line separates headers from the body.',
    ],
    solution: `An HTTP response has three logical parts: a status line (such as "HTTP/1.1 200 OK"), zero or more headers, and an optional body. The blank line, formed by the second carriage-return-newline, marks the boundary between the headers and the body. The minimal response shown contains only a status line; it has no headers and an empty body. A real response usually adds at least a Content-Length header and some HTML in the body.`,
    tags: ['http', 'response', 'basics'],
  },
  {
    id: 'rs-ch20-t-004',
    chapter: 20,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'Why a Thread Pool Instead of Unlimited Threads',
    prompt: `One simple way to make a server handle many requests at once is to spawn a brand new thread for every connection. The book instead builds a thread pool with a fixed number of threads. Give two reasons a thread pool is preferable to spawning an unbounded number of threads.`,
    hints: [
      'Threads are not free; each one uses memory.',
      'Think about what a flood of requests could do.',
    ],
    solution: `Spawning one thread per connection means an attacker or a traffic spike could create an unlimited number of threads, exhausting memory and possibly crashing the machine. A thread pool caps the number of threads at a fixed size, so resource usage stays bounded and predictable no matter how many requests arrive. The extra requests simply wait in a queue until a worker is free, instead of overwhelming the system. Reusing a small set of long-lived threads also avoids the repeated cost of creating and tearing down a thread for every single request.`,
    tags: ['thread-pool', 'design', 'basics'],
  },
  {
    id: 'rs-ch20-t-005',
    chapter: 20,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Why a Buffered Reader for the Request',
    prompt: `The server reads the incoming request with BufReader::new(&mut stream) and then reads lines from it. Why wrap the TcpStream in a BufReader instead of reading raw bytes directly from the stream? What convenience does it give you?`,
    hints: [
      'Reading line by line off a raw stream is awkward.',
      'BufReader implements a handy trait.',
    ],
    solution: `A raw TcpStream gives you bytes, and reading a request a line at a time directly from it would mean repeatedly issuing small reads and reassembling lines yourself. BufReader wraps the stream, reads a chunk of bytes into an internal buffer, and provides the BufRead trait, which offers the lines() method. That lets you iterate over the request as a sequence of String lines, each with the trailing newline stripped. This makes parsing the request line and headers much simpler and avoids many tiny system calls.`,
    tags: ['bufreader', 'reading', 'request'],
  },
  {
    id: 'rs-ch20-t-006',
    chapter: 20,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Reading Just the Request Line',
    prompt: `To decide what to serve, the server only needs the first line of the HTTP request, the request line. Using BufReader's lines() iterator, describe how you would obtain just that first line, and explain why each item from lines() is a Result.`,
    hints: [
      'lines() is an iterator; you want its first element.',
      'Reading from a stream can fail at any line.',
    ],
    solution: `You call buf_reader.lines().next() to pull the first item from the iterator, which corresponds to the request line such as "GET / HTTP/1.1". Because next() returns an Option, you unwrap it to get past the "is there a line at all" question, and because each line read can fail, the inner value is a Result<String, Error> that you also unwrap. So a common pattern is buf_reader.lines().next().unwrap().unwrap(). Each item is a Result because reading from a network stream is fallible: the connection could drop or produce invalid UTF-8 mid-read.`,
    tags: ['request-line', 'lines', 'parsing'],
  },
  {
    id: 'rs-ch20-t-007',
    chapter: 20,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Matching on the Request Line',
    prompt: `The server routes by comparing the request line. The book uses a match where the arm pattern is the string literal "GET / HTTP/1.1". Why does the code match against the whole request line string rather than only against the path "/"? What does this imply about which requests hit that arm?`,
    hints: [
      'The request line contains method, path, and version together.',
      'Matching the whole string is strict.',
    ],
    solution: `The request line is a single string that combines the method, the path, and the HTTP version, for example "GET / HTTP/1.1". By matching the whole literal, the arm only fires for a GET request to "/" using HTTP/1.1 exactly. This is a simple teaching approach: it does not parse out the path separately, so a POST to "/" or a GET to "/" over a different HTTP version would not match that arm and would fall through to the default. A more robust server would split the line and route on just the method and path, but for the book's purposes matching the full string is enough.`,
    tags: ['routing', 'match', 'request-line'],
  },
  {
    id: 'rs-ch20-t-008',
    chapter: 20,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Why fs::read_to_string Returns a Result',
    prompt: `To serve a page, the server calls fs::read_to_string("hello.html") and uses the contents as the response body. Why does this function return a Result, and what is a realistic failure case you must consider when the server runs?`,
    hints: [
      'Reading a file touches the file system.',
      'What if the file is missing or unreadable?',
    ],
    solution: `fs::read_to_string returns a Result because reading a file is an I/O operation that can fail for many reasons beyond the program's control. A realistic failure is that the file does not exist, for example if hello.html was deleted or the server was started from the wrong working directory. Other causes include lacking permission to read the file or the file containing invalid UTF-8. The Result forces you to acknowledge these possibilities, and in the book's code it is handled with unwrap, which would panic the worker thread if the file could not be read.`,
    tags: ['fs', 'result', 'serving-files'],
  },
  {
    id: 'rs-ch20-t-009',
    chapter: 20,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Why Content-Length Matters',
    prompt: `When the server builds a full response, it computes contents.len() and includes a header like "Content-Length: 42". What does this header tell the client, and what might go wrong if you sent the body but forgot the Content-Length header?`,
    hints: [
      'The client needs to know when the body ends.',
      'Bytes on a stream do not announce their own end.',
    ],
    solution: `Content-Length tells the client exactly how many bytes of body to expect, so the client knows when it has read the whole response and can stop. Without it, the client cannot tell where the body ends on the byte stream, so it may hang waiting for more data, or render the page incompletely, depending on the client. The value must equal the byte length of the body you actually send. The book computes it from contents.len() so the header always matches the body it pairs with.`,
    tags: ['headers', 'content-length', 'response'],
  },
  {
    id: 'rs-ch20-t-010',
    chapter: 20,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Writing the Response as Bytes',
    prompt: `The server sends the response with stream.write_all(response.as_bytes()). Why must the response String be converted to bytes with as_bytes() before writing, and why is write_all preferred over a single write call?`,
    hints: [
      'A TcpStream deals in raw bytes.',
      'A plain write might not send everything at once.',
    ],
    solution: `A TcpStream works in terms of raw bytes, so the textual response must be turned into a byte slice with as_bytes() before it can be written. write_all is preferred over write because a single write call is allowed to send only part of the buffer and return how many bytes it actually wrote, leaving you to loop and send the rest. write_all does that looping for you, guaranteeing that the entire slice is written before it returns (or it returns an error). This avoids accidentally sending a truncated response.`,
    tags: ['write', 'tcpstream', 'response'],
  },
  {
    id: 'rs-ch20-t-011',
    chapter: 20,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Returning 404 for Unknown Paths',
    prompt: `The default arm of the routing match serves a 404 response with a status line "HTTP/1.1 404 NOT FOUND" and a small 404.html page as the body. Explain the role of the status code 404 here, and why the server can still send an HTML body alongside a 404 status.`,
    hints: [
      'The status code is metadata about the outcome.',
      'A body and a status code are independent parts.',
    ],
    solution: `The status code 404 in the status line tells the client that the requested resource was not found, which is machine-readable metadata separate from any page content. Because the status line, headers, and body are independent parts of the response, you are free to attach a human-friendly HTML body that explains the error while still signalling 404 in the status line. So the server uses the same response-building code, just swapping the status line to the 404 one and the body to 404.html. The client sees "not found" and also gets a nice page to display.`,
    tags: ['404', 'status-code', 'routing'],
  },
  {
    id: 'rs-ch20-t-012',
    chapter: 20,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Single-Threaded Server Blocks',
    prompt: `In the single-threaded version, one route deliberately calls thread::sleep for five seconds to simulate slow work. While that request is being handled, a second browser tab requesting the fast page hangs until the slow one finishes. Explain why the single-threaded design causes this.`,
    hints: [
      'There is only one thread processing the loop.',
      'Connections are handled strictly one at a time.',
    ],
    solution: `In the single-threaded server, the main loop handles one connection completely before moving to the next item from incoming(). When a request triggers the five-second sleep, the single thread is stuck inside that handler and cannot pull the next connection off the listener. So the second request waits in the operating system's connection queue, unable to be processed until the slow handler returns. The fix is concurrency: process connections on separate threads (a thread pool) so a slow request no longer blocks fast ones.`,
    tags: ['single-threaded', 'blocking', 'concurrency'],
  },
  {
    id: 'rs-ch20-t-013',
    chapter: 20,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'The execute Method Signature',
    prompt: `The ThreadPool exposes pool.execute(|| { handle_connection(stream); }). The execute method takes a closure. Before writing the body, what should the signature of execute look like in terms of its generic type and the trait bound on the closure? You do not need the implementation, just the signature shape and why.`,
    hints: [
      'execute receives some closure to run later.',
      'thread::spawn has bounds you can imitate.',
    ],
    solution: `execute should be generic over a type F that is the closure, with a signature like fn execute<F>(&self, f: F) where F: FnOnce() + Send + 'static. It takes &self because running a job does not consume or mutate the pool's public interface, and it takes the closure by value as f. The bounds are modeled on thread::spawn: FnOnce because the job runs once, Send so it can be moved to another thread, and 'static so it does not borrow anything that could disappear. These bounds let the pool ship the closure to a worker thread safely.`,
    tags: ['thread-pool', 'execute', 'closures'],
  },
  {
    id: 'rs-ch20-t-014',
    chapter: 20,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Why FnOnce for a Job',
    prompt: `The job closures stored by the thread pool are bounded by FnOnce, not Fn or FnMut. Explain why FnOnce is the correct choice for a job that a worker runs, and what would be lost if you required Fn instead.`,
    hints: [
      'How many times does a single job ever run?',
      'A request handler may consume the values it captured.',
    ],
    solution: `Each job is meant to run exactly one time on a worker, so the most permissive bound that fits is FnOnce, which only promises the closure can be called at least once. FnOnce also allows the closure to consume the values it captured, for instance taking ownership of the TcpStream and moving it into handle_connection. If you required Fn, you would only accept closures that can be called many times and that merely borrow their captures, which would reject handlers that move or consume captured data. Since the pool never needs to call a job more than once, FnOnce is both correct and the least restrictive choice.`,
    tags: ['fnonce', 'closures', 'trait-bounds'],
  },
  {
    id: 'rs-ch20-t-015',
    chapter: 20,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Why the Job Must Be Boxed',
    prompt: `The pool defines a type alias roughly like type Job = Box<dyn FnOnce() + Send + 'static> and sends Jobs through a channel. Why can't the channel carry a bare "dyn FnOnce() + Send + 'static" value directly, and what does boxing fix?`,
    hints: [
      'Different closures have different sizes.',
      'The channel needs items of one known size.',
    ],
    solution: `Every closure is a distinct anonymous type, and different closures capture different things, so they have different sizes at compile time. A bare dyn FnOnce() is an unsized trait object: the compiler does not know how big it is, and values you store in a channel or a variable must have a known size. Boxing puts the closure on the heap and gives you a Box, which is a fixed-size pointer you can send through the channel. So Box<dyn FnOnce() + Send + 'static> turns many differently sized closures into uniformly sized, sendable Job values.`,
    tags: ['boxing', 'trait-objects', 'sized'],
  },
  {
    id: 'rs-ch20-t-016',
    chapter: 20,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Why Send and Static on the Job',
    prompt: `The Job type carries the bounds Send and 'static in addition to FnOnce. Separately explain what each of those two bounds guarantees and why a worker thread running the job needs both.`,
    hints: [
      'One bound is about crossing thread boundaries.',
      'The other is about how long captured references live.',
    ],
    solution: `Send means the value, including everything the closure captured, can be safely transferred from the thread that created it to the worker thread that will run it; without Send the compiler would not let you move the job across the channel into another thread. The 'static bound means the closure does not hold any borrowed reference that could become invalid; it either owns its captured data or only borrows things that live for the whole program. A worker needs Send because the job is created on one thread and executed on another, and it needs 'static because the pool cannot know how long any borrowed data would stay valid once the job is running independently. Together they make it sound to run an arbitrary captured closure on a different, long-lived thread.`,
    tags: ['send', 'static', 'trait-bounds'],
  },
  {
    id: 'rs-ch20-t-017',
    chapter: 20,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Receiver Shared with Arc and Mutex',
    prompt: `The pool has one channel receiver but several Worker threads that all need to pull jobs from it. The book wraps the receiver in Arc<Mutex<Receiver<Job>>>. Explain why both Arc and Mutex are needed here, and what role each one plays.`,
    hints: [
      'Many threads need to own the same receiver.',
      'But only one thread should take a job at a time.',
    ],
    solution: `There is only one receiving end of the channel, yet every Worker thread must be able to use it, so the receiver needs shared ownership across threads, which is exactly what Arc provides: multiple threads can hold a clone of the same Arc pointing at one receiver. But a Receiver cannot be used by several threads simultaneously, and you also want each job to go to exactly one worker, so the Mutex ensures that only one worker locks the receiver and calls recv() at a time. Arc gives shared ownership; Mutex gives exclusive, one-at-a-time access. Combined as Arc<Mutex<Receiver<Job>>>, the workers safely take turns pulling jobs off the single channel.`,
    tags: ['arc', 'mutex', 'shared-state'],
  },
  {
    id: 'rs-ch20-t-018',
    chapter: 20,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'loop with lock then recv',
    prompt: `Inside each Worker the book writes a loop that does, roughly: let job = receiver.lock().unwrap().recv().unwrap(); then runs the job. There is a subtle reason this single-statement form releases the lock before running the job. Explain why, and why that timing matters for throughput.`,
    hints: [
      'A MutexGuard is a temporary in this one statement.',
      'When does that temporary get dropped?',
    ],
    solution: `In that single let statement, the MutexGuard returned by lock() is a temporary value that lives only for the duration of the statement, so once recv() produces a job and the statement ends, the guard is dropped and the lock is released. That means the worker holds the lock only long enough to receive a job, not while it actually runs the (possibly slow) job. If instead the lock were held for the whole time the job ran, every other worker would be blocked from taking new jobs, serializing all the work and destroying the benefit of having a pool. Releasing the lock immediately lets other workers grab the next job concurrently while this one is busy.`,
    tags: ['mutex-guard', 'lock', 'throughput'],
  },
  {
    id: 'rs-ch20-t-019',
    chapter: 20,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'while let Holds the Lock Too Long',
    prompt: `Suppose a learner rewrites the worker loop as while let Ok(job) = receiver.lock().unwrap().recv() { job(); } expecting it to behave like the book's version. The book warns this is subtly wrong. Explain how the lifetime of the lock differs between the while let form and the let-statement form.`,
    hints: [
      'A while let binds temporaries for the whole loop body.',
      'Compare when the MutexGuard is dropped.',
    ],
    solution: `With the let-statement form, the MutexGuard temporary is dropped at the end of the statement, so the lock is free before job() runs. With while let, any temporaries created in the condition expression, including the MutexGuard, live for the entire body of the loop, so the lock stays held the whole time job() executes. That means a worker keeps the receiver locked while running its job, blocking all other workers from taking new jobs and effectively serializing the pool. The two forms look almost identical but differ exactly in when the guard is dropped, which is why the book uses the plain let form.`,
    tags: ['while-let', 'lock-scope', 'pitfall'],
  },
  {
    id: 'rs-ch20-t-020',
    chapter: 20,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Why assert the Pool Size',
    prompt: `ThreadPool::new(size) begins with assert!(size > 0). Why does the book choose to panic on a size of zero rather than return a Result, and what is the design tradeoff being made by using new versus a fallible constructor like build?`,
    hints: [
      'A zero-size pool can never run anything.',
      'Panicking versus returning Result is a contract choice.',
    ],
    solution: `A pool with zero threads can never run any job, so a size of zero is a programmer error, not a recoverable runtime condition, and the book treats it that way by asserting and panicking. Using new with an assert encodes the contract "passing zero is a bug, fix your code." The tradeoff is between new, which panics on invalid input, and a fallible alternative like build that returns Result<ThreadPool, PoolCreationError> so the caller can handle a bad size gracefully. The book mentions you could provide build for callers who prefer to recover, but new is simpler and appropriate when an invalid size truly indicates a bug.`,
    tags: ['assert', 'panic', 'api-design'],
  },
  {
    id: 'rs-ch20-t-021',
    chapter: 20,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'with_capacity Over a Plain Vec',
    prompt: `When constructing the pool, the book writes let mut workers = Vec::with_capacity(size); before pushing each Worker. What is the practical benefit of with_capacity(size) compared to Vec::new() here, and is the program incorrect without it?`,
    hints: [
      'You already know exactly how many workers you will push.',
      'This is about allocation behavior, not correctness.',
    ],
    solution: `Vec::with_capacity(size) preallocates enough room for exactly size workers up front, so the loop that pushes each Worker never has to reallocate and copy the vector as it grows. With plain Vec::new(), the vector would start empty and grow by repeatedly reallocating as you push, which does extra work. The program is not incorrect without it; with_capacity is purely a performance optimization that is worthwhile precisely because you already know the final length. It avoids the small but unnecessary cost of growing a vector whose size is known in advance.`,
    tags: ['vec', 'with-capacity', 'allocation'],
  },
  {
    id: 'rs-ch20-t-022',
    chapter: 20,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Worker Holds an id and a JoinHandle',
    prompt: `Each Worker struct stores an id (a usize) and a thread (a JoinHandle). What is each field used for in the finished server, and why does the pool keep the workers in a Vec rather than discarding the handles after spawning?`,
    hints: [
      'The id is mostly for humans.',
      'The handle is needed at shutdown time.',
    ],
    solution: `The id is a simple numeric label, useful mainly for logging and debugging so you can print messages like "Worker 3 got a job." The thread field stores the JoinHandle returned by thread::spawn, which represents the running worker thread. The pool keeps the workers in a Vec, holding onto those handles, because at shutdown the Drop implementation iterates the workers and calls join on each handle to wait for them to finish. If the handles were dropped right after spawning, you would have no way to wait for the workers during graceful shutdown.`,
    tags: ['worker', 'join-handle', 'struct'],
  },
  {
    id: 'rs-ch20-t-023',
    chapter: 20,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Limiting Requests with take',
    prompt: `To demonstrate graceful shutdown, the book changes the main loop to for stream in listener.incoming().take(2). Predict exactly what behavior this produces, and explain what happens to the listener and the pool after the loop ends.`,
    hints: [
      'take(2) limits how many items the iterator yields.',
      'Leaving the loop leads into the pool being dropped.',
    ],
    solution: `take(2) makes the incoming() iterator yield only its first two connections and then stop, so the server handles exactly two requests and then the for loop ends. After the loop ends, main reaches the end of the scope that owns the ThreadPool, so the pool goes out of scope and its Drop implementation runs. That Drop logic signals the workers to stop and joins their threads, letting any in-progress jobs finish before the program exits. The effect is a controlled demonstration: serve two requests, then shut down gracefully instead of looping forever.`,
    tags: ['take', 'graceful-shutdown', 'drop'],
  },
  {
    id: 'rs-ch20-t-024',
    chapter: 20,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Why thread Becomes an Option',
    prompt: `For graceful shutdown the Worker's thread field changes from JoinHandle<()> to Option<JoinHandle<()>>. Explain why wrapping it in an Option is necessary to call join during Drop, given that join consumes the handle by value.`,
    hints: [
      'join takes the handle by value (self), not by reference.',
      'In Drop you only have &mut self.',
    ],
    solution: `JoinHandle::join consumes the handle by taking self by value, but inside drop you only have &mut self, so you cannot move the handle out of a regular field without leaving the struct in a partially moved, invalid state, which Rust forbids. Wrapping the field in Option lets you call self.thread.take(), which moves the JoinHandle out and leaves None behind, keeping the struct valid. You then have an owned handle you can call join on. Without the Option you would have no legal way to take ownership of the handle out of a borrowed Worker during Drop.`,
    tags: ['option-take', 'drop', 'join'],
  },
  {
    id: 'rs-ch20-t-025',
    chapter: 20,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Dropping the Sender to Signal Shutdown',
    prompt: `The pool's Drop implementation first does drop(self.sender.take()) and only then joins the workers. Explain the chain of events: how does dropping the sender cause the workers' recv() calls to return, and why must this happen before joining?`,
    hints: [
      'recv() returns Err when all senders are gone.',
      'A worker blocked on recv would never let join finish.',
    ],
    solution: `Each worker loops calling recv() on the shared receiver, which blocks while the channel is open. When the pool drops the only sender, the channel has no transmitters left, so every pending and future recv() returns Err instead of blocking forever. The workers' loops are written to break out of the loop when recv() returns Err, so they finish their loop and the thread is free to end. This drop must happen before the join loop because, otherwise, a worker stuck waiting on recv() would never finish, and calling join on it would block forever, hanging shutdown.`,
    tags: ['drop-sender', 'recv-error', 'shutdown'],
  },
  {
    id: 'rs-ch20-t-026',
    chapter: 20,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'sender as an Option Field',
    prompt: `Along with the worker thread, the ThreadPool stores its transmitter as sender: Option<Sender<Job>>. Why does the sender also need to be an Option, and how does execute have to change to account for that wrapping?`,
    hints: [
      'Drop needs to take the sender to drop it explicitly.',
      'execute can no longer use sender directly.',
    ],
    solution: `Drop needs to move the sender out of the pool and drop it to close the channel, but it only has &mut self, so the sender is wrapped in Option to allow self.sender.take() in the same way the worker's handle is taken. Because the field is now Option<Sender<Job>>, execute can no longer call self.sender.send(...) directly; it must reach inside the Option first, typically with self.sender.as_ref().unwrap().send(job).unwrap(). The as_ref borrows the Option's contents without moving the sender, and the unwrap assumes the sender is still present, which it is during normal operation since only Drop ever takes it. This keeps sending working while still allowing Drop to close the channel.`,
    tags: ['option', 'sender', 'execute'],
  },
  {
    id: 'rs-ch20-t-027',
    chapter: 20,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Order of Drop and Join',
    prompt: `A learner writes a Drop that joins each worker inside the same loop where it also wants to send a shutdown message through the channel, interleaving join and send. Explain why interleaving like that can deadlock, and why the book separates dropping the sender from the join loop into two distinct phases.`,
    hints: [
      'Joining worker 0 waits for it to finish.',
      'But it cannot finish until the channel is closed for all.',
    ],
    solution: `If you join worker 0 before the channel is fully closed for everyone, you wait for worker 0 to finish, but worker 0 may still be blocked on recv() because the sender has not been dropped yet, so join never returns and you deadlock. The fix is two clean phases: first drop the sender once, which closes the channel so every worker's recv() returns Err and each worker's loop can break, and only then loop over the workers calling join. With the channel already closed, every worker is guaranteed to be on its way to finishing, so each join completes. Mixing the two concerns in one loop reintroduces the ordering hazard the two-phase design exists to avoid.`,
    tags: ['deadlock', 'drop-order', 'graceful-shutdown'],
  },
  {
    id: 'rs-ch20-t-028',
    chapter: 20,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Why a New Worker Type at All',
    prompt: `An early design attempt tries to make execute directly spawn a thread that immediately runs the job, storing thread::spawn handles in the pool. This conflicts with the whole point of a pool. Explain why introducing the Worker abstraction, where threads are spawned once in new and then wait for jobs, is the correct design.`,
    hints: [
      'A pool reuses threads; it does not make one per job.',
      'When are the threads actually created?',
    ],
    solution: `If execute spawned a fresh thread for every job, you would just be doing one-thread-per-request again, never bounding the thread count, which defeats the purpose of a pool. The Worker abstraction lets you create a fixed number of threads once, inside ThreadPool::new, and have each thread sit in a loop waiting to receive jobs from the shared channel. execute then merely sends a job down the channel, and whichever idle worker grabs it runs it; no new thread is created per job. This is what makes the pool reuse a small, fixed set of long-lived threads instead of spawning unboundedly, which is the entire reason the Worker type exists.`,
    tags: ['worker', 'design', 'thread-reuse'],
  },
  {
    id: 'rs-ch20-t-029',
    chapter: 20,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Predict the Shutdown Messages',
    prompt: `The pool has two workers and the Drop logic prints "Shutting down worker {id}" right before joining each worker in order, after dropping the sender. With the loop limited so no jobs are pending at shutdown, predict what gets printed during shutdown and explain whether the order of these messages is guaranteed.`,
    hints: [
      'The join loop iterates the workers vector in order.',
      'Printing happens before each join call.',
    ],
    solution: `During shutdown, after the sender is dropped, the join loop walks the workers vector in index order, so it prints "Shutting down worker 0" then joins worker 0, then prints "Shutting down worker 1" then joins worker 1. The order of these printed "Shutting down" lines is guaranteed because the loop processes the vector sequentially on the dropping thread, one worker at a time. What is not guaranteed is the order in which the worker threads themselves actually finished their work earlier, since that depends on the scheduler. So you reliably see the two shutdown messages in worker-id order, even though which worker handled which earlier job is nondeterministic.`,
    tags: ['predict-output', 'shutdown', 'ordering'],
  },
  {
    id: 'rs-ch20-t-030',
    chapter: 20,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Compare: Channel-Based Pool vs Spawn-Per-Connection',
    prompt: `You must justify the final architecture to a teammate. Compare the book's channel-based thread pool against a simple "spawn a thread per connection" approach across three dimensions: resource bounds under load, latency for a request that arrives during a traffic burst, and complexity of the code. Give the tradeoff honestly.`,
    hints: [
      'The pool caps threads; spawn-per-connection does not.',
      'A capped pool means some requests queue and wait.',
    ],
    solution: `On resource bounds, the channel-based pool wins decisively: it caps threads at a fixed size so memory and scheduler pressure stay bounded under any load, while spawn-per-connection can create unbounded threads and exhaust resources during a flood. On latency during a burst, the tradeoff flips somewhat: spawn-per-connection starts every request immediately (until the machine chokes), whereas the pool makes extra requests wait in the channel queue until a worker frees up, so individual requests may see added queuing delay even though the system stays stable. On complexity, spawn-per-connection is trivially simple, while the pool adds real machinery: a channel, Arc<Mutex<Receiver>>, Worker threads, and graceful-shutdown Drop logic. The honest summary is that the pool trades some code complexity and per-request queuing latency for stability and predictable resource usage, which is the right tradeoff for a real server exposed to untrusted load.`,
    tags: ['compare', 'architecture', 'tradeoffs'],
  },
]

export default problems
