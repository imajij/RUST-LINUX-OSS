import type { Problem } from '../../types'

const problems: Problem[] = [
  {
    id: 'rs-ch15-t-001',
    chapter: 15,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'What a Box Stores Where',
    prompt: `You write let b = Box::new(5);. Where does the value 5 live, and where does the variable b live? When do you reach for a Box in the first place?`,
    hints: [
      'A Box has a known, fixed size on the stack.',
      'The pointed-to data is somewhere else.',
    ],
    solution: `Box::new(5) allocates the value 5 on the heap and gives you back a Box, which is a smart pointer whose data (a pointer to the heap) lives on the stack. So b itself sits on the stack but the 5 it owns sits on the heap. You reach for a Box when you have a value whose size is not known at compile time but you need a known size (such as recursive types), when you have a large value and want to transfer ownership without copying the whole thing, or when you want to own a value and only care that it implements a particular trait.`,
    tags: ['box', 'heap', 'smart-pointer'],
  },
  {
    id: 'rs-ch15-t-002',
    chapter: 15,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'The Dereference Operator on a Box',
    prompt: `Given let b = Box::new(7);, explain what the expression *b evaluates to and why you can use the same * operator that you use on a regular reference.`,
    hints: [
      'Box behaves like a reference here.',
      'Think about what value you get back after the star.',
    ],
    solution: `The expression *b evaluates to 7, the value stored on the heap. You can use *, the dereference operator, because Box implements the Deref trait, which lets you treat the smart pointer like a reference and follow it to the value it points to. So assert_eq!(7, *b) succeeds just as it would for a plain reference &7. Box adds heap storage but otherwise behaves like a reference when you dereference it.`,
    tags: ['box', 'deref', 'dereference'],
  },
  {
    id: 'rs-ch15-t-003',
    chapter: 15,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'When Drop Runs',
    prompt: `You define a struct with a custom Drop implementation that prints a message. You never call any cleanup function yourself. When does the printed message appear during the program?`,
    hints: [
      'Drop is tied to scope, not to an explicit call.',
      'Think about the end of the block where the value lives.',
    ],
    solution: `The Drop trait's drop method runs automatically when the value goes out of scope, that is, when execution reaches the end of the block (or otherwise the point) where the value's owner is dropped. You do not call drop yourself; Rust inserts the call for you. So if you create the value inside main and never move it, the message prints right before main ends. This automatic cleanup is how Rust runs your destructor logic deterministically without a garbage collector.`,
    tags: ['drop', 'scope', 'cleanup'],
  },
  {
    id: 'rs-ch15-t-004',
    chapter: 15,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'Why Rc Exists',
    prompt: `Most values in Rust have a single owner. Describe a situation where you genuinely need more than one owner of the same data, and name the smart pointer this chapter gives you for that case.`,
    hints: [
      'Think about a graph or a list where one node is shared.',
      'The name stands for "reference counted".',
    ],
    solution: `Sometimes a single value must be owned by several parts of a program at once, and you cannot know at compile time which part will finish using it last; the classic example is a graph where multiple edges point at the same node, which should stay alive as long as any edge references it. For these cases you use Rc, the reference counting smart pointer. Rc keeps a count of how many owners exist and frees the data only when that count drops to zero. It is meant for single-threaded scenarios where you want shared ownership of read-only data.`,
    tags: ['rc', 'ownership', 'sharing'],
  },
  {
    id: 'rs-ch15-t-005',
    chapter: 15,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Why a Recursive Cons List Needs a Box',
    prompt: `Consider enum List { Cons(i32, List), Nil }. The compiler rejects this with an error about infinite size. Explain why, and how wrapping the recursive part in Box fixes it.`,
    hints: [
      'The compiler must compute a fixed size for the type.',
      'A Box has a known size regardless of what it points to.',
    ],
    solution: `To lay out a type, the compiler must know its size, but Cons(i32, List) contains another List, which contains another List, and so on forever, so the size is infinite and cannot be computed. Wrapping the recursive field as Cons(i32, Box<List>) breaks the chain: a Box is just a pointer with a fixed, known size no matter how big the data it points to is. The list's size becomes an i32 plus a pointer, which is finite. The actual next List lives on the heap, reached through the Box, so the type is no longer infinitely large.`,
    tags: ['box', 'recursive', 'cons-list'],
  },
  {
    id: 'rs-ch15-t-006',
    chapter: 15,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'What Deref Returns',
    prompt: `You implement the Deref trait for a type MyBox<T>. What is the signature of the required method, what associated type must you set, and what does the method return so that *instance works?`,
    hints: [
      'There is one associated type called Target.',
      'The method returns a reference, not the value itself.',
    ],
    solution: `You implement Deref by setting the associated type Target (here type Target = T) and defining fn deref(&self) -> &Self::Target. The method returns a reference to the inner value, for example &self.0 if MyBox wraps a tuple struct. Rust then turns the expression *instance into *(instance.deref()), so deref hands back a reference and the compiler applies the plain dereference operator to it. Returning a reference, not the value, matters because returning the value would move ownership out of self, which you usually do not want.`,
    tags: ['deref', 'mybox', 'trait'],
  },
  {
    id: 'rs-ch15-t-007',
    chapter: 15,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Predict the Drop Output Order',
    prompt: `Two values c and d (in that order) each have a Drop impl that prints their name. They are created as let c = ...; let d = ...; and nothing else moves them. In what order do the drop messages print at the end of the block, and why?`,
    hints: [
      'Variables drop in the reverse order of creation.',
      'Last created, first dropped.',
    ],
    solution: `The drop messages print d first, then c. Rust drops variables in the reverse order of their declaration, so d, declared last, is dropped first, and c, declared first, is dropped last. This reverse (LIFO) ordering is like unwinding a stack: later values may depend on earlier ones, so tearing them down in reverse is the safe default. So you would see d's message before c's message.`,
    tags: ['drop', 'order', 'scope'],
  },
  {
    id: 'rs-ch15-t-008',
    chapter: 15,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Calling drop Directly Is an Error',
    prompt: `A learner writes c.drop(); intending to clean up early, and the compiler complains with "explicit destructor calls not allowed". Why does Rust forbid calling the drop method by hand, and what should they call instead?`,
    hints: [
      'Think about what would happen at the end of scope afterward.',
      'The standard library offers a free function for early dropping.',
    ],
    solution: `Rust forbids calling the Drop::drop method directly because the value would still be dropped automatically at the end of its scope, causing a double free of the same resources. To prevent that bug, the language rejects explicit destructor calls outright. When you genuinely need to clean a value up before its scope ends, you call std::mem::drop(c) instead. That function takes ownership of the value and moves it out of scope, so the automatic drop happens exactly once.`,
    tags: ['drop', 'mem-drop', 'compile-error'],
  },
  {
    id: 'rs-ch15-t-009',
    chapter: 15,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Reading strong_count After Cloning',
    prompt: `You write let a = Rc::new(5);, then let b = Rc::clone(&a);, then let c = Rc::clone(&a);. What does Rc::strong_count(&a) report after each step, and what does Rc::clone actually do?`,
    hints: [
      'clone here does not deep-copy the data.',
      'Each clone bumps a counter.',
    ],
    solution: `After creating a the count is 1, after cloning into b it is 2, and after cloning into c it is 3. Rc::clone does not make a deep copy of the underlying data; it only increments the strong reference count and hands back another Rc pointing to the same heap allocation. That is why it is cheap and why the book uses Rc::clone(&a) rather than a.clone(), to signal visually that this is a reference-count bump, not a full copy. All three variables share one value of 5 on the heap.`,
    tags: ['rc', 'strong-count', 'clone'],
  },
  {
    id: 'rs-ch15-t-010',
    chapter: 15,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'What Interior Mutability Means',
    prompt: `RefCell<T> is described as providing "interior mutability". In one or two sentences, what does that phrase mean, and how does it differ from ordinary mutability with mut and &mut?`,
    hints: [
      'Think about mutating data even through a shared, immutable-looking handle.',
      'The borrow rules still apply, just checked later.',
    ],
    solution: `Interior mutability is a pattern that lets you mutate the data inside a value even when you only have an immutable reference to that value. Normally Rust's borrowing rules forbid this: to mutate you need &mut, and you cannot get &mut through an immutable handle. RefCell<T> works around it by enforcing the borrowing rules at runtime instead of at compile time, so the type itself can be immutable while its contents change. If you break the rules, you get a panic at runtime rather than a compile error.`,
    tags: ['refcell', 'interior-mutability', 'borrowing'],
  },
  {
    id: 'rs-ch15-t-011',
    chapter: 15,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'borrow Versus borrow_mut',
    prompt: `RefCell<T> exposes borrow and borrow_mut. What does each return, and how do the returned smart pointers relate to the usual & and &mut references?`,
    hints: [
      'They return guard types, not bare references.',
      'Those guards themselves implement Deref.',
    ],
    solution: `borrow returns a Ref<T>, a smart pointer that acts like a shared &T, while borrow_mut returns a RefMut<T>, which acts like an exclusive &mut T. Both Ref and RefMut implement Deref, so you can use them as if they were plain references to the inner value. RefCell counts how many Ref and RefMut guards are active and enforces the borrowing rules dynamically: many Refs are allowed at once, but only one RefMut and no other borrows alongside it. When a guard goes out of scope it decrements the count, freeing the borrow.`,
    tags: ['refcell', 'borrow', 'borrow-mut'],
  },
  {
    id: 'rs-ch15-t-012',
    chapter: 15,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Why Use a Box of Trait Object',
    prompt: `In the cons-list example the value held by each node is a plain i32, but elsewhere you see Box<dyn Trait>. Describe the second reason this chapter gives for using Box: storing a value when you only know it implements a certain trait.`,
    hints: [
      'Different concrete types can have different sizes.',
      'A pointer gives them a uniform size.',
    ],
    solution: `Sometimes you want to own a value and only care that it implements a particular trait, not its exact concrete type; different implementors can have different sizes, so you cannot store them inline in one variable or collection. Putting the value behind a Box, as Box<dyn Trait>, gives every implementor the same size (one pointer) and lets you treat them uniformly through the trait. The Box owns the heap-allocated value and the trait object lets you call the trait's methods. This is the use case where you want a trait without knowing the concrete type at compile time.`,
    tags: ['box', 'trait-object', 'dyn'],
  },
  {
    id: 'rs-ch15-t-013',
    chapter: 15,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Rc Is Read-Only',
    prompt: `You have several Rc handles to the same value and you try to mutate the value through one of them. Why does Rc alone not let you do this, and what is the usual fix?`,
    hints: [
      'Rc only ever gives you shared, immutable access.',
      'Combine it with another type from this chapter.',
    ],
    solution: `Rc gives every owner only an immutable, shared view of the data, because allowing mutation while multiple owners hold references would violate Rust's rule against aliasing mutable data. So Rc by itself cannot hand out a &mut to the inner value. The usual fix is to put a RefCell inside the Rc, as Rc<RefCell<T>>: the Rc provides shared ownership and the RefCell provides interior mutability with runtime-checked borrows. Each owner can then call borrow_mut to change the shared value while the Rc keeps it alive.`,
    tags: ['rc', 'refcell', 'mutability'],
  },
  {
    id: 'rs-ch15-t-014',
    chapter: 15,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Deref Coercion in a Function Call',
    prompt: `You have fn hello(name: &str) {} and a value m of type MyBox<String> where MyBox implements Deref with Target = String. The call hello(&m) compiles. Walk through the chain of coercions the compiler applies to make &MyBox<String> match &str.`,
    hints: [
      'Deref can fire more than once in a row.',
      'String also implements Deref to str.',
    ],
    solution: `Deref coercion automatically converts a reference to a type into a reference to whatever that type derefs to, applying repeatedly until the argument matches the parameter. Here &m is &MyBox<String>; calling deref on MyBox<String> yields &String. String itself implements Deref with Target = str, so a second coercion turns &String into &str, which is exactly what hello expects. The compiler inserts these as many .deref() calls as needed at compile time, so there is no runtime cost. Without deref coercion you would have had to write hello(&(*m)[..]) by hand.`,
    tags: ['deref-coercion', 'mybox', 'str'],
  },
  {
    id: 'rs-ch15-t-015',
    chapter: 15,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Two RefCell Borrows at Runtime',
    prompt: `Inside one scope you write let one = cell.borrow_mut(); let two = cell.borrow_mut(); on the same RefCell. This compiles fine but the program panics when it runs. Explain exactly what RefCell detected and why it is a panic and not a compile error.`,
    hints: [
      'Two mutable borrows alive at the same time break the rules.',
      'RefCell checks at runtime, so the compiler stays silent.',
    ],
    solution: `RefCell tracks active borrows at runtime. When borrow_mut is called the second time, the first RefMut from one is still alive, so there would be two mutable borrows of the same data simultaneously, which violates the rule that you may have only one mutable borrow at a time. RefCell detects this and panics with a message like "already borrowed: BorrowMutError". It is a runtime panic rather than a compile error because RefCell deliberately moves the borrow checking from compile time to run time; the compiler cannot see the violation, so it surfaces only when the code executes. Letting one go out of scope before calling borrow_mut again would avoid the panic.`,
    tags: ['refcell', 'panic', 'runtime-borrow'],
  },
  {
    id: 'rs-ch15-t-016',
    chapter: 15,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'strong_count Through Nested Scopes',
    prompt: `let a = Rc::new(String::from("hi")); then let b = Rc::clone(&a); then inside an inner block { let c = Rc::clone(&a); /* print count */ } and after the block /* print count */. What count does each print show, and what happens to c's reference when the block ends?`,
    hints: [
      'Cloning raises the count; going out of scope lowers it.',
      'Drop is what decrements the count.',
    ],
    solution: `After a and b the count is 2. Inside the block, cloning into c raises it to 3, so the print inside the block shows 3. When the inner block ends, c goes out of scope and its Drop implementation decrements the count back to 2, so the print after the block shows 2. This shows the symmetry of Rc: Rc::clone increments strong_count, and dropping an Rc decrements it. The data is freed only when the count finally reaches zero.`,
    tags: ['rc', 'strong-count', 'drop'],
  },
  {
    id: 'rs-ch15-t-017',
    chapter: 15,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Why a Mock Object Needs RefCell',
    prompt: `You are testing code that takes a &T where the trait's method send(&self, msg: &str) takes &self, not &mut self. Your mock wants to record each message into a vector field. Why must that field be a RefCell<Vec<String>> rather than a plain Vec<String>?`,
    hints: [
      'The trait method signature only gives you &self.',
      'You still need to push onto the vector.',
    ],
    solution: `The trait dictates that send takes &self, an immutable reference, so inside the mock you only have an immutable view and cannot get a &mut to a plain Vec field to push onto it. You also cannot change the trait's signature to &mut self, since you must match the interface being tested. Wrapping the field as RefCell<Vec<String>> gives you interior mutability: even through &self you can call self.sent_messages.borrow_mut().push(...) to record the message. The RefCell enforces the borrow rules at runtime, which is fine because the test pushes one message at a time.`,
    tags: ['refcell', 'mock', 'interior-mutability'],
  },
  {
    id: 'rs-ch15-t-018',
    chapter: 15,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Compare Box, Rc, and RefCell Ownership',
    prompt: `Summarize the difference between Box<T>, Rc<T>, and RefCell<T> along two axes: how many owners each allows, and whether borrow checking is done at compile time or run time.`,
    hints: [
      'One of them shares ownership; the others do not.',
      'One of them checks borrows at run time.',
    ],
    solution: `Box<T> has a single owner and its borrow rules are checked at compile time; it just moves a single value to the heap. Rc<T> allows multiple owners of the same data through reference counting, but it gives out only immutable references, and those are still checked at compile time. RefCell<T> has a single owner, but unlike the others it enforces the borrowing rules at run time, which is what lets it provide interior mutability. So if you need many owners you reach for Rc, if you need to mutate through a shared handle you reach for RefCell, and combining Rc<RefCell<T>> gives you multiple owners that can also mutate the shared data.`,
    tags: ['box', 'rc', 'refcell'],
  },
  {
    id: 'rs-ch15-t-019',
    chapter: 15,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Mutating Shared Data Through Rc of RefCell',
    prompt: `value is an Rc<RefCell<i32>> shared by three list nodes a, b, and c. You write *value.borrow_mut() += 10;. After this line, what do all three nodes observe when they read value, and why?`,
    hints: [
      'All three Rc handles point at one RefCell.',
      'borrow_mut hands you a mutable view of that one cell.',
    ],
    solution: `All three nodes observe the same updated number, because each holds an Rc clone pointing at the exact same RefCell on the heap, not separate copies. borrow_mut returns a RefMut that dereferences to the inner i32, and *value.borrow_mut() += 10 mutates that single shared cell. Since a, b, and c all read through the same RefCell, every one of them now sees the value increased by 10. This is the power of Rc<RefCell<T>>: shared ownership plus interior mutability means a change made through one handle is visible to all of them.`,
    tags: ['rc', 'refcell', 'shared-mutable'],
  },
  {
    id: 'rs-ch15-t-020',
    chapter: 15,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Move drop Up to Change the Order',
    prompt: `Variables a and b are created in that order, each with a Drop impl that prints. Before the block ends you insert drop(a); right after b is created. Predict the order of all printed messages and explain how std::mem::drop changed the outcome.`,
    hints: [
      'The explicit drop fires immediately at that line.',
      'Whatever remains drops in reverse at scope end.',
    ],
    solution: `The order is: a's message first (from the explicit drop(a) call, which runs immediately at that line), then b's message at the end of the block. Without the explicit drop, normal scope cleanup would have dropped b first then a (reverse declaration order). By calling std::mem::drop(a), you take ownership of a and force its destructor to run early, so a is gone well before the block ends. When the block does end, only b remains to be dropped automatically, producing the reversed-looking but correct sequence a then b.`,
    tags: ['drop', 'mem-drop', 'order'],
  },
  {
    id: 'rs-ch15-t-021',
    chapter: 15,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Why RefCell Is Not Thread Safe',
    prompt: `The book notes that RefCell<T> is only for single-threaded code. Without going into later chapters, explain the design reason RefCell is restricted this way, and what general kind of type you would expect to need for the multithreaded case.`,
    hints: [
      'The runtime borrow tracking is not synchronized.',
      'There is a thread-safe cousin you will meet later.',
    ],
    solution: `RefCell tracks its active borrow counts with plain, non-synchronized bookkeeping, so two threads touching the same RefCell could update those counts unsafely and corrupt the borrow tracking; the type therefore is not Sync and the compiler will not let you share it across threads. Its design trades thread safety for low single-threaded overhead. For the multithreaded case you would expect a thread-safe equivalent that uses atomic operations or locking to coordinate access, which the concurrency chapter introduces (such as Mutex). The key point now is that RefCell's runtime checks assume one thread.`,
    tags: ['refcell', 'thread-safety', 'single-thread'],
  },
  {
    id: 'rs-ch15-t-022',
    chapter: 15,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Deref Coercion and Mutability Rules',
    prompt: `Deref coercion can turn &T into &U, and DerefMut can turn &mut T into &mut U. The book also says &mut T can coerce to &T, but never the reverse. Why is coercing from a mutable reference to an immutable one safe, while the reverse is forbidden?`,
    hints: [
      'Think about how many references can exist at once.',
      'A shared reference promises no one else mutates.',
    ],
    solution: `Going from &mut T to &T is always safe because if you hold the one exclusive mutable reference, you certainly satisfy the weaker guarantee of an immutable reference; you are giving up a capability, not gaining one. The reverse, &T to &mut T, is forbidden because an immutable reference does not promise exclusivity: there could be other shared references to the same data, and conjuring a mutable reference would let you mutate data that others are reading, breaking the borrowing rules. So Rust allows the safe downgrade but never the unsound upgrade. This is why DerefMut coercion needs a mutable source in the first place.`,
    tags: ['deref-coercion', 'mutability', 'borrowing'],
  },
  {
    id: 'rs-ch15-t-023',
    chapter: 15,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'How a Reference Cycle Leaks Memory',
    prompt: `Two nodes a and b are each Rc<RefCell<...>> and you make a point to b and b point back to a, both via Rc clones stored in RefCell fields. Explain why dropping your local a and b variables does not free either node, and what the strong_count of each is when the program ends.`,
    hints: [
      'Each node holds an Rc to the other.',
      'Rc frees only when strong_count hits zero.',
    ],
    solution: `After linking them, each node's strong_count is 2: one count from your local variable and one from the other node's RefCell holding an Rc to it. When main ends and the local a and b variables are dropped, each count drops only to 1, because the two nodes still hold Rc clones of each other. Since neither count ever reaches zero, neither node's data is freed, even though nothing outside the cycle can reach them; this is a memory leak. Rust's ownership system does not prevent reference cycles, so creating Rc<RefCell<...>> structures that point at each other can leak memory, and it is your responsibility to avoid that.`,
    tags: ['reference-cycle', 'rc', 'memory-leak'],
  },
  {
    id: 'rs-ch15-t-024',
    chapter: 15,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Weak Versus Strong References',
    prompt: `Rc::downgrade produces a Weak<T> instead of another Rc<T>. Contrast a strong reference with a weak one in terms of ownership, which count each affects, and what each implies about whether the data is still alive.`,
    hints: [
      'Only one of them keeps the data alive.',
      'There are two separate counts being tracked.',
    ],
    solution: `A strong reference (an Rc<T>) expresses ownership: it increments strong_count and the data is kept alive exactly as long as strong_count is greater than zero. A weak reference (a Weak<T> from Rc::downgrade) expresses a non-owning relationship: it increments weak_count, not strong_count, and so it does not keep the value alive. The value is dropped as soon as strong_count reaches zero, regardless of how many Weak references remain. Therefore a Weak might refer to data that has already been dropped, which is why you cannot use it directly and must check whether the value still exists before using it.`,
    tags: ['weak', 'rc', 'downgrade'],
  },
  {
    id: 'rs-ch15-t-025',
    chapter: 15,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Why upgrade Returns an Option',
    prompt: `To use a Weak<T> you call upgrade, which returns Option<Rc<T>> rather than Rc<T> directly. Explain why the return type must be an Option, and what each variant means about the referenced value.`,
    hints: [
      'A Weak does not keep the value alive.',
      'The value may already be gone.',
    ],
    solution: `Because a Weak reference does not own the value, the value it points to may already have been dropped (its strong_count could have reached zero). upgrade cannot promise that a live Rc exists, so it returns Option<Rc<T>> to encode that uncertainty safely. If the value is still alive, upgrade returns Some(Rc) and incrementing the strong count to give you a real owning handle; if the value has already been dropped, it returns None. Forcing you to handle the Option means you can never accidentally use a dangling reference, so weak references stay memory-safe.`,
    tags: ['weak', 'upgrade', 'option'],
  },
  {
    id: 'rs-ch15-t-026',
    chapter: 15,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Designing Parent and Child Links in a Tree',
    prompt: `You are modeling a tree where each node knows its children and also its parent. If children are stored as Rc and parent is also stored as Rc, you risk a cycle. What ownership direction should be strong and which should be weak, and why does this choice prevent leaks while still letting a child reach its parent?`,
    hints: [
      'A parent should own its children.',
      'A child referring back up should not own the parent.',
    ],
    solution: `The parent should own its children with strong Rc references, while each child should refer to its parent with a Weak reference. This direction is right because a parent conceptually owns its children, so the children's lifetime should depend on the parent, but a child pointing back up should not keep the parent alive, which would create a cycle. With the parent holding strong references and the child holding a Weak, dropping the parent drops the children, and the child's upward link, being weak, never contributes to the parent's strong_count. The child can still reach its parent by calling upgrade on the Weak, getting Some only while the parent is still alive.`,
    tags: ['weak', 'tree', 'parent-child'],
  },
  {
    id: 'rs-ch15-t-027',
    chapter: 15,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Tracking strong_count and weak_count in a Tree',
    prompt: `A leaf node is shared by being a child of a branch (strong) and also having a Weak link from the leaf back up to the branch. Describe, in general terms, how strong_count and weak_count of the branch and leaf reflect these relationships, and why the leaf's strong_count stays low.`,
    hints: [
      'Children are strong; parent back-links are weak.',
      'Each kind of link bumps a different counter.',
    ],
    solution: `The branch owns the leaf with a strong Rc, so the leaf's strong_count includes that ownership plus any other strong handles you keep, but the leaf's upward link to the branch is a Weak, so it adds to the branch's weak_count rather than its strong_count. As a result, the leaf's strong_count stays low (it is owned by the branch and perhaps your local variable), and the branch's weak_count reflects how many children point back up to it weakly. Because the back-links are weak, they never raise the branch's strong_count, so when the branch's strong owners are gone the branch is freed even though weak references still nominally exist. This separation of the two counts is exactly how Weak breaks the cycle while preserving navigability.`,
    tags: ['weak', 'strong-count', 'weak-count'],
  },
  {
    id: 'rs-ch15-t-028',
    chapter: 15,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Predict Output: Drop Order with Box and mem drop',
    prompt: `You create let outer = Box::new(Resource("outer")); let inner = Resource("inner");, where Resource prints on drop. Then you call std::mem::drop(outer); and let the scope end. List the printed messages in order and explain why "inner" prints last.`,
    hints: [
      'The explicit drop fires immediately.',
      'Dropping a Box drops its heap contents.',
    ],
    solution: `The messages print in this order: "outer" first, then "inner". The call std::mem::drop(outer) runs immediately, taking ownership of the Box; dropping the Box drops the heap-allocated Resource inside it, so "outer" prints right there. After that line, only inner remains in scope. When the block ends, inner is dropped automatically and "inner" prints. So even though inner was created after outer, the explicit early drop of outer means "inner" is the last message, since it is the only value left to clean up at scope end.`,
    tags: ['drop', 'box', 'mem-drop'],
  },
  {
    id: 'rs-ch15-t-029',
    chapter: 15,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'When to Choose Rc of RefCell Over Plain Ownership',
    prompt: `A teammate proposes Rc<RefCell<T>> for a data structure that has a single, clear owner and never needs to be mutated through a shared handle. Argue why plain ownership (or just Box) is the better choice here, and name the concrete costs Rc<RefCell<T>> would add.`,
    hints: [
      'Reach for the simplest tool that fits.',
      'Rc and RefCell each add a runtime cost.',
    ],
    solution: `If there is a single clear owner and no need to mutate through a shared handle, Rc<RefCell<T>> is overkill and you should prefer plain ownership or a simple Box. Rc adds reference-count bookkeeping on every clone and drop and only ever yields immutable access, while RefCell moves borrow checking to run time, costing extra checks and risking a panic if you accidentally double-borrow. You would also lose the compiler's compile-time guarantees that catch borrow mistakes early. Reaching for the simplest type that expresses the actual ownership keeps the code faster, clearer, and safer; you bring in Rc<RefCell<T>> only when you truly need multiple owners that can also mutate shared data.`,
    tags: ['rc', 'refcell', 'design'],
  },
  {
    id: 'rs-ch15-t-030',
    chapter: 15,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Fill In the Rule: Avoiding Leaks With Rc',
    prompt: `Complete and justify this rule of thumb: "Rust's ownership and borrow checking guarantee memory safety, but they do NOT prevent ___ when you use Rc with interior mutability. To avoid the resulting leaks, make sure that in any cycle of references, at least one link is a ___." Fill in both blanks and explain the reasoning.`,
    hints: [
      'The first blank is the failure mode from cyclic Rc graphs.',
      'The second blank is the non-owning reference type.',
    ],
    solution: `The first blank is "reference cycles" (memory leaks from cyclic Rc references) and the second is "weak reference" (a Weak<T>). Rust's compile-time checks prevent dangling references and data races, but they cannot detect that two or more Rc values point at each other in a loop, so such cycles keep each other's strong_count above zero forever and the memory is never reclaimed. The fix is to ensure at least one link in any potential cycle is a Weak reference, created with Rc::downgrade, since weak references do not contribute to strong_count and therefore do not keep the data alive. With one weak link, dropping the owning references lets the strong_count reach zero and the whole structure is freed.`,
    tags: ['reference-cycle', 'weak', 'memory-leak'],
  },
]

export default problems
