import type { Problem } from '../../types'

const problems: Problem[] = [
  {
    id: 'lx-ch11-t-001',
    chapter: 11,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'What an Interrupt Is',
    prompt: `A device finishes a job (a key was pressed, a packet arrived) and needs the CPU's attention. In plain terms, what is a hardware interrupt, and what does the CPU do to the code it was running when one arrives?`,
    hints: ['It is an asynchronous signal from hardware.', 'The current work is paused, not abandoned.'],
    solution: `A hardware interrupt is an asynchronous electrical signal raised by a device on an IRQ line to tell the CPU that something needs servicing. When it arrives, the CPU stops executing whatever instruction stream it was in (whether kernel or user code), saves enough state to resume later, and jumps to a kernel entry point that eventually calls the registered interrupt handler for that IRQ. After the handler returns, the kernel restores the saved state and resumes the interrupted work as if nothing happened. The key idea is that interrupts let hardware get serviced promptly without the kernel having to constantly poll devices.`,
    tags: ['kernel', 'interrupts', 'irq'],
  },
  {
    id: 'lx-ch11-t-002',
    chapter: 11,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'Registering a Handler',
    prompt: `Your driver wants to be told whenever IRQ number 17 fires. Name the function you call to register an interrupt handler for that line, and list the essential pieces of information you must hand it.`,
    hints: ['Think request_irq.', 'Which line, which function, what flags, a name, a cookie.'],
    solution: `You call request_irq(irq, handler, flags, name, dev), typically passing irq = 17. The arguments are: the IRQ number you want; a pointer to your handler function (an irq_handler_t with signature irqreturn_t fn(int irq, void *dev)); flags such as IRQF_SHARED that describe how the line behaves; a name string that shows up in /proc/interrupts; and a dev cookie that is passed back to your handler and is also used to identify your registration when you later call free_irq. If request_irq returns 0 the line is yours; a negative return means it failed (for example the line is already taken by a non-shareable handler).`,
    tags: ['kernel', 'interrupts', 'request_irq'],
  },
  {
    id: 'lx-ch11-t-003',
    chapter: 11,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'Top Half vs Bottom Half',
    prompt: `Interrupt handling is often described as having a "top half" and a "bottom half." In one or two sentences each, what is each part responsible for, and which one runs immediately when the IRQ fires?`,
    hints: ['One is fast and urgent; one is deferred.', 'The hard IRQ handler is the top half.'],
    solution: `The top half is the actual interrupt handler that runs immediately when the IRQ fires; it should do only the minimum urgent work, for example acknowledging the device and grabbing data, then return quickly. The bottom half is deferred work that the top half schedules to run a little later (as a softirq, tasklet, or workqueue) to do the heavier or sleepable processing. The split exists so the top half stays short, because while it runs the CPU is in a constrained interrupt context and (depending on configuration) other interrupts may be held off.`,
    tags: ['kernel', 'interrupts', 'bottom-half'],
  },
  {
    id: 'lx-ch11-t-004',
    chapter: 11,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'The Cardinal Rule of IRQ Context',
    prompt: `There is one rule beginners must memorize about code that runs in interrupt (hard IRQ) context. State the rule, and give one concrete example of a call that breaks it.`,
    hints: ['It is about blocking.', 'msleep, mutex_lock, and GFP_KERNEL allocations can all block.'],
    solution: `The cardinal rule is: you must never sleep (block) in interrupt context. There is no process to put to sleep and no safe way to reschedule, so blocking would wedge the kernel. Concrete examples that break it include calling msleep() or schedule(), taking a mutex or a semaphore (which may sleep), copying to or from user space with copy_to_user (which can fault and block), and allocating memory with GFP_KERNEL (which may wait for reclaim). In a handler you must use non-sleeping equivalents: spinlocks instead of mutexes, GFP_ATOMIC instead of GFP_KERNEL, and defer any sleepable work to a workqueue.`,
    tags: ['kernel', 'interrupts', 'atomic'],
  },
  {
    id: 'lx-ch11-t-005',
    chapter: 11,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Why GFP_ATOMIC In a Handler',
    prompt: `Inside an interrupt handler a driver needs a small buffer and calls kmalloc(size, GFP_KERNEL). Explain why this flag is wrong here and what to use instead.`,
    hints: ['GFP_KERNEL may sleep waiting for memory.', 'Interrupt context cannot sleep.'],
    solution: `GFP_KERNEL allows the allocator to block: if memory is tight it can put the caller to sleep while it reclaims pages or swaps. That is fine in process context, but an interrupt handler runs in atomic context where sleeping is forbidden, so a GFP_KERNEL allocation could trigger a "sleeping function called from invalid context" bug or deadlock. The correct flag is GFP_ATOMIC, which tells the allocator it must not sleep and to fail fast instead, drawing from an emergency reserve if needed. Because GFP_ATOMIC can fail more easily, the handler must check for a NULL return; better still, preallocate the buffer outside IRQ context when you can.`,
    tags: ['kernel', 'interrupts', 'gfp'],
  },
  {
    id: 'lx-ch11-t-006',
    chapter: 11,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'The irqreturn_t Return Value',
    prompt: `An interrupt handler returns a value of type irqreturn_t. What are the two common values it can return, and what is the kernel telling itself when a handler reports each one?`,
    hints: ['IRQ_HANDLED and IRQ_NONE.', 'Did this device actually raise the interrupt?'],
    solution: `A handler returns IRQ_HANDLED when it recognized that its device raised the interrupt and serviced it, or IRQ_NONE when the interrupt was not from its device and it did nothing. The kernel uses this to track spurious interrupts: on a line where every registered handler returns IRQ_NONE, the core counts an unhandled interrupt, and if too many pile up it may disable the line to protect the system. (There is also IRQ_WAKE_THREAD, used with threaded IRQs to ask the kernel to run the threaded handler.) Returning the right value matters most on shared lines, where several handlers run and each must honestly report whether the interrupt was theirs.`,
    tags: ['kernel', 'interrupts', 'irqreturn'],
  },
  {
    id: 'lx-ch11-t-007',
    chapter: 11,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Why Bottom Halves Exist',
    prompt: `If the top half can already run code when an interrupt fires, why does the kernel bother with bottom halves at all? Give the main reason in terms of latency and the cost of long handlers.`,
    hints: ['What is blocked while a handler runs?', 'Long handlers hurt responsiveness.'],
    solution: `While a top half runs, it executes in interrupt context and, at least for its own line, the device's interrupt is being serviced; depending on configuration other interrupts may also be held off, which adds latency to the whole system. A long-running handler therefore delays other interrupts and the threads waiting to run, hurting overall responsiveness and risking missed events. Bottom halves let the top half do only the urgent minimum and quickly return, pushing the bulk of the processing to a deferred mechanism (softirq, tasklet, or workqueue) that runs with interrupts enabled and, in the workqueue case, in a context where it may even sleep. The result is short, predictable interrupt latency without giving up the heavier work that still needs doing.`,
    tags: ['kernel', 'interrupts', 'bottom-half'],
  },
  {
    id: 'lx-ch11-t-008',
    chapter: 11,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Tasklet vs Workqueue at a Glance',
    prompt: `A driver author asks: "For my bottom half, should I use a tasklet or a workqueue?" Give the single most important difference between the two that decides the answer.`,
    hints: ['One runs in atomic context, one in process context.', 'Can the work sleep?'],
    solution: `The deciding difference is whether the deferred work needs to sleep. A tasklet runs in atomic (softirq) context, so it cannot sleep, take a mutex, or do blocking I/O, but it runs soon and with low overhead. A workqueue runs its handler in process context on a kernel worker thread, so it may sleep, take mutexes, allocate with GFP_KERNEL, and do longer or blocking work. So: pick a tasklet for short, fast, non-sleeping deferred work, and a workqueue whenever the bottom half must do anything that could block.`,
    tags: ['kernel', 'tasklet', 'workqueue'],
  },
  {
    id: 'lx-ch11-t-009',
    chapter: 11,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Scheduling a Tasklet',
    prompt: `Your top half wants to defer some quick processing to a tasklet. After you have a struct tasklet_struct set up with its function, what call inside the handler actually asks the kernel to run it, and when does that work execute?`,
    hints: ['tasklet_schedule.', 'It runs as a softirq, shortly after, not inside the handler.'],
    solution: `Inside the top half you call tasklet_schedule(&my_tasklet), which marks the tasklet as pending. The tasklet does not run immediately or inline; the kernel runs pending tasklets a little later as part of softirq processing, typically on return from the interrupt or via ksoftirqd. Scheduling the same tasklet again while it is already pending does not queue a second run; it stays pending once and runs once. Because tasklets execute in softirq (atomic) context, the tasklet function still must not sleep, but it runs with interrupts enabled and off the critical hard-IRQ path.`,
    tags: ['kernel', 'tasklet', 'bottom-half'],
  },
  {
    id: 'lx-ch11-t-010',
    chapter: 11,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Where the dev Cookie Goes',
    prompt: `request_irq takes a final void *dev argument, and free_irq takes the same value. Why does the kernel insist you pass a dev cookie, especially on a shared interrupt line, and where does it show up later?`,
    hints: ['It identifies which registration to remove.', 'It is also handed to your handler.'],
    solution: `The dev cookie is a unique token that identifies your particular registration on a line. On a shared line several drivers register handlers for the same IRQ number, so the IRQ number alone is not enough to tell them apart; when you call free_irq(irq, dev) the kernel uses the dev value to find and remove exactly your handler. The same pointer is also passed to your handler as its second argument, which is how a driver gets at its own device or private data without using globals. For these reasons the cookie must be non-NULL on a shared line and is conventionally the pointer to your driver's private structure.`,
    tags: ['kernel', 'interrupts', 'request_irq'],
  },
  {
    id: 'lx-ch11-t-011',
    chapter: 11,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Interrupt Context vs Process Context',
    prompt: `Two pieces of kernel code do similar work, but one runs in process context and the other in interrupt context. List two concrete capabilities the process-context code has that the interrupt-context code does not.`,
    hints: ['Sleeping and copying to user space.', 'Is there a current process to refer to?'],
    solution: `Process-context code runs on behalf of a task and may sleep: it can take mutexes and semaphores, allocate with GFP_KERNEL, wait on conditions, and call schedule(). Interrupt-context code may do none of those because there is no task to put to sleep. Second, process context has a meaningful current pointer and a valid user address space behind it, so it can call copy_to_user / copy_from_user (which may fault and block); interrupt context has no associated user process, so it must not touch user memory or rely on current. In short, the process-context side can block and reach user space, while the interrupt-context side must stay atomic and self-contained.`,
    tags: ['kernel', 'interrupts', 'context'],
  },
  {
    id: 'lx-ch11-t-012',
    chapter: 11,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Splitting a Network Card Driver',
    prompt: `A network card raises an interrupt every time a packet arrives. Sketch how you would split the handling into a top half and a bottom half: what belongs in each, and why?`,
    hints: ['Acknowledge fast, process later.', 'Protocol processing is heavy.'],
    solution: `The top half should do only the urgent, quick steps: read the device's status register to confirm this card raised the interrupt, acknowledge or clear the interrupt at the hardware, and grab or note the freshly arrived data (or disable further RX interrupts), then schedule the bottom half and return IRQ_HANDLED. The bottom half then does the heavier work: walking the received packets, running protocol processing, and handing buffers up the stack. This keeps the hard-IRQ path short so other interrupts are not delayed, while the bulk processing happens deferred with interrupts enabled. If any step must sleep (rare for RX, but for example a control path that blocks), that part belongs in a workqueue rather than a tasklet or softirq.`,
    tags: ['kernel', 'interrupts', 'bottom-half'],
  },
  {
    id: 'lx-ch11-t-013',
    chapter: 11,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'free_irq at the Right Time',
    prompt: `A driver calls request_irq in its probe/open path. A reviewer warns that forgetting the matching free_irq, or calling it too late, is dangerous. Explain what goes wrong if the IRQ is still registered after the driver's data is gone.`,
    hints: ['The handler might run after teardown.', 'Order matters in module exit.'],
    solution: `If the line is still registered after the driver has freed its private data or unloaded, the device can fire an interrupt and the kernel will call a handler whose code or data no longer exists, leading to a use-after-free or a jump into freed memory and a crash. So free_irq must be called during teardown, and crucially it must run before you free the structures the handler touches. free_irq also waits for any in-progress handler to finish before returning, so after it completes you are guaranteed the handler is not running and will not run again, making it safe to release the device's resources. Pairing request_irq with free_irq in the correct order is part of clean, leak-free driver lifecycle management.`,
    tags: ['kernel', 'interrupts', 'lifecycle'],
  },
  {
    id: 'lx-ch11-t-014',
    chapter: 11,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Sharing Data Between Top Half and Bottom Half',
    prompt: `A top half collects samples into a ring buffer and a tasklet bottom half drains it. Both run on the system at unpredictable times. What synchronization primitive protects the shared buffer, and what subtle issue arises because one accessor is the hard-IRQ handler?`,
    hints: ['Spinlock, not mutex.', 'Disable interrupts on the lock to avoid self-deadlock.'],
    solution: `Because neither side may sleep, you protect the shared ring with a spinlock, not a mutex. The subtle issue is that the top half runs in hard-IRQ context: if the bottom half (or any other process-context code) holds the spinlock with interrupts enabled and the IRQ fires on the same CPU, the handler will spin forever waiting for a lock its own CPU already holds, which is a deadlock. The fix is that any non-IRQ code sharing data with the handler must take the lock with spin_lock_irqsave (and release with spin_unlock_irqrestore), disabling local interrupts while it holds the lock. That way the handler cannot preempt a lock holder on the same CPU, and the buffer stays consistent.`,
    tags: ['kernel', 'interrupts', 'locking'],
  },
  {
    id: 'lx-ch11-t-015',
    chapter: 11,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Why copy_to_user Cannot Live in a Handler',
    prompt: `A developer wants their interrupt handler to push fresh data straight to a waiting user buffer with copy_to_user, to "save a step." Explain two distinct reasons this is fundamentally broken in IRQ context.`,
    hints: ['copy_to_user can fault and sleep.', 'Whose address space is current in a handler?'],
    solution: `First, copy_to_user can page-fault: if the target user page is not present, the kernel must fault it in, which may sleep, and sleeping is forbidden in interrupt context. Second, an interrupt arrives asynchronously with no associated user process; the current task and its user address space are whatever happened to be running when the IRQ fired, so there is no guarantee the intended user buffer is even mapped, and writing to current's address space would target the wrong process. The correct pattern is to have the handler stash the data in a kernel buffer (under a lock) and wake any waiting reader, then let that reader, running in its own process context, perform the copy_to_user safely.`,
    tags: ['kernel', 'interrupts', 'copy_to_user'],
  },
  {
    id: 'lx-ch11-t-016',
    chapter: 11,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Shared Line Etiquette',
    prompt: `Three drivers share IRQ 19 (all registered with IRQF_SHARED). When the line fires, the kernel calls all three handlers in turn. Describe how each handler decides what to return, and what would break if one of them always returned IRQ_HANDLED regardless.`,
    hints: ['Check your own status register first.', 'Lying about IRQ_HANDLED masks spurious interrupts.'],
    solution: `On a shared line, each handler must first read its own device's status register to determine whether that device actually raised this interrupt. If it did, the handler services it and returns IRQ_HANDLED; if not, it must do nothing and return IRQ_NONE. If one driver always returns IRQ_HANDLED regardless, it falsely claims every interrupt on the line. That defeats the kernel's spurious-interrupt detection: a genuinely stuck or unserviced device on the same line would keep re-asserting the interrupt, but because a handler always reports HANDLED, the core never sees an all-NONE pass and never disables the wedged line, so the system can lock up in an interrupt storm. Honest per-device IRQ_NONE / IRQ_HANDLED reporting is what makes sharing safe.`,
    tags: ['kernel', 'interrupts', 'shared-irq'],
  },
  {
    id: 'lx-ch11-t-017',
    chapter: 11,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Threaded IRQ Motivation',
    prompt: `request_threaded_irq lets you supply both a quick "hard" handler and a "thread" handler that runs in its own kernel thread. Why is this useful, and what can the thread handler do that a plain top half or a tasklet cannot?`,
    hints: ['The thread runs in process context.', 'It may sleep.'],
    solution: `request_threaded_irq splits handling into a small hard-IRQ part and a threaded part that the kernel runs in a dedicated process-context kernel thread. The hard handler does the minimal urgent acknowledgement and returns IRQ_WAKE_THREAD to ask the kernel to run the thread function; the thread function then does the heavier work. Because the thread runs in process context, it may sleep: it can take mutexes, do blocking I/O over a slow bus like I2C or SPI, and allocate with GFP_KERNEL, none of which a plain top half or a tasklet (both atomic) can do. This makes threaded IRQs the natural choice for devices whose interrupt servicing needs to talk to a slow, sleepable bus, and it is also the default model under preemptible/realtime configurations.`,
    tags: ['kernel', 'interrupts', 'threaded-irq'],
  },
  {
    id: 'lx-ch11-t-018',
    chapter: 11,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Tasklet That Needs to Sleep',
    prompt: `A driver schedules a tasklet, and inside the tasklet function it calls i2c_transfer to read a sensor over a slow I2C bus, which can block. The system warns about sleeping in atomic context. Diagnose the design error and give the fix.`,
    hints: ['Tasklets run in softirq/atomic context.', 'Blocking bus access needs process context.'],
    solution: `The design error is doing sleepable work in a tasklet: tasklets run in softirq (atomic) context, where sleeping is forbidden, but i2c_transfer can block while the slow bus completes, which trips the "scheduling while atomic / sleeping in invalid context" warning. The fix is to move that work into a process context that is allowed to sleep. Replace the tasklet with a workqueue: schedule a work item from the top half and do the i2c_transfer in the work function, which runs on a kernel worker thread in process context. Alternatively, use a threaded IRQ and perform the I2C access in the thread handler. Either way the sleepable bus access ends up where blocking is legal.`,
    tags: ['kernel', 'tasklet', 'workqueue'],
  },
  {
    id: 'lx-ch11-t-019',
    chapter: 11,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Softirq vs Tasklet Concurrency',
    prompt: `A given tasklet is guaranteed to never run concurrently with itself, even on a multiprocessor machine, while a single softirq handler can run on several CPUs at once. Explain this difference and why it makes tasklets easier to write.`,
    hints: ['Tasklets are serialized per tasklet.', 'Softirqs are inherently SMP-parallel.'],
    solution: `Softirqs are designed for high-performance, SMP-scalable work: the same softirq handler (for example the network RX softirq) can run simultaneously on multiple CPUs, so its code must be fully reentrant and protect every shared structure with its own locks. Tasklets are built on top of softirqs but add a serialization guarantee: a particular tasklet will only ever run on one CPU at a time, so it never races against itself. That makes tasklets easier to write because the tasklet's own private state needs no extra locking against concurrent copies of itself, though it still needs locking against the top half and other contexts that touch shared data. The trade-off is that tasklets give up the per-CPU parallelism that raw softirqs provide.`,
    tags: ['kernel', 'softirq', 'tasklet'],
  },
  {
    id: 'lx-ch11-t-020',
    chapter: 11,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Disabling Interrupts Too Long',
    prompt: `A driver protects a long critical section by calling local_irq_disable(), doing several milliseconds of work, then local_irq_enable(). Even though no lock is held wrong, this is a serious problem. Explain the consequences and the better approach.`,
    hints: ['Interrupts are masked the whole time.', 'Keep IRQ-off regions tiny.'],
    solution: `While interrupts are disabled, the CPU cannot service any hardware interrupt: timer ticks, device IRQs, and reschedules are all held off on that core. Doing several milliseconds of work with interrupts off means devices go unserviced, timers and latency-sensitive events are delayed, packets or input can be lost, and overall system responsiveness collapses; it can also cause watchdog or "irqs disabled too long" warnings. The right approach is to keep any interrupts-disabled region as short as possible: only disable interrupts (or use spin_lock_irqsave) around the brief moment you truly must exclude the handler, and move the bulk of the multi-millisecond work outside the IRQ-off region, deferring sleepable parts to a workqueue. Long work simply does not belong inside an interrupts-disabled section.`,
    tags: ['kernel', 'interrupts', 'latency'],
  },
  {
    id: 'lx-ch11-t-021',
    chapter: 11,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Wake the Reader, Do Not Copy',
    prompt: `A character driver lets a process block in read() until the device's interrupt delivers new data. Describe the clean division of labor: what the interrupt handler does versus what the read() syscall path does, and why the copy happens where it does.`,
    hints: ['Handler stores data and wakes; read() copies.', 'copy_to_user belongs in process context.'],
    solution: `The interrupt handler runs in atomic context, so it does only safe, non-sleeping work: under a spinlock it stores the new data into a kernel buffer, sets a "data ready" flag, and calls wake_up on the wait queue the reader is sleeping on. The read() syscall runs in process context on behalf of the calling task: it waits on that wait queue (sleeping until woken), and once data is available it performs copy_to_user to move it into the user's buffer. The copy must happen in read(), not in the handler, because copy_to_user can fault and sleep and needs the correct user address space, both of which only process context provides. This handler-stores-and-wakes / reader-copies pattern is the standard way to bridge interrupt-driven data to a blocking read.`,
    tags: ['kernel', 'interrupts', 'waitqueue'],
  },
  {
    id: 'lx-ch11-t-022',
    chapter: 11,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Edge vs Level and Acknowledgement',
    prompt: `An edge-triggered interrupt signals on a transition, while a level-triggered interrupt stays asserted as long as the device's condition holds. For a level-triggered device, what must the handler be careful to do, and what happens if it forgets?`,
    hints: ['Clear the condition at the device.', 'Otherwise the line stays asserted.'],
    solution: `For a level-triggered interrupt, the line remains asserted as long as the device still holds its interrupt condition, so the handler must actually clear or acknowledge that condition at the device (for example by reading or writing the relevant status register) before it returns. If it forgets, the device keeps the line asserted; the moment the handler returns, the kernel sees the interrupt is still pending and immediately re-enters the handler, producing an interrupt storm that wedges the CPU. With edge-triggered lines the signal is the transition itself, so a missed clear is less likely to storm but can instead lose the next event if a new edge arrives while you are not watching. The general lesson is that proper device-side acknowledgement is an essential part of every top half.`,
    tags: ['kernel', 'interrupts', 'ack'],
  },
  {
    id: 'lx-ch11-t-023',
    chapter: 11,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'spin_lock vs spin_lock_irqsave Decision',
    prompt: `You have a spinlock protecting data shared between a process-context function and a hard-IRQ handler. A colleague uses plain spin_lock() in the process-context function and spin_lock() in the handler. Walk through the exact scenario that deadlocks, and state the rule for which lock variant each side must use.`,
    hints: ['Same CPU, handler preempts the lock holder.', 'Process side needs irqsave; handler side does not.'],
    solution: `The deadlock: the process-context function takes the spinlock with plain spin_lock(), leaving interrupts enabled. While it holds the lock, the device fires its interrupt on the same CPU; the kernel invokes the hard-IRQ handler, which calls spin_lock() on the same lock. But the lock is held by the very code the handler interrupted on this CPU, and that code cannot make progress until the handler returns, while the handler cannot proceed until the lock is released, so the CPU spins forever. The rule: any code that shares a spinlock with a hard-IRQ handler must, in the non-handler contexts, take it with spin_lock_irqsave (which disables local interrupts while held) so the handler cannot preempt a lock holder on the same CPU. The handler itself can use plain spin_lock because interrupts are already off while it runs; it never needs irqsave for that same line.`,
    tags: ['kernel', 'interrupts', 'locking'],
  },
  {
    id: 'lx-ch11-t-024',
    chapter: 11,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Choosing Among Three Bottom Halves',
    prompt: `For a deferred bottom half you can pick a softirq, a tasklet, or a workqueue. Give a one-line rule for when each is the right tool, and explain why an ordinary driver almost never defines its own softirq.`,
    hints: ['Sleep -> workqueue. Atomic + simple -> tasklet. Atomic + extreme scale -> softirq.', 'Softirqs are a fixed, statically defined set.'],
    solution: `Use a workqueue when the deferred work may sleep (mutexes, blocking I/O, GFP_KERNEL), because it runs in process context. Use a tasklet for short, non-sleeping deferred work that needs low latency and self-serialization, which covers the vast majority of driver bottom halves. Reserve a softirq for code that must run in atomic context AND scale to run concurrently on many CPUs at very high frequency, such as the core networking and block layers. Ordinary drivers almost never define their own softirq because softirqs are a small, fixed, statically registered set baked into the kernel rather than something a module allocates on the fly; they must be fully reentrant with their own fine-grained locking, and tasklets (which are built on softirqs) already give drivers deferred execution without that complexity.`,
    tags: ['kernel', 'softirq', 'workqueue'],
  },
  {
    id: 'lx-ch11-t-025',
    chapter: 11,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Lost Wakeup From a Handler',
    prompt: `A reader sleeps when no data is ready; the interrupt handler sets a flag and calls wake_up. A maintainer worries about a "lost wakeup" race between checking the flag and going to sleep. Describe the race and the standard mechanism that closes it.`,
    hints: ['The wakeup can land between the test and the sleep.', 'Think wait_event / prepare_to_wait ordering.'],
    solution: `The race: the reader checks the "data ready" flag and finds it false, but before it actually puts itself to sleep, the interrupt fires, sets the flag, and calls wake_up on a wait queue where the reader is not yet queued. The wakeup hits nobody, and then the reader finishes going to sleep and waits forever even though data is already available. The standard fix is to add yourself to the wait queue and set your task state to interruptible BEFORE the final flag check, which is exactly what wait_event_interruptible (built on prepare_to_wait) does: it enqueues, sets TASK_INTERRUPTIBLE, tests the condition, and only then schedules away. Because the handler's wake_up sets the queued task back to runnable, any wakeup arriving after enqueue is not lost. The handler's side just needs to set the flag and then call wake_up so the ordering pairs correctly.`,
    tags: ['kernel', 'interrupts', 'waitqueue'],
  },
  {
    id: 'lx-ch11-t-026',
    chapter: 11,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Threaded IRQ vs Workqueue Bottom Half',
    prompt: `Two drivers both need to do sleepable work in response to an interrupt. One uses a hard top half that schedules a workqueue item; the other uses request_threaded_irq with a thread handler. Compare the two designs: what does the threaded IRQ give you that the top-half-plus-workqueue path makes you manage yourself?`,
    hints: ['Threaded IRQ ties the thread to the line, with masking.', 'Workqueue path is more manual but more flexible.'],
    solution: `Both end up doing the sleepable part in process context, but the threaded IRQ integrates that with the IRQ subsystem for you. With request_threaded_irq, the kernel manages a dedicated thread bound to the line, runs your thread function when the hard handler returns IRQ_WAKE_THREAD, and for many controllers automatically masks the interrupt line between the hard handler and the completion of the thread so the device does not keep re-firing while the thread is still working; free_irq also cleanly tears the thread down. With the top-half-plus-workqueue design you must arrange that interrupt masking and re-enabling yourself, handle the case where the interrupt fires again before the work item runs, and manage the work item's lifecycle and flushing at teardown. The threaded IRQ is the cleaner default for "interrupt needs to sleep"; the explicit workqueue path is more manual but gives you full control, for example when you want to batch work, control which workqueue or CPU runs it, or decouple it from the line's lifetime.`,
    tags: ['kernel', 'threaded-irq', 'workqueue'],
  },
  {
    id: 'lx-ch11-t-027',
    chapter: 11,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'free_irq Deadlock From Inside a Handler',
    prompt: `A clever cleanup path calls free_irq() from within the very interrupt handler that is being freed. Explain why this hangs, using what you know about free_irq's behavior with in-flight handlers.`,
    hints: ['free_irq waits for running handlers to finish.', 'You are that running handler.'],
    solution: `free_irq is documented to wait until any currently executing instance of the handler has finished before it removes the registration and returns; this is what guarantees that after free_irq the handler is provably not running. If you call free_irq from inside the handler itself, free_irq will wait for the in-flight handler to complete, but the in-flight handler is the very code that is blocked inside free_irq, so it can never complete. The result is a deadlock: free_irq waits for the handler, and the handler waits for free_irq. The correct pattern is to free the IRQ from a separate context, the driver's remove/exit or release path in process context, never from within the handler that is being torn down.`,
    tags: ['kernel', 'interrupts', 'lifecycle'],
  },
  {
    id: 'lx-ch11-t-028',
    chapter: 11,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Re-Entrancy and Interrupt Nesting',
    prompt: `On modern Linux, a hard-IRQ handler generally runs with interrupts disabled and a given line is masked while its handler runs, so the same handler does not re-enter itself. Given that, why is a shared global counter incremented by both a handler and a process-context function still unsafe without a lock, and how does that shape your locking choice?`,
    hints: ['Different CPUs and different contexts still race.', 'Non-handler side needs irqsave.'],
    solution: `Even if a handler cannot re-enter itself, the handler and the process-context function can still race in two ways: on an SMP machine the handler can run on one CPU while the process function runs on another, and on a single CPU the handler can interrupt the process function between its read and write of the counter. A plain increment is read-modify-write, not atomic, so either race can lose an update or corrupt the value. So you still need synchronization on the shared counter: take a spinlock around the access, and because one of the racing parties is the hard-IRQ handler, the process-context side must use spin_lock_irqsave so the handler cannot preempt it mid-update on the same CPU. (Alternatively, for a simple counter, an atomic_t with atomic_inc avoids the lock entirely since the operation is then indivisible.) The presence of an interrupt-context accessor is exactly what forces the irqsave variant or an atomic type.`,
    tags: ['kernel', 'interrupts', 'locking'],
  },
  {
    id: 'lx-ch11-t-029',
    chapter: 11,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Interrupt Storm Triage',
    prompt: `After loading a driver, the machine becomes sluggish and /proc/interrupts shows one line climbing tens of thousands per second while the device is idle. List the most likely root causes inside the handler and how each produces the storm.`,
    hints: ['Missing or wrong acknowledgement.', 'Shared line returning the wrong irqreturn value.'],
    solution: `The classic cause is failing to acknowledge the device: on a level-triggered line, if the handler returns without clearing the device's interrupt condition, the line stays asserted and the handler is re-entered immediately and endlessly. A related cause is acknowledging the wrong register or doing it in the wrong order so the condition is never actually cleared. On a shared line, a handler that returns IRQ_HANDLED when the interrupt was not its own (or one that genuinely owns the line but does not service it) can keep the kernel from detecting and disabling a wedged source, sustaining the storm. Misconfigured trigger type, for example treating a level-triggered source as edge or vice versa, can also leave the device perpetually signalling. Triage by confirming the handler reads and clears the correct status register, returns IRQ_NONE when the device did not fire, and matches the device's real trigger type; the kernel's own spurious-interrupt detection may also kick in and disable the line, which is the symptom to look for in the logs.`,
    tags: ['kernel', 'interrupts', 'debugging'],
  },
  {
    id: 'lx-ch11-t-030',
    chapter: 11,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Designing the Full Path for a Slow Sensor',
    prompt: `Design the end-to-end interrupt handling for a sensor on an I2C bus: the chip raises an IRQ when a sample is ready, but reading the sample requires a blocking I2C transaction, and a user process blocks in read() to get samples. Lay out top half, deferred work, synchronization, and the read() path, justifying each choice with the rules of interrupt context.`,
    hints: ['I2C read must sleep -> process context.', 'Threaded IRQ or workqueue; spinlock for the buffer; wake the reader.'],
    solution: `Because reading the sample requires a blocking I2C transaction, that part must run in process context, so use request_threaded_irq (or a top half that queues a workqueue item). The hard top half does only atomic-safe work: confirm the chip raised the IRQ, and return IRQ_WAKE_THREAD; it does not touch the I2C bus. The thread handler runs in process context where sleeping is legal, so it performs the blocking i2c_transfer to read the sample, then stores the result into a kernel ring buffer under a spinlock and calls wake_up on the driver's wait queue; if a controller does not auto-mask, you also ensure the line is masked until the thread finishes so the chip does not re-fire mid-read. The shared buffer is guarded by a spinlock taken with spin_lock_irqsave on any side that could race the deferred context, keeping the critical section tiny. The read() syscall runs in its own process context: it sleeps on the wait queue with wait_event_interruptible until data is ready, then copies the sample out with copy_to_user (safe here because read() has a valid user address space and may fault). This split honors every rule: nothing sleeps in the hard IRQ, all blocking I2C and user copying happen in process context, and the only shared state crosses contexts through a properly locked buffer and a wait queue.`,
    tags: ['kernel', 'interrupts', 'design'],
  },
]

export default problems
