import type { Problem } from '../../types'

const problems: Problem[] = [
  {
    id: 'lx-ch10-t-001',
    chapter: 10,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'What a Race Condition Is',
    prompt: `Two threads both run the statement counter++ on a shared global, starting from counter == 0, and you sometimes observe a final value of 1 instead of 2. In plain terms, what is a race condition, and why can counter++ produce a wrong result even though it looks like a single operation?`,
    hints: ['counter++ is read-modify-write, not atomic.', 'Think about interleaving the load and store.'],
    solution: `A race condition is when the correctness of the result depends on the timing or interleaving of two or more concurrent execution paths touching shared state without coordination. The statement counter++ is not one indivisible step: the CPU loads counter into a register, increments the register, then stores it back. If both threads load 0 before either stores, each computes 1 and each stores 1, so one increment is lost and the final value is 1. The window between the load and the store is the unprotected critical section. To fix it you must make the update atomic or serialize access with a lock so the read-modify-write cannot be interleaved.`,
    tags: ['kernel', 'locking', 'race'],
  },
  {
    id: 'lx-ch10-t-002',
    chapter: 10,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'What a Critical Section Is',
    prompt: `A reviewer says "wrap the critical section, not the whole function." What does the term critical section mean, and why is it good practice to keep the locked region as small as the data being protected requires rather than locking large swaths of code?`,
    hints: ['It is the code that touches shared state.', 'Holding a lock too long hurts concurrency.'],
    solution: `A critical section is the region of code that accesses shared data that must not be touched by another execution path at the same time. You protect it with a lock so that only one path executes the section at a time, preserving the data's invariants. Keeping the section small matters because while a lock is held, every other path that wants the same lock must wait, so an oversized locked region serializes work that did not need to be serialized and reduces concurrency. It also reduces the chance of doing something forbidden inside the section (such as sleeping while holding a spinlock) and makes deadlocks easier to reason about. The rule of thumb is: hold the lock only while actually touching the shared data.`,
    tags: ['kernel', 'locking', 'critical-section'],
  },
  {
    id: 'lx-ch10-t-003',
    chapter: 10,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'Why atomic_t Instead of int',
    prompt: `A driver keeps a reference count as a plain int and increments and decrements it from multiple contexts. What is atomic_t, and why is converting that int to an atomic_t (using atomic_inc / atomic_dec) the right fix instead of just declaring it volatile?`,
    hints: ['Atomic ops are indivisible read-modify-write.', 'volatile does not make ++ atomic.'],
    solution: `atomic_t is a kernel type that wraps an integer so it can be updated with operations (atomic_inc, atomic_dec, atomic_add, atomic_read, atomic_set, etc.) that the hardware guarantees to perform as a single indivisible read-modify-write. A plain int incremented from several contexts suffers the lost-update race because ++ is load-modify-store. Marking it volatile only tells the compiler not to cache the value in a register; it does nothing to make the increment indivisible, so concurrent updates still corrupt it. Using atomic_t with the atomic_* helpers gives a correct, lock-free counter for simple integer updates, which is exactly what a reference count needs. For anything more complex than single-variable updates you would need a real lock.`,
    tags: ['kernel', 'locking', 'atomic'],
  },
  {
    id: 'lx-ch10-t-004',
    chapter: 10,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'Spinlock vs Mutex In One Sentence',
    prompt: `At the highest level, a spinlock and a mutex both provide mutual exclusion but differ in what a waiter does when the lock is taken. Describe that core behavioral difference, and state the single most important rule it implies about where each may be used.`,
    hints: ['One spins (busy-waits), one sleeps.', 'Sleeping is forbidden in atomic context.'],
    solution: `A spinlock makes a waiting CPU busy-wait (spin) in a tight loop until the lock becomes free, never giving up the processor. A mutex instead puts the waiting task to sleep and lets the scheduler run something else, waking it when the lock is released. The key implication is context: because a spinlock never sleeps, it is the lock you must use in atomic contexts such as interrupt handlers, while a mutex may only be acquired where sleeping is allowed (process context). Spinlocks suit very short critical sections; mutexes suit longer or sleepable ones. Choosing the wrong one either wastes CPU or, worse, sleeps where sleeping is illegal.`,
    tags: ['kernel', 'locking', 'spinlock', 'mutex'],
  },
  {
    id: 'lx-ch10-t-005',
    chapter: 10,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Spot The Lost Update',
    prompt: `A kernel module has: static int total; and two functions called concurrently, one doing total += amount; and another doing total -= amount;. The maintainer insists "addition and subtraction are simple, no lock needed." Explain precisely why this is unsafe and give two acceptable fixes.`,
    hints: ['+= and -= are read-modify-write.', 'atomic_t or a spinlock both work.'],
    solution: `Both total += amount and total -= amount compile to load total, compute, store total, which is a non-atomic read-modify-write. If the two paths interleave their loads and stores, one update overwrites the other and the net change is wrong, exactly the lost-update race. Simplicity of the arithmetic is irrelevant; it is the lack of atomicity that breaks it. One fix is to make total an atomic_t and use atomic_add(amount, &total) and atomic_sub(amount, &total), which perform the update indivisibly. The other is to guard both accesses with the same spinlock (spin_lock/spin_unlock) so only one path is in the critical section at a time. Either approach serializes the conflicting updates correctly.`,
    tags: ['kernel', 'locking', 'race', 'atomic'],
  },
  {
    id: 'lx-ch10-t-006',
    chapter: 10,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'atomic_read Then atomic_set Is Not Atomic',
    prompt: `Someone writes: if (atomic_read(&x) == 0) atomic_set(&x, 1); to "atomically claim" a flag. Each call is atomic, so why is this sequence still racy, and which single API solves it correctly?`,
    hints: ['Two atomic ops are not one atomic op.', 'Think compare-and-swap.'],
    solution: `atomic_read and atomic_set are each individually atomic, but the test-then-set spans two separate operations with a gap in between. Two CPUs can both read 0, both see the condition true, and both proceed to set 1 (or do whatever the claim implies), so the mutual exclusion you wanted does not happen. The race lives in the window between the read and the set. The correct primitive is a single atomic compare-and-swap, atomic_cmpxchg(&x, 0, 1), which atomically checks that x is 0 and, only if so, stores 1, returning the old value so you know whether you won. For a "claim once" pattern, atomic_cmpxchg (or test_and_set_bit for a bit flag) does the whole test-and-set indivisibly.`,
    tags: ['kernel', 'locking', 'atomic', 'cmpxchg'],
  },
  {
    id: 'lx-ch10-t-007',
    chapter: 10,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Always Unlock On Every Path',
    prompt: `A function does spin_lock(&lock); then has an error path that returns -ENOMEM without calling spin_unlock, while the success path unlocks correctly. Describe concretely what goes wrong at runtime, and state the discipline that prevents this class of bug.`,
    hints: ['The lock is left held forever.', 'Every exit path must release.'],
    solution: `If the error path returns while the lock is still held, the spinlock is never released, so the next CPU that tries to acquire it spins forever (with interrupts possibly disabled), effectively hanging that core and anything waiting behind it. This is a classic lock leak. The discipline is that every exit path out of a locked region must unlock exactly once and in the correct order, which is why kernel code commonly uses a single goto-style cleanup label (for example goto out_unlock;) so all returns funnel through one place that releases the lock. Keeping critical sections small and having one unlock point makes it much harder to forget a path. Tools like lockdep can also catch some held-lock-on-return mistakes.`,
    tags: ['kernel', 'locking', 'spinlock', 'cleanup'],
  },
  {
    id: 'lx-ch10-t-008',
    chapter: 10,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'When To Reach For A Mutex',
    prompt: `You are protecting a critical section that calls kmalloc(size, GFP_KERNEL) and copy_from_user(), both of which may block. Which lock type is appropriate here, a spinlock or a mutex, and what is the deciding factor?`,
    hints: ['Can the operations sleep?', 'GFP_KERNEL and copy_from_user can block.'],
    solution: `A mutex is the right choice because the critical section can sleep: kmalloc with GFP_KERNEL may block to reclaim memory, and copy_from_user can fault and sleep while a page is brought in. The deciding factor is whether the code inside the lock can sleep; if it can, you must use a sleepable lock. A spinlock forbids sleeping while held, so doing either of these operations under a spinlock would be a bug that can deadlock or trigger a "scheduling while atomic" / "sleeping function called from invalid context" warning. Since this runs in process context and may block, the mutex is correct and also more CPU-friendly because waiters sleep instead of spinning.`,
    tags: ['kernel', 'locking', 'mutex', 'sleep'],
  },
  {
    id: 'lx-ch10-t-009',
    chapter: 10,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Same Lock Or No Protection',
    prompt: `A structure has two fields guarded by two different spinlocks, lock_a and lock_b. Writer 1 takes lock_a to update field a, and writer 2 takes lock_b to update field b. A third path updates a while holding only lock_b. Why is that third path broken?`,
    hints: ['Mutual exclusion needs the SAME lock.', 'Field a is protected by lock_a.'],
    solution: `A lock only provides mutual exclusion among paths that acquire that same lock instance. Field a's invariant is upheld by everyone agreeing to hold lock_a before touching a. The third path touches a while holding lock_b, a different lock, so it does not exclude writer 1 (which holds lock_a). Writer 1 and the third path can therefore modify a at the same time, producing the very race the locking was supposed to prevent. The fix is to consistently associate each piece of data with exactly one lock and always take that lock before accessing the data; holding a different lock gives no protection at all.`,
    tags: ['kernel', 'locking', 'spinlock', 'discipline'],
  },
  {
    id: 'lx-ch10-t-010',
    chapter: 10,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'rwlock For Read-Mostly Data',
    prompt: `A routing table is read on nearly every packet but updated only rarely. Compare protecting it with a plain spinlock versus a read/write lock (rwlock). What does the rwlock buy you, and what is its main downside?`,
    hints: ['Multiple readers can hold a read lock.', 'Writers still need exclusivity.'],
    solution: `A plain spinlock serializes everything: only one CPU at a time may be inside, even two readers that do not conflict. An rwlock distinguishes read_lock from write_lock, allowing many readers to hold the lock concurrently while writers still get exclusive access, which is a big win for read-mostly data like a routing table since the common case (reads) no longer blocks each other. The downsides are that rwlocks have more overhead than a simple spinlock, and naive rwlocks can starve writers if readers keep arriving. In practice, for read-mostly hot paths the kernel often prefers RCU or seqlocks over rwlocks, but rwlock is still the straightforward "many readers, rare writer" tool.`,
    tags: ['kernel', 'locking', 'rwlock'],
  },
  {
    id: 'lx-ch10-t-011',
    chapter: 10,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Atomic Counter vs Spinlock-Guarded int',
    prompt: `For a simple statistics counter incremented from many CPUs, compare using an atomic_t with atomic_inc against a plain int guarded by a spinlock. When is the atomic clearly preferable, and is there any case where the spinlock approach is still justified?`,
    hints: ['Single-variable update fits atomic_t.', 'A lock is needed for multi-field invariants.'],
    solution: `For a single counter, atomic_inc(&c) is preferable: it is one indivisible instruction with no lock to acquire/release, so it is lower overhead and cannot leak a held lock. A spinlock-guarded int adds the cost of lock/unlock around what is fundamentally a one-variable update, which is overkill. The spinlock approach becomes justified when the counter is not updated in isolation but as part of a larger invariant, for example when you must increment the counter and update another field together, or read several fields consistently. In that case a single atomic_inc cannot span multiple variables, so you need a lock to make the whole multi-field update one critical section.`,
    tags: ['kernel', 'locking', 'atomic', 'spinlock'],
  },
  {
    id: 'lx-ch10-t-012',
    chapter: 10,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'spin_lock_init Before First Use',
    prompt: `A dynamically allocated struct contains a spinlock_t. The driver kzalloc()s the struct and immediately calls spin_lock() on it. The author thinks zeroed memory is "good enough" to initialize a spinlock. Why is this fragile, and what is the correct step?`,
    hints: ['Locks have internal state beyond zero.', 'spin_lock_init exists for a reason.'],
    solution: `A spinlock_t is not just a zero/one flag: depending on configuration it carries extra fields such as lockdep tracking, debugging magic, and owner information. Relying on zeroed memory may happen to work in a minimal build but breaks debugging and validation, and is not guaranteed to leave the lock in a usable state. The correct step is to call spin_lock_init(&s->lock) after allocation and before any use, which sets up all internal fields properly. Statically defined locks instead use DEFINE_SPINLOCK(name) at declaration. The same principle applies to mutex_init for mutexes. Initialize the lock explicitly; do not assume zero-init suffices.`,
    tags: ['kernel', 'locking', 'spinlock', 'init'],
  },
  {
    id: 'lx-ch10-t-013',
    chapter: 10,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Mutex Owner Must Unlock',
    prompt: `Process A calls mutex_lock(&m), and the design intends for process B to later call mutex_unlock(&m) to release it. Why does this "lock in one task, unlock in another" pattern violate how mutexes are meant to be used, and what primitive fits a producer/consumer hand-off instead?`,
    hints: ['A mutex has an owner.', 'Semaphores or completions allow cross-task signaling.'],
    solution: `A mutex is an ownership lock: the task that locks it is recorded as the owner and is expected to be the one that unlocks it. Unlocking from a different task breaks that invariant, defeats lockdep and deadlock detection, and is explicitly not the intended use (the kernel even warns about it). Mutexes are for mutual exclusion of a critical section, not for signaling between tasks. For a producer/consumer hand-off where one task signals another, the right tools are a semaphore (which has no owner and supports up/down across tasks) or a completion. Use a mutex to protect data; use a semaphore/completion to signal events between contexts.`,
    tags: ['kernel', 'locking', 'mutex', 'semaphore'],
  },
  {
    id: 'lx-ch10-t-014',
    chapter: 10,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Why You Cannot Sleep Holding A Spinlock',
    prompt: `Explain mechanistically why sleeping while holding a spinlock is forbidden. Walk through what a spinlock does to the CPU/preemption and what would happen if the holder were scheduled out while another CPU spins waiting for the same lock.`,
    hints: ['spin_lock disables preemption.', 'A sleeping holder cannot release; spinners cannot sleep.'],
    solution: `When you take a spinlock the kernel disables preemption on that CPU (and with the irqsave variant, local interrupts too), so the holder is not supposed to be scheduled away. Sleeping means calling the scheduler to run another task, which both violates the disabled-preemption state and leaves the lock held by a task that is no longer running. Meanwhile any other CPU wanting the lock is busy-waiting in a tight loop, burning cycles and unable to make progress because spinning never yields. If the sleeping holder needs that spinning CPU's resources to wake up, you can deadlock; on a uniprocessor it is even worse since the spinner can never be preempted to let the holder run. Because the holder must release the lock promptly and waiters cannot sleep, the section under a spinlock must itself never sleep (no GFP_KERNEL allocations, no copy_to_user, no mutex_lock).`,
    tags: ['kernel', 'locking', 'spinlock', 'atomic'],
  },
  {
    id: 'lx-ch10-t-015',
    chapter: 10,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'spin_lock_irqsave In Shared Data',
    prompt: `A shared queue is touched both by process-context code and by an interrupt handler. The process-context code uses plain spin_lock(&q->lock). Explain the deadlock scenario this allows, and why spin_lock_irqsave is required on the process-context side.`,
    hints: ['The IRQ can fire on the same CPU mid-section.', 'The handler then spins for a lock the interrupted code holds.'],
    solution: `Suppose process-context code on CPU 0 takes the lock with plain spin_lock and is in the middle of the critical section. The device interrupt fires on CPU 0, preempting that code, and the interrupt handler tries to take the same lock. The handler spins waiting for the lock, but the only holder is the interrupted process-context code on the very same CPU, which cannot resume until the handler returns. That is a self-deadlock. The fix is for the process-context side to use spin_lock_irqsave, which disables local interrupts while holding the lock so the handler cannot fire on that CPU mid-section. (The interrupt handler itself can use plain spin_lock since interrupts are already off there.) Whenever a lock is shared with an interrupt handler, the non-interrupt side must mask interrupts via the irqsave variant.`,
    tags: ['kernel', 'locking', 'spinlock', 'interrupt'],
  },
  {
    id: 'lx-ch10-t-016',
    chapter: 10,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'irqsave vs irq vs Plain spin_lock',
    prompt: `Compare spin_lock, spin_lock_irq, and spin_lock_irqsave. Under what conditions is each correct, and why is irqsave the safest default when you are not certain whether interrupts are already disabled?`,
    hints: ['irq unconditionally re-enables on unlock.', 'irqsave saves and restores prior state.'],
    solution: `spin_lock just acquires the lock and disables preemption; use it when the lock is never taken from an interrupt handler. spin_lock_irq additionally disables local interrupts and, on unlock with spin_unlock_irq, unconditionally re-enables them; it is correct only when you know interrupts were enabled on entry, because it will wrongly turn them on otherwise. spin_lock_irqsave saves the current interrupt state into a flags variable, disables interrupts, and spin_unlock_irqrestore puts the exact prior state back, so it is safe even if interrupts were already disabled (for example you are already inside an interrupt handler or nested under another irqsave). Because it never clobbers the caller's interrupt state, irqsave is the safe default when the calling context's interrupt state is unknown; the only cost is passing the flags variable.`,
    tags: ['kernel', 'locking', 'spinlock', 'interrupt'],
  },
  {
    id: 'lx-ch10-t-017',
    chapter: 10,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Lock Ordering And Deadlock',
    prompt: `Path 1 takes lock A then lock B; path 2 takes lock B then lock A. The code "works" most of the time but occasionally hangs. Explain the AB-BA deadlock and state the standard rule that prevents it.`,
    hints: ['Each holds what the other needs.', 'Always acquire locks in a fixed global order.'],
    solution: `This is the classic AB-BA deadlock. If path 1 grabs A and, at the same time, path 2 grabs B, then path 1 blocks waiting for B (held by path 2) while path 2 blocks waiting for A (held by path 1). Neither can proceed and neither will release, so both hang; it only happens when the two paths interleave at just the wrong moment, which is why it is intermittent. The standard prevention is lock ordering: define a single global order for acquiring locks (say always A before B) and have every path obey it, so a cycle can never form. The kernel's lockdep validator detects inconsistent acquisition orders and warns about potential AB-BA deadlocks even before one actually occurs.`,
    tags: ['kernel', 'locking', 'deadlock', 'ordering'],
  },
  {
    id: 'lx-ch10-t-018',
    chapter: 10,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'RCU Read-Side Basics',
    prompt: `Describe what happens on the RCU read side: what rcu_read_lock()/rcu_read_unlock() actually do, why readers are so cheap, and what rcu_dereference() is for. Also state the one big restriction on RCU read-side critical sections.`,
    hints: ['Readers take no real lock and do not block writers.', 'No sleeping inside a classic RCU read section.'],
    solution: `On the read side, rcu_read_lock() and rcu_read_unlock() mark the boundaries of a read-side critical section but take no actual lock and contend with nothing; in classic RCU they essentially disable preemption, so readers are extremely cheap and never block writers or each other. rcu_dereference() is used to load an RCU-protected pointer; it fetches the pointer with the memory ordering guarantees that ensure you see fully initialized data the publisher set up with rcu_assign_pointer(). The mechanism lets readers proceed lock-free while a writer publishes a new version and defers freeing the old one until all pre-existing readers have finished (a grace period). The big restriction is that a classic RCU read-side critical section must not sleep or block, because the grace-period machinery assumes readers complete promptly; if you need to sleep, you need a sleepable variant rather than classic RCU.`,
    tags: ['kernel', 'locking', 'rcu'],
  },
  {
    id: 'lx-ch10-t-019',
    chapter: 10,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'RCU Update: Publish Then Defer Free',
    prompt: `An RCU-protected linked list lets readers traverse without locking. A writer wants to delete a node. Explain why the writer must not kfree the node immediately after unlinking it, and what mechanism lets it free the node safely.`,
    hints: ['A reader may still hold a pointer to the node.', 'Wait for a grace period before freeing.'],
    solution: `RCU readers traverse the list with no lock, so at the moment the writer unlinks a node from the list, one or more readers may already hold a pointer into that node and still be dereferencing it. If the writer kfrees the node immediately, those readers would touch freed memory, a use-after-free. The RCU rule is publish/unlink then defer the free: after unlinking (so no new reader can reach the node), the writer waits for a grace period to elapse, the point by which every reader that could have held a reference has finished its rcu_read_lock/unlock section. It does this with synchronize_rcu() (blocking) or call_rcu()/kfree_rcu() (asynchronous callback). Only after the grace period is it safe to free, because no reader can still be looking at the node.`,
    tags: ['kernel', 'locking', 'rcu', 'grace-period'],
  },
  {
    id: 'lx-ch10-t-020',
    chapter: 10,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Per-CPU Variables Reduce Contention',
    prompt: `A high-frequency statistics counter is a global atomic_t and shows up as a hot cache line under load on a many-core machine. Explain how converting it to a per-CPU variable removes the contention, and what you must be careful about when reading the total and when accessing the per-CPU value.`,
    hints: ['Each CPU updates its own copy, no sharing.', 'Use get_cpu_var/this_cpu and sum on read.'],
    solution: `A single global atomic_t means every CPU's increment hammers the same cache line, bouncing it between cores (cache-line ping-pong) and serializing what should be independent updates. A per-CPU variable gives each CPU its own private instance, so a CPU increments only its own copy with no cross-CPU sharing and no contention; the updates need no lock because no other CPU touches that instance. Two cautions: when accessing the local instance you must prevent the task from migrating mid-update, which is why this_cpu_inc() or get_cpu_var()/put_cpu_var() (the latter disabling preemption) are used so you operate on a stable CPU's copy. When you read the overall total you must sum across all CPUs (for_each_possible_cpu), and that sum is only an approximate snapshot since other CPUs may be updating concurrently. Per-CPU is ideal for counters where exact instantaneous totals are not required.`,
    tags: ['kernel', 'locking', 'percpu', 'contention'],
  },
  {
    id: 'lx-ch10-t-021',
    chapter: 10,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Choose The Lock: Four Scenarios',
    prompt: `For each scenario, name the most appropriate synchronization primitive and justify in one line: (1) bumping a refcount; (2) a short critical section shared with a hardware IRQ handler; (3) a long critical section in a syscall path that calls kmalloc(GFP_KERNEL); (4) a read-mostly lookup table on a hot path.`,
    hints: ['Match context and duration to the primitive.', 'Sleepable vs atomic; reader-heavy vs balanced.'],
    solution: `(1) Refcount: an atomic_t with atomic_inc/atomic_dec (or refcount_t), since it is a single-variable update that needs no lock. (2) Short section shared with an IRQ handler: a spinlock, and the non-interrupt side must use spin_lock_irqsave so the handler cannot deadlock by firing mid-section on the same CPU. (3) Long sleepable section that allocates with GFP_KERNEL: a mutex, because the code can sleep and a spinlock forbids sleeping while held. (4) Read-mostly hot-path lookup: RCU (readers lock-free, writers defer frees), or a seqlock/rwlock if RCU is too heavy; the point is to let the many readers proceed without serializing on each other. The decision hinges on context (can it sleep?), duration (short vs long), and the read/write ratio.`,
    tags: ['kernel', 'locking', 'design'],
  },
  {
    id: 'lx-ch10-t-022',
    chapter: 10,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Semaphore Counting vs Binary',
    prompt: `Contrast a counting semaphore with a mutex. Give a concrete use case where a counting semaphore (initialized to N > 1) is the right tool and a mutex is not, and explain what the count represents.`,
    hints: ['A mutex allows one holder; a semaphore allows N.', 'Think pools of identical resources.'],
    solution: `A mutex enforces strictly one holder at a time and has an owner that must release it, so it models exclusive access to one resource. A counting semaphore allows up to N holders simultaneously: its count represents the number of available units of an interchangeable resource, where down() takes a unit (sleeping if none are free) and up() returns one. The right use case is a pool of N identical resources, for example N DMA buffers or N slots in a bounded queue: initialize the semaphore to N so up to N tasks proceed concurrently and the N+1th blocks until one is released. A mutex cannot express "up to N at once," and a semaphore also permits the up() to come from a different context than the down(), which fits producer/consumer signaling. Use a mutex for exclusive critical sections, a counting semaphore for limited multi-unit resources.`,
    tags: ['kernel', 'locking', 'semaphore', 'mutex'],
  },
  {
    id: 'lx-ch10-t-023',
    chapter: 10,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Why Memory Barriers Are Needed',
    prompt: `A producer writes data into a buffer, then sets a ready flag; a consumer spins until it sees the flag, then reads the buffer. Even with both accesses correct individually, the consumer sometimes reads stale buffer contents. Explain the reordering problem and how memory barriers (smp_wmb / smp_rmb) fix it.`,
    hints: ['CPUs/compilers may reorder independent stores and loads.', 'Pair a write barrier with a read barrier.'],
    solution: `The producer's two stores (fill buffer, then set flag) are independent as far as the CPU and compiler can tell, so they may be reordered or become visible to other CPUs out of order; the consumer might observe the flag set before the buffer writes have propagated, reading stale data. Symmetrically, the consumer's load of the flag and its subsequent loads of the buffer can be reordered. Memory barriers enforce ordering: the producer issues smp_wmb() between writing the buffer and setting the flag so the buffer stores are globally visible before the flag store. The consumer issues smp_rmb() after seeing the flag and before reading the buffer so it does not load the buffer early. Write and read barriers must be paired to be meaningful. (In real code you would more likely use smp_store_release / smp_load_acquire or an existing primitive, but the barrier reasoning is the same: prevent the flag from overtaking the data.)`,
    tags: ['kernel', 'locking', 'memory-barrier', 'ordering'],
  },
  {
    id: 'lx-ch10-t-024',
    chapter: 10,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Check-Then-Act Across The Lock Boundary',
    prompt: `Code does: spin_lock; p = find_item(list); spin_unlock; ... use(p);. Under the same lock, another path can remove and kfree the item. Explain why dropping the lock before using p is unsafe, and contrast two correct fixes (hold the lock longer vs reference counting).`,
    hints: ['p can be freed in the gap after unlock.', 'Either widen the critical section or pin with a refcount.'],
    solution: `The lookup is done under the lock, but the pointer p is used after the lock is dropped. In that gap, the other path can acquire the lock, unlink the item, and kfree it, so when this code dereferences p it touches freed memory, a use-after-free. The lock protected only the lookup, not the lifetime of the object beyond the unlock. Fix one: extend the critical section so you both find and finish using p while still holding the lock, guaranteeing no one frees it meanwhile; this is simplest but holds the lock longer and forbids sleeping if it is a spinlock. Fix two: while holding the lock, take a reference (kref_get / atomic_inc on the object's refcount) before unlocking, so the object cannot be freed until you drop your reference; then use p outside the lock and put the reference when done. Reference counting decouples object lifetime from the lock and is the standard pattern when you must use the object after releasing the lock (and the latter even works with sleepable use). RCU is a third option for read-mostly lookups.`,
    tags: ['kernel', 'locking', 'use-after-free', 'refcount'],
  },
  {
    id: 'lx-ch10-t-025',
    chapter: 10,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Hidden Sleep Under A Spinlock',
    prompt: `A critical section guarded by a spin_lock calls a helper that "looks innocent" but internally does kmalloc(size, GFP_KERNEL). On a debug kernel this triggers a "BUG: sleeping function called from invalid context" splat. Explain the chain of causation, and give two ways to fix it.`,
    hints: ['GFP_KERNEL may sleep to reclaim memory.', 'Use GFP_ATOMIC or restructure to not allocate under the lock.'],
    solution: `Holding a spinlock disables preemption (and the code is treated as atomic context), and the cardinal rule is that you may not sleep in atomic context. The helper allocates with GFP_KERNEL, which permits the allocator to block and reclaim memory, i.e. it can sleep. So the call chain reaches a potential sleep while a spinlock is held, which the debug check detects and reports as "sleeping function called from invalid context." Fix one: do not allocate under the lock. Allocate the memory before taking the spinlock (or after releasing it) and only do the pointer manipulation inside the critical section. Fix two: if you truly must allocate inside the section, use GFP_ATOMIC, which tells the allocator not to sleep (it may fail instead, so you must handle a NULL return); GFP_ATOMIC is the allocation flag for atomic contexts like this and interrupt handlers. The cleaner approach is usually to restructure so no sleeping operation runs under the spinlock.`,
    tags: ['kernel', 'locking', 'gfp', 'atomic'],
  },
  {
    id: 'lx-ch10-t-026',
    chapter: 10,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Seqlock For A Wide Read-Mostly Value',
    prompt: `A 64-bit timestamp pair is updated rarely by one writer and read constantly by many readers on a hot path. A seqlock is proposed. Explain how a seqlock read works (the retry loop), why readers never block writers, and the key constraint on what reader code may do.`,
    hints: ['Read a sequence counter before and after.', 'Retry if it changed or was odd; reader data must be re-readable.'],
    solution: `A seqlock pairs the data with an even/odd sequence counter. A writer increments the sequence to odd before updating, writes the data, then increments to even when done (writers among themselves still use a spinlock). A reader does read_seqbegin to snapshot the sequence, reads the data optimistically without taking any lock, then read_seqretry checks whether the sequence is unchanged and even; if a writer was active (odd) or the count changed, the reader simply loops and retries. Because readers take no lock, they never block the writer, which makes seqlocks great for write-rare, read-hot, small-but-multi-word values like timestamps where torn reads must be avoided. The key constraint is that reader code inside the retry loop must be safe to run on possibly inconsistent/torn data and be freely repeatable: it must not have side effects, must not dereference pointers that the writer could have changed to point at freed memory, and ideally just copies primitive values into locals. Seqlocks can starve readers under a constant write storm, so they fit the write-rare case.`,
    tags: ['kernel', 'locking', 'seqlock'],
  },
  {
    id: 'lx-ch10-t-027',
    chapter: 10,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'What Lockdep Actually Catches',
    prompt: `A maintainer says "it passes lockdep" as evidence the locking is correct. Explain what lockdep is, the categories of bugs it can detect (including ones that have not yet caused a failure), and at least one real locking bug class it cannot catch.`,
    hints: ['It validates lock-ordering and context rules at runtime.', 'It cannot detect missing locks or pure data races.'],
    solution: `Lockdep is the kernel's runtime lock dependency validator. As locks are acquired it builds a graph of observed lock-ordering relationships and checks each new acquisition against the rules. It catches inconsistent lock ordering that could cause AB-BA deadlocks even if the deadlock has not actually happened yet (it sees A-before-B in one place and B-before-A in another and warns). It also catches taking a lock that is also used in IRQ context without disabling interrupts, recursive/self-deadlock on a non-recursive lock, sleeping while holding a spinlock, and freeing or exiting with a lock still held. What it cannot catch is the absence of locking: if a programmer simply forgets to take any lock around shared data, lockdep sees nothing to validate, so it does not detect missing locks or pure data races on unlocked memory. (Tools like KCSAN target data races; lockdep targets lock-usage correctness.) So "passes lockdep" means the locks you do take are used consistently, not that you locked everything you should have.`,
    tags: ['kernel', 'locking', 'lockdep', 'debugging'],
  },
  {
    id: 'lx-ch10-t-028',
    chapter: 10,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'RCU vs rwlock For The Same Workload',
    prompt: `Both rwlock and RCU support many readers with rare writers. For a read-dominated hot path, argue why RCU usually outperforms an rwlock, and identify a property of the readers that RCU requires which makes it unsuitable in some cases.`,
    hints: ['rwlock readers still write a shared counter.', 'RCU readers must not block; updates pay a grace-period cost.'],
    solution: `An rwlock read_lock still performs an atomic update on a shared counter to track readers, so every reader writes to the same cache line and contends, which on a many-core read-hot path becomes a scalability bottleneck even though readers do not exclude each other logically. RCU readers, by contrast, do essentially nothing on the read side (mark a section, often just preemption boundaries) and never write shared state, so reads scale almost perfectly and never block writers. The cost shifts to the writer, which must publish a new version and wait out a grace period before freeing the old one. The property RCU demands is that readers be short and non-blocking (classic RCU read sections cannot sleep) and that the data structure be amenable to read-copy-update style publication of immutable snapshots. If readers need to sleep, or the structure cannot be updated by atomic pointer publication, plain RCU does not fit and an rwlock (or a sleepable RCU variant) may be required instead. So RCU wins on read scalability when its readers-must-not-block constraint is acceptable.`,
    tags: ['kernel', 'locking', 'rcu', 'rwlock'],
  },
  {
    id: 'lx-ch10-t-029',
    chapter: 10,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Two Atomics Do Not Make An Invariant',
    prompt: `A free-list allocator keeps atomic_t free_count and a separately-locked list head. Code does: if (atomic_read(&free_count) > 0) { take_node(); atomic_dec(&free_count); }. Even though every individual operation is correct, the allocator occasionally underflows or hands out a node that is gone. Explain the flaw and how to fix it.`,
    hints: ['The count and the list are two pieces of one invariant.', 'They must change together under one lock.'],
    solution: `The bug is that free_count and the list are two parts of a single invariant (count must equal the number of nodes), but they are protected separately, so the combined operation "check there is a node, then take it and decrement" is not atomic as a whole. Between the atomic_read returning a positive value and the take/dec, another CPU can take the last node, so this path proceeds to take_node() on an empty or shrinking list and decrements free_count below zero (underflow) or hands out a node that was already removed. Each atomic op being individually correct does not make the multi-step, multi-object transaction correct. The fix is to put the count and the list under the same lock and perform the check-and-update as one critical section: take the lock, verify a node exists (or just try to pop), update both the list and the count together, then unlock. Once both fields move together atomically the invariant holds; an atomic_t is fine for an independent counter but cannot coordinate with a second data structure on its own.`,
    tags: ['kernel', 'locking', 'invariant', 'atomic'],
  },
  {
    id: 'lx-ch10-t-030',
    chapter: 10,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Per-CPU Plus Preemption Equals Subtle Bug',
    prompt: `Code reads a pointer to a per-CPU buffer with this_cpu_ptr(&buf) outside any preempt-disabled region, then does a long operation writing through that pointer, including a call that may sleep. Describe the two distinct things that can go wrong, and what the correct access discipline is.`,
    hints: ['The task can migrate to another CPU mid-operation.', 'this_cpu_ptr requires preemption disabled; sleeping forbids holding it across.'],
    solution: `this_cpu_ptr returns the address of the current CPU's instance, but it is only valid as long as the task stays on that CPU. Two things can go wrong. First, if preemption is enabled, the scheduler can migrate the task to a different CPU after the pointer is computed; the code then keeps writing through a pointer to the original CPU's buffer while actually running on another CPU, corrupting the wrong CPU's data and racing with that other CPU's legitimate user. Second, the operation calls a function that may sleep, which both implies a scheduling point (another chance to migrate) and means you are relying on per-CPU locality across a sleep, which is unsound. The correct discipline is to bracket per-CPU access so the task cannot migrate during use: use get_cpu_ptr()/put_cpu_ptr() or this_cpu_* accessors, which disable preemption around the access, and keep that region short and non-sleeping. If you need a stable per-CPU resource across a sleepable operation, you must pin to the CPU another way or redesign so the long/sleeping work does not depend on staying on one CPU. In short: do not hold a raw this_cpu_ptr across a preemptible or sleeping window.`,
    tags: ['kernel', 'locking', 'percpu', 'preemption'],
  },
]

export default problems
