import type { Problem } from '../../types'

const problems: Problem[] = [
  {
    id: 'lx-ch12-t-001',
    chapter: 12,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'What HZ Means',
    prompt: `Your kernel is built with CONFIG_HZ=250. A coworker says "jiffies counts microseconds." Is that right? Explain what HZ is, what one jiffy represents on this build, and how many times per second the jiffies counter is incremented.`,
    hints: [
      'HZ is the timer interrupt frequency.',
      'One jiffy is the period between two ticks.',
    ],
    solution: `No, jiffies does not count microseconds. HZ is the number of timer ticks per second, so with CONFIG_HZ=250 the kernel takes 250 ticks each second and the global jiffies counter is incremented 250 times per second. One jiffy is therefore the time between two ticks, which is 1/250 of a second, or 4 milliseconds, on this build. The exact duration of a jiffy is build-dependent (common values are 100, 250, 300, or 1000), which is precisely why code should use HZ rather than hardcoding a number. So a jiffy is a coarse, configurable unit, not a fixed microsecond.`,
    tags: ['kernel', 'jiffies', 'hz'],
  },
  {
    id: 'lx-ch12-t-002',
    chapter: 12,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'Convert Milliseconds To Jiffies',
    prompt: `You want a timer to fire roughly 500 ms in the future. You write 'timeout = jiffies + 500;'. Why is hardcoding 500 wrong, and what helper should you use instead so the code is correct regardless of HZ?`,
    hints: [
      'jiffies advances at HZ per second, not at 1000 per second.',
      'There is a standard conversion helper.',
    ],
    solution: `Adding a literal 500 to jiffies only means "500 ms" if HZ happens to be 1000; on a HZ=250 build, 500 jiffies is 2 seconds, and on HZ=100 it is 5 seconds. Jiffies advance at HZ ticks per second, so the number of jiffies in a time interval depends on HZ. The correct, portable approach is to convert the millisecond value with msecs_to_jiffies(500), giving 'timeout = jiffies + msecs_to_jiffies(500);'. There is also usecs_to_jiffies() and the reverse jiffies_to_msecs(). Using these helpers keeps the delay constant in real time across every HZ configuration.`,
    tags: ['kernel', 'jiffies', 'conversion'],
  },
  {
    id: 'lx-ch12-t-003',
    chapter: 12,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'Sleep Versus Busy-Wait',
    prompt: `Two drivers each need to wait 10 ms. Driver A calls mdelay(10); driver B calls msleep(10). In one sentence each, explain what the CPU is doing during the wait for each driver, and which one a normal process-context driver should prefer.`,
    hints: [
      'mdelay spins; msleep blocks.',
      'Think about whether the CPU can do other work.',
    ],
    solution: `During mdelay(10) the CPU spins in a busy loop for the full 10 ms, burning cycles and doing no useful work, so nothing else runs on that CPU. During msleep(10) the calling task is put to sleep and the scheduler runs other tasks until roughly 10 ms have elapsed, so the CPU stays useful. A normal driver running in process context should strongly prefer msleep, because sleeping yields the CPU; busy-delays like mdelay are only justified for very short waits or in contexts where sleeping is forbidden. For a 10 ms wait, mdelay is wasteful and should be avoided.`,
    tags: ['kernel', 'delay', 'sleep'],
  },
  {
    id: 'lx-ch12-t-004',
    chapter: 12,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'Reading The Current Time',
    prompt: `A driver wants a human-readable wall-clock timestamp (year/month/day) to log when an event happened. Another wants a monotonic value to measure how long an operation took. Briefly: which kind of clock does each need, and why would using jiffies for the second case be limiting?`,
    hints: [
      'Wall-clock vs monotonic.',
      'jiffies resolution is one tick.',
    ],
    solution: `The first driver needs real/wall-clock time (for example via ktime_get_real_ts64), because it wants an absolute calendar timestamp that humans can read. The second needs a monotonic clock (for example ktime_get or ktime_get_ns), which only moves forward and is not disturbed by settimeofday or NTP adjustments, making it correct for measuring elapsed durations. Using jiffies for the duration is possible but limiting because its resolution is only one tick (e.g. 4 ms at HZ=250), so short operations cannot be measured accurately, and jiffies is not real time. Monotonic ktime gives nanosecond-class resolution for interval measurement.`,
    tags: ['kernel', 'time', 'monotonic'],
  },
  {
    id: 'lx-ch12-t-005',
    chapter: 12,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Timer Callback Context',
    prompt: `You arm a timer_list and its callback fires. A junior dev wants to call mutex_lock() and then copy_to_user() inside that callback. Explain in which context the timer callback runs and why both of those calls are illegal there.`,
    hints: [
      'Classic timer_list callbacks run in softirq context.',
      'Atomic context cannot sleep or fault.',
    ],
    solution: `A classic timer_list callback runs in softirq (bottom-half) context, which is atomic: you cannot sleep there. mutex_lock() may block and put the task to sleep if the mutex is contended, which is forbidden in atomic context and can deadlock or trigger a "scheduling while atomic" bug. copy_to_user() may take a page fault and sleep while the page is brought in, so it too cannot run in atomic context, and besides there is no well-defined user process whose address space applies in a timer callback. Inside such a callback you must stick to non-sleeping primitives like spinlocks and defer any user-copy or blocking work to process context (for example via a workqueue).`,
    tags: ['kernel', 'timer', 'atomic'],
  },
  {
    id: 'lx-ch12-t-006',
    chapter: 12,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'mod_timer To Rearm',
    prompt: `A periodic timer wants to fire every 100 ms. Inside the callback, a dev sets a new expiry by writing 'timer.expires = jiffies + msecs_to_jiffies(100);' and returns, expecting it to fire again. Why does this not re-arm the timer, and what should they call instead?`,
    hints: [
      'Setting expires does not register the timer.',
      'mod_timer both sets expiry and arms.',
    ],
    solution: `Merely assigning timer.expires changes a field but does not put the timer back onto the kernel's timer wheel, so the timer is not pending and will never fire again. To re-arm a one-shot timer_list for another interval you must call mod_timer(&timer, jiffies + msecs_to_jiffies(100)), which both updates the expiry and (re)activates the timer atomically. mod_timer is the standard way to schedule or reschedule a timer and is safe to call whether or not the timer is currently pending. Calling it from within the callback is the normal idiom for building a periodic timer out of a one-shot timer_list.`,
    tags: ['kernel', 'timer', 'mod_timer'],
  },
  {
    id: 'lx-ch12-t-007',
    chapter: 12,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Why time_after Beats Plain Comparison',
    prompt: `A loop polls a device until a deadline: 'while (jiffies < deadline) { ... }'. Explain the subtle bug that appears when the jiffies counter wraps around, and which macro fixes it.`,
    hints: [
      'jiffies is an unsigned counter that wraps.',
      'time_after/time_before handle wraparound.',
    ],
    solution: `jiffies is an unsigned integer that wraps around to zero when it overflows, so a direct 'jiffies < deadline' comparison breaks near the wrap boundary: if deadline was computed just before the wrap, the freshly wrapped (small) jiffies value can compare as "less than" deadline even though the deadline has actually passed, or vice versa, causing the loop to spin far too long or exit early. The fix is to use the time comparison macros, which are written to handle wraparound by comparing signed differences: write 'while (time_before(jiffies, deadline))' or equivalently 'while (!time_after(jiffies, deadline))'. These macros (time_after, time_before, time_after_eq, time_before_eq) are the only correct way to compare jiffies values.`,
    tags: ['kernel', 'jiffies', 'time_after'],
  },
  {
    id: 'lx-ch12-t-008',
    chapter: 12,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'udelay In An Interrupt Handler',
    prompt: `Inside a hard interrupt handler, a driver needs to wait 5 microseconds for a chip to settle. Is calling udelay(5) acceptable here? Contrast it with calling usleep_range(5, 10) in the same spot.`,
    hints: [
      'Interrupt handlers are atomic.',
      'udelay spins; usleep_range sleeps.',
    ],
    solution: `Yes, udelay(5) is acceptable in a hard interrupt handler because it is a pure busy-spin that never sleeps, and 5 microseconds is short enough that briefly burning the CPU is tolerable. usleep_range(5, 10), by contrast, sleeps the calling task, and sleeping is illegal in an interrupt handler (atomic context), so that call would be a bug and trigger a "scheduling while atomic" error. The general rule is that for very short waits in atomic context you must busy-delay with udelay/ndelay/mdelay, while the sleeping variants (usleep_range, msleep) are only legal in process context. So the udelay is fine; the usleep_range is not.`,
    tags: ['kernel', 'delay', 'interrupt'],
  },
  {
    id: 'lx-ch12-t-009',
    chapter: 12,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'mdelay Versus msleep For 50 ms',
    prompt: `In a process-context function you must wait 50 ms. Compare mdelay(50) and msleep(50): what is the cost of each, and what is the practical downside of mdelay(50) on a busy system?`,
    hints: [
      'mdelay loops over udelay.',
      '50 ms of spinning is a long time.',
    ],
    solution: `mdelay(50) is essentially a loop that busy-spins for 50 ms (it is built on udelay), so the CPU does nothing useful for that entire time and cannot run other tasks on that core. msleep(50) instead sleeps the task and lets the scheduler run other work, waking it after about 50 ms (it may round up to the next tick or a bit more). On a busy system mdelay(50) is a serious problem: it monopolizes a CPU for 50 ms, hurts throughput and latency for everything else, and is exactly the kind of long busy-wait that should never happen in process context. For any multi-millisecond wait where sleeping is legal, msleep is the right choice.`,
    tags: ['kernel', 'delay', 'sleep'],
  },
  {
    id: 'lx-ch12-t-010',
    chapter: 12,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Why usleep_range Takes A Range',
    prompt: `Instead of usleep(50), the kernel offers usleep_range(min, max), e.g. usleep_range(50, 100). Why is the API designed around a min and max rather than a single exact value? What benefit does giving a wider range provide?`,
    hints: [
      'Short sleeps use hrtimers.',
      'A range lets the kernel coalesce wakeups.',
    ],
    solution: `Short sleeps below a few milliseconds are backed by hrtimers, and waking a CPU at a precise instant is expensive in power and scheduling overhead. By accepting a [min, max] window, usleep_range lets the kernel coalesce your wakeup with other nearby timer events rather than programming a dedicated, exact wakeup, which improves power efficiency and reduces interrupts. The min guarantees you sleep at least that long; the max gives slack the kernel can exploit. A wider range gives the kernel more freedom to batch wakeups, so you should choose the largest max your driver can tolerate. This is also why there is no plain "usleep(exact)" for these short durations.`,
    tags: ['kernel', 'sleep', 'hrtimer'],
  },
  {
    id: 'lx-ch12-t-011',
    chapter: 12,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'schedule_timeout Needs A State',
    prompt: `A dev writes 'schedule_timeout(HZ);' expecting the task to sleep one second, but the call returns almost immediately. They forgot something. What must be done before calling schedule_timeout, and what is the usual two-line idiom?`,
    hints: [
      'schedule_timeout only sleeps if the task state is not RUNNING.',
      'Set TASK_INTERRUPTIBLE or TASK_UNINTERRUPTIBLE first.',
    ],
    solution: `schedule_timeout only actually sleeps if the current task's state has been set to something other than TASK_RUNNING; if you leave it RUNNING, schedule() returns at once and the timeout has no effect. The idiom is to set the state first, e.g. set_current_state(TASK_INTERRUPTIBLE); schedule_timeout(HZ);. Choosing TASK_INTERRUPTIBLE lets signals wake the task early (and schedule_timeout returns the remaining jiffies so you can detect that), while TASK_UNINTERRUPTIBLE ignores signals. In modern code the convenience wrappers schedule_timeout_interruptible() and schedule_timeout_uninterruptible() set the state for you, which is why they are preferred over the raw call.`,
    tags: ['kernel', 'schedule_timeout', 'sleep'],
  },
  {
    id: 'lx-ch12-t-012',
    chapter: 12,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Delayed Work Basics',
    prompt: `You want to run a cleanup function 2 seconds after a device closes, and the cleanup may sleep (it calls kfree on many objects and grabs a mutex). Would you use a timer_list callback or schedule_delayed_work? Justify the choice.`,
    hints: [
      'Where does each run?',
      'Cleanup that sleeps needs process context.',
    ],
    solution: `Use schedule_delayed_work, because delayed work runs later in a kernel worker thread, which is process context where sleeping is allowed, so it can safely take a mutex and do bulk frees. A classic timer_list callback runs in softirq (atomic) context, where calling mutex_lock() is illegal and any blocking would be a bug. delayed_work is essentially "a workqueue item with a built-in timer," giving you a deferred, sleep-capable callback. You would declare it with INIT_DELAYED_WORK and arm it with schedule_delayed_work(&dwork, msecs_to_jiffies(2000)). That matches the requirement of a sleeping cleanup after a delay.`,
    tags: ['kernel', 'workqueue', 'delayed_work'],
  },
  {
    id: 'lx-ch12-t-013',
    chapter: 12,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'jiffies Must Be Read Fresh',
    prompt: `A driver caches 'unsigned long now = jiffies;' once at probe time and later compares incoming events against 'now' to see how much time has passed. Explain why this gives wrong elapsed-time results, and what it should do instead.`,
    hints: [
      'jiffies keeps advancing.',
      'You need the current value at each check.',
    ],
    solution: `The variable 'now' is just a snapshot of jiffies taken at probe; it does not update, while the real jiffies counter keeps advancing. So later, comparing events against the stale 'now' measures time relative to probe, not relative to when you check, giving meaningless elapsed values. To measure elapsed time you must read jiffies freshly at the moment of each comparison, e.g. compute 'elapsed = jiffies - event_start;' using a start timestamp captured when the event began. The jiffies global is volatile and is meant to be re-read each time you need the current tick count.`,
    tags: ['kernel', 'jiffies', 'time'],
  },
  {
    id: 'lx-ch12-t-014',
    chapter: 12,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Timer Self-Rearm Versus del_timer Race',
    prompt: `A periodic timer_list re-arms itself with mod_timer inside its own callback. The driver's release path calls del_timer(&t) and then immediately frees the structure containing the timer. Describe the race and why del_timer_sync would be needed instead.`,
    hints: [
      'del_timer does not wait for a running callback.',
      'The callback may be executing on another CPU.',
    ],
    solution: `del_timer only deactivates a pending timer; it does not wait for a callback that is already executing on another CPU. So even after del_timer returns, the self-rearming callback could still be mid-execution and may even call mod_timer to re-arm itself, re-activating the timer you just deleted, or it may touch the structure you are about to free, causing a use-after-free. The fix is del_timer_sync(&t), which deletes the timer and then blocks until any concurrently running instance of the callback has finished, guaranteeing the callback is neither running nor pending before you free. Note del_timer_sync must not be called from the timer's own context and you must ensure the callback will not re-arm after deletion.`,
    tags: ['kernel', 'timer', 'race'],
  },
  {
    id: 'lx-ch12-t-015',
    chapter: 12,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Why hrtimer Over timer_list',
    prompt: `A driver needs to generate a pulse exactly 20 microseconds wide. The author tries to do this with a timer_list set to 'jiffies + 1'. Explain why timer_list cannot achieve 20 us precision, and what facility is designed for this.`,
    hints: [
      'timer_list granularity is the tick.',
      'hrtimers use nanosecond resolution.',
    ],
    solution: `timer_list resolution is bounded by the tick: the smallest nonzero delay you can express is one jiffy, which at HZ=250 is 4 ms and even at HZ=1000 is 1 ms, vastly larger than 20 microseconds, and the timer wheel only checks expirations around tick boundaries. So 'jiffies + 1' can fire anywhere up to a full tick late and can never give microsecond precision. High-resolution timers (hrtimer) are the right tool: they are programmed in ktime nanoseconds, backed by the hardware clockevent device, and can fire with microsecond-class accuracy. You would arm an hrtimer for 20000 ns (using hrtimer_start with HRTIMER_MODE_REL). For sub-millisecond timing, hrtimers, not timer_list, are required.`,
    tags: ['kernel', 'hrtimer', 'precision'],
  },
  {
    id: 'lx-ch12-t-016',
    chapter: 12,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'msleep Rounds Up',
    prompt: `A measurement loop calls msleep(1) one thousand times and expects exactly 1.000 second of total sleep, but measures noticeably more (sometimes 4+ seconds on some kernels). Explain why msleep(1) often sleeps significantly longer than 1 ms.`,
    hints: [
      'msleep guarantees a minimum, not exact.',
      'Tick granularity and rounding matter.',
    ],
    solution: `msleep guarantees you sleep at least the requested time, never less, and it is implemented on top of schedule_timeout, whose granularity is the jiffy. For a 1 ms request msleep converts to jiffies and rounds up to ensure the minimum is met, so on a HZ=250 kernel each msleep(1) becomes at least one 4 ms tick, plus you only wake on a tick boundary and after the scheduler picks you. The result is that each call can take several milliseconds, and a thousand of them accumulate well past one second. For short, accurate sleeps you should use usleep_range, which is hrtimer-based; msleep is only appropriate for coarse, multi-millisecond delays where over-sleeping is acceptable.`,
    tags: ['kernel', 'msleep', 'jiffies'],
  },
  {
    id: 'lx-ch12-t-017',
    chapter: 12,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Delayed Work Re-Queue Semantics',
    prompt: `A dev calls schedule_delayed_work(&dwork, msecs_to_jiffies(1000)) and then, 200 ms later, calls it again with the same dwork and a 1000 ms delay, expecting the timer to be pushed out to 1000 ms from now. What actually happens, and what call would reset the delay?`,
    hints: [
      'schedule_delayed_work is a no-op if already queued/pending.',
      'mod_delayed_work changes the delay.',
    ],
    solution: `schedule_delayed_work does not reschedule an item that is already pending: if the delayed_work is still armed (it has not fired yet), the second call is effectively a no-op and the original 1000 ms timer from the first call stands, so it fires about 800 ms later, not 1000 ms from the second call. To actually change the delay of an already-queued delayed_work you must call mod_delayed_work(wq, &dwork, msecs_to_jiffies(1000)), which re-arms it to the new expiry. This parallels the timer_list situation where mod_timer, not a second add, is what changes an existing timer. So the behavior is "keep first delay" unless you explicitly use the mod_ variant.`,
    tags: ['kernel', 'delayed_work', 'workqueue'],
  },
  {
    id: 'lx-ch12-t-018',
    chapter: 12,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Spinlock Held, Need A Delay',
    prompt: `Code holds a spinlock and now needs to wait ~2 ms for hardware. Option A: msleep(2). Option B: mdelay(2). Option C: drop the lock, msleep(2), reacquire. Evaluate each and recommend one.`,
    hints: [
      'Holding a spinlock disables preemption (atomic).',
      'You cannot sleep with a spinlock held.',
    ],
    solution: `Holding a spinlock puts you in atomic context (preemption disabled, possibly interrupts disabled), so Option A is a bug: msleep sleeps, and sleeping while holding a spinlock can deadlock and triggers "scheduling while atomic." Option B (mdelay) is technically legal because it only busy-spins, but spinning 2 ms with a lock held is terrible: it blocks every other CPU waiting for that lock and wrecks latency. Option C is the correct design when feasible: drop the spinlock, msleep(2) in the now-non-atomic context, then reacquire and re-validate any state that may have changed while unlocked. So recommend Option C; if the lock truly cannot be dropped, that is a sign the design needs rework rather than a 2 ms busy-wait.`,
    tags: ['kernel', 'spinlock', 'sleep'],
  },
  {
    id: 'lx-ch12-t-019',
    chapter: 12,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Polling With A Timeout',
    prompt: `You must poll a status bit, succeeding when it sets, but giving up after 100 ms in process context. Sketch the structure of a correct polling loop using jiffies, time_before, and a sleeping pause, and note the one thing you must do at each iteration.`,
    hints: [
      'Compute a deadline = jiffies + msecs_to_jiffies(100).',
      'Re-read the bit and re-read jiffies each pass.',
    ],
    solution: `Compute the deadline once: 'unsigned long deadline = jiffies + msecs_to_jiffies(100);'. Then loop while time_before(jiffies, deadline): re-read the hardware status each pass, return success the moment the bit is set, otherwise pause briefly with a sleeping call such as usleep_range(min, max) (in process context, since we may sleep) so you do not busy-spin. After the loop, do one final read to avoid a race where the bit set just as the timeout expired, then report timeout if still unset. The key per-iteration requirement is to re-read both the live status and live jiffies (via time_before) each time, never caching them, and to compare jiffies only with the time_* macros so wraparound is handled. This bounds the wait to ~100 ms while keeping the CPU available.`,
    tags: ['kernel', 'polling', 'time_after'],
  },
  {
    id: 'lx-ch12-t-020',
    chapter: 12,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'hrtimer Callback Context',
    prompt: `An hrtimer callback (the function returning enum hrtimer_restart) wants to kmalloc(GFP_KERNEL) a buffer and wait on a wait queue. Explain why this is unsafe, and how the callback should be structured if it needs to do sleeping work.`,
    hints: [
      'hrtimer callbacks typically run in hard-irq/atomic context.',
      'GFP_KERNEL may sleep.',
    ],
    solution: `By default an hrtimer callback runs in hard interrupt / softirq context (atomic), so it must not sleep. kmalloc with GFP_KERNEL is allowed to block and reclaim memory, which can sleep, so it is unsafe there; you would need GFP_ATOMIC at most, and even that should be minimal. Waiting on a wait queue actively blocks the task, which is outright illegal in atomic context. The correct pattern is to keep the hrtimer callback short and non-sleeping, and if real work must sleep, have the callback hand off to process context, for example by scheduling a workqueue item (schedule_work) or waking a kthread, and do the GFP_KERNEL allocation and any blocking there. The callback then just returns HRTIMER_RESTART or HRTIMER_NORESTART.`,
    tags: ['kernel', 'hrtimer', 'atomic'],
  },
  {
    id: 'lx-ch12-t-021',
    chapter: 12,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Passing Data To A Timer Callback',
    prompt: `In modern kernels the timer callback signature is 'void cb(struct timer_list *t)' and takes no private void* argument. Your driver has per-device state and needs the callback to find its own device struct. Explain the standard mechanism used to recover the device from the timer pointer.`,
    hints: [
      'Embed the timer_list inside the device struct.',
      'from_timer / container_of.',
    ],
    solution: `Modern timer_list callbacks receive only a pointer to the timer itself, so you recover your data by embedding the struct timer_list as a field inside your per-device structure and then converting from the timer pointer back to the containing struct. The idiom is the from_timer() macro (a thin wrapper over container_of): 'struct mydev *dev = from_timer(dev, t, my_timer);', where my_timer is the field name. You set this up with timer_setup(&dev->my_timer, cb, 0) at init. This embed-and-container_of pattern replaces the old setup_timer void* data argument and lets each timer instance locate its owning object without a separate global or private pointer.`,
    tags: ['kernel', 'timer', 'container_of'],
  },
  {
    id: 'lx-ch12-t-022',
    chapter: 12,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'ndelay Versus udelay Accuracy',
    prompt: `A driver needs a 300 ns wait and writes 'udelay(1);' reasoning "1 us is close enough to 300 ns and udelay is more common." Critique this, including both the over-wait and whether ndelay would be strictly better.`,
    hints: [
      'udelay(1) waits ~1000 ns, over 3x the need.',
      'ndelay exists for sub-microsecond waits.',
    ],
    solution: `udelay(1) busy-waits about 1000 ns, which is more than three times the 300 ns actually required, so it over-delays and, in a hot path executed often, that extra spinning wastes measurable CPU time. The proper primitive is ndelay(300), which is designed for nanosecond-scale busy-waits and expresses the intent precisely. One caveat is that very short delays are limited by the accuracy of the calibrated loop and timer resolution, so ndelay is allowed to round up and may not be exact at the low nanosecond end; it will still wait at least the requested time. So ndelay(300) is strictly more appropriate than udelay(1): same busy-wait family, but matched to the required magnitude rather than blindly over-waiting.`,
    tags: ['kernel', 'delay', 'ndelay'],
  },
  {
    id: 'lx-ch12-t-023',
    chapter: 12,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Timer Drift In Periodic Re-Arm',
    prompt: `A periodic timer re-arms each fire with 'mod_timer(&t, jiffies + HZ);' to tick once per second. Over many hours the ticks drift later and later relative to wall time. Explain the source of the drift and how re-arming relative to the previous expiry fixes it.`,
    hints: [
      'jiffies at callback time is already past the intended expiry.',
      'Anchor to expires, not to current jiffies.',
    ],
    solution: `When the callback runs, jiffies has already advanced to (or slightly past) the scheduled expiry, and there is extra latency before the callback executes, so 'jiffies + HZ' measures the next interval from a point already later than the ideal tick. Each period thus inherits the previous period's lateness, and the small errors accumulate, making the ticks drift progressively later. The fix is to anchor the next expiry to the intended schedule rather than to current jiffies: re-arm with 'mod_timer(&t, t.expires + HZ);' (the previous expires plus one period), so missed-by-a-little intervals are compensated and the average rate stays one per second. If the timer falls so far behind that the new expires is already in the past, you handle that by catching up, but anchoring to expires removes the systematic cumulative drift.`,
    tags: ['kernel', 'timer', 'drift'],
  },
  {
    id: 'lx-ch12-t-024',
    chapter: 12,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'del_timer_sync Deadlock With A Lock',
    prompt: `The timer callback takes spinlock L. A separate teardown path holds L and, while holding it, calls del_timer_sync(&t). On an SMP machine this can hang forever. Explain the deadlock and how to restructure the teardown.`,
    hints: [
      'del_timer_sync waits for the running callback.',
      'The callback is blocked acquiring L which teardown holds.',
    ],
    solution: `del_timer_sync waits until any currently running instance of the callback finishes. On SMP, the callback may already be executing on another CPU and trying to acquire spinlock L, while the teardown path is spinning inside del_timer_sync still holding L. The callback cannot finish because it is blocked on L, and the teardown cannot release L because it is stuck waiting in del_timer_sync, so the two spin forever, a classic AB deadlock. The fix is to not hold L (any lock the callback also takes) while calling del_timer_sync: drop the lock first, call del_timer_sync, then reacquire if needed, or restructure so the callback and the teardown never both contend the same lock across the sync. Setting a "shutting down" flag under the lock before releasing it can also stop the callback from re-arming.`,
    tags: ['kernel', 'timer', 'deadlock'],
  },
  {
    id: 'lx-ch12-t-025',
    chapter: 12,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Choosing Among Five Wait Mechanisms',
    prompt: `For each scenario pick the best wait primitive and say why: (a) 2 us settle delay inside a spinlock-held region; (b) 200 ms wait in an ioctl that may be interrupted by a signal; (c) fire a one-shot action 5 s from now without blocking the caller; (d) precise 50 us pulse timing.`,
    hints: [
      'Atomic vs process context.',
      'Blocking vs deferred vs high-resolution.',
    ],
    solution: `(a) Use udelay(2): you are in atomic context (spinlock held), sleeping is illegal, and 2 us is short, so a busy-delay is correct. (b) Use msleep_interruptible(200) or a wait with TASK_INTERRUPTIBLE via schedule_timeout_interruptible: you are in process context (ioctl), the wait is long enough to sleep, and you want signals to be able to break the wait and return -EINTR/-ERESTARTSYS. (c) Use schedule_delayed_work (or a timer_list) armed for 5 s: it defers the action without blocking the calling thread, and delayed_work additionally gives you process context if the action must sleep. (d) Use an hrtimer programmed for 50000 ns: only high-resolution timers give microsecond-class precision, far below the jiffy granularity of timer_list. The selection turns on context (atomic vs process), whether you may block, and the precision required.`,
    tags: ['kernel', 'delay', 'design'],
  },
  {
    id: 'lx-ch12-t-026',
    chapter: 12,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Why Not Spin On time_before For Long Waits',
    prompt: `A dev replaces a sleep with a tight busy-loop in process context: 'while (time_before(jiffies, deadline)) cpu_relax();' to wait 200 ms, arguing it is "more accurate than msleep." Critique correctness and system impact, including what happens if interrupts are disabled.`,
    hints: [
      'jiffies only advances on the timer tick.',
      'Spinning 200 ms wastes a CPU.',
    ],
    solution: `Even though the loop is technically correct with respect to wraparound (it uses time_before), busy-spinning 200 ms in process context is a serious waste: it monopolizes a CPU core for a fifth of a second doing nothing, hurting throughput, latency, and power, whereas msleep would yield the CPU to real work. The accuracy argument is also weak, because jiffies only advances on the timer tick, so the loop's resolution is still one jiffy, no better than tick-based sleeping. Worst of all, if interrupts happen to be disabled, the timer interrupt cannot fire, jiffies never advances, deadline is never reached, and the loop hangs forever. The correct choice is msleep (or usleep_range for shorter, more precise waits); a busy-loop is only justified for very short atomic-context delays via udelay, not a 200 ms wait.`,
    tags: ['kernel', 'jiffies', 'busy-wait'],
  },
  {
    id: 'lx-ch12-t-027',
    chapter: 12,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Timer Versus Workqueue For Periodic Sleeping Work',
    prompt: `You need a job that runs every 500 ms and each run does sleeping work (I2C transfers that block). A colleague proposes a periodic timer_list that schedule_work()s a worker each fire. Compare that to a self-requeuing delayed_work, and argue which is cleaner.`,
    hints: [
      'timer fires in atomic context, so it can only defer.',
      'delayed_work already provides delay + process context.',
    ],
    solution: `The timer_list approach works but is awkward: the timer callback runs in atomic context, so it cannot do the I2C itself; it can only schedule_work() to a worker and re-arm the timer, meaning you maintain two coupled objects (a timer plus a work item) and reason about their interaction during teardown. A self-requeuing delayed_work is cleaner: the delayed_work callback already runs in process context where the blocking I2C is legal, and at the end of each run it simply calls schedule_delayed_work(&dwork, msecs_to_jiffies(500)) to schedule the next iteration. This collapses the timing and the sleeping work into one object with one cancel path (cancel_delayed_work_sync), eliminating the timer-plus-work duplication. So for periodic work that must sleep, a self-requeuing delayed_work is the simpler, idiomatic design. Note the period measures from the end of each run, which is acceptable here.`,
    tags: ['kernel', 'delayed_work', 'design'],
  },
  {
    id: 'lx-ch12-t-028',
    chapter: 12,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'jiffies On 32-Bit And jiffies_64',
    prompt: `On a 32-bit kernel with HZ=1000, a dev stores a timestamp in 'unsigned long' and compares two timestamps that may be more than 50 days apart using time_after. Explain the wraparound limit here and why jiffies_64 / get_jiffies_64 may be needed.`,
    hints: [
      'A 32-bit counter at 1000 Hz wraps in ~49.7 days.',
      'time_after only handles a half-period of distance.',
    ],
    solution: `On a 32-bit kernel 'unsigned long' jiffies is 32 bits, and at HZ=1000 it increments a thousand times a second, so it wraps the full 32-bit range in about 49.7 days. The time_after family correctly handles wraparound only when the two values are within half the counter's range of each other (here ~24.8 days); beyond that the signed-difference trick aliases and the comparison can give the wrong answer. So two timestamps more than ~25 days apart cannot be compared reliably with 32-bit jiffies. The remedy is to use the 64-bit counter jiffies_64, read with get_jiffies_64(), which at 1000 Hz effectively never wraps in any realistic uptime, making long-interval comparisons safe. (On 64-bit kernels 'unsigned long' is already 64 bits, so the issue is specific to 32-bit builds.)`,
    tags: ['kernel', 'jiffies', 'wraparound'],
  },
  {
    id: 'lx-ch12-t-029',
    chapter: 12,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Measuring Short Durations Correctly',
    prompt: `A driver times a fast hardware operation expected to take ~30 us. The author uses 'unsigned long t0 = jiffies; ...; delta = jiffies - t0;' and is surprised delta is almost always 0. Explain why, and what to use to measure tens of microseconds accurately.`,
    hints: [
      'jiffies resolution is one tick.',
      'Use ktime/ns-resolution clocks.',
    ],
    solution: `jiffies only advances once per tick, so its resolution is the jiffy: at HZ=1000 that is 1 ms, and at HZ=250 it is 4 ms. A 30 us operation is far shorter than one tick, so jiffies almost never changes across it and delta reads 0 (occasionally 1 if a tick happens to land inside the window). To measure tens of microseconds you need a high-resolution monotonic clock: capture 'ktime_t t0 = ktime_get();' before and 'ktime_t t1 = ktime_get();' after, then 's64 ns = ktime_to_ns(ktime_sub(t1, t0));'. ktime_get reads a nanosecond-resolution monotonic source, giving the precision required, whereas jiffies is only suitable for millisecond-and-coarser intervals.`,
    tags: ['kernel', 'time', 'ktime'],
  },
  {
    id: 'lx-ch12-t-030',
    chapter: 12,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Cancel-Then-Free For Delayed Work',
    prompt: `A device's remove() does 'cancel_delayed_work(&dev->dwork);' and then kfree(dev). The delayed_work callback dereferences dev. Even ignoring re-queue, explain the residual race and which cancel variant closes it; also note the extra hazard if the worker re-queues itself.`,
    hints: [
      'cancel_delayed_work does not wait for a running callback.',
      'cancel_delayed_work_sync waits; re-queue needs a guard.',
    ],
    solution: `cancel_delayed_work only cancels a pending (not-yet-started) item; it returns immediately and does not wait for a callback that is already running on a worker thread. So the callback could be executing and dereferencing dev at the exact moment remove() does kfree(dev), producing a use-after-free. The fix is cancel_delayed_work_sync(&dev->dwork), which cancels the pending timer/work and then blocks until any in-progress callback has finished, so after it returns the worker is guaranteed not to touch dev. The extra hazard is self-requeuing: if the callback ends by calling schedule_delayed_work on itself, even the _sync cancel can race with a fresh re-arm, so you must first set a "stopping" flag (checked by the callback before it re-queues) and only then call cancel_delayed_work_sync, ensuring no new instance can be scheduled after the cancel.`,
    tags: ['kernel', 'delayed_work', 'use-after-free'],
  },
]

export default problems
