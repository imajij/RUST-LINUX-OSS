import type { Problem } from '../../types'

const problems: Problem[] = [
  {
    id: 'lx-ch14-t-001',
    chapter: 14,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'What printk Log Levels Are For',
    prompt: `A newcomer writes printk("hello\\n") and notices the line sometimes appears on the console and sometimes not. In one short paragraph, explain what a printk log level is and why prefixing a message with something like KERN_ERR versus KERN_DEBUG changes whether it reaches the console.`,
    hints: ['Each message carries a numeric severity, 0 (emerg) to 7 (debug).', 'The console has a loglevel threshold it compares against.'],
    solution: `Every printk message carries a log level, a small integer from 0 (KERN_EMERG) down in importance to 7 (KERN_DEBUG), supplied by a prefix macro such as KERN_ERR or KERN_DEBUG. The console only prints messages whose level is more urgent than the current console_loglevel threshold (tunable via /proc/sys/kernel/printk or the loglevel= boot option). A bare printk with no prefix uses a default level, so whether it shows up depends on how that default compares to the threshold. Messages always go into the kernel ring buffer regardless, so you can still read them with dmesg even when they were filtered from the console.`,
    tags: ['printk', 'loglevel', 'console'],
  },
  {
    id: 'lx-ch14-t-002',
    chapter: 14,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'WARN_ON Versus BUG_ON',
    prompt: `Two macros let you flag a "this should never happen" condition: WARN_ON(cond) and BUG_ON(cond). For a driver that hits an unexpected but recoverable state, explain which one you should reach for first and why the other is usually the wrong choice in modern kernel code.`,
    hints: ['One kills the current context; one keeps running.', 'Think about what happens to the rest of the system.'],
    solution: `WARN_ON is the right default: when its condition is true it prints a warning plus a stack trace to the log but lets execution continue, so you learn about the bug without taking down the machine. BUG_ON is far more drastic; it triggers a kernel oops/panic at that line, which can crash the system or at least kill the current process, taking other unrelated work down with it. Modern kernel style strongly prefers WARN_ON (or returning an error) for recoverable situations and reserves BUG_ON for truly unrecoverable corruption where continuing would be worse. For a driver in a merely unexpected state, WARN_ON plus graceful error handling keeps the system alive and still surfaces the problem.`,
    tags: ['warn_on', 'bug_on', 'debugging'],
  },
  {
    id: 'lx-ch14-t-003',
    chapter: 14,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'Why dmesg Exists',
    prompt: `You add several printk calls to debug a boot-time problem, but by the time you log in, the console has scrolled far past them. Explain where those messages went and why the dmesg command can still show them.`,
    hints: ['printk writes into an in-kernel ring buffer.', 'The console is just one consumer of that buffer.'],
    solution: `printk does not write directly and only to the console; it appends each formatted message to the kernel log ring buffer, a fixed-size circular buffer in kernel memory. The console is just one consumer that prints messages above the console loglevel as they arrive. dmesg reads the ring buffer itself (via syslog/kmsg), so it can show messages that scrolled off or were filtered from the console, as long as they have not yet been overwritten by newer entries. Because the buffer is circular and bounded, very old or very chatty logging can still be lost once the buffer wraps.`,
    tags: ['printk', 'dmesg', 'ring-buffer'],
  },
  {
    id: 'lx-ch14-t-004',
    chapter: 14,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'Reading the Word "Oops"',
    prompt: `In a bug report you see a kernel log block that starts with "Oops:" followed by register dumps and a "Call Trace:". In plain terms, what does an oops mean happened, and is the machine guaranteed to be dead afterward?`,
    hints: ['An oops is the kernel detecting a serious fault.', 'Oops is not always the same as panic.'],
    solution: `An oops is the kernel's report that it hit a serious internal fault, most often a bad memory access such as dereferencing a NULL or invalid pointer in kernel context. It dumps the CPU registers, the faulting instruction pointer, and a call trace so you can see where things went wrong. An oops typically kills the offending process or context, but the machine is not necessarily dead: the kernel marks itself "tainted" and may keep running, sometimes unstably. It becomes a fatal panic only if the fault happens somewhere unrecoverable (like in an interrupt handler or with panic_on_oops set), in which case the whole system halts.`,
    tags: ['oops', 'panic', 'crash'],
  },
  {
    id: 'lx-ch14-t-005',
    chapter: 14,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Decoding func+0x2a/0x80',
    prompt: `A call trace line reads "my_driver_probe+0x2a/0x80". Break down what the two hex numbers mean and explain how this notation helps you find the exact source line without a debugger.`,
    hints: ['The first offset is into the function; the second is the function size.', 'Symbol plus offset maps back to an instruction.'],
    solution: `The notation is symbol+offset/size: my_driver_probe is the nearest preceding symbol, 0x2a is the byte offset of the faulting/return instruction from the start of that function, and 0x80 is the total size of the function in bytes. Because 0x2a is well under 0x80 it is a plausible in-function address rather than a corrupted one. With the symbol and offset you can disassemble the function (objdump or gdb on the vmlinux/.ko) and find the instruction at that offset, or use a tool like addr2line/decode_stacktrace.sh to map it back to the exact file and line. This lets you pinpoint the crash site even though the trace contains only addresses, not source.`,
    tags: ['stack-trace', 'symbols', 'decode'],
  },
  {
    id: 'lx-ch14-t-006',
    chapter: 14,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'When Dynamic Debug Beats a Rebuild',
    prompt: `You want to turn on a handful of pr_debug() messages in a shipped, already-running kernel that has dynamic debug enabled. Explain why you do not need to recompile, and roughly how you would switch those specific messages on at runtime.`,
    hints: ['pr_debug compiles to dynamic-debug callsites when CONFIG_DYNAMIC_DEBUG is set.', 'Control them through the debugfs control file.'],
    solution: `With CONFIG_DYNAMIC_DEBUG enabled, each pr_debug()/dev_dbg() callsite is compiled in but normally disabled, gated by a cheap runtime switch (a jump label) rather than being compiled out. That means the message text already lives in the kernel, so you can enable it live without rebuilding. You write a query to the control file at /sys/kernel/debug/dynamic_debug/control, for example echoing "file my_driver.c +p" or "module my_driver +p" to turn on printing for those callsites, and "-p" to turn them off again. You can scope queries by file, line, function, module, or format string, so you light up exactly the messages you want and nothing else.`,
    tags: ['dynamic-debug', 'pr_debug', 'debugfs'],
  },
  {
    id: 'lx-ch14-t-007',
    chapter: 14,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Why ftrace Needs No Recompile',
    prompt: `A colleague claims that to trace which kernel functions run you must add print statements and rebuild. Explain how ftrace's function tracer can record the same information on a stock kernel without any source changes.`,
    hints: ['The compiler inserts mcount/__fentry__ hooks into every traceable function.', 'Those hooks are nop until ftrace patches them.'],
    solution: `Stock kernels are usually built with CONFIG_FUNCTION_TRACER, which makes the compiler insert a call to a tracing stub (__fentry__/mcount) at the start of nearly every function. At boot those call sites are patched to nops so they cost almost nothing. When you enable the function tracer through tracefs, ftrace live-patches the selected nops back into calls to its handler, which records the function and timestamp into a per-CPU ring buffer. So the instrumentation points already exist in the running kernel; ftrace just turns them on and off dynamically, no recompilation or source edits required.`,
    tags: ['ftrace', 'function-tracer', 'tracefs'],
  },
  {
    id: 'lx-ch14-t-008',
    chapter: 14,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Tracepoint Versus a printk',
    prompt: `For a hot path executed millions of times per second, a maintainer rejects "just add a printk" and asks for a tracepoint instead. Give two concrete reasons a static tracepoint is better suited to a hot path than a printk.`,
    hints: ['A disabled tracepoint is nearly free.', 'Tracepoints emit structured binary data, not formatted text.'],
    solution: `First, cost when off: a tracepoint is guarded by a jump label, so when no one is listening it is essentially a nop and adds negligible overhead, whereas every printk formats a string and touches the log buffer even if nobody reads it. Second, data shape and volume: a tracepoint records structured binary fields into a fast per-CPU ring buffer that tools parse on demand, while printk produces formatted text that floods dmesg, can be rate-limited or dropped, and is expensive to generate. Tracepoints are also a stable, named instrumentation contract that tools like perf and trace-cmd can attach to selectively. For a path running millions of times a second, those properties make a tracepoint vastly more appropriate than a printk.`,
    tags: ['tracepoints', 'printk', 'overhead'],
  },
  {
    id: 'lx-ch14-t-009',
    chapter: 14,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'What a kprobe Attaches To',
    prompt: `You want to inspect arguments to an internal kernel function that has no tracepoint and that you cannot modify. Explain what a kprobe lets you do here and, at a high level, how it intercepts the function.`,
    hints: ['kprobes can be planted on almost any kernel instruction address.', 'It swaps in a breakpoint and runs your handler.'],
    solution: `A kprobe lets you attach your own handler to almost any kernel instruction address, including the entry of a function that has no tracepoint and that you cannot recompile. When you register the probe, the kernel saves the original instruction at that address and replaces it with a breakpoint (or an optimized jump); when the CPU reaches it, control transfers to your pre-handler where you can read registers and arguments, then the original instruction is single-stepped and execution resumes. A kretprobe variant additionally fires on function return so you can capture the return value. This gives you dynamic, no-rebuild visibility into arbitrary kernel functions, at the cost of being tied to specific symbols and calling conventions.`,
    tags: ['kprobes', 'dynamic-tracing', 'instrumentation'],
  },
  {
    id: 'lx-ch14-t-010',
    chapter: 14,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Sampling Versus Counting in perf',
    prompt: `Someone runs perf to find why a workload is slow and is confused about the difference between "perf stat" and "perf record". Explain what each does and which you would start with to locate a hotspot.`,
    hints: ['stat aggregates counters; record samples.', 'Hotspots come from sampled instruction pointers.'],
    solution: `perf stat counts events: it runs the workload and reports aggregate totals such as cycles, instructions, cache misses, and IPC, giving you a high-level picture of whether the program is, say, cache- or branch-bound. perf record samples: it periodically interrupts on an event (by default CPU cycles) and records the instruction pointer and call stack at each sample, building a profile of where time is spent, which you then explore with perf report. To locate a hotspot you typically start with perf stat for a quick characterization, then use perf record/report to see which functions and lines dominate. stat tells you what kind of problem you have; record tells you where it lives.`,
    tags: ['perf', 'profiling', 'sampling'],
  },
  {
    id: 'lx-ch14-t-011',
    chapter: 14,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'What KASAN Catches That a Crash Does Not',
    prompt: `A use-after-free bug sometimes crashes and sometimes silently returns wrong data. Explain how building with KASAN turns this flaky symptom into a precise, immediate report.`,
    hints: ['KASAN shadow-tracks the validity of memory.', 'It checks accesses, not just whether the access faults.'],
    solution: `Without a sanitizer, a use-after-free only crashes if the freed memory happens to be unmapped or reused destructively; otherwise the access "works" and corrupts or reads stale data, producing flaky, far-away symptoms. KASAN (Kernel Address Sanitizer) maintains a shadow region recording whether each byte of memory is currently valid to access, and the compiler instruments every load and store to consult that shadow first. When you touch freed (poisoned) memory, KASAN reports a use-after-free at the exact faulting access, with the allocation and free stack traces, instead of waiting for a downstream crash. This converts an intermittent, hard-to-localize bug into a deterministic report pointing at the real offending line.`,
    tags: ['kasan', 'use-after-free', 'memory-safety'],
  },
  {
    id: 'lx-ch14-t-012',
    chapter: 14,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'What lockdep Is Really Checking',
    prompt: `lockdep splatted a "possible circular locking dependency detected" even though your code did not actually deadlock during the test. Explain what lockdep tracks and why it can warn about a deadlock that did not happen.`,
    hints: ['lockdep records lock acquisition order as a graph.', 'It reasons about all observed orderings, not just this run.'],
    solution: `lockdep does not wait for an actual deadlock; it builds a graph of lock-ordering rules from every acquisition it observes, grouping locks into classes and recording "class A was taken before class B." If it ever sees A-before-B in one place and B-before-A in another, those two orderings could, on the right interleaving of CPUs, form a cycle and deadlock, so lockdep reports the dangerous dependency proactively. That is why you get a splat even though the threads did not collide this time: the unsafe ordering was merely possible. The point is to catch latent lock-order bugs early from any execution that exercises both orderings, rather than relying on a real deadlock to occur in production.`,
    tags: ['lockdep', 'deadlock', 'locking'],
  },
  {
    id: 'lx-ch14-t-013',
    chapter: 14,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Reading the Tainted Flags',
    prompt: `After a crash, the oops header shows "Tainted: G    B" (with letters set). Explain what "tainted" means for a bug report and why maintainers care about which taint flags are set before they dig in.`,
    hints: ['Taint marks that something happened that may compromise trust in the report.', 'Out-of-tree modules and prior warnings each set a flag.'],
    solution: `A tainted kernel is one where some event occurred that could undermine confidence that the bug lives in mainline code, such as loading an out-of-tree or proprietary module, forcing a module load, a previous WARN/oops, or running with a known-bad condition. Each cause sets a specific letter in the taint mask (for example G/P for module licensing, B for a bad page, W for a prior warning). Maintainers look at the flags first because a taint can mean a third-party module corrupted state, making the trace untrustworthy or out of scope. If the kernel is tainted by an out-of-tree driver, they may ask you to reproduce on a clean, untainted kernel before investing time.`,
    tags: ['taint', 'oops', 'bug-report'],
  },
  {
    id: 'lx-ch14-t-014',
    chapter: 14,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Why the Stack Trace Has Question Marks',
    prompt: `A "Call Trace:" lists some frames plainly and others prefixed with a "?" like "? bad_func+0x10/0x40". Explain what the question mark signals about the reliability of those frames and what you should and should not conclude from them.`,
    hints: ['The unwinder sometimes guesses from leftover stack contents.', 'Reliable frames come from frame pointers or ORC metadata.'],
    solution: `A "?" marks a frame the stack unwinder is not confident about: rather than following a verified return-address chain, it found a value on the stack that looks like a kernel text address and printed it as a best guess. These are often stale addresses left over from earlier calls, not part of the real call path. So you should trust the unprefixed frames as the actual call chain and treat the "?" entries as low-confidence hints that may be noise. Reliable unwinding comes from frame pointers or ORC unwinder metadata; where that runs out, the kernel falls back to the heuristic scan that produces the question-marked lines. Building the unwound stack from question-marked frames can send you chasing functions that were never actually called.`,
    tags: ['stack-trace', 'unwinder', 'decode'],
  },
  {
    id: 'lx-ch14-t-015',
    chapter: 14,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'The WARN_ON Inside a Hot Loop Trap',
    prompt: `A developer guards a frequently-false condition with WARN_ON(cond) inside a per-packet path, reasoning it is "harmless because it only warns." Explain two ways this can backfire even though WARN_ON does not crash, and what the better idiom is.`,
    hints: ['What happens if the condition does fire often?', 'There is a "once" variant for exactly this.'],
    solution: `WARN_ON is cheap when its condition is false, but it is not free if the condition ever becomes true on a hot path: each firing prints a full stack trace, so a condition that goes true at packet rate can flood dmesg, stall the system on console output, and even mask the original problem under a wall of identical splats. It also taints the kernel and, if panic_on_warn is set (common on test/CI fleets), the very first hit will panic. The better idiom is WARN_ON_ONCE(cond), which fires at most once so you still learn about the bug without the log storm, or to handle the condition with a quiet error path plus rate-limited logging if you genuinely need to count occurrences. Assuming "it only warns" ignores both the per-firing cost and policy knobs like panic_on_warn.`,
    tags: ['warn_on', 'warn_on_once', 'hot-path'],
  },
  {
    id: 'lx-ch14-t-016',
    chapter: 14,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Function Tracer Versus Function Graph',
    prompt: `ftrace offers both the "function" tracer and the "function_graph" tracer. Compare what each shows and explain which you would pick to find which callee inside a slow function is eating the time.`,
    hints: ['function logs entries as a flat list; function_graph shows entry and exit.', 'Durations come from matching entry/exit.'],
    solution: `The function tracer logs a flat, time-ordered list of function entries: you see which functions ran and in what order, which is great for "what is being called" questions but does not directly show how long each call took. The function_graph tracer records both entry and exit, drawing a nested, indented call tree and, crucially, printing the duration of each function (including a marker on unusually long ones). To find which callee inside a slow function dominates, you want function_graph, because it attributes elapsed time to individual calls and their children, letting you read the tree and spot the expensive subtree. Use the plain function tracer when you only need the sequence of calls, and function_graph when you need timing and call structure.`,
    tags: ['ftrace', 'function-graph', 'latency'],
  },
  {
    id: 'lx-ch14-t-017',
    chapter: 14,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Tracepoint Versus kprobe Tradeoffs',
    prompt: `You can observe a subsystem either through an existing static tracepoint or by planting a kprobe on an internal function. Give the main advantage of each, and explain why kprobes are considered more fragile across kernel versions.`,
    hints: ['Tracepoints are a maintained, named contract; kprobes target raw symbols.', 'Inlining and refactors move or erase functions.'],
    solution: `A static tracepoint's advantage is stability and clarity: it is a deliberately placed, named instrumentation point with documented fields that maintainers try not to break, so tools written against it keep working and the data is meaningful. A kprobe's advantage is reach: it can observe any function or address, including internal ones the developers never exposed, with no source changes. kprobes are more fragile because they bind to specific symbols, offsets, and argument registers that are implementation details; when a function is renamed, refactored, inlined away, or its signature changes between kernel versions, the probe either fails to attach or reads the wrong data silently. So prefer tracepoints when one exists, and reach for kprobes when you need visibility that no tracepoint provides, accepting the version-coupling cost.`,
    tags: ['tracepoints', 'kprobes', 'stability'],
  },
  {
    id: 'lx-ch14-t-018',
    chapter: 14,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Why KASAN Slows Things Down',
    prompt: `A team wants to leave KASAN enabled on production servers "just to be safe." Explain the runtime and memory costs that make KASAN a development/testing tool rather than a production default, and what lighter option exists for production-like hardening.`,
    hints: ['Every memory access gets an instrumented shadow check.', 'Shadow memory consumes a fraction of all RAM.'],
    solution: `KASAN instruments every load and store with a shadow-memory check, which typically slows the kernel by roughly 2-3x and increases memory pressure because the shadow region reserves about one eighth of address space (generic KASAN). It also enlarges allocations with redzones and quarantines freed objects, further raising memory use. Those costs are fine in CI and development, where catching the bug is worth the slowdown, but unacceptable for production throughput and capacity. For production-like hardening you would instead use much cheaper, always-on mechanisms such as stack protectors, slab redzoning/poisoning, or hardware-tag-based KASAN on supporting CPUs (MTE), accepting weaker coverage in exchange for low overhead. KASAN's job is to find bugs during testing, not to run forever.`,
    tags: ['kasan', 'overhead', 'production'],
  },
  {
    id: 'lx-ch14-t-019',
    chapter: 14,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'kgdb Versus Crash Dump Analysis',
    prompt: `For a bug that you can reproduce on demand at your desk, you are choosing between attaching kgdb for a live debugging session and collecting a crash dump (kdump/vmcore) to analyze later. Compare the two approaches and say when each shines.`,
    hints: ['kgdb is interactive and live; the dump is a postmortem snapshot.', 'Think about reproducibility and whether the system can be paused.'],
    solution: `kgdb gives you an interactive, live session: you stop the running kernel, set breakpoints, single-step, inspect and even modify variables and memory through a gdb front end (usually over serial or the network). It shines when you can reproduce the bug at will and can afford to halt the machine, because you can poke at live state and watch it evolve. A crash dump (kdump capturing a vmcore) is a postmortem snapshot taken automatically at the moment of panic, analyzed afterward with crash/gdb. It shines for rare or production failures you cannot pause or reproduce on demand, since it preserves the exact failing state without needing the system to stay up. In short: kgdb for reproducible, interactive bench debugging; kdump for capturing one-shot or field crashes for later analysis.`,
    tags: ['kgdb', 'kdump', 'debugging'],
  },
  {
    id: 'lx-ch14-t-020',
    chapter: 14,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'printk From Inside an Interrupt',
    prompt: `A driver writer wants to add a printk inside a hard IRQ handler that fires thousands of times a second. Explain why heavy printk-ing from interrupt context can hang or destabilize the system, and what safer observability choices exist.`,
    hints: ['Synchronous console output can be slow and is done with interrupts off.', 'Tracepoints and ratelimiting avoid the storm.'],
    solution: `Although printk itself is designed to be callable from interrupt context, emitting to a slow console (especially a serial port) can take a long time, and that work can happen with interrupts disabled, so a high-rate IRQ handler that printks on every invocation can spend so long in console output that it starves other interrupts, trips watchdogs, or appears to hang. The flood also wraps the ring buffer, drowning useful messages. Safer choices are to instrument the path with a tracepoint or ftrace (cheap, binary, off by default), to use trace_printk for quick debugging since it writes to the trace buffer rather than the console, or at minimum to use printk_ratelimited / pr_*_ratelimited and a WARN_ON_ONCE so you log a few times instead of thousands. The key idea is to keep heavy, synchronous text output out of hot interrupt paths.`,
    tags: ['printk', 'interrupt', 'tracing'],
  },
  {
    id: 'lx-ch14-t-021',
    chapter: 14,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Stripped Symbols and addr2line',
    prompt: `You have an oops from a customer running a kernel you built, but the call trace shows raw addresses and "[<ffffffffa0...>] ?" with no usable names for your module. Explain what artifact you need to recover real source lines and why the running stripped kernel alone is not enough.`,
    hints: ['You need the matching vmlinux/.ko with symbols and debug info.', 'Addresses must map back through the original build.'],
    solution: `To map raw addresses to functions and source lines you need the unstripped build artifacts from the exact same build: the vmlinux with symbols (and ideally CONFIG_DEBUG_INFO) for core kernel addresses, and the unstripped .ko (plus its load address) for module addresses. The running kernel is stripped for size, so its in-memory text has no line-number information; the addresses are only meaningful when fed back through the original object that knows where each function and line landed. With the right vmlinux/.ko you run addr2line (or scripts/decode_stacktrace.sh, or gdb's list/info line) on each address to get file:line. If the build is not byte-for-byte the same, the offsets will not line up and you will get wrong or misleading source locations, which is why preserving per-build debug artifacts matters.`,
    tags: ['stack-trace', 'addr2line', 'debuginfo'],
  },
  {
    id: 'lx-ch14-t-022',
    chapter: 14,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Filtering ftrace To Cut Noise',
    prompt: `Tracing all functions with ftrace instantly overflows the ring buffer and drops events. Describe at least two mechanisms ftrace gives you to reduce the data to only what you care about, and the tradeoff of each.`,
    hints: ['set_ftrace_filter restricts which functions are traced.', 'Per-event filters and triggers cut by condition; pid/cpu narrows scope.'],
    solution: `First, function filtering: writing patterns to set_ftrace_filter (and exclusions to set_ftrace_notrace) restricts tracing to a chosen set of functions, dramatically shrinking volume, at the cost of possibly missing relevant calls you did not anticipate. Second, scope narrowing: set_event_pid or set_ftrace_pid and per-CPU buffers limit tracing to specific processes or CPUs, which is great for a known workload but blind to anything outside that scope. Third, event filters and triggers: tracepoint events accept a filter expression (for example only when a field exceeds a threshold) and triggers that start/stop tracing on a condition, so you record only interesting samples but must know the condition in advance. Each mechanism trades completeness for a manageable, readable buffer; the art is filtering aggressively enough to keep events without discarding the moment you need.`,
    tags: ['ftrace', 'filtering', 'ring-buffer'],
  },
  {
    id: 'lx-ch14-t-023',
    chapter: 14,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Heisenbug From Adding printk',
    prompt: `A race condition reliably crashes, but once a developer adds a printk near the suspect code, the crash disappears. Explain mechanistically how the printk could be hiding the bug, and what observation tools you would prefer that are less likely to perturb timing.`,
    hints: ['printk changes timing and can take locks / disable preemption.', 'Low-overhead, passive tracing perturbs less.'],
    solution: `A printk is not free of side effects: it formats a string, takes the logbuf lock, may disable preemption or interrupts during console output, and can take a long time on a slow console. All of that changes the timing and serialization of the surrounding code, so a narrow race window that was reliably hit can be widened, closed, or reordered by the printk, masking the bug, a classic heisenbug. The fix is to observe with tools that perturb timing as little as possible: a tracepoint or trace_printk that writes a few bytes to a per-CPU buffer without console I/O or heavy locking, or even better a passive analysis tool like KASAN or lockdep that reports the defect from instrumentation rather than from a print whose latency moves the race. If you must print, do it after the fact from the buffered trace, not synchronously in the hot path.`,
    tags: ['heisenbug', 'printk', 'race-condition'],
  },
  {
    id: 'lx-ch14-t-024',
    chapter: 14,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Why lockdep Misses a Real Deadlock',
    prompt: `A production deadlock involving two spinlocks was never flagged by lockdep in testing, yet it is plainly an A-then-B / B-then-A inversion. Explain how it is possible for lockdep to stay silent about a genuine lock-order bug, and what this implies about how you should run lockdep.`,
    hints: ['lockdep only learns orderings it actually observes at runtime.', 'A path never exercised teaches it nothing.'],
    solution: `lockdep is observational, not a static analyzer: it can only learn a dependency from acquisitions it actually witnesses. If one of the two inverse orderings lives on a rarely-taken path (an error branch, a specific device, a race-only window) that your tests never exercised, lockdep never records that edge, so it cannot detect the cycle and stays silent even though the bug is real. It also tracks per lock class, so misclassified or dynamically keyed locks can hide an inversion. The implication is that lockdep's value depends entirely on coverage: you must run a lockdep-enabled kernel across as many code paths, error injections, and workloads as possible (fuzzing, stress, all driver configs) to teach it both orderings. Absence of a lockdep splat is not proof of correctness, only that the dangerous pair was never both seen.`,
    tags: ['lockdep', 'deadlock', 'coverage'],
  },
  {
    id: 'lx-ch14-t-025',
    chapter: 14,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'KASAN Reports a Slab But Not the Root Cause',
    prompt: `KASAN cleanly reports a "slab-out-of-bounds write" with the faulting access, the allocation stack, and the free stack. A developer fixes the line KASAN pointed at, but the bug returns elsewhere. Explain why the reported access may not be where the real defect lives, and how the allocation/free stacks help you reason about the true cause.`,
    hints: ['The detected access is the first victim, not necessarily the culprit.', 'Allocation/free traces tell you the object lifecycle.'],
    solution: `KASAN reports the first access that lands in poisoned or red-zoned memory, which is the symptom, not necessarily the source: a write that overruns object A by a few bytes might be caused by a wrong length computed far away, by an off-by-one in a shared helper, or by a different object being too small for its real use. "Fixing" the flagged line can merely move the corruption so the next out-of-bounds access trips somewhere else. The allocation stack tells you who created and sized the object (so you can check whether it was allocated too small, or whether the size assumption is wrong), and for use-after-free the free stack tells you who released it and whether a dangling reference outlived it. Reading those lifecycle traces lets you reason about the object's intended size and lifetime and find the actual logic error, rather than patching the spot where the damage happened to be noticed.`,
    tags: ['kasan', 'root-cause', 'slab'],
  },
  {
    id: 'lx-ch14-t-026',
    chapter: 14,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Choosing perf, ftrace, or a kprobe',
    prompt: `You are handed an open-ended report: "the system has occasional 50ms latency spikes under load." Outline a principled order in which you would reach for perf, ftrace, and kprobes to localize the cause, and justify why that order minimizes wasted effort.`,
    hints: ['Start broad and low-overhead, then drill into the suspect path.', 'Latency tracers and function_graph expose where time goes.'],
    solution: `Start broad and cheap, then narrow. First use perf (stat/record or perf sched / perf top) to characterize the system: is the latency on-CPU compute, scheduling delay, I/O, or lock contention? This costs little and tells you which subsystem to suspect. Next move to ftrace's latency-oriented tracers (for example the function_graph tracer to time call subtrees, or wakeup/irqsoff/preemptoff tracers) scoped with filters and a trigger that captures only when a 50ms threshold is exceeded, so you see exactly which functions or disabled-interrupt windows eat the time. Finally, once you have a specific suspect function or boundary, plant a kprobe/kretprobe (or a histogram trigger) to measure its arguments, return values, and per-call durations precisely. This order, observe-wide then drill-narrow, avoids the trap of deploying an expensive, finely targeted kprobe before you know where to point it, and keeps overhead low until you have earned the right to be specific.`,
    tags: ['perf', 'ftrace', 'kprobes'],
  },
  {
    id: 'lx-ch14-t-027',
    chapter: 14,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Designing a Tracepoint Worth Merging',
    prompt: `You are adding a static tracepoint to a subsystem you maintain so others can observe it long-term. List the design decisions that make a tracepoint genuinely useful and stable, and explain the danger of recording raw internal pointers as fields.`,
    hints: ['Think about stability of the field set and the ABI implications.', 'Pointers leak addresses and break across versions/layouts.'],
    solution: `A good tracepoint is placed at a meaningful semantic event (request submitted, packet dropped, state changed), not at an arbitrary line, and records the fields a consumer actually needs to reason about that event: stable identifiers, sizes, error codes, and human-meaningful values, with clear names and units. Because tracepoints become a de facto user-visible ABI that tools depend on, you should keep the field set stable, avoid encoding fragile internal details, and add new fields rather than repurposing old ones. Recording raw kernel pointers is dangerous on two counts: it leaks kernel addresses (an information-disclosure and KASLR-defeat risk, which is why %p is hashed), and pointer values are meaningless across runs and depend on internal layout, so they break tools and reveal nothing portable. Prefer logging stable derived attributes (an id, an index, a state enum) over raw addresses, and keep the cost low so the tracepoint stays nearly free when disabled.`,
    tags: ['tracepoints', 'abi', 'design'],
  },
  {
    id: 'lx-ch14-t-028',
    chapter: 14,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'When the Oops Trace Is a Liar',
    prompt: `An oops points the faulting instruction at a well-tested, obviously-correct function that "cannot" be the bug. Give two distinct mechanisms by which the real defect lives elsewhere yet manifests as a crash in innocent code, and how you would confirm each.`,
    hints: ['Memory corruption from one place crashes another.', 'Stack overflow and bad function pointers redirect control.'],
    solution: `First, heap or slab corruption: a buggy writer elsewhere overruns a buffer or uses memory after free, and the innocent function simply later dereferences the corrupted data or a smashed list pointer and faults; the crash site is the victim, not the culprit. You confirm this by rebuilding with KASAN or slab debugging (redzones/poison), which catch the corrupting access at its source rather than at the downstream crash. Second, a corrupted control transfer: an overflowed kernel stack or a clobbered function pointer / return address sends the CPU into the wrong place, so the trace blames whatever code it landed in; you confirm this by enabling stack-overflow detection (CONFIG_VMAP_STACK guard pages, CONFIG_SCHED_STACK_END_CHECK) and by sanity-checking the registers and stack in the dump for nonsense values. The general lesson is that an oops reports where execution died, which under memory corruption is frequently far from where the bug was committed.`,
    tags: ['oops', 'corruption', 'root-cause'],
  },
  {
    id: 'lx-ch14-t-029',
    chapter: 14,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Sampling Bias in a perf Profile',
    prompt: `A perf profile says 80% of time is in a tiny function, but rewriting it gives no speedup. Explain how sampling, skid, and inlining can make a perf profile misattribute time, and how you would cross-check before optimizing.`,
    hints: ['PMU interrupts attribute the sample to a nearby, not exact, instruction.', 'Inlined and leaf functions distort attribution; check call stacks and counters.'],
    solution: `perf record is statistical and imperfect: the PMU raises the sampling interrupt some cycles after the event, so the recorded instruction pointer can "skid" onto a neighboring instruction or the next function, piling samples onto an innocent leaf. Attribution is further distorted by inlining (a hot inlined body is credited to its caller, or vice versa), by lack of frame pointers making stacks unreliable, and by symbol mismatches that fold time into the wrong name. A tiny function showing 80% may really be absorbing skid from an expensive caller, or be memory-stalled rather than compute-bound so changing its code does nothing. To cross-check, capture call stacks (perf record -g with good unwinding), look at the surrounding callers in perf report, use precise/PEBS events to reduce skid, and confirm the bottleneck's nature with perf stat counters (cache misses, stalls). Only optimize once independent evidence agrees on where and why the time goes.`,
    tags: ['perf', 'sampling-bias', 'profiling'],
  },
  {
    id: 'lx-ch14-t-030',
    chapter: 14,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'A Layered Debug-Build Strategy',
    prompt: `For a kernel you maintain, design a tiered debugging configuration: which checkers (KASAN, lockdep, and friends) you would enable in CI versus a developer's daily build versus production, and justify the tradeoffs at each tier.`,
    hints: ['Heavy sanitizers in CI; cheaper checks for daily use; near-zero overhead in production.', 'Match overhead to how often the build runs and how much speed matters.'],
    solution: `Match instrumentation cost to each tier's purpose. In CI, enable the heavy, high-coverage checkers because correctness beats speed there: KASAN, KMSAN/uninitialized-memory checks where available, full lockdep and other lock debugging, slab redzoning/poison, KCSAN for data races, and DEBUG_INFO for decodable traces, accepting the multi-x slowdown and extra memory because runs are automated and finding bugs is the whole point. A developer's daily build wants a usable middle ground: keep lockdep and lightweight DEBUG options and panic_on_warn for fast feedback, but often drop the most expensive sanitizers (or run KASAN only when chasing a memory bug) so the system stays interactive. Production runs near-zero-overhead hardening only: stack protectors, KASLR, lightweight WARN reporting, kdump for postmortem, and maybe hardware-tag KASAN on capable CPUs, never generic KASAN or full lockdep, since throughput and capacity dominate. The unifying principle is that a check's cost is paid as often as the tier runs, so you front-load expensive verification into automation and keep production lean.`,
    tags: ['kasan', 'lockdep', 'kconfig'],
  },
]

export default problems
