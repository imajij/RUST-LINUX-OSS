import type { Problem } from '../../types'

const problems: Problem[] = [
  {
    id: 'lx-ch02-t-001',
    chapter: 2,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'Naming The Four Compilation Stages',
    prompt: `Building a C program with gcc goes through four classic stages. Name them in order, and for each one say in a sentence what it consumes and what it produces.`,
    hints: [
      'Think about what the # directives need handled first.',
      'There is a stage that produces human-readable assembly and one that produces machine code.',
    ],
    solution: `The four stages are preprocess, compile, assemble, and link. Preprocessing (cpp) takes the .c source, expands #include, #define, and conditionals, and emits a single expanded translation unit (.i). Compiling takes that and produces assembly text (.s) for the target architecture. Assembling (as) turns the .s into a relocatable object file (.o) containing machine code plus symbol/relocation tables. Linking (ld) combines one or more .o files and libraries, resolving symbol references, into a final executable or shared object. You can stop after any stage with gcc flags: -E (preprocess only), -S (stop at assembly), and -c (stop at object file).`,
    tags: ['compilation', 'gcc', 'toolchain'],
  },
  {
    id: 'lx-ch02-t-002',
    chapter: 2,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'What Exit Code Zero Means',
    prompt: `In the shell you run a program and then check the variable that holds its result. A successful run reports 0 and a failure reports a small nonzero number. Why is this convention "backwards" from booleans (where 0 is false), and how do you inspect this value after running a command?`,
    hints: ['There is exactly one way to succeed but many ways to fail.', 'Look at the special $? variable.'],
    solution: `By POSIX convention a process returns 0 to mean success and any value from 1 to 255 to indicate some kind of failure. It seems backwards versus C booleans, but the reasoning is that there is only one way to succeed, while there are many distinct failure reasons, so nonzero codes can encode which failure occurred. In the shell you read the exit status of the last command from the special variable $? (for example, run a command then echo $?). This is also why main() returning 0, or calling exit(0)/EXIT_SUCCESS, signals success to the shell.`,
    tags: ['shell', 'exit-code'],
  },
  {
    id: 'lx-ch02-t-003',
    chapter: 2,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'Header Versus Source File Roles',
    prompt: `A coworker puts the full body of a function inside a .h header and includes that header in three different .c files of the same program. The build fails at link time. Explain what a header is supposed to hold versus a source file, and why this caused a problem.`,
    hints: ['Headers usually hold declarations; source files hold definitions.', 'Think about how many copies of the function body now exist.'],
    solution: `A header file is meant to hold declarations: function prototypes, type and struct declarations, macros, and extern variable declarations, so multiple translation units can agree on interfaces. The actual definition (the function body) belongs in exactly one .c source file. Because the full body was placed in the header and that header was included by three .c files, each translation unit got its own definition of the same function, producing three identical external symbols. The linker then sees a multiply-defined symbol and fails. The fix is to keep only the prototype in the header and put the single definition in one .c file (or mark it static/inline if it truly must live in the header).`,
    tags: ['headers', 'linking', 'translation-unit'],
  },
  {
    id: 'lx-ch02-t-004',
    chapter: 2,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'Which Man Page Section To Read',
    prompt: `You type "man printf" and the page describes a command-line program that formats text, not the C library function you wanted. Explain how man pages are organized into sections, and how you ask for the C library version instead.`,
    hints: ['Section 1 is user commands; sections 2 and 3 cover programming.', 'You can pass a section number explicitly.'],
    solution: `Man pages are grouped into numbered sections by category. Section 1 is user commands (shell programs), section 2 is system calls (direct kernel entry points), section 3 is library functions (the C library and other libraries), section 5 is file formats, section 7 is overviews/conventions, and section 8 is admin commands. By default "man printf" found the section 1 command. To get the C function you ask for section 3 explicitly with "man 3 printf". This same trick distinguishes, for example, the open() system call ("man 2 open") from any unrelated section 1 entry.`,
    tags: ['man-pages', 'documentation'],
  },
  {
    id: 'lx-ch02-t-005',
    chapter: 2,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Why -E Stops Early',
    prompt: `You run "gcc -E foo.c" and the terminal fills with thousands of lines, including content you never wrote, even though foo.c is short. Explain what -E does and why the output is so large.`,
    hints: ['-E runs only the first stage.', 'Consider what #include <stdio.h> pulls in.'],
    solution: `The -E flag tells gcc to stop after the preprocessing stage and print the expanded translation unit instead of compiling. Preprocessing textually expands every #include by pasting in the entire contents of the included headers, recursively, plus expanding macros and resolving conditional compilation. A short foo.c that does #include <stdio.h> drags in stdio.h and everything stdio.h itself includes, which is why thousands of lines of declarations appear. This output is exactly what the real compiler stage would then parse, which is useful for debugging macro expansions or surprising header behavior.`,
    tags: ['preprocessor', 'gcc', 'compilation'],
  },
  {
    id: 'lx-ch02-t-006',
    chapter: 2,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Object File Has Undefined Symbols',
    prompt: `You compile one source file with "gcc -c util.c" and it succeeds, producing util.o. But util.c calls a function defined elsewhere. Will the compile step complain that the function is missing? When is that resolved? Use nm to describe what you would see.`,
    hints: ['Compilation works one translation unit at a time.', 'nm marks undefined symbols with U.'],
    solution: `No, "gcc -c" only compiles and assembles a single translation unit into a relocatable object file; it does not need the definitions of functions that live in other files. The call to the external function becomes an unresolved reference recorded in util.o's symbol table. If you run "nm util.o" you will see that symbol marked with a capital U (undefined), meaning "this object needs it but does not provide it." That reference is only satisfied later, at link time, when the linker combines util.o with the object or library that actually defines the function. If no object ultimately provides it, the linker reports an undefined-reference error.`,
    tags: ['nm', 'linking', 'symbols'],
  },
  {
    id: 'lx-ch02-t-007',
    chapter: 2,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'A Minimal Makefile Rule',
    prompt: `Describe the three parts of a basic make rule (target, prerequisites, recipe) and explain how make decides whether the recipe actually needs to run. Why must recipe lines be indented with a real tab?`,
    hints: ['make compares file modification timestamps.', 'A space-indented recipe is a classic make error.'],
    solution: `A make rule has a target (the file to build), a list of prerequisites (files it depends on), and a recipe (the shell commands to build it), written as "target: prereqs" followed by tab-indented command lines. make runs the recipe only when the target is missing or older than at least one prerequisite, by comparing modification timestamps; if the target is newer than all prerequisites it is considered up to date and skipped. This timestamp logic is what lets make rebuild only what changed. Recipe lines must begin with an actual tab character, not spaces, because make uses the leading tab to distinguish a command line from other syntax; using spaces produces the classic "missing separator" error.`,
    tags: ['make', 'makefile', 'build'],
  },
  {
    id: 'lx-ch02-t-008',
    chapter: 2,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Setting A gdb Breakpoint',
    prompt: `You compiled a program and want to pause execution right when a specific function begins so you can inspect its arguments. Explain how to set a breakpoint on that function in gdb, how to start the program, and what one compile flag makes this inspection far more useful.`,
    hints: ['break by function name, then run.', 'Without -g you lose line numbers and local variable names.'],
    solution: `In gdb you set a breakpoint with "break funcname" (or "b funcname"), which tells gdb to stop whenever execution enters that function. You then start the program under gdb with "run" (plus any program arguments); it executes normally until it hits the breakpoint, where it pauses and you can use "print" or "info args" to inspect arguments and locals, and "backtrace" to see the call stack. The crucial compile flag is -g, which embeds debugging information (symbol names, types, and line-number mappings) into the binary. Without -g, gdb can often still break on a function address but cannot show source lines, parameter names, or local variable values meaningfully.`,
    tags: ['gdb', 'debugging'],
  },
  {
    id: 'lx-ch02-t-009',
    chapter: 2,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Static Versus Dynamic Link Size',
    prompt: `You build the same program two ways: once with "-static" and once normally (dynamic). The static binary is much larger. Explain why, and one practical advantage and one disadvantage of the static build.`,
    hints: ['Static linking copies library code into the executable.', 'Think about deploying to a machine without the library.'],
    solution: `With static linking the linker copies the machine code of the library functions your program actually references directly into the executable, so the file is self-contained and therefore larger. With dynamic linking the executable instead records that it needs a shared library (a .so), and the loader maps that library in at run time, so the binary stays small and many programs share one in-memory copy. A practical advantage of static linking is portability: the binary can run on a system that lacks the library (or has a different version), with no runtime dependency. A disadvantage is that bug or security fixes to the library require recompiling and redistributing every static binary, whereas dynamically linked programs pick up a fixed .so automatically, and static binaries waste disk and memory by duplicating common code.`,
    tags: ['linking', 'static', 'dynamic'],
  },
  {
    id: 'lx-ch02-t-010',
    chapter: 2,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Pipeline Exit Status Surprise',
    prompt: `In bash you run "grep pattern file.txt | sort" where grep finds nothing (so grep exits nonzero), yet checking the status afterward reports 0. Explain why, and how you would detect the grep failure instead.`,
    hints: ['By default the pipeline status comes from the last command.', 'Look at PIPESTATUS or set -o pipefail.'],
    solution: `By default the exit status of a pipeline is the exit status of its last command only. Here sort succeeded with status 0 even though grep exited 1 (no match), so $? reflects sort, not grep, and the failure is hidden. To detect grep's result you have a few options: inspect the bash array PIPESTATUS, which records the status of every command in the pipeline (so the first element is grep's), or enable "set -o pipefail" so the pipeline returns the rightmost nonzero status if any command fails. Understanding this matters in scripts where a silently failing middle stage would otherwise be masked by a successful final command.`,
    tags: ['shell', 'pipeline', 'exit-code'],
  },
  {
    id: 'lx-ch02-t-011',
    chapter: 2,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'System Call Versus Library Function',
    prompt: `Both "man 2 write" and "man 3 fwrite" exist. Conceptually, what is the difference between something documented in section 2 versus section 3, and why might a library function in section 3 ultimately call into section 2?`,
    hints: ['Section 2 entries cross into the kernel.', 'Buffered stdio sits on top of raw I/O.'],
    solution: `Section 2 documents system calls, which are the thin entry points where user space requests a service from the kernel (a controlled trap into kernel mode), such as write() moving bytes to a file descriptor. Section 3 documents library functions, which run entirely in user space as part of the C library or other libraries, such as fwrite() in the buffered stdio layer. A section 3 function often wraps and eventually invokes section 2 calls: fwrite() accumulates data in a user-space buffer and, when the buffer fills or is flushed, issues one or more write() system calls. So the distinction is which side of the user/kernel boundary the code primarily lives on, and library functions frequently batch or convenience-wrap the raw syscalls underneath.`,
    tags: ['man-pages', 'syscall', 'libc'],
  },
  {
    id: 'lx-ch02-t-012',
    chapter: 2,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Why Recompile After Editing A Header',
    prompt: `Your Makefile lists "main.o: main.c" as a rule, but main.c includes config.h. You edit config.h and run make, and nothing rebuilds. Why did make miss the change, and how should the dependency be expressed?`,
    hints: ['make only knows the prerequisites you tell it.', 'Headers are inputs to a translation unit too.'],
    solution: `make decides what to rebuild purely from the prerequisites you declare. The rule only lists main.c as a prerequisite of main.o, so make compares main.o's timestamp against main.c, never against config.h. Editing config.h leaves main.c untouched, so main.o still looks up to date and make skips it, even though the compiled output would actually differ. The fix is to add the header as a prerequisite, e.g. "main.o: main.c config.h", so editing config.h makes the header newer than main.o and triggers a rebuild. In larger projects this is automated by generating dependency files (gcc -MMD/-MP) so every included header is tracked.`,
    tags: ['make', 'headers', 'dependencies'],
  },
  {
    id: 'lx-ch02-t-013',
    chapter: 2,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Reading Disassembly With objdump',
    prompt: `You have a compiled object file foo.o and want to see the actual assembly instructions generated for its functions, interleaved with the original C source lines. Which tool and flags help, and what compile flag must have been used for the source interleaving to work?`,
    hints: ['objdump disassembles object files.', 'Source interleaving needs debug info.'],
    solution: `Use objdump to disassemble the object file: "objdump -d foo.o" prints the machine instructions for each section that contains code, grouped by symbol. To interleave the original C source lines with the disassembly you add the source option, "objdump -S foo.o" (often combined as -d -S). For that source view to appear, foo.o must have been compiled with -g so that debug line-number information mapping instructions back to source is present; without -g you still get the raw disassembly but no source lines. This is a common way to understand exactly what the compiler emitted, for example to check whether an optimization fired.`,
    tags: ['objdump', 'disassembly', 'debugging'],
  },
  {
    id: 'lx-ch02-t-014',
    chapter: 2,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Declaration Without Definition At Link Time',
    prompt: `main.c declares "extern int counter;" and uses it, but no .c file ever defines "int counter;". The code compiles each file fine but fails to link. Contrast this with the case where main.c forgot the declaration entirely and just used counter. Explain both failures and which stage catches each.`,
    hints: ['extern promises a definition exists elsewhere.', 'A use with no declaration at all is caught earlier.'],
    solution: `"extern int counter;" is a declaration, not a definition: it promises the variable exists somewhere and tells the compiler its type so each translation unit compiles cleanly, leaving an unresolved reference. Because no .c file actually defines "int counter;" (which allocates storage), the linker finds nothing to satisfy that reference and reports an undefined-reference error at link time. In the contrasting case where main.c uses counter with no declaration at all, the compiler itself errors during the compile stage, because the identifier is unknown in that translation unit (in modern C, an undeclared identifier is a compile error). So a missing definition is a link-time failure, while a missing declaration is a compile-time failure; the fix is to declare it (extern) in a header and define it exactly once in one .c file.`,
    tags: ['linking', 'symbols', 'extern'],
  },
  {
    id: 'lx-ch02-t-015',
    chapter: 2,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'One Definition Across Many Files',
    prompt: `Three source files all include "shared.h", which contains "int global_count = 0;" (a definition with an initializer). Each file compiles, but linking fails with "multiple definition of global_count". Explain the rule being violated and two correct ways to share this variable.`,
    hints: ['A definition emits storage; that must happen once.', 'extern in the header, definition in one .c.'],
    solution: `Putting "int global_count = 0;" in a header that three files include means each translation unit emits its own definition (storage) for global_count, so the linker sees the same external symbol defined three times and rejects it; this violates the rule that an external object may be defined exactly once across the whole program. One correct fix is to declare it in the header as "extern int global_count;" (a declaration, no storage) and place the single definition "int global_count = 0;" in exactly one .c file. Another approach, if the variable should be private to one file, is to mark it "static int global_count = 0;" so it has internal linkage and each file gets its own independent copy with no symbol clash. The header should generally hold only the extern declaration so every including file agrees on the type without redefining the object.`,
    tags: ['linking', 'headers', 'odr'],
  },
  {
    id: 'lx-ch02-t-016',
    chapter: 2,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Include Guards And Double Inclusion',
    prompt: `A header types.h defines "struct point { int x, y; };". Another header geometry.h includes types.h, and a .c file includes both types.h and geometry.h. Without include guards the compile fails. Explain the failure and how an include guard (or pragma once) prevents it.`,
    hints: ['The struct gets textually pasted twice into one translation unit.', 'A guard macro makes the second inclusion a no-op.'],
    solution: `Preprocessing is textual: when the .c file includes types.h directly and also includes geometry.h (which itself includes types.h), the body of types.h is pasted into the translation unit twice. The C compiler then sees "struct point" defined twice in the same translation unit, which is an illegal redefinition, and compilation fails. An include guard wraps the header body in "#ifndef TYPES_H / #define TYPES_H / ... / #endif": the first inclusion defines TYPES_H and emits the contents, and any later inclusion finds TYPES_H already defined so the #ifndef is false and the body is skipped. The "#pragma once" directive achieves the same effect by telling the preprocessor to include the file at most once. Either way the struct is defined exactly once per translation unit.`,
    tags: ['preprocessor', 'headers', 'include-guard'],
  },
  {
    id: 'lx-ch02-t-017',
    chapter: 2,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Why Optimized Builds Are Hard To Debug',
    prompt: `A program built with "-O2 -g" is being debugged in gdb. The developer notices that some local variables print as "optimized out" and that stepping through source lines jumps around oddly. Explain why optimization causes this, and what build option would make line-by-line debugging behave more intuitively.`,
    hints: ['Optimization rearranges and eliminates code.', 'Consider -O0.'],
    solution: `At -O2 the compiler aggressively transforms code: it may keep a variable only in a register or eliminate it when its value is no longer needed, so gdb reports it as "optimized out" because there is no longer a stable memory location holding it. The compiler also reorders, merges, and inlines instructions, so the machine code no longer maps cleanly one-to-one onto source lines, which makes stepping appear to jump backward and forward. The -g flag still records debug info, but it can only describe what the optimizer actually produced. For intuitive step debugging you build with -O0 (no optimization, usually with -g), which keeps variables in memory and preserves a straightforward source-to-instruction mapping, at the cost of slower, larger code. This is why many debug builds use "-O0 -g".`,
    tags: ['gdb', 'optimization', 'debugging'],
  },
  {
    id: 'lx-ch02-t-018',
    chapter: 2,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Conditional Build With Make And Exit Codes',
    prompt: `A shell script runs "make && ./run_tests" and you find run_tests never executes after a failed build. Separately, "make ; ./run_tests" always runs the tests even when the build broke. Explain how the shell operators && and ; relate to exit codes, and which one you want here.`,
    hints: ['&& runs the right side only on success.', '; ignores the left side status.'],
    solution: `The shell chooses what runs next based on exit codes. The "&&" operator runs the right-hand command only if the left-hand command exited 0 (success), so "make && ./run_tests" correctly skips the tests when make fails (make returns nonzero on a broken build). The ";" operator is just a sequencer: it runs the next command unconditionally regardless of the previous exit status, so "make ; ./run_tests" runs the tests even on a failed build, which can waste time or produce confusing results against stale binaries. There is also "||", which runs the right side only on failure (nonzero). For a build-then-test workflow you want "&&" so testing is gated on a successful build.`,
    tags: ['shell', 'make', 'exit-code'],
  },
  {
    id: 'lx-ch02-t-019',
    chapter: 2,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Static Library Versus Shared Library',
    prompt: `You can package reusable code as a static archive (libfoo.a) or a shared object (libfoo.so). Compare how each is incorporated into a program at build and run time, and explain what happens to already-built programs when you ship a bug fix as a new libfoo.so versus a new libfoo.a.`,
    hints: ['.a is pulled in at link time; .so is loaded at run time.', 'Consider who has to rebuild.'],
    solution: `A static archive (libfoo.a) is a bundle of .o files; at link time the linker copies the needed object code straight into each executable, so there is no runtime dependency and no library is loaded separately. A shared object (libfoo.so) is not copied in; the linker only records the dependency, and the dynamic loader maps the .so into the process at run time, with one on-disk and in-memory copy shared by many programs. When you ship a bug fix as a new libfoo.so, every dynamically linked program automatically uses the fixed code the next time it runs, with no rebuild. When you ship a fix as a new libfoo.a, already-built programs still contain the old copied-in code and gain nothing until each one is relinked and redeployed. This is the central trade-off: shared libraries ease updates and save space, static libraries maximize self-containment and portability.`,
    tags: ['linking', 'static', 'dynamic'],
  },
  {
    id: 'lx-ch02-t-020',
    chapter: 2,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Inspecting A Binary With readelf',
    prompt: `You are handed an unknown executable and want to know: is it dynamically linked, and if so which shared libraries does it expect? Describe what readelf can tell you about the ELF file, and what part of the file records the needed libraries.`,
    hints: ['ELF files have a header plus program/section headers.', 'The dynamic section lists NEEDED entries.'],
    solution: `readelf decodes the ELF (Executable and Linkable Format) structure. "readelf -h" prints the ELF header, which states whether the file is an executable, a shared object, or relocatable, plus the architecture and entry point. To learn about dynamic linking you look at the dynamic section with "readelf -d": a dynamically linked binary has an interpreter entry and a series of "NEEDED" entries, each naming a shared library (e.g. libc.so.6) the loader must provide at run time. If the file is fully static there is no interpreter and no NEEDED list. You can also use "readelf -s" to inspect the symbol tables. The NEEDED entries in the dynamic section are precisely the recorded shared-library dependencies.`,
    tags: ['readelf', 'elf', 'dynamic'],
  },
  {
    id: 'lx-ch02-t-021',
    chapter: 2,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Static Symbols And nm Case',
    prompt: `Running "nm helper.o" shows one symbol as "t process_item" (lowercase t) and another as "T public_api" (uppercase T). Explain what the case of the symbol letter tells you, and how that relates to the C keyword likely used on process_item.`,
    hints: ['Uppercase means external/global; lowercase means local.', 'Section letters: T/t is the text (code) section.'],
    solution: `In nm output the letter indicates the symbol type and section, and its case indicates linkage: uppercase means a global (external) symbol visible to other object files, lowercase means a local symbol with internal linkage not exported beyond this object. The letter T (or t) specifically means the symbol lives in the text section, i.e. it is a function/code symbol. So "T public_api" is a globally visible function other translation units can link against, while "t process_item" is a file-local function. The lowercase t strongly suggests process_item was declared "static" in the C source, which gives a function internal linkage and hides it from the linker's global namespace. Marking helpers static is good practice: it prevents name clashes and signals the function is an implementation detail.`,
    tags: ['nm', 'symbols', 'static'],
  },
  {
    id: 'lx-ch02-t-022',
    chapter: 2,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Kbuild Object Selection',
    prompt: `In a kernel subdirectory's Makefile you see "obj-y += foo.o" in one place and "obj-m += bar.o" in another, plus "foo-y := foo_main.o foo_util.o". Without writing full rules like a normal Makefile, explain what these Kbuild assignments mean and how they differ from a hand-written application Makefile.`,
    hints: ['obj-y is built into the kernel; obj-m builds a module.', 'foo-y lists the parts of a composite object.'],
    solution: `The kernel build system (Kbuild) is declarative: instead of writing target/prerequisite/recipe rules, you populate goal lists and Kbuild's generic rules do the compiling and linking. "obj-y += foo.o" means foo.o is compiled and linked directly into the built-in kernel image (vmlinux). "obj-m += bar.o" means bar.o is built as a loadable kernel module (bar.ko) that can be inserted/removed at run time rather than compiled in. The "foo-y := foo_main.o foo_util.o" line says foo.o is a composite object assembled from those two pieces, so the multiple source files are combined into the single foo.o. This differs from an application Makefile, where you typically spell out gcc commands and dependencies yourself; Kbuild abstracts that away so subsystem Makefiles mostly just list which objects belong where.`,
    tags: ['kbuild', 'make', 'kernel'],
  },
  {
    id: 'lx-ch02-t-023',
    chapter: 2,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Link Order And Static Library Resolution',
    prompt: `A build does "gcc -lm prog.o -o prog" and fails with an undefined reference to sqrt, but "gcc prog.o -lm -o prog" succeeds. Both reference the same math library. Explain why link order matters with the traditional one-pass static linker and what rule this implies for ordering objects and libraries.`,
    hints: ['The linker pulls only symbols still unresolved when it reaches a library.', 'prog.o needs sqrt; -lm provides it.'],
    solution: `The traditional Unix linker processes its inputs in a single left-to-right pass, maintaining a set of currently-unresolved symbols, and pulls from an archive/library only those members that satisfy symbols already pending. In "gcc -lm prog.o", when the linker reaches -lm there are no unresolved symbols yet (prog.o has not been seen), so it takes nothing from libm; then prog.o introduces an unresolved sqrt, but libm has already been passed, leaving sqrt undefined. In "gcc prog.o -lm", prog.o is processed first and records the unresolved sqrt, so when -lm is reached the linker finds and pulls in sqrt to satisfy it, and linking succeeds. The general rule is to list object files first and libraries after, ordered so that each library appears after everything that depends on it (with mutual dependencies sometimes needing a library listed twice or grouped). Modern GNU ld can relax this, but the safe portable habit is "objects, then libraries that they need."`,
    tags: ['linking', 'static', 'link-order'],
  },
  {
    id: 'lx-ch02-t-024',
    chapter: 2,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'ABI Break In A Shared Library',
    prompt: `A shared library exports "struct config" and a function that takes "struct config *". A new release adds a field in the middle of struct config and rebuilds libfoo.so, but does not change the soname or recompile the application. The application starts crashing or behaving wrongly. Explain the root cause and how versioning/sonames are meant to prevent it.`,
    hints: ['The application was compiled against the old struct layout.', 'Adding a field shifts member offsets.'],
    solution: `The application's machine code was compiled against the old struct layout, so member accesses are baked in as fixed byte offsets and the application allocates objects of the old size. Adding a field in the middle of struct config shifts the offsets of all subsequent members and changes the struct's total size; this is an ABI (application binary interface) break. At run time the application passes an old-layout, old-sized struct to a library that now reads it with new offsets, so members are misread, and the library may also write past what the application allocated, causing wrong values or memory corruption and crashes. The mechanism meant to prevent this is the library soname: an incompatible ABI change should bump the soname major version (libfoo.so.1 to libfoo.so.2), so the loader refuses to silently bind the old application to the new incompatible library, forcing a recompile against the matching headers. Symbol versioning can offer finer-grained compatibility, but a silent in-place ABI change with an unchanged soname is exactly the unsafe situation here.`,
    tags: ['dynamic', 'abi', 'linking'],
  },
  {
    id: 'lx-ch02-t-025',
    chapter: 2,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Debugging A Stripped Kernel Module',
    prompt: `You have a crash backtrace from a kernel module that shows only hex addresses and offsets like "my_func+0x4a/0x80", not meaningful source lines. You still have the original built .ko and its build tree. Explain conceptually how the address-to-source mapping is recovered, and why a fully stripped binary alone makes this impossible.`,
    hints: ['Symbol names plus offsets identify a function and a point within it.', 'Debug info / symbol tables are what map addresses to source.'],
    solution: `The notation "my_func+0x4a/0x80" already tells you the crash is 0x4a bytes into a function whose total size is 0x80, so even with just the symbol table you can identify which function and roughly where. To get the exact source line you need the mapping from instruction addresses to source files and line numbers, which lives in the symbol table and DWARF debug information of the unstripped object. Using the preserved .ko (built with debug info) and tools such as objdump -dS or addr2line/gdb, you resolve "my_func+0x4a" to the precise source line by looking up that offset in the debug line table. A fully stripped binary has had its symbol table and debug sections removed to save space, so there is no record connecting addresses back to names or lines; the same hex address then carries no recoverable meaning unless you have a separate unstripped copy or debug file built from the identical source and compiler. This is why keeping unstripped/debug builds (or System.map and vmlinux) around is essential for diagnosing kernel oopses.`,
    tags: ['debugging', 'kernel', 'symbols'],
  },
  {
    id: 'lx-ch02-t-026',
    chapter: 2,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Inline Function In A Header',
    prompt: `A performance-sensitive helper is placed in a header as "static inline int clamp(int v, int lo, int hi)" so several .c files can use it. Explain why "static inline" (rather than a plain non-static definition) is the right choice for a header, and what would go wrong with each of the alternatives: a plain definition, or just a prototype.`,
    hints: ['Plain definition in a header repeats across translation units.', 'A prototype alone has no body to inline.'],
    solution: `"static inline" in a header is the standard idiom because each translation unit that includes the header gets its own private definition (static = internal linkage), so there is no clashing external symbol when several .c files include it, and inline hints the compiler it may expand the body at each call site for speed. A plain non-static definition in the header would emit an external definition in every including translation unit, producing duplicate global symbols and a multiple-definition link error (the same problem as putting any function body in a shared header). Just a prototype provides no body at all, so the function must be defined exactly once in some .c file, the compiler cannot inline it across translation units, and you lose the performance benefit and the convenience of a header-only helper. The kernel uses static inline helpers in headers pervasively for exactly these reasons: shareable, inlinable, and free of cross-file symbol collisions.`,
    tags: ['headers', 'inline', 'linking'],
  },
  {
    id: 'lx-ch02-t-027',
    chapter: 2,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Preprocessor Macro Versus Inline Function',
    prompt: `Compare a function-like macro "#define SQUARE(x) ((x)*(x))" with a real (inline) function "int square(int x)". Discuss two concrete ways the macro can misbehave that the function cannot, and one thing the macro can do that an ordinary function cannot. Why is the kernel cautious about complex macros?`,
    hints: ['Macros do textual substitution with no type checking and no single evaluation.', 'Think about SQUARE(i++) and about type genericity.'],
    solution: `A macro is pure textual substitution done by the preprocessor before type checking, so it has classic pitfalls a function avoids. First, double evaluation: SQUARE(i++) expands to ((i++)*(i++)), incrementing i twice with undefined behavior, whereas square(i++) evaluates the argument exactly once. Second, lack of type checking and precedence/operand surprises: without enough parentheses something like a naive SQUARE would mis-bind operators, and even the parenthesized version does no type checking, so wrong-typed arguments are silently accepted while the function enforces its parameter type. The thing the macro can do that an ordinary function cannot is operate generically on any type (it is not tied to int), and it can stringize or paste tokens. The kernel is cautious with complex macros because the double-evaluation and unhygienic-expansion bugs are easy to introduce and hard to spot; it favors static inline functions when a function suffices, reserving macros for genuinely type-generic or token-manipulating needs, and wrapping multi-statement macros in do { ... } while (0) to behave like a single statement.`,
    tags: ['preprocessor', 'inline', 'kernel'],
  },
  {
    id: 'lx-ch02-t-028',
    chapter: 2,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Why Kernel Code Is Not Linked Against libc',
    prompt: `An application can call printf, malloc, and sqrt from the C library, but kernel code cannot link against glibc and instead uses printk, kmalloc, and its own routines. Explain why the standard C library is unavailable to the kernel, and what this implies about how kernel objects are ultimately built and combined.`,
    hints: ['libc is user-space code that itself relies on system calls.', 'The kernel is what implements those services.'],
    solution: `The standard C library is user-space code that ultimately implements its services by making system calls into the kernel (and by relying on a hosted runtime, dynamic loader, and process environment). The kernel is the thing that provides those services, so it cannot call down into a library that calls back up into the kernel; there is no syscall layer beneath the kernel for libc to use, and libc assumes a process address space and runtime the kernel does not have. Consequently the kernel is built freestanding: it cannot link against glibc and instead provides its own equivalents, such as printk for output, kmalloc/kfree for allocation, and its own string and math helpers, compiled with flags that disable the hosted C environment (e.g. -ffreestanding, -nostdinc using only kernel headers). Kernel objects (.o files) are linked together by the kernel build into vmlinux or into .ko modules using kernel-internal symbols, not against any user-space shared library. This separation is also why you must use kernel-appropriate APIs and respect kernel constraints (allocation context, no floating point by default, etc.) rather than reaching for familiar libc calls.`,
    tags: ['kernel', 'linking', 'libc'],
  },
  {
    id: 'lx-ch02-t-029',
    chapter: 2,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Forward Declaration Versus Full Definition',
    prompt: `Header a.h needs to mention "struct node *" in a function prototype, and it could either include node.h (the full struct definition) or just write "struct node;" as a forward declaration. Explain when a forward declaration is sufficient and when the full definition is required, and why preferring forward declarations reduces compile-time coupling.`,
    hints: ['A pointer to an incomplete type is fine; dereferencing needs the layout.', 'Fewer #includes means less recompilation.'],
    solution: `A forward declaration "struct node;" introduces an incomplete type: the compiler knows node is a struct but not its size or members. That is sufficient anywhere you only need a pointer to it, such as a function prototype taking or returning "struct node *", because all pointers have a known size regardless of what they point to. You need the full definition (by including node.h) whenever the compiler must know the layout: to declare a value or array of struct node, to take sizeof(struct node), to access a member like n->value, or to allocate or copy one. Preferring a forward declaration in a header avoids pulling in node.h and everything it transitively includes, which reduces compile-time coupling: fewer translation units depend on node.h, so editing node.h triggers fewer rebuilds and compilation is faster. The full definition is then included only in the .c files that actually manipulate the struct's internals, which also improves encapsulation.`,
    tags: ['headers', 'forward-declaration', 'compilation'],
  },
  {
    id: 'lx-ch02-t-030',
    chapter: 2,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Debugging A Reproducible Crash With A Core Dump',
    prompt: `A user-space program built with -g crashes intermittently with a segmentation fault, and core dumps are enabled. Describe how you would use the core file and the executable in gdb to find where it crashed and inspect the state at that moment, and explain why a core dump can be more useful than re-running under gdb for an intermittent bug.`,
    hints: ['gdb can load an executable together with a core file.', 'backtrace and frame let you inspect saved state.'],
    solution: `A core dump is a snapshot of the process's memory and register state captured by the kernel at the instant it crashed. You load it with "gdb ./prog core", which reconstructs the dying process's state from the binary's debug info (present because of -g) plus the core. Then "backtrace" shows the call stack at the crash, immediately revealing which function and source line faulted; "frame N" selects a stack frame and "print"/"info locals" let you inspect the arguments and local variables that led to the bad access (for example a null or dangling pointer being dereferenced). This is often more useful than re-running under gdb for an intermittent bug because the crash may be rare or timing-dependent and may not reproduce while you watch; the core captures the exact failing state the one time it happened, so you can analyze it after the fact (post-mortem debugging) without having to trigger the bug again. It also lets you debug crashes that occurred in production or on another machine, as long as you have a matching unstripped/-g binary.`,
    tags: ['gdb', 'core-dump', 'debugging'],
  },
]

export default problems
