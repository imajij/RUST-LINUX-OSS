import type { Problem } from '../../types'

const problems: Problem[] = [
  {
    id: 'rs-ch12-t-001',
    chapter: 12,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'Where Command-Line Arguments Come From',
    prompt: `You are writing a small command-line tool like grep. Which standard library function gives you the arguments the user typed after the program name, and what does it return?`,
    hints: [
      'It lives in the std::env module.',
      'It does not return a Vec directly.',
    ],
    solution: `You call std::env::args, which returns an iterator over the command-line arguments as String values. To work with them like a list, you typically collect the iterator into a vector, for example let args: Vec<String> = std::env::args().collect(). The very first item, args[0], is the path to the program itself, and the real arguments follow it. There is also std::env::args_os for arguments that may not be valid Unicode, but args is the common choice in this chapter.`,
    tags: ['cli', 'env', 'args'],
  },
  {
    id: 'rs-ch12-t-002',
    chapter: 12,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'Reading a Whole File at Once',
    prompt: `Your program needs the entire contents of a text file as one String. Which function from the std::fs module does this in a single call, and what type does it return?`,
    hints: ['Think about a function whose name describes reading to a String.'],
    solution: `You use std::fs::read_to_string, passing the path to the file. It reads the full file and returns a Result, specifically Result<String, std::io::Error>. On success you get an Ok holding the file contents as a single String; on failure (for example the file does not exist or you lack permission) you get an Err with an io::Error. Because it returns a Result, you must handle the error case rather than assuming the read always works.`,
    tags: ['fs', 'file', 'result'],
  },
  {
    id: 'rs-ch12-t-003',
    chapter: 12,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'stdout Versus stderr',
    prompt: `A command-line program produces normal results and also reports errors. Why is it useful to send error messages to standard error instead of standard output, and which Rust macro prints to each stream?`,
    hints: [
      'Think about a user piping your output into a file or another command.',
      'println! goes to one stream; another macro goes to the other.',
    ],
    solution: `Standard output should carry the program's real results, while standard error carries diagnostics and error messages. Separating them lets a user redirect just the results to a file (for example with > output.txt) while still seeing errors on the screen, and lets other tools in a pipeline consume clean data without error text mixed in. In Rust, println! writes to standard output and eprintln! writes to standard error. So you print successful matches with println! and report failures with eprintln!.`,
    tags: ['stderr', 'stdout', 'eprintln'],
  },
  {
    id: 'rs-ch12-t-004',
    chapter: 12,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'Signaling Failure With an Exit Code',
    prompt: `When your tool fails (say the file could not be read), you want the operating system to know the run was unsuccessful. Which function ends the program with a chosen exit code, and what code conventionally means success versus failure?`,
    hints: ['It is in the std::process module.'],
    solution: `You call std::process::exit, passing an integer exit code, for example process::exit(1). By convention an exit code of 0 means success and any nonzero code means an error occurred, so other programs and shell scripts can detect the failure. process::exit stops the program immediately and returns that code to the operating system. A common pattern is to print an error with eprintln! and then call process::exit(1) so both the message and the status reflect the failure.`,
    tags: ['process', 'exit', 'exit-code'],
  },
  {
    id: 'rs-ch12-t-005',
    chapter: 12,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Why Collect Into a Vec',
    prompt: `std::env::args returns an iterator, yet the chapter writes let args: Vec<String> = std::env::args().collect(). Why annotate the variable as Vec<String>, and what role does that annotation play?`,
    hints: ['collect can build many different collection types.'],
    solution: `collect is generic over the collection it produces, so the compiler cannot tell whether you want a Vec, a HashSet, a String, or something else just from the call. The type annotation Vec<String> tells collect exactly which collection to build, which is why the annotation is required here. Each argument arrives as a String, so the element type is String. With the annotation in place, args is a vector you can index into, like args[1] for the first real argument.`,
    tags: ['collect', 'vec', 'iterator'],
  },
  {
    id: 'rs-ch12-t-006',
    chapter: 12,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Indexing Args Can Panic',
    prompt: `An early version reads arguments with let query = &args[1]; and let path = &args[2];. The user runs the program with no extra arguments. What happens, and why is this a poor user experience?`,
    hints: ['What is the length of args when no arguments are given?'],
    solution: `When the user supplies no extra arguments, args has length 1 (only the program name), so indexing args[1] is out of bounds and the program panics. The panic produces a confusing message about an index being out of bounds along with internal details, which does not tell the user that they simply forgot to pass a query and a file path. A friendlier design checks args.len() first and prints a clear usage error to standard error, then exits with a nonzero code instead of panicking.`,
    tags: ['args', 'panic', 'indexing'],
  },
  {
    id: 'rs-ch12-t-007',
    chapter: 12,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Why Config::build Returns Result',
    prompt: `The chapter changes a Config constructor from one that panics on too few arguments into Config::build that returns Result<Config, &'static str>. What does this change buy you compared to panicking?`,
    hints: ['Who decides what to do when arguments are missing?'],
    solution: `Returning a Result turns "not enough arguments" into a recoverable error rather than an immediate crash, so the caller decides how to respond. main can inspect the Err, print a clean usage message to standard error with eprintln!, and exit with a nonzero code, giving the user a helpful experience instead of a panic backtrace. It also keeps argument validation as ordinary control flow that is easy to test. A panic is appropriate for true bugs, but bad user input is expected and is better modeled as a returned error.`,
    tags: ['config', 'result', 'error-handling'],
  },
  {
    id: 'rs-ch12-t-008',
    chapter: 12,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'The Meaning of a Static String Error',
    prompt: `Config::build returns Result<Config, &'static str> and on failure returns Err("not enough arguments"). Why is the error type &'static str a reasonable choice here, and what does the 'static lifetime tell you about that string?`,
    hints: ['Where does a string literal live in memory?'],
    solution: `The error is a fixed string literal baked into the program, and string literals have the 'static lifetime, meaning they are valid for the entire run of the program. So &'static str describes a borrowed string slice that never goes out of scope, which is exactly what a constant message is. It is a lightweight, simple error type that avoids allocating a new String just to report a known message. The tradeoff is that you cannot include runtime data in the message; for richer errors you would use an owned type, but a static message is enough for this small tool.`,
    tags: ['lifetimes', 'static', 'error-type'],
  },
  {
    id: 'rs-ch12-t-009',
    chapter: 12,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Cloning the Argument Strings',
    prompt: `Inside Config::build the code writes query: args[1].clone(). Some readers worry about the cost of clone. Why does the chapter accept the clone here, and what would the alternative require?`,
    hints: ['Think about who owns the Strings in args.'],
    solution: `The args vector owns its Strings, and Config wants to own copies it can hold independently of args, so cloning gives Config its own String values without fighting the borrow checker over references and lifetimes. The clones are small and happen once at startup, so their runtime cost is negligible for a command-line tool. The alternative would be to store string slices in Config and manage lifetimes so Config never outlives args, which adds complexity for little benefit here. The chapter explicitly favors the simpler clone now and notes that iterators (a later refinement) can avoid it.`,
    tags: ['clone', 'ownership', 'config'],
  },
  {
    id: 'rs-ch12-t-010',
    chapter: 12,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'unwrap_or_else in main',
    prompt: `In main the chapter writes let config = Config::build(&args).unwrap_or_else(|err| { eprintln!("Problem parsing arguments: {err}"); process::exit(1); });. Explain what unwrap_or_else does here and why it suits this situation better than unwrap.`,
    hints: ['What does the closure receive, and what type does it produce?'],
    solution: `unwrap_or_else returns the inner Ok value when the Result is Ok, but when it is Err it runs the closure you provide, passing the error value as the argument. Here the closure receives the error message, prints it to standard error, and calls process::exit(1), so a failure produces a clean message and a proper exit code instead of a panic. Plain unwrap would panic with a backtrace, which is noisier and not what you want for ordinary bad input. Because the closure ends in process::exit, which never returns, the program stops there on error and otherwise main continues with a valid Config.`,
    tags: ['unwrap_or_else', 'closure', 'process'],
  },
  {
    id: 'rs-ch12-t-011',
    chapter: 12,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Why Search Returns a Vector of Slices',
    prompt: `The search function has signature fn search<'a>(query: &str, contents: &'a str) -> Vec<&'a str>. Why does it return Vec<&'a str> (string slices) rather than Vec<String>?`,
    hints: ['The matching lines already exist somewhere.'],
    solution: `Each matching line already lives inside the contents String, so search can return slices that borrow directly into contents instead of allocating brand-new owned Strings for every match. This avoids copying text and keeps the function efficient. The lifetime 'a ties the returned slices to contents, promising the caller that the slices stay valid as long as contents does. Returning Vec<String> would work but would needlessly duplicate data that the caller already holds.`,
    tags: ['search', 'slices', 'lifetimes'],
  },
  {
    id: 'rs-ch12-t-012',
    chapter: 12,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Test-Driven Search',
    prompt: `The chapter builds the search function using test-driven development. Describe the basic TDD cycle it follows and why writing the failing test first is helpful.`,
    hints: ['Red, then green, then improve.'],
    solution: `The cycle is: first write a test that describes the desired behavior and watch it fail because the code does not exist or only returns a placeholder (the red step). Next write just enough code to make that test pass (the green step). Then refactor the implementation to be cleaner while keeping the test passing. Writing the failing test first forces you to define exactly what the function should do, gives you a precise target, and guarantees the test can actually fail, so a later pass is meaningful rather than accidental.`,
    tags: ['tdd', 'testing', 'search'],
  },
  {
    id: 'rs-ch12-t-013',
    chapter: 12,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Reading One Line at a Time',
    prompt: `The search function iterates with for line in contents.lines(). What does the lines method give you, and why is it the right tool for searching text line by line?`,
    hints: ['Think about what each loop iteration represents.'],
    solution: `The lines method returns an iterator that yields each line of the string as a string slice, with the line endings stripped off. That is exactly what a line-oriented search needs: on each iteration you get one clean line you can test against the query. Because it yields slices that borrow from contents, no copying happens. You then check whether a line contains the query and, if so, push that line slice into the results vector.`,
    tags: ['lines', 'iterator', 'search'],
  },
  {
    id: 'rs-ch12-t-014',
    chapter: 12,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Why the Lifetime Annotation Is Required',
    prompt: `Suppose you wrote fn search(query: &str, contents: &str) -> Vec<&str> with no explicit lifetimes. The compiler complains it cannot figure out the output lifetime. Explain why, and how the 'a annotation resolves it.`,
    hints: [
      'The output borrows from one of two inputs.',
      'Lifetime elision cannot guess which input the result ties to.',
    ],
    solution: `The returned slices borrow from one of the two string-slice parameters, and the compiler must know which one so it can enforce that the result does not outlive its source. With two reference parameters, lifetime elision rules cannot infer which input the output borrows from, so it asks you to be explicit. By writing fn search<'a>(query: &str, contents: &'a str) -> Vec<&'a str>, you state that the results borrow from contents (the place the matching lines actually come from) and not from query. That lets the borrow checker tie the output's validity to contents, which is the correct relationship.`,
    tags: ['lifetimes', 'borrow-checker', 'search'],
  },
  {
    id: 'rs-ch12-t-015',
    chapter: 12,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Designing Case-Insensitive Search',
    prompt: `You want a search_case_insensitive function so that searching for "rust" also matches "Rust" and "RUST". Describe how to make the comparison case-insensitive, and one subtle consequence of the approach the chapter uses.`,
    hints: [
      'Normalize both sides before comparing.',
      'to_lowercase produces a new String, not a slice.',
    ],
    solution: `You lowercase both the query and each line before comparing, for example let query = query.to_lowercase() and then check line.to_lowercase().contains(&query). Because both sides are lowercased, differences in case no longer affect the match. A subtle consequence is that to_lowercase allocates a new String each time (the query once, and each line as it is checked), so the lowercased query is now an owned String and you must pass it to contains by reference with &query. Also note that returned lines are still slices of the original contents, so they keep their original casing even though the comparison was lowercased.`,
    tags: ['case-insensitive', 'to_lowercase', 'search'],
  },
  {
    id: 'rs-ch12-t-016',
    chapter: 12,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Reading an Environment Variable to Toggle Behavior',
    prompt: `The tool decides between case-sensitive and case-insensitive search using an environment variable named IGNORE_CASE. Which function reads it, what type does it return, and how does the chapter turn its presence into a bool?`,
    hints: [
      'The function is in std::env.',
      'Its return value is a Result.',
    ],
    solution: `You call std::env::var("IGNORE_CASE"), which returns Result<String, VarError>: Ok with the value if the variable is set, or an Err if it is not present. The chapter does not care about the actual value, only whether the variable exists, so it converts the Result to a bool with .is_ok(): let ignore_case = env::var("IGNORE_CASE").is_ok(). This makes ignore_case true whenever the variable is set to anything and false when it is unset. Config then stores that bool, and run picks the search function based on it.`,
    tags: ['env-var', 'env-var-fn', 'config'],
  },
  {
    id: 'rs-ch12-t-017',
    chapter: 12,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Extracting the run Function',
    prompt: `After parsing arguments in main, the chapter moves the file-reading and searching logic into a separate run(config: Config) function. What is the purpose of this extraction, and roughly what should run return?`,
    hints: ['Think about separation of concerns and testability.'],
    solution: `Extracting run separates the program's two responsibilities: main handles argument parsing and process-level concerns (exiting, error display), while run holds the actual program logic. This keeps each piece small, makes the logic reusable and easier to test, and follows the chapter's separation-of-concerns guidance. run should return a Result that signals success or failure, typically Result<(), Box<dyn Error>>, so it can propagate errors with the ? operator instead of panicking. On success it returns Ok(()), and main decides what to do with any Err it returns.`,
    tags: ['refactor', 'run', 'separation-of-concerns'],
  },
  {
    id: 'rs-ch12-t-018',
    chapter: 12,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Handling run\'s Error Differently',
    prompt: `main calls Config::build with unwrap_or_else but handles run with an if-let: if let Err(e) = run(config) { eprintln!("Application error: {e}"); process::exit(1); }. Why use if-let here instead of unwrap_or_else, and why is there no value to unwrap?`,
    hints: ['What does run return on success?'],
    solution: `run returns Result<(), Box<dyn Error>>, so on success it yields Ok(()) which carries no useful value to extract. Because there is nothing to unwrap on success, the code only needs to react when run fails, and if let Err(e) cleanly captures just the error case. unwrap_or_else is designed to return the inner Ok value, which makes sense for Config::build whose success holds a Config, but it would be awkward when the success value is the empty unit. So both branches print to standard error and exit with code 1, but the shapes differ because one result carries a value and the other does not.`,
    tags: ['if-let', 'run', 'error-handling'],
  },
  {
    id: 'rs-ch12-t-019',
    chapter: 12,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Why a Trait Object for run\'s Error',
    prompt: `run is declared to return Result<(), Box<dyn Error>>. Reading the file can fail with one error type and other operations could fail with another. How does Box<dyn Error> help, and what does it let you do with the ? operator?`,
    hints: [
      'Different operations return different concrete error types.',
      '? converts the error before returning.',
    ],
    solution: `Box<dyn Error> is a trait object representing any type that implements the Error trait, so it can hold whatever concrete error a step inside run produces, regardless of which specific error type it is. This lets run report many different kinds of failure through one uniform return type without listing them all. The ? operator then automatically converts the concrete error into Box<dyn Error> and returns it early, so for example reading the file with fs::read_to_string(&config.file_path)? propagates an io::Error as a boxed error. This keeps run flexible and concise while still surfacing the original error message.`,
    tags: ['trait-object', 'box-dyn-error', 'question-mark'],
  },
  {
    id: 'rs-ch12-t-020',
    chapter: 12,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Moving Code Into a Library Crate',
    prompt: `The chapter splits the program into src/main.rs and src/lib.rs. Which pieces move into lib.rs, what stays in main.rs, and why is this split good for testing?`,
    hints: ['What can integration-style tests reach?'],
    solution: `The logic moves into lib.rs: the Config struct and its build method, the run function, and the search functions, all marked pub so they can be used from outside. main.rs stays thin, keeping only the code that wires things together: reading args, calling Config::build, handling errors, calling run, and exiting. This split is good for testing because tests can call into the library's public functions directly, while code inside main.rs cannot be tested the same way. Separating logic from the binary's entry point is the core refactoring lesson of the chapter.`,
    tags: ['lib-crate', 'refactor', 'testing'],
  },
  {
    id: 'rs-ch12-t-021',
    chapter: 12,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Predict the Search Output',
    prompt: `Given contents:
"Rust:
safe, fast, productive.
Pick three.
Trust me."
and the call search("rust", contents) using the case-sensitive version, which lines are returned and in what order?`,
    hints: ['Case-sensitive contains; check each line literally for the lowercase substring.'],
    solution: `The case-sensitive search returns only the lines that contain the exact substring "rust" with that casing. The line "Rust:" has a capital R, so it does not match. The line "Trust me." contains "rust" as part of the word Trust, so it does match. The other two lines contain no "rust" substring. So the result is a vector with a single element, "Trust me.". Order follows the file, so it is just that one line.`,
    tags: ['search', 'predict-output', 'case-sensitive'],
  },
  {
    id: 'rs-ch12-t-022',
    chapter: 12,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Predict the Case-Insensitive Output',
    prompt: `Using the same contents:
"Rust:
safe, fast, productive.
Pick three.
Trust me."
what does search_case_insensitive("rUsT", contents) return, and how does it differ from the case-sensitive result?`,
    hints: ['Both the query and each line are lowercased before comparing.'],
    solution: `The query "rUsT" lowercases to "rust", and each line is lowercased before the contains check. The line "Rust:" lowercases to "rust:", which contains "rust", so it matches. The line "Trust me." lowercases to "trust me.", which contains "rust" inside "trust", so it also matches. The result is a vector with two elements in file order: "Rust:" and "Trust me." (returned with their original casing). This differs from the case-sensitive version, which matched only "Trust me." because "Rust:" did not contain a lowercase "rust".`,
    tags: ['case-insensitive', 'predict-output', 'search'],
  },
  {
    id: 'rs-ch12-t-023',
    chapter: 12,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Why Not Just print the Error in build',
    prompt: `A learner suggests simplifying Config::build so that when arguments are missing it prints "not enough arguments" with eprintln! and calls process::exit(1) itself, instead of returning a Result. Argue why returning the Result is the better design.`,
    hints: [
      'Think about who should own process-exiting decisions.',
      'Think about testing build.',
    ],
    solution: `If build prints and exits on its own, it hard-codes a process-level decision into a piece of logic, which mixes concerns and makes build impossible to test, since calling it with bad input would terminate the test process. Returning a Result keeps build pure logic that simply reports failure, leaving the choice of how to react (which message, which stream, which exit code, or whether to exit at all) to the caller in main. This separation lets you unit-test build by asserting it returns an Err for missing arguments, and it lets the same function be reused in contexts that should not call process::exit. The chapter's whole refactoring theme is pushing such decisions out to the thin main layer.`,
    tags: ['design', 'separation-of-concerns', 'testability'],
  },
  {
    id: 'rs-ch12-t-024',
    chapter: 12,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'The Hidden Cost of an Eagerly Collected Vec',
    prompt: `search builds a Vec<&str> of all matches before returning. Compare this with an approach that processes each matching line as it is found. For a very large file with few matches, what is the tradeoff in memory, and why might the eager Vec still be acceptable for this tool?`,
    hints: ['What must the vector hold at once?'],
    solution: `The eager approach allocates a vector that must hold references to every matching line at the same time before returning, so its peak memory grows with the number of matches. Processing lines as they are found could print each match immediately and keep almost nothing in memory, which is friendlier for huge inputs or streaming. The tradeoff is mostly about peak memory and latency to first output, not correctness. For this chapter's tool the eager Vec is acceptable because the matches are just borrowed slices (no copying of text), the result sets are usually small, and returning a clean Vec keeps the function simple and easy to test. If scaling demanded it, you would refactor toward returning or driving an iterator.`,
    tags: ['performance', 'memory', 'design'],
  },
  {
    id: 'rs-ch12-t-025',
    chapter: 12,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Tracing an End-to-End Run',
    prompt: `A user runs the finished program as: IGNORE_CASE=1 minigrep To poem.txt, where poem.txt contains lines with mixed casing. Walk through, in order, the major steps the program takes from start to printing results, naming the key functions involved.`,
    hints: ['Args, then Config, then env var, then run, then search, then print.'],
    solution: `First main collects the arguments with env::args().collect() into a Vec<String>, giving ["minigrep", "To", "poem.txt"]. It passes those to Config::build, which reads args[1] as the query "To" and args[2] as the file path "poem.txt", clones them, and also calls env::var("IGNORE_CASE").is_ok(), which is true because the variable is set, so ignore_case is true; build returns Ok(config). main unwraps that and calls run(config). Inside run, fs::read_to_string(&config.file_path)? loads poem.txt into a String, and because ignore_case is true run calls search_case_insensitive(&config.query, &contents), which lowercases both sides and collects matching lines. Finally run loops over the returned slices printing each with println!, then returns Ok(()), and main exits normally.`,
    tags: ['end-to-end', 'control-flow', 'integration'],
  },
  {
    id: 'rs-ch12-t-026',
    chapter: 12,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'A Lifetime That Borrows the Wrong Source',
    prompt: `Imagine a buggy signature fn search<'a>(query: &'a str, contents: &str) -> Vec<&'a str> that ties the output to query instead of contents. Even if the body still returns lines from contents, why is this signature wrong, and what could go wrong for a caller?`,
    hints: [
      'The returned slices really come from contents.',
      'A caller could drop contents while keeping query alive.',
    ],
    solution: `The returned slices actually point into contents, but the signature promises they live only as long as query, which is a false statement about where the data comes from. The borrow checker would either reject the body (because contents does not have lifetime 'a) or, if you contorted things to satisfy it, the contract would let a caller keep the results alive while contents is dropped. In that situation the slices would dangle, referencing freed memory, which is exactly the unsafety lifetimes exist to prevent. The correct signature ties the output to contents, so the compiler forces contents to outlive the returned slices and the bug becomes impossible.`,
    tags: ['lifetimes', 'borrow-checker', 'dangling'],
  },
  {
    id: 'rs-ch12-t-027',
    chapter: 12,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Why Tests Did Not Catch a stderr Bug',
    prompt: `Early in the chapter, error messages were printed with println! (to standard output). The automated tests still passed even though errors went to the wrong stream. Explain why a redirect like cargo run > output.txt revealed the problem that tests did not.`,
    hints: [
      'Where do the unit tests look?',
      'What does redirecting only standard output do?',
    ],
    solution: `The unit tests exercise the library functions (like search and build) directly and assert on their return values, so they never inspect where the binary prints messages; printing to the wrong stream does not change any function's return value. The bug only shows up at the process level, in how output is routed. Redirecting just standard output with > output.txt sends real results to the file but is supposed to leave errors on the screen; when errors were written with println! they went into output.txt too, polluting the results and disappearing from view. Switching error messages to eprintln! puts them on standard error so redirecting standard output no longer captures them, which is the behavior the redirect test demonstrated.`,
    tags: ['stderr', 'testing', 'redirection'],
  },
  {
    id: 'rs-ch12-t-028',
    chapter: 12,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Choosing an Owned Error Over a Static One',
    prompt: `Config::build returns Err with a fixed &'static str. Suppose you want the message to include the actual number of arguments received, like "expected 3 arguments but got 1". Why does this push you away from &'static str, and what error type would you reach for instead?`,
    hints: [
      'A literal cannot embed runtime values.',
      'Building a message at runtime allocates.',
    ],
    solution: `A &'static str must be a fixed compile-time literal, so it cannot embed a value computed at runtime like the actual argument count. To build a message with format! you would produce an owned String, which means the error type can no longer be &'static str. You would change build to return Result<Config, String> and return Err(format!("expected 3 arguments but got {n}", n = args.len() - 1)). The tradeoff is a small heap allocation per error in exchange for a far more informative message; alternatively, in a larger program you might define a custom error type or use Box<dyn Error> for richer reporting.`,
    tags: ['error-type', 'string', 'design'],
  },
  {
    id: 'rs-ch12-t-029',
    chapter: 12,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Precedence: Flag Versus Environment Variable',
    prompt: `Currently case-insensitivity is controlled only by the IGNORE_CASE environment variable. Suppose you also want a command-line flag to override it. Using only Chapter 12 features, sketch how Config::build could combine both sources and decide which wins, and note one ambiguity you must resolve in the design.`,
    hints: [
      'You already read args into a Vec and read env vars with env::var.',
      'Decide a rule when both are set and disagree.',
    ],
    solution: `In build you already have the args vector and can read the environment, so you compute two booleans: one from scanning args for the flag (for example checking whether any argument equals "-i") and one from env::var("IGNORE_CASE").is_ok(). You then combine them into the final ignore_case stored in Config according to a precedence rule, such as letting an explicit command-line flag take priority over the environment variable, or treating the variable as a default that the flag can override. The ambiguity you must resolve is what happens when the two disagree (flag says case-sensitive but the variable is set, or vice versa); you must pick and document a clear winner, otherwise the behavior is surprising. All of this uses only Vec, string comparison, env::var, and bool logic from this chapter and earlier ones.`,
    tags: ['design', 'env-var', 'cli-flag'],
  },
  {
    id: 'rs-ch12-t-030',
    chapter: 12,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Why run Returns Result Instead of Panicking',
    prompt: `run could simply call fs::read_to_string(&config.file_path).expect("could not read file") and panic on failure. Instead the chapter has run return Result and propagate with ?. Compare the user-facing behavior of the two approaches when the file is missing, including exit code and output stream.`,
    hints: [
      'A panic prints differently than a controlled error.',
      'Think about what the operating system sees.',
    ],
    solution: `With expect, a missing file triggers a panic: Rust prints a panic message and possibly a backtrace, the wording exposes internal details, and the program aborts with a panic-style exit. With the Result-and-? approach, the io::Error is converted to Box<dyn Error> and returned from run; main catches it in if let Err(e), prints a clean "Application error: ..." message to standard error with eprintln!, and calls process::exit(1). The controlled path gives the user a tidy message on the correct stream and a deliberate nonzero exit code that scripts can rely on, whereas the panic is noisier and signals a crash rather than an expected, handled failure. Treating a missing file as a recoverable error is more appropriate because bad input is expected, not a bug in the program.`,
    tags: ['result', 'panic', 'error-handling'],
  },
]

export default problems
