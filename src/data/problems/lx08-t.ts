import type { Problem } from '../../types'

const problems: Problem[] = [
  {
    id: 'lx-ch08-t-001',
    chapter: 8,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'What dev_t Encodes',
    prompt: `Every character device the kernel knows about is identified by a value of type dev_t. In plain terms, what two numbers are packed inside a dev_t, and what role does each one play in routing an open() of a /dev node to the right driver?`,
    hints: ['Think major and minor.', 'One picks the driver, the other picks the instance.'],
    solution: `A dev_t packs two numbers: a major number and a minor number. The major number identifies which driver owns the device, so when a process opens a /dev node the kernel uses the major to route the call to the correct registered driver. The minor number distinguishes individual devices or instances that the same driver manages, for example /dev/ttyS0 versus /dev/ttyS1. You do not pack or unpack dev_t by hand: you use MAJOR(dev), MINOR(dev) to extract the parts and MKDEV(ma, mi) to build one. Together they give every character device a unique kernel-wide identity.`,
    tags: ['kernel', 'chardev', 'dev_t'],
  },
  {
    id: 'lx-ch08-t-002',
    chapter: 8,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'Why a /dev Node Is Needed',
    prompt: `You register a character driver in the kernel, but there is no file under /dev for it yet. A user runs cat on a path that does not exist and gets "No such file or directory." Explain what a /dev node actually is and why user space needs one even though the driver is already loaded.`,
    hints: ['User space talks through the filesystem.', 'The node carries the major/minor.'],
    solution: `A /dev node is a special file whose inode stores a device type (character) and a dev_t (major/minor) rather than any real file data. User space cannot call kernel driver functions directly; it opens a path, and the open() of a device node is what connects the process to the driver. Registering a cdev with the kernel only reserves the major/minor and wires up the file_operations; it does not by itself create any path in /dev. Until a node with the matching major/minor exists (made by mknod, by udev, or via a class/device_create call), there is nothing for the user to open, so the open fails with ENOENT.`,
    tags: ['kernel', 'chardev', 'devnode'],
  },
  {
    id: 'lx-ch08-t-003',
    chapter: 8,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'The file_operations Table',
    prompt: `A character driver supplies a struct file_operations. In one or two sentences, what is this structure, and what is the relationship between a function you put in it (say .read) and a user-space system call (read())?`,
    hints: ['It is a table of callbacks.', 'The VFS dispatches syscalls through it.'],
    solution: `struct file_operations is a table of function pointers: it tells the kernel which driver function to call for each operation on an open file, such as .open, .read, .write, .release, and .llseek. When user space invokes a system call like read() on a file descriptor backed by your device, the virtual filesystem layer looks up the file's file_operations and dispatches to your .read callback. Fields you leave NULL mean "not supported," and the VFS handles that gracefully (often returning a default or an error). So the structure is the bridge that maps generic syscalls onto your specific device behavior.`,
    tags: ['kernel', 'chardev', 'file_operations'],
  },
  {
    id: 'lx-ch08-t-004',
    chapter: 8,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'Why Not Just memcpy From User Pointers',
    prompt: `A beginner's read handler receives a user buffer pointer and does memcpy(buf, kdata, count) straight into it. Explain why this is wrong in kernel code and name the function that should be used instead.`,
    hints: ['The pointer is an untrusted user-space address.', 'There is a dedicated copy helper.'],
    solution: `The buf pointer is a user-space virtual address that the kernel must not dereference directly: it could be invalid, unmapped, or even a malicious value pointing at kernel memory, and a plain memcpy would either fault uncontrollably or breach security. The kernel provides copy_to_user(buf, kdata, count) (and copy_from_user for the other direction), which safely validate the address range, handle page faults, and refuse access outside the user's own address space. These helpers return the number of bytes that could NOT be copied (0 means full success). So a correct read handler uses copy_to_user and treats a nonzero return as an -EFAULT error.`,
    tags: ['kernel', 'chardev', 'copy_to_user'],
  },
  {
    id: 'lx-ch08-t-005',
    chapter: 8,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'register_chrdev_region vs alloc_chrdev_region',
    prompt: `Two ways to reserve device numbers are register_chrdev_region (you pick the major) and alloc_chrdev_region (the kernel picks). When would you prefer each, and why is the dynamic one usually recommended for new drivers?`,
    hints: ['Hard-coded majors can collide.', 'Read the assigned number back from a variable.'],
    solution: `register_chrdev_region asks for a specific major you chose at compile time, which makes sense only when that major is officially assigned to your device or when a legacy node must keep a fixed number. alloc_chrdev_region lets the kernel pick an unused major at load time and writes the chosen dev_t back through a pointer, then you read it with MAJOR(). The dynamic form is recommended because picking a fixed major risks colliding with another driver already using that number, causing registration to fail or worse. The cost is that the major is not known until load time, so the matching /dev node must be created from the assigned value (typically via udev or by reading it from sysfs).`,
    tags: ['kernel', 'chardev', 'alloc_chrdev_region'],
  },
  {
    id: 'lx-ch08-t-006',
    chapter: 8,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Order of cdev_init and cdev_add',
    prompt: `A driver does cdev_add(&my_cdev, devno, 1) and THEN later calls cdev_init(&my_cdev, &fops). Predict what goes wrong and explain the correct ordering relative to when the device becomes live.`,
    hints: ['cdev_add publishes the device.', 'Operations can arrive the instant it is added.'],
    solution: `The ordering is backwards. cdev_init(&my_cdev, &fops) sets up the cdev and points it at your file_operations, and cdev_add(&my_cdev, devno, 1) is what publishes it to the kernel so that opens can immediately be routed to it. Calling cdev_add before cdev_init means the device goes live with an uninitialized (or stale) fops pointer, so an open arriving in that window can dispatch through garbage and crash. The correct sequence is: fully initialize the cdev (cdev_init, set cdev.owner = THIS_MODULE), make sure all data the handlers need is ready, and only then call cdev_add as the last step. After cdev_add succeeds you must assume a syscall can arrive at any moment.`,
    tags: ['kernel', 'chardev', 'cdev'],
  },
  {
    id: 'lx-ch08-t-007',
    chapter: 8,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'What copy_to_user Returns',
    prompt: `A read handler writes: if (copy_to_user(buf, kbuf, n) != 0) return -EFAULT; A coworker thinks the check is inverted and that nonzero means success. Explain exactly what the return value means and why -EFAULT is the right error.`,
    hints: ['Zero means everything was copied.', 'The return is a remaining-bytes count.'],
    solution: `copy_to_user returns the number of bytes that could NOT be copied, so a return of 0 means the full transfer succeeded and any nonzero value means part (or all) of the copy failed, typically because the user buffer is not fully accessible. Therefore != 0 correctly detects failure; the coworker has it backwards. The conventional response is to return -EFAULT ("bad address"), which is exactly the errno for a user pointer that the kernel could not safely access. Note that on partial failure some bytes may already have been copied, so robust code may account for the bytes actually transferred, but failing the call with -EFAULT is the standard simple behavior.`,
    tags: ['kernel', 'chardev', 'copy_to_user'],
  },
  {
    id: 'lx-ch08-t-008',
    chapter: 8,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'The Role of file->private_data',
    prompt: `Many drivers stash a pointer in filp->private_data inside .open. What problem does private_data solve, and how does it let one driver cleanly serve several open file descriptors or several minor devices at once?`,
    hints: ['Each open gets its own struct file.', 'Per-open state lives off private_data.'],
    solution: `Every open() produces a distinct struct file, and private_data is a free pointer slot in it that the driver can aim at whatever per-open or per-device state it needs, such as a buffer, position, or a pointer to the device structure for that minor. In .open the driver typically figures out which device this is (often from the inode's minor or from container_of on the cdev) and stores a pointer to that device's struct in filp->private_data. Then .read, .write, .llseek, and .release all retrieve it from filp->private_data instead of using globals. This is what lets a single set of file_operations correctly serve many simultaneous opens and many minor numbers without mixing up their state.`,
    tags: ['kernel', 'chardev', 'private_data'],
  },
  {
    id: 'lx-ch08-t-009',
    chapter: 8,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Honoring the count Argument',
    prompt: `A .read handler ignores the count parameter and always copies the driver's entire internal buffer to the user. Explain two distinct things that can go wrong, and state the contract a read handler must honor regarding count and *f_pos.`,
    hints: ['The user buffer may be smaller than count.', 'Return value drives the next read.'],
    solution: `Ignoring count is dangerous: the user buffer is only guaranteed to hold count bytes, so writing more risks copy_to_user failing or, worse, overrunning the caller's memory, and it also breaks programs that asked for a specific amount. The correct contract is to copy at most count bytes (clamp to what is actually available from the current *f_pos), use copy_to_user, advance *f_pos by the number of bytes delivered, and return that byte count. Returning the number of bytes read (which may be fewer than count) tells the caller how much it got; returning 0 signals end of data. So a read handler must respect count, track position through *f_pos, and report the true amount transferred.`,
    tags: ['kernel', 'chardev', 'read'],
  },
  {
    id: 'lx-ch08-t-010',
    chapter: 8,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Returning 0 From read Means EOF',
    prompt: `A device's .read keeps returning a positive count even after all data is exhausted, looping the same stale bytes. A user program running "cat /dev/mydev" never terminates. What return value signals end-of-file, and why does this matter for tools like cat?`,
    hints: ['cat loops until read returns 0.', 'Positive means more data.'],
    solution: `A read handler signals end-of-file by returning 0 (zero bytes read). Tools like cat call read in a loop and stop only when read returns 0; a positive return means "here are N more bytes, call again," and a negative return means an error. If the driver keeps returning positive counts of recycled data, cat sees an endless stream and never exits. So once the device has no more data to give from the current position, the handler must return 0 rather than re-serving old bytes. This is the standard streaming convention that the whole read()/EOF model depends on.`,
    tags: ['kernel', 'chardev', 'read'],
  },
  {
    id: 'lx-ch08-t-011',
    chapter: 8,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Why .owner = THIS_MODULE Matters',
    prompt: `A driver forgets to set cdev.owner = THIS_MODULE (or leaves .owner unset in file_operations on older interfaces). Explain what protection .owner provides and what bad thing can happen during rmmod if it is missing.`,
    hints: ['Module reference counting.', 'Unloading a module that is in use.'],
    solution: `Setting .owner = THIS_MODULE ties open files of the device to the module's reference count, so while a user has the device open the kernel knows the module is in use and bumps its refcount. Without it, the kernel may allow the module to be unloaded (rmmod) while a process still has the device open and is calling into its file_operations. The handler code would then have been freed, so the next read/write/ioctl jumps into deallocated memory and crashes. In short, .owner is the safety interlock that prevents unloading code that is still actively serving an open file descriptor.`,
    tags: ['kernel', 'chardev', 'module'],
  },
  {
    id: 'lx-ch08-t-012',
    chapter: 8,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'open Without a Matching release',
    prompt: `A driver allocates a per-open buffer with kmalloc in .open and stores it in private_data, but its .release just returns 0 without freeing it. Predict the consequence over many open/close cycles and state the rule about pairing open and release.`,
    hints: ['One release per last close.', 'kmalloc needs kfree.'],
    solution: `Each open that allocates a buffer but is never freed leaks that memory; over many open/close cycles the leak accumulates and can exhaust kernel memory. The rule is that .release is the place to undo what .open set up: whatever you kmalloc (or otherwise acquire) per open should be released when the file is finally closed. Note that .release is called once when the last reference to the struct file is dropped, not necessarily on every close() if the descriptor was duplicated, but it is the matching cleanup point for .open. So the fix is to kfree(filp->private_data) (and clear it) in .release. Every per-open acquisition needs a corresponding per-release teardown.`,
    tags: ['kernel', 'chardev', 'release'],
  },
  {
    id: 'lx-ch08-t-013',
    chapter: 8,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Why ioctl Has Encoded Command Numbers',
    prompt: `Instead of letting drivers pick arbitrary integers like 1, 2, 3 for ioctl commands, the kernel encourages building them with _IO, _IOR, _IOW, and _IOWR macros. What information do these macros encode, and what kind of bug do they help catch?`,
    hints: ['Direction and argument size are baked in.', 'A magic number distinguishes drivers.'],
    solution: `The _IO family encodes a type ("magic") byte, a command number, the data-transfer direction (none, read, write, or both, from the application's point of view), and the size of the argument struct into a single 32-bit command code. Because a magic byte and size are baked in, a command number meant for one driver is very unlikely to collide with another's, and a mismatched argument size can be detected, catching the classic bug of an application and driver disagreeing on the ioctl's struct or sending a command to the wrong device. Raw integers like 1, 2, 3 carry none of this and silently alias across drivers. So the macros turn ioctl numbers into self-describing, collision-resistant identifiers.`,
    tags: ['kernel', 'chardev', 'ioctl'],
  },
  {
    id: 'lx-ch08-t-014',
    chapter: 8,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Cleanup Ordering on Module Exit',
    prompt: `On the exit path a driver does, in order: unregister_chrdev_region(devno, 1); then cdev_del(&my_cdev);. Explain why this ordering is racy and give the correct teardown order, mirroring the setup.`,
    hints: ['cdev_del is what stops new opens.', 'Tear down in reverse of init.'],
    solution: `Releasing the device number first leaves the cdev still live and reachable, so a new open could still be routed to it during the window before cdev_del runs, even though the number it used is now "free" to be reassigned, which is a race and a use-after-free hazard. The correct order is the reverse of setup: first cdev_del(&my_cdev) to remove the device so no new opens can reach it, and only then unregister_chrdev_region(devno, 1) to give back the major/minor. The general rule for module exit is to tear things down in the opposite order they were created. So: stop new operations (cdev_del), then release the numbers, then free any remaining resources.`,
    tags: ['kernel', 'chardev', 'cleanup'],
  },
  {
    id: 'lx-ch08-t-015',
    chapter: 8,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'copy_from_user Can Fault and Sleep',
    prompt: `A driver takes a spinlock, then inside the critical section calls copy_from_user to pull the write payload from user space, then releases the lock. Explain why this is unsafe, focusing on what copy_from_user may do internally.`,
    hints: ['User memory may be paged out.', 'Spinlocks disable preemption / cannot sleep.'],
    solution: `copy_from_user may fault: the user page might be swapped out or not yet mapped, so internally it can trigger page-fault handling that blocks and sleeps until the page is brought in. Holding a spinlock means you are in atomic context where sleeping is forbidden, because the lock disables preemption (and possibly interrupts) and sleeping there can deadlock the system. So calling copy_from_user under a spinlock is unsafe. The fix is to do the user copy outside the lock, for example copy into a kernel buffer first while not holding the spinlock, then take the spinlock only to update the shared driver state. As a rule, never invoke routines that may sleep while holding a spinlock.`,
    tags: ['kernel', 'chardev', 'atomic'],
  },
  {
    id: 'lx-ch08-t-016',
    chapter: 8,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Designing llseek for a Bounded Buffer',
    prompt: `Your device is a fixed-size in-memory buffer of SIZE bytes that supports random access. Describe how a .llseek handler should compute the new position for SEEK_SET, SEEK_CUR, and SEEK_END, and what it must do with an out-of-range result.`,
    hints: ['Combine offset with the right base.', 'Reject negatives; update filp->f_pos.'],
    solution: `A bounded-buffer llseek computes a new offset relative to a base chosen by whence: for SEEK_SET the new position is offset; for SEEK_CUR it is filp->f_pos + offset; for SEEK_END it is SIZE + offset. After computing it, the handler must validate: a negative resulting position is invalid and should return -EINVAL. For a fixed buffer you typically also clamp or reject positions past SIZE depending on intended semantics (often you allow seeking up to SIZE but reads past the end return EOF). On success you store the result into filp->f_pos and return the new position. So llseek's job is base selection by whence, range checking, updating f_pos, and returning the new offset (or a negative errno).`,
    tags: ['kernel', 'chardev', 'llseek'],
  },
  {
    id: 'lx-ch08-t-017',
    chapter: 8,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Why a Seekable Device Needs llseek',
    prompt: `A driver leaves .llseek NULL but expects users to lseek to arbitrary byte offsets in its register space. Explain what happens when user space calls lseek on this device, and contrast a device that genuinely should reject seeking (like a serial stream).`,
    hints: ['Default seek behavior depends on the fops.', 'no_llseek vs a real handler.'],
    solution: `If .llseek is NULL, the kernel applies a default seek behavior rather than your intended random access, and historically that default may not move f_pos the way your register-mapped device needs, so positioned reads/writes land at the wrong offset; to get correct arbitrary seeking you must provide a real .llseek (or use the generic helper) that updates f_pos. For a device that is a pure stream, such as a serial line where "position" is meaningless, the right choice is to explicitly forbid seeking (assign no_llseek / return -ESPIPE) so callers get a clear error instead of a silently ignored seek. The design point: seekability is a deliberate property of the device, and you express it by either supplying a meaningful llseek or explicitly refusing one.`,
    tags: ['kernel', 'chardev', 'llseek'],
  },
  {
    id: 'lx-ch08-t-018',
    chapter: 8,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Recovering the Device From an inode',
    prompt: `In .open the driver needs to know which device instance (which minor) is being opened so it can set private_data. Describe two pieces of information available in .open that let it identify the instance, and why hard-coding a single global device struct breaks for multi-instance drivers.`,
    hints: ['iminor(inode) gives the minor.', 'container_of on inode->i_cdev.'],
    solution: `In .open you receive the inode and the struct file. The minor number is available via iminor(inode), and if you embedded your cdev inside your per-device structure you can recover that structure with container_of(inode->i_cdev, struct my_dev, cdev). Either approach lets you find the exact device instance and store a pointer to it in filp->private_data for the other handlers to use. A driver that always uses one global device struct works only when there is exactly one minor; with several minors every open would manipulate the same shared state, mixing up data between distinct devices. So multi-instance drivers must derive the instance from the inode rather than assuming a single global device.`,
    tags: ['kernel', 'chardev', 'open'],
  },
  {
    id: 'lx-ch08-t-019',
    chapter: 8,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Validating an ioctl Command',
    prompt: `An ioctl handler immediately switches on cmd and dereferences the arg pointer for a write command without any checks. List the validation steps a robust unlocked_ioctl should perform before acting on a command and its argument.`,
    hints: ['Check the magic and command number.', 'arg is a user pointer; use copy_*_user.'],
    solution: `A robust handler first checks that cmd belongs to this driver by verifying the magic/type byte (and that the command number is within the valid range), returning -ENOTTY for anything it does not recognize. The arg for read/write commands is typically a user-space address, so the handler must NOT dereference it directly; it uses copy_from_user / copy_to_user (or access_ok plus the copy) to move the argument safely and returns -EFAULT on failure. It should also handle each known command in a switch and return -ENOTTY (or -EINVAL) for unknown ones. So the pattern is: validate the command identity, treat arg as untrusted user memory, copy it safely, then act, with -ENOTTY as the canonical "unsupported ioctl" error.`,
    tags: ['kernel', 'chardev', 'ioctl'],
  },
  {
    id: 'lx-ch08-t-020',
    chapter: 8,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Partial Writes and the Return Value',
    prompt: `A .write handler has room for only 100 more bytes but the user asked to write 4096. Compare returning -ENOSPC, returning 100, and copying all 4096 anyway. What does the write() contract expect, and how does a well-behaved program react to a short write?`,
    hints: ['Returning a short count is legitimate.', 'Programs loop on the residual.'],
    solution: `The write() contract allows a "short write": a handler may accept fewer bytes than requested and return the number actually written. Returning 100 is the well-behaved choice here: it copies 100 bytes, advances *f_pos, and returns 100, after which a correct program loops and tries to write the remaining bytes. Copying all 4096 when there is room for only 100 would overflow the device and corrupt memory, and returning -ENOSPC throws away even the bytes you could have accepted (and is only appropriate when truly zero bytes can be taken, often after blocking is not an option). So the right behavior is to write what fits, return that count, and rely on the caller to handle the residual. A return of 0 with a nonzero request typically signals the caller to retry.`,
    tags: ['kernel', 'chardev', 'write'],
  },
  {
    id: 'lx-ch08-t-021',
    chapter: 8,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'class_create and device_create vs mknod',
    prompt: `One driver tells users to run mknod manually to make /dev/mydev; another calls class_create then device_create in its init. Compare the two approaches for creating the device node, especially when the major number is allocated dynamically.`,
    hints: ['Dynamic major is unknown until load.', 'udev reacts to device_create.'],
    solution: `With a manually created node, the user must know the major/minor and run mknod, which is fragile when the major is allocated dynamically because it is not known until the module loads, so the user would have to read it from sysfs first and could easily make a stale or wrong node. Calling class_create + device_create makes the kernel register the device with the driver core, which causes udev to automatically create (and later remove) the correct /dev node with the right permissions, picking up whatever major was assigned. This is the robust approach for dynamic majors and for hot plug/unplug, since the node lifecycle follows the device. The trade-off is a little more setup code and the need to tear down with device_destroy and class_destroy on exit, in reverse order.`,
    tags: ['kernel', 'chardev', 'devnode'],
  },
  {
    id: 'lx-ch08-t-022',
    chapter: 8,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Stable Read While Writers Mutate the Buffer',
    prompt: `Your device has a shared in-kernel buffer. A reader's .read copies bytes out with copy_to_user while another process's .write is modifying the same buffer. Without telling you to use any later-topic mechanism, explain what kind of corruption can occur and the general principle for protecting the shared buffer.`,
    hints: ['Concurrent readers and writers race.', 'Serialize access to shared state.'],
    solution: `Read and write can run concurrently (on different CPUs or via preemption), so a reader copying out the buffer while a writer mutates it can observe a torn, half-updated state: some bytes old, some new, or a size field that no longer matches the data, producing inconsistent results. The general principle is mutual exclusion: shared driver state touched by multiple file operations must be serialized so that one operation does not see another's partially completed update. You guard the critical region that reads or modifies the buffer (and its length) so only one runs at a time, while being careful to do the user copy in a way that does not sleep inside a non-sleepable lock. The key idea is simply that concurrent access to shared state needs serialization to stay consistent.`,
    tags: ['kernel', 'chardev', 'concurrency'],
  },
  {
    id: 'lx-ch08-t-023',
    chapter: 8,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Half-Initialized Driver on Failed Registration',
    prompt: `In init, alloc_chrdev_region succeeds, then cdev_add fails and the function returns its error WITHOUT calling unregister_chrdev_region. Predict what state the kernel is left in, what happens on a second insmod, and the correct error-handling pattern.`,
    hints: ['The reserved major leaks.', 'Unwind acquired resources on failure.'],
    solution: `If cdev_add fails and you return without unregistering the region, the major/minor range stays reserved by a module that did not finish loading, leaking those numbers; the device is also not usable since cdev_add failed. On a second insmod, alloc_chrdev_region (or a fixed register attempt) may now fail or get a different range, and the system accumulates a permanently reserved number until reboot. The correct pattern is goto-based unwinding: on each failure jump to a label that releases exactly the resources acquired so far, in reverse order. Here that means: if cdev_add fails, call unregister_chrdev_region before returning the error. Every partially successful init must clean up after itself so a failed load leaves no leaked resources.`,
    tags: ['kernel', 'chardev', 'errorpath'],
  },
  {
    id: 'lx-ch08-t-024',
    chapter: 8,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Why You Cannot Hold a Pointer Across copy_to_user',
    prompt: `A handler takes a spinlock guarding a linked list, walks to a node, then calls copy_to_user to ship that node's contents to user space while still holding the lock and the node pointer. Beyond "copy_to_user may sleep," explain the deeper structural problem and a safe redesign.`,
    hints: ['Sleeping with a lock is forbidden.', 'Copy into a local buffer first.'],
    solution: `Two problems stack up. First, copy_to_user may fault and sleep, which is illegal while holding a spinlock, so the design is already invalid. Second, even setting sleeping aside, you cannot safely perform an operation that may block or be preempted while pinning a data-structure pointer under a lock you must not sleep under, because dropping the lock to do the copy would let the list change and invalidate the node pointer (use-after-free). The safe redesign is to, under the lock, copy the needed bytes from the node into a small on-stack or pre-allocated kernel buffer, release the lock, and only then call copy_to_user from that stable kernel buffer. This separates "touch shared state under the lock" from "talk to user space without the lock," which is the standard kernel idiom.`,
    tags: ['kernel', 'chardev', 'locking'],
  },
  {
    id: 'lx-ch08-t-025',
    chapter: 8,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'TOCTOU With access_ok Plus a Bare Copy',
    prompt: `A driver calls access_ok on a user pointer, and if it passes, later dereferences that pointer directly with a plain pointer read (not copy_from_user) to fetch the data. Explain why access_ok alone does not make a bare dereference safe.`,
    hints: ['access_ok only checks range, not mapping.', 'The copy helpers handle faults.'],
    solution: `access_ok only verifies that the address range lies within the user portion of the address space; it does NOT guarantee the pages are present, mapped, or readable right now, and it certainly does not perform the access. A bare pointer dereference of user memory can still fault (page swapped out or unmapped) and, because there is no fixup, crash the kernel; the pages can also change between the check and the use. copy_from_user / copy_to_user are what actually perform the access with fault handling: they install an exception fixup so a fault becomes a clean -EFAULT instead of an oops, and they do the range check too. So the safe rule is to always move user data with the copy helpers (or get_user/put_user), never by dereferencing the raw user pointer after access_ok.`,
    tags: ['kernel', 'chardev', 'safety'],
  },
  {
    id: 'lx-ch08-t-026',
    chapter: 8,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Two Minors Sharing One file_operations',
    prompt: `A driver registers a contiguous range of 4 minors with a single cdev and one file_operations. Design how .open, .read, and .write distinguish the four logical devices, and explain why putting the buffer in a single global variable would be a bug.`,
    hints: ['Derive the instance from the minor.', 'Store per-device state, reach it via private_data.'],
    solution: `Allocate an array (or set) of per-device structures, one per minor, each holding its own buffer, length, and any lock. In .open, compute the index from the minor (iminor(inode) minus the base minor, or via container_of if each minor has its own embedded cdev) and store a pointer to that device's structure in filp->private_data. Then .read and .write simply use filp->private_data to reach the correct device's buffer, so all four minors share the same code but operate on separate state. A single global buffer would be a bug because every minor would read and write the same memory, so opening /dev/mydev2 would see /dev/mydev0's data and concurrent users of different minors would corrupt each other. Per-instance state reached through private_data is what makes one fops correctly serve many devices.`,
    tags: ['kernel', 'chardev', 'minor'],
  },
  {
    id: 'lx-ch08-t-027',
    chapter: 8,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Why Drivers Avoid Global f_pos State',
    prompt: `A naive driver tracks the read position in a single global variable instead of using each file's *f_pos. Two processes open the device and read independently. Predict the misbehavior and explain why per-file f_pos is the correct place for position.`,
    hints: ['Each open has its own struct file and f_pos.', 'Shared position interleaves badly.'],
    solution: `With a global position, the two opens share one cursor: process A's read advances the variable, so process B's next read starts wherever A left off, and they each see a scrambled, interleaved subset of the data rather than two independent full reads. The kernel already provides a per-open position: each struct file has its own f_pos, and read/write/llseek receive it as the *f_pos argument (or operate on filp->f_pos). Updating *f_pos keeps each file descriptor's progress separate, which is exactly the semantics user space expects, including independent dup'd descriptors versus shared ones. So position belongs in the per-file f_pos, not in driver-global state, otherwise concurrent or even sequential independent opens corrupt each other's view.`,
    tags: ['kernel', 'chardev', 'f_pos'],
  },
  {
    id: 'lx-ch08-t-028',
    chapter: 8,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'GFP_KERNEL Allocation in an Atomic Handler',
    prompt: `A driver path that runs while holding a spinlock calls kmalloc(size, GFP_KERNEL) to grow its buffer. Explain why the GFP flag is wrong here, what GFP_KERNEL permits, and what the safe choice and better structure would be.`,
    hints: ['GFP_KERNEL may sleep to reclaim memory.', 'GFP_ATOMIC does not sleep.'],
    solution: `GFP_KERNEL allows the allocator to sleep: under memory pressure it may block to reclaim or swap pages before returning. Inside a spinlock-held (atomic) context sleeping is forbidden, so a GFP_KERNEL allocation there can deadlock or trigger a "sleeping in atomic context" bug. If you truly must allocate while holding the lock, GFP_ATOMIC is the flag that promises not to sleep, but it draws from limited emergency reserves and is more likely to fail, so it is a last resort. The better structure is to allocate with GFP_KERNEL outside the critical section, before taking the spinlock, and only enter the lock to splice the new buffer into the shared state. So: match the GFP flag to the context, and prefer to do sleeping work (including normal allocation) outside atomic regions.`,
    tags: ['kernel', 'chardev', 'gfp'],
  },
  {
    id: 'lx-ch08-t-029',
    chapter: 8,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Choosing read/write vs ioctl for an Operation',
    prompt: `A teammate wants every device operation (including "set the baud rate" and "reset the chip") funneled through write() by encoding commands as text. Argue when read/write are the right interface and when ioctl is the better fit, and the downside of overloading write with control commands.`,
    hints: ['read/write move a data stream.', 'ioctl is for out-of-band control.'],
    solution: `read and write are designed to move a stream or block of device DATA, with position and short-transfer semantics, so they fit operations like "give me the next samples" or "send these bytes." Control actions that are not data flow, such as setting baud rate or resetting the chip, are out-of-band commands with structured arguments and a clear success/failure, which is exactly what ioctl exists for: a typed command code plus an argument, validated by magic/size. Overloading write to carry text commands muddies the data path (now write must parse and distinguish control strings from real data, breaking the byte-stream contract and short-write semantics), makes the protocol fragile and ambiguous, and loses ioctl's command-number safety. So use read/write for data and ioctl for discrete control operations.`,
    tags: ['kernel', 'chardev', 'design'],
  },
  {
    id: 'lx-ch08-t-030',
    chapter: 8,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Open Succeeds, Then rmmod, Then read',
    prompt: `A user opens /dev/mydev (open succeeds), the admin runs rmmod mydev, and then the user calls read on the still-open descriptor. Walk through what mechanisms determine whether this is safe or a crash, referencing module refcounting and the exit path.`,
    hints: ['Owner refcount blocks unload while open.', 'cdev_del before freeing handlers.'],
    solution: `Whether this is safe hinges on reference counting. If file_operations/cdev .owner is set to THIS_MODULE, the open bumps the module's refcount, so rmmod sees the module busy and refuses to unload while the descriptor is open; the later read then runs against still-present code safely. If .owner is missing, the kernel may let rmmod proceed even with the file open, and the subsequent read jumps into freed module code, causing a crash. A correct exit path also matters: module_exit must cdev_del (stopping new opens) and only release resources the module owns, but it cannot magically protect an already-open fd unless refcounting held the unload off. So the safe design is .owner = THIS_MODULE plus a proper teardown order, which together make "open, rmmod, read" either blocked or safe rather than a use-after-free.`,
    tags: ['kernel', 'chardev', 'lifecycle'],
  },
]

export default problems
