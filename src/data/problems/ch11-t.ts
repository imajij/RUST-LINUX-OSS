import type { Problem } from '../../types'

const problems: Problem[] = [
  {
    id: 'rs-ch11-t-001',
    chapter: 11,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'What Marks a Function as a Test',
    prompt: `In Rust, an ordinary function and a test function look almost identical. What single thing turns a plain function into something that cargo test will run, and on what line do you place it? Describe what you would write directly above fn it_works().`,
    hints: [
      'It is an attribute, written in square brackets.',
      'It goes on the line immediately above the fn keyword.',
    ],
    solution: `You mark a function as a test by writing the attribute #[test] on the line directly above the function definition, like #[test] then fn it_works(). When you run cargo test, the test runner builds a special test binary and calls every function annotated with #[test]. Functions without that attribute are treated as ordinary helper code and are not run as tests, even if they live in the same module. So the attribute, not the name or location, is what makes a function a test.`,
    tags: ['test-attribute', 'basics'],
  },
  {
    id: 'rs-ch11-t-002',
    chapter: 11,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'How a Test Reports Failure',
    prompt: `A test function does not return a pass or fail value the way you might expect. By default, how does a #[test] function signal success, and how does it signal failure? Mention the role of panicking.`,
    hints: [
      'Think about what happens when an assertion does not hold.',
      'A test that simply returns normally is considered to have done what?',
    ],
    solution: `By default a test function that returns normally without panicking is counted as a pass, and a test that panics is counted as a failure. Each test runs in its own thread, and when that thread panics the test harness marks the test as failed and records the panic message. This is why the assertion macros are written to panic when their condition is not met: a failing assert! causes a panic, which the runner translates into a failed test. So "success" simply means "the function reached its end without panicking."`,
    tags: ['panic', 'basics'],
  },
  {
    id: 'rs-ch11-t-003',
    chapter: 11,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'assert! Versus assert_eq!',
    prompt: `Both assert! and assert_eq! are used to check things in tests. In one or two sentences each, explain what kind of value assert! expects, what assert_eq! expects, and one advantage assert_eq! has over writing assert!(a == b).`,
    hints: [
      'One takes a single boolean; the other takes two values to compare.',
      'Think about what gets printed when the check fails.',
    ],
    solution: `assert! takes a single boolean expression and panics if that expression is false, so you use it for any condition you can phrase as true or false. assert_eq! takes two values and panics if they are not equal. The advantage of assert_eq! over assert!(a == b) is that when the check fails, assert_eq! prints both the left and right values it compared, so you immediately see what was expected versus what was produced, instead of only learning that "something was false."`,
    tags: ['assert', 'assert-eq', 'macros'],
  },
  {
    id: 'rs-ch11-t-004',
    chapter: 11,
    kind: 'thinking',
    difficulty: 'intro',
    title: 'Where Unit Tests Live',
    prompt: `The book recommends putting unit tests in the same file as the code they test, inside a module. What is the conventional name of that module, and what attribute is placed on it? Explain in plain words what that attribute does for a normal cargo build.`,
    hints: [
      'The module is conventionally named tests.',
      'The attribute starts with cfg and mentions test.',
    ],
    solution: `Unit tests conventionally go in a module named tests, and that module is annotated with #[cfg(test)]. The cfg(test) attribute tells the compiler to include that module only when you run cargo test, not when you run cargo build or build a release. This means the test code and its helpers are compiled and shipped only during testing, so they add nothing to the size of the final library or binary. It is the standard way to keep tests next to the code without bloating the production artifact.`,
    tags: ['cfg-test', 'unit-tests', 'organization'],
  },
  {
    id: 'rs-ch11-t-005',
    chapter: 11,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Predict the Assertion Failure Output',
    prompt: `Suppose a function add(a: i32, b: i32) returns a + b, but it has a bug and actually returns a - b. A test runs assert_eq!(add(2, 3), 5). Describe what the test result will be and roughly what the failure message will show about the two values involved.`,
    hints: [
      'Compute what add(2, 3) actually returns with the bug.',
      'assert_eq! prints both sides it compared.',
    ],
    solution: `With the bug, add(2, 3) computes 2 - 3 which is -1, while the test expects 5, so the values are not equal and assert_eq! panics, marking the test as failed. The failure output will report something like "left == right" failing with left being -1 (the value the function produced) and right being 5 (the literal in the test). Seeing both numbers makes it clear the function returned -1 instead of the expected 5, which is exactly the diagnostic benefit of assert_eq! over a bare assert!.`,
    tags: ['assert-eq', 'predict-output'],
  },
  {
    id: 'rs-ch11-t-006',
    chapter: 11,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Choosing assert_ne!',
    prompt: `You are testing a function add_one that should never return its input unchanged. Which assertion macro best expresses "the result must differ from the input," and how would you phrase the check for add_one(5)? Briefly say when assert_ne! is more natural than assert_eq!.`,
    hints: [
      'There is a macro that passes when two values are NOT equal.',
      'Use it when you only care that the result changed, not its exact value.',
    ],
    solution: `The right macro is assert_ne!, which passes when its two arguments are not equal and panics when they are equal. You would write assert_ne!(add_one(5), 5), asserting that adding one to 5 must produce something different from 5. assert_ne! is more natural than assert_eq! when you cannot easily predict the exact correct output but you do know a value the result must not equal, such as confirming a transformation actually changed the input or that a generated id is not zero.`,
    tags: ['assert-ne', 'when-to-use'],
  },
  {
    id: 'rs-ch11-t-007',
    chapter: 11,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Adding a Custom Failure Message',
    prompt: `An assert! check on greeting.contains("Carol") fails, but the default message only says the expression was false, which is not very helpful. Explain how you can attach a custom message that also prints the actual greeting value, and what the extra arguments to assert! mean.`,
    hints: [
      'assert! and assert_eq! accept additional arguments after the first.',
      'Those arguments are a format string and its values, like println!.',
    ],
    solution: `Any arguments you pass to assert! after the boolean condition are treated as a format string and its values, exactly like the arguments to println!. So you could write assert!(greeting.contains("Carol"), "Greeting did not contain name, value was {}", greeting). When the assertion fails, Rust formats that message, substituting the real greeting into the placeholder, and prints it instead of the generic "assertion failed" text. This turns an unhelpful failure into one that shows the actual value that caused the problem, which makes debugging much faster.`,
    tags: ['assert', 'custom-message'],
  },
  {
    id: 'rs-ch11-t-008',
    chapter: 11,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'A Basic should_panic Test',
    prompt: `You have a Guess::new function that panics when given a value outside 1 to 100. You want a test that confirms it panics for the input 200. What attribute do you add to the test function, and why does the test pass when the code panics instead of failing?`,
    hints: [
      'There is an attribute that inverts the usual pass/fail rule.',
      'It goes alongside #[test].',
    ],
    solution: `You add the #[should_panic] attribute to the test function, placed together with #[test] above the function. With should_panic, the harness flips the normal rule: the test passes if the code inside panics and fails if the code runs to completion without panicking. So a body that calls Guess::new(200) and triggers a panic is exactly what makes this test pass. It is the standard way to verify that a function correctly rejects bad input by panicking.`,
    tags: ['should-panic', 'panic'],
  },
  {
    id: 'rs-ch11-t-009',
    chapter: 11,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Why Test Output Is Sometimes Hidden',
    prompt: `A developer adds println! calls inside a function to debug it, runs cargo test, and is confused that nothing prints for the tests that pass. Explain the default behavior of the test runner regarding printed output and what changes for tests that fail.`,
    hints: [
      'By default the runner captures standard output of passing tests.',
      'Failing tests are treated differently so you can debug them.',
    ],
    solution: `By default the test harness captures everything a passing test prints to standard output and discards it, so your println! output does not appear when the test succeeds. The idea is to keep the test report clean and show only meaningful results. For tests that fail, however, the captured output is shown along with the failure, so any println! you added is visible exactly when you need it. If you want to see output even from passing tests, you can pass an option to show it (discussed in a later subtopic).`,
    tags: ['output', 'println', 'test-runner'],
  },
  {
    id: 'rs-ch11-t-010',
    chapter: 11,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Showing Output From Passing Tests',
    prompt: `You want to see the println! output of every test, even the ones that pass, without removing your debugging prints. What flag do you add when invoking cargo test, and how do you write the command so the flag reaches the test binary rather than cargo itself?`,
    hints: [
      'The flag is show-output.',
      'Arguments after -- go to the test binary, not to cargo.',
    ],
    solution: `You add the --show-output flag, and because that flag is meant for the test binary rather than for cargo, you separate it with -- on the command line: cargo test -- --show-output. The double dash tells cargo that the following arguments should be passed through to the compiled test executable. With this flag set, the harness no longer discards the standard output of passing tests, so your println! lines appear for all tests. This is handy for inspecting intermediate values while everything still passes.`,
    tags: ['show-output', 'cli', 'test-runner'],
  },
  {
    id: 'rs-ch11-t-011',
    chapter: 11,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Running a Single Named Test',
    prompt: `Your test suite has functions named add_two_and_two, add_three_and_two, and one_hundred. You only want to run add_two_and_two right now. Write the cargo test command that runs just that one test, and explain what the test runner matches the argument against.`,
    hints: [
      'You pass the test name as an argument to cargo test.',
      'No -- is needed because the name goes to cargo as a filter.',
    ],
    solution: `You run cargo test add_two_and_two, passing the test name directly as an argument. The runner uses that string to filter which tests to run by matching it against test function names, and because the name uniquely identifies one function, only add_two_and_two runs while the other two are reported as filtered out. This name argument goes to cargo's test filtering, so no -- separator is required here. It is the quickest way to focus on a single failing test.`,
    tags: ['run-by-name', 'cli'],
  },
  {
    id: 'rs-ch11-t-012',
    chapter: 11,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Filtering Tests by a Shared Prefix',
    prompt: `Given tests named add_two_and_two, add_three_and_two, and one_hundred, you want to run both of the "add" tests but not one_hundred. What single cargo test command achieves this, and why does it select two tests instead of one?`,
    hints: [
      'The name filter matches any test whose name contains the string.',
      'Both add tests share a common piece of their name.',
    ],
    solution: `You run cargo test add. The name you pass to cargo test is treated as a substring filter, so the runner runs every test whose name contains "add". Both add_two_and_two and add_three_and_two contain that substring, so both run, while one_hundred does not match and is filtered out. This partial-match behavior lets you run a whole related group of tests by naming them with a shared prefix or word.`,
    tags: ['run-by-name', 'filtering', 'cli'],
  },
  {
    id: 'rs-ch11-t-013',
    chapter: 11,
    kind: 'thinking',
    difficulty: 'easy',
    title: 'Testing a Private Function',
    prompt: `You have a private function internal_adder in your library that is not pub. A colleague claims you must make it pub to test it. Explain why the unit test in the same file can call it without making it public, referring to Rust's module privacy rules.`,
    hints: [
      'The tests module is a child module of the code it tests.',
      'Child modules can see the private items of their ancestors.',
    ],
    solution: `Your colleague is mistaken: the tests module is a child module of the module that contains internal_adder, and in Rust a child module can use the private items of its ancestor modules. Inside the tests module you bring the parent items into scope with use super::*, and then you can call internal_adder directly even though it is not pub. Because Rust's privacy is about hiding items from outside the module tree, not from within it, there is no need to expose the function publicly just to test it. This is one reason unit tests are kept in the same file as the code.`,
    tags: ['private-functions', 'privacy', 'unit-tests'],
  },
  {
    id: 'rs-ch11-t-014',
    chapter: 11,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Tests That Return Result',
    prompt: `Instead of using assert_eq!, you can write a test whose signature is fn it_works() -> Result<(), String>. Explain how such a test signals pass and fail, what the body typically returns on success, and one concrete advantage of this style for tests that call functions returning Result.`,
    hints: [
      'Returning Ok means pass; returning Err means fail.',
      'The ? operator works inside a function that returns Result.',
    ],
    solution: `A test that returns Result<(), String> passes when it returns Ok(()) and fails when it returns an Err value, instead of relying on a panic. The body usually ends with Ok(()) after its checks succeed, and it returns Err(...) with a message when something is wrong. The concrete advantage is that you can use the ? operator inside the test body on calls that themselves return Result, so any early error is converted into a failed test automatically without writing unwrap or a manual match. One caveat: you cannot use #[should_panic] on a Result-returning test, since failure is expressed by returning Err, not by panicking.`,
    tags: ['result', 'question-mark', 'test-style'],
  },
  {
    id: 'rs-ch11-t-015',
    chapter: 11,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Making should_panic Precise',
    prompt: `A should_panic test currently passes whenever the code panics for any reason, including unrelated bugs. You want it to pass only when the panic message mentions "less than or equal to 100". How do you tighten the attribute, and what does the runner check about the panic message?`,
    hints: [
      'should_panic can take an expected parameter.',
      'The check is a substring match against the panic message.',
    ],
    solution: `You add an expected string to the attribute, writing #[should_panic(expected = "less than or equal to 100")]. With this, the runner does not just confirm that a panic occurred; it also checks that the panic message contains the given expected text as a substring. The test now passes only when the code panics and that panic message includes "less than or equal to 100", so a panic from an unrelated bug with a different message would make the test fail. This guards against a should_panic test passing for the wrong reason.`,
    tags: ['should-panic', 'expected', 'precision'],
  },
  {
    id: 'rs-ch11-t-016',
    chapter: 11,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Why Tests Run in Parallel by Default',
    prompt: `Two tests both write to a file named test_output.txt and then read it back, and they sometimes fail intermittently when run together. Explain the default threading behavior of cargo test that causes this, and one way to force the tests to run one at a time.`,
    hints: [
      'By default tests run at the same time on multiple threads.',
      'There is a test-threads option you can set to 1.',
    ],
    solution: `By default cargo test runs the tests in parallel using multiple threads, so the two tests can be executing at the same moment and stomp on the same test_output.txt, producing flaky, order-dependent failures. Tests are supposed to be independent, so shared mutable external state like a single file breaks that assumption. One way to force serial execution is to limit the runner to a single thread with cargo test -- --test-threads=1, which makes the tests run one after another. A more robust fix is to give each test its own file so they no longer share state, but reducing the thread count is the quick diagnostic.`,
    tags: ['parallelism', 'test-threads', 'flaky'],
  },
  {
    id: 'rs-ch11-t-017',
    chapter: 11,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Ignoring a Slow Test',
    prompt: `One test takes two minutes to run and you do not want it to run on every cargo test, but you still want the ability to run it on demand. What attribute do you add, how does it change the default run, and what command runs only the ignored tests?`,
    hints: [
      'The attribute is ignore, placed next to #[test].',
      'There is a flag --ignored to run them explicitly.',
    ],
    solution: `You annotate the test with #[ignore] in addition to #[test]. With #[ignore] the test is skipped during a normal cargo test run and reported as ignored rather than run, so the expensive test does not slow down your usual workflow. When you do want to run only the ignored tests, you invoke cargo test -- --ignored, which runs exactly the tests that carry the ignore attribute. This lets you keep slow or environment-dependent tests in the suite while running them only deliberately.`,
    tags: ['ignore', 'cli', 'slow-tests'],
  },
  {
    id: 'rs-ch11-t-018',
    chapter: 11,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Unit Tests Versus Integration Tests',
    prompt: `Contrast unit tests and integration tests in Rust along two axes: where each kind of test code physically lives in the project, and what part of your code each kind is able to reach (private versus only public items). Give one sentence on the purpose each is best suited for.`,
    hints: [
      'Unit tests live inside src; integration tests live in a top-level directory.',
      'Think about which can call non-pub functions.',
    ],
    solution: `Unit tests live inside the src files alongside the code, in a #[cfg(test)] module, and because they are part of the same crate they can exercise private (non-pub) functions and internals directly. Integration tests live in a separate top-level tests directory, where each file is compiled as its own crate that uses your library from the outside, so they can call only the public API just like a real external user would. Unit tests are best for checking small pieces of logic in isolation, including private helpers, while integration tests are best for verifying that the public interface works correctly when many parts are combined.`,
    tags: ['integration-tests', 'unit-tests', 'comparison'],
  },
  {
    id: 'rs-ch11-t-019',
    chapter: 11,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'The use Line in an Integration Test',
    prompt: `An integration test file under tests/integration_test.rs starts with use adder; and then calls adder::add_two(2). Explain why an integration test needs this use line when a unit test inside src usually does not, and what name "adder" refers to.`,
    hints: [
      'Integration tests are a separate crate from your library.',
      '"adder" is the name of the library crate being tested.',
    ],
    solution: `Each file in the tests directory is compiled as a separate crate, external to your library, so an integration test must bring your library into scope with a use line, here use adder, where adder is the name of your library crate as declared in Cargo.toml. A unit test inside src does not need this because it lives inside the same crate as the code and reaches items through super or the crate's own module paths. Because the integration test is an outside consumer, it can only see the public items of adder and calls them through the crate name, as in adder::add_two(2). This mirrors exactly how another project depending on your crate would use it.`,
    tags: ['integration-tests', 'crates', 'use'],
  },
  {
    id: 'rs-ch11-t-020',
    chapter: 11,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Reading the Three Sections of Test Output',
    prompt: `When you run cargo test on a library that has unit tests, integration tests, and doc tests, the output is grouped into separate sections. Describe what the three sections correspond to and why each integration test file produces its own group within the integration section.`,
    hints: [
      'One section is for unit tests, one for the tests directory, one for documentation.',
      'Each file in tests is a separate crate.',
    ],
    solution: `The output has a section for the unit tests (run from the library's own test binary), a section for the integration tests (run from the tests directory), and a section for documentation tests (run from example code in doc comments). The integration section itself shows a separate group per file because each file under tests is compiled and run as its own crate, so the runner reports each one's results independently with its own summary line. This grouping helps you see at a glance whether a failure is in your internal logic, in your public-facing integration, or in your documentation examples.`,
    tags: ['test-output', 'sections', 'doc-tests'],
  },
  {
    id: 'rs-ch11-t-021',
    chapter: 11,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Predict Pass or Fail Across Several Tests',
    prompt: `Consider four tests: (a) assert_eq!(2 + 2, 4); (b) assert!(false, "boom"); (c) a #[should_panic] test whose body calls a function that returns normally; (d) a Result test whose body ends with Ok(()). For each, state whether it passes or fails and give a one-line reason.`,
    hints: [
      'should_panic fails if no panic happens.',
      'A Result test passes on Ok and fails on Err.',
    ],
    solution: `(a) Passes, because 2 + 2 equals 4 so assert_eq! does not panic. (b) Fails, because assert! is given false, so it panics with the message "boom". (c) Fails, because should_panic expects the body to panic, but the called function returns normally without panicking, which violates the expectation. (d) Passes, because a Result-returning test signals success by returning Ok(()), which this body does. The key rules are that normal completion means pass, a panic means fail, should_panic inverts that, and a Result test is judged by Ok versus Err.`,
    tags: ['predict-output', 'should-panic', 'result'],
  },
  {
    id: 'rs-ch11-t-022',
    chapter: 11,
    kind: 'thinking',
    difficulty: 'medium',
    title: 'Why super Appears in the tests Module',
    prompt: `Almost every #[cfg(test)] module begins with use super::*;. Explain what super refers to here, what the glob * brings in, and what would go wrong if you omitted this line while trying to call a function defined in the same file's parent module.`,
    hints: [
      'super means the parent module of tests.',
      'The tests module does not automatically see its parent items by name.',
    ],
    solution: `super refers to the parent module of the tests module, which is the module containing the code under test in that file. The glob * imports all of the parent's items into the tests module's scope, so functions like add or internal_adder can be called by their short names. If you omitted use super::* and tried to call such a function directly, the compiler would not find it in scope and would report an unresolved name, because a child module does not automatically pull its parent's names into scope; you must import them. Writing use super::* is the idiomatic way to make the parent's items conveniently available to the tests.`,
    tags: ['super', 'unit-tests', 'scope'],
  },
  {
    id: 'rs-ch11-t-023',
    chapter: 11,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'A Shared Common Module Without Extra Test Output',
    prompt: `You want a setup() helper shared by several integration test files. If you put it in tests/common.rs, the runner treats common as its own test file and prints an empty test section for it. What is the recommended file layout that avoids this, and why does that layout stop common from being treated as a test crate?`,
    hints: [
      'Move the code into a subdirectory module file.',
      'Files in subdirectories of tests are not compiled as separate test crates.',
    ],
    solution: `The recommended layout is to create the file tests/common/mod.rs instead of tests/common.rs and put the shared setup() code there. Rust does not treat files in subdirectories of the tests directory as separate integration test crates, so tests/common/mod.rs is compiled only as a module that other test files include, not as its own test binary. As a result it no longer appears in the output as an empty test section. Each integration test file then brings it in with mod common; and calls common::setup(), sharing the helper cleanly.`,
    tags: ['common-module', 'integration-tests', 'layout'],
  },
  {
    id: 'rs-ch11-t-024',
    chapter: 11,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Why Binary Crates Resist Integration Tests',
    prompt: `A project that is only a binary crate (it has src/main.rs but no src/lib.rs) cannot have its functions called directly from files in the tests directory. Explain why this is, and what common project structure people adopt so that the logic can be both tested by integration tests and used by the binary.`,
    hints: [
      'Integration tests can only use a library crate via use.',
      'A binary crate exposes no functions for other crates to call.',
    ],
    solution: `Integration tests work by bringing your crate into scope with a use line, but that only works for a library crate; a binary crate built from src/main.rs does not expose its functions for another crate to call, so the tests directory has nothing to import. The common solution is to move the real logic into src/lib.rs as a library crate and keep src/main.rs as a thin shell that calls into the library. The integration tests can then use the library crate and exercise its public functions, while main.rs simply wires up the program. This is why many Rust binaries are structured as a small main plus a substantial library.`,
    tags: ['binary-crates', 'integration-tests', 'project-structure'],
  },
  {
    id: 'rs-ch11-t-025',
    chapter: 11,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Comparing Failure Diagnostics of assert! Styles',
    prompt: `You are testing that result equals an expected Vec. Compare three approaches: assert!(result == expected), assert_eq!(result, expected), and assert!(result == expected, "got {:?}", result). Rank them by how useful their failure output is and explain what each does and does not tell you when the test fails.`,
    hints: [
      'Think about what each prints when the comparison is false.',
      'assert_eq! prints both sides automatically.',
    ],
    solution: `The least useful is assert!(result == expected): on failure it only reports that the expression was false, telling you neither the actual nor the expected value. The middle option, assert!(result == expected, "got {:?}", result), is better because the custom message shows the actual result, but it still does not print expected, so you must look it up in the source. The most useful is assert_eq!(result, expected): when it fails it automatically prints both the left and right values, so you see exactly what was produced and what was wanted in one message. For comparing two values for equality, assert_eq! gives the richest diagnostics with the least effort.`,
    tags: ['assert', 'assert-eq', 'comparison', 'diagnostics'],
  },
  {
    id: 'rs-ch11-t-026',
    chapter: 11,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'should_panic Versus a Result Test for Errors',
    prompt: `A function parse_config returns Result<Config, String> for bad input rather than panicking. A teammate writes a #[should_panic] test for the bad-input case and is puzzled that it fails. Explain why should_panic is the wrong tool here, and describe the test you would write instead.`,
    hints: [
      'should_panic requires the code to actually panic.',
      'Returning Err is not the same as panicking.',
    ],
    solution: `should_panic only passes when the tested code actually panics, but parse_config does not panic on bad input; it returns Err, which is a normal value, so the test body completes without panicking and the should_panic test fails. The right approach is to call parse_config on bad input and assert on the returned Result, for example checking that the result is Err with assert!(parse_config(bad).is_err()), or matching on it to inspect the error message. You could also write the test to return Result and use the value directly. The general rule is to use should_panic for code that panics and assertions on Result for code that reports errors by returning Err.`,
    tags: ['should-panic', 'result', 'error-handling', 'when-to-use'],
  },
  {
    id: 'rs-ch11-t-027',
    chapter: 11,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'When a Test Names a Symbol That cfg Removed',
    prompt: `A developer marks a helper function with #[cfg(test)] so it only exists during testing, then calls that helper from non-test code in the same module. The project builds fine with cargo test but fails to compile with cargo build. Explain precisely why, in terms of when cfg(test) code exists.`,
    hints: [
      'cfg(test) items are compiled only during cargo test.',
      'Non-test code is always compiled, including in a normal build.',
    ],
    solution: `An item marked #[cfg(test)] exists only when the crate is compiled for testing, so during cargo build that helper function is simply not present in the compiled crate at all. The non-test code that calls the helper, however, is always compiled, including during cargo build, so during a normal build it references a function that does not exist, and the compiler reports an unresolved name. It works under cargo test only because then the cfg(test) helper is included. The fix is to not gate a function behind cfg(test) if ordinary code needs it; only test-only code should depend on test-only items.`,
    tags: ['cfg-test', 'compilation', 'does-not-compile'],
  },
  {
    id: 'rs-ch11-t-028',
    chapter: 11,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Designing Tests for an add and subtract Pair',
    prompt: `You are asked to design a small unit test suite for two functions: add(a, b) and divide(a, b), where divide panics if b is 0. List the distinct tests you would write to cover the important behaviors, and for each say which assertion or attribute you would use and why.`,
    hints: [
      'Cover a normal case for each function.',
      'Cover the panic case for divide with should_panic and an expected message.',
    ],
    solution: `A reasonable suite has at least three tests. First, a test for add such as assert_eq!(add(2, 3), 5), using assert_eq! so a failure shows the actual and expected sums. Second, a normal-case test for divide such as assert_eq!(divide(10, 2), 5), again with assert_eq! for clear output. Third, a panic test for the zero divisor: a #[should_panic(expected = "divide by zero")] test whose body calls divide(1, 0), using should_panic with an expected message so the test passes only when divide panics for the right reason and not because of some unrelated bug. Together these cover the happy path of each function plus the documented failure behavior of divide.`,
    tags: ['test-design', 'should-panic', 'assert-eq'],
  },
  {
    id: 'rs-ch11-t-029',
    chapter: 11,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Reasoning About Parallel Tests and Shared State',
    prompt: `Two tests share a global mutable counter through helper functions, each incrementing it and then asserting its value. They pass when run individually but fail unpredictably when run together with cargo test. Diagnose the root cause in terms of the default test runner, and give two different ways to make the suite reliable.`,
    hints: [
      'The default runner uses multiple threads.',
      'Either remove the shared state or force serial execution.',
    ],
    solution: `The root cause is that cargo test runs tests in parallel on multiple threads by default, so both tests touch the same global counter at overlapping times; the value one test asserts on depends on whether the other has already incremented it, which produces nondeterministic, order-dependent failures. The cleanest fix is to remove the shared mutable state so each test owns its own independent data, making the tests truly isolated. A second, weaker option is to force serial execution with cargo test -- --test-threads=1, which removes the interleaving but also gives up parallel speed and only hides the fragile design. Tests should be written to be independent, so eliminating the shared state is the better remedy.`,
    tags: ['parallelism', 'test-threads', 'isolation', 'debugging'],
  },
  {
    id: 'rs-ch11-t-030',
    chapter: 11,
    kind: 'thinking',
    difficulty: 'hard',
    title: 'Combining Filters, Ignore, and Show Output',
    prompt: `You have a slow integration-style test marked #[ignore] named full_pipeline, and it has println! tracing inside. You want to run only that test, including its ignored status, while also seeing its printed output. Construct the full cargo test command and explain why each part is needed and where the -- separator goes.`,
    hints: [
      'Name filtering goes to cargo; --ignored and --show-output go to the test binary.',
      'Everything after -- is passed to the test binary.',
    ],
    solution: `A working command is cargo test full_pipeline -- --ignored --show-output. The bare full_pipeline argument is the name filter, telling the runner to run only tests whose name contains that string. The -- separator marks the end of cargo's own arguments, so everything after it is handed to the test binary. --ignored is needed because the test carries #[ignore] and would otherwise be skipped; it tells the harness to run ignored tests. --show-output is needed so the harness does not discard the standard output of the passing test, letting your println! tracing appear. Placing the name before -- and the binary flags after -- is what routes each option to the right place.`,
    tags: ['cli', 'ignore', 'show-output', 'filtering'],
  },
]

export default problems
